---
title: 'Story 27.4 retention verification, operations runbook, and A41 close-out'
type: 'feature'
created: '2026-09-02'
status: 'in-review'
baseline_revision: 'e9653980cf8b55e17afde30a5a8587cb30405e78'
baseline_commit: 'e9653980cf8b55e17afde30a5a8587cb30405e78'
review_loop_iteration: 2
followup_review_recommended: false
context:
  - '_bmad-output/project-context.md'
  - 'docs/dev/adr-27.1-001-access-telemetry-lifecycle.md'
warnings:
  - 'oversized'
deferred: []
operator_actions:
  - 'Execute C0 and canonical C1.1-C1.25 on the approved exact-profile non-Production qualification target, retain the immutable packets, and obtain the independent C1 Platform Operations and Security approvals.'
  - 'Run the reviewed C2-C4 producers with an external evidence root and C3 resume journal, retain their immutable packets, and verify the final gate-disabled, empty-Lease, lifecycle-zero, and clock-zero state.'
  - 'Obtain independent post-evidence C5 and C6 approvals, pass terminal validation, then run the A41 inventory, preflight, postflight, close-out publication, and remote publish verification before resolving A41.'
---

<intent-contract>

## Intent

**Problem:** Story 27.4 has no production-shaped C2-C6 evidence producers, canonical close-out matrix, or lifecycle operations runbooks, while the required C1 predecessor packet and independent approvals are still absent. A41 must remain open until immutable same-profile evidence proves the deployed lifecycle.

**Approach:** Complete and test every repository-owned producer, fail-closed validator, dashboard, runbook, and close-out guard now. Leave Production disabled and all evidence/closure states pending until authorized operators execute the controlled deployment scenarios and two independent reviewers approve the same immutable `PG-ONPREM-1` profile hash.

## Boundaries & Constraints

**Always:** Reuse the existing Dapr-only lifecycle, immutable profile/capacity primitives, redacted command observations, and structure-aware document guards. Bind every packet to commands, UTC timestamps, source/profile/workload hashes, immutable artifact hashes, owner, and review state. Require explicit nonzero results, exact same-profile equality, zero skips/failures, and tenant/privacy denial before dependencies. Preserve JSON-console/optional-OTLP audit continuity and the assurance limit: bounded infrastructure telemetry, not tamper-evident, append-only, legally compliant, or certified audit retention. Finalize repository-complete work as `awaiting-operator` with non-empty imperative `operator_actions` when live deployment actions or approvals remain.

**Block If:** A repository implementation decision would change the accepted Dapr-only architecture, immutable adapter profile, retention bounds, authority separation, durability/RPO/RTO claim, or A41 closure semantics without an approved governing decision. Missing cluster access, credentials, published images, running-target packets, or human approvals is an operator tail, not a `blocked` outcome.

**Never:** Write or revert `_bmad-output/implementation-artifacts/sprint-status.yaml`; treat its rows as verification; synthesize or share-discharge any C1 gate; contact an unapproved target; expose secrets/raw tenant content; enable Production lifecycle writes; weaken the zero-replica overlay; mutate A41 to resolved; reopen historical Epic 20/Story 20.5 records; or claim physical reclamation from logical deletion, broad health, static manifests, fakes, scheduling, or documentation alone.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Offline repository validation | No cluster credentials or live evidence | Producers, fixtures, docs, dashboard, and guards validate; canonical matrix stays pending | No target query and no A41 mutation |
| Controlled lifecycle proof | Authorized exact-profile target and complete per-checkpoint inputs | Immutable C2-C4 packets capture two writers, replacements/faults, expiry/purge/reclamation, observability, and tenant/privacy denial | Write a secret-safe blocker packet and exit nonzero on any missing, stale, skipped, or mismatched observation |
| Close-out preflight | C0-C5 and terminal checks passed on one profile hash | Emit exhaustive A41 inventory, exact allowed mutation set, and recoverable pre-mutation snapshot | Reject unclassified paths, incomplete approvals, profile drift, or non-passing evidence |
| Postflight or publish drift | Staged tree/commit differs from approved mutation manifest | Keep A41 open and report exact mismatch | Never repair by broad rewrite or by changing historical status |

</intent-contract>

## Code Map

- `_bmad-output/planning-artifacts/epics.md:5018` -- authoritative Story 27.4 predecessor gate and three acceptance criteria; Story 27.7-27.31/C1 evidence remains absent.
- `docs/dev/adr-27.1-001-access-telemetry-lifecycle.md:769` -- accepted handoff, immutable `PG-ONPREM-1` profile/hashes, human approval separation, operational scope, and A41 close-out prohibition.
- `_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:81` -- current transfer record assigning former lifecycle proof/runbook/close-out tasks to Story 27.4.
- `tools/verify-access-telemetry-lifecycle.py:23` -- CLI currently accepts only `adapter-profile`; extend with C2-C4 and A41 inventory/preflight/postflight/publish modes while retaining pre-query identity checks.
- `tools/verify_access_telemetry_lifecycle.py:99` -- reuse environment identity, checked capacity, immutable profile hashing, redacted observations, and per-run packet writing; `run_adapter_profile_checkpoint` is intentionally not behavioral proof.
- `tools/verify-access-telemetry-c1.ps1:284` -- reuse immutable, secret-safe packet and source/command hash conventions; C1.15 remains `not-evaluated` until external review.
- `tools/verify-production-deployment.ps1:89` -- reuse context refusal, evidence redaction, fault/recovery polling, and diagnostic patterns without treating the disposable lane as Production proof.
- `tests/tooling/access_telemetry_lifecycle/test_adapter_profile.py:1` -- existing offline fixture pattern; add exact retention/reclamation, failure/privacy, close-out inventory, staged-tree, and publish-integrity cases.
- `tests/Hexalith.Memories.IntegrationTests/Telemetry/AccessTelemetryLifecycleIntegrationCheckpointTests.cs:27` -- portable two-writer/fault/continuity baseline; use as lower-layer evidence, never a running-target substitute.
- `tests/Hexalith.Memories.Server.Tests/Architecture/AccessTelemetryRetentionDecisionTests.cs:23` -- extend structure-aware A41/runbook guards and leaked-markup rejection.
- `deploy/grafana/dashboards/memories-operability.json` -- add bounded, privacy-safe lifecycle panels; no lifecycle panels exist today.
- `deploy/kubernetes/overlays/production/access-telemetry-disabled-patch.yaml:1` -- read-only safety boundary during this run; lifecycle and clock remain zero replicas with an unproven profile.
- `docs/dev/telemetry.md:176` and `docs/operations/*.md` -- project implemented truth into one new neutral runbook, one PostgreSQL appendix, and narrow cross-links instead of duplicating procedures.
- `_bmad-output/implementation-artifacts/deferred-work.md:115` -- canonical open A41 residual plus DW-572/DW-578/DW-579/DW-584 reconciliation inputs; do not resolve before publish verification.
- `_bmad-output/implementation-artifacts/sprint-status.yaml` -- orchestrator-owned and always read-only; status/action rows are neither defects nor proof.

## Trusted Evidence Contract

- **Qualification transition.** The checked-in Production overlay, the real Production namespace, and the checked-in qualification overlay remain at zero lifecycle/clock replicas. A passing C1 packet authorizes only a time-bounded, non-Production qualification session in the exact approved target/profile. C1 itself records writes disabled and `qualification_authorized: true`; it never claims that Production writes are enabled. The host-side repository runner validates target/profile/source identity, required `kubectl auth can-i` permissions, and the named approval for shared `dapr-system` effects before it acquires one namespace-scoped Kubernetes Lease, scales the qualification lifecycle/clock workloads, and opens the expiring qualification gate. It records every transition, permits only one fault at a time, and restores the gate plus lifecycle/clock replicas to disabled/zero in `finally`. C2-C4 pass only when their packets prove the final disabled state; a stale Lease or expired gate rejects and is recovered explicitly rather than bypassed.
- **Pragmatic execution boundary.** The reviewed host-side Python producers own Kubernetes orchestration, replacement/fault observation, readiness/accounting, and PostgreSQL measurement through fixed `kubectl` argument vectors. They do not call a fictional or generic `/operations/qualification/{checkpoint}/{command}` lifecycle API and do not embed a Kubernetes client in an application. Server exposes only one qualification-gated fixed-workload surface, invoked separately inside each of the two named Server pods, which emits through the normal access-telemetry logging/delivery pipeline and returns measured attempted/acknowledged/conflicted accounting. It is unavailable outside the Qualification environment, rejects an absent/expired runtime gate or wrong profile, accepts no arbitrary rate/duration/tenant/content/command input, and has focused authorization/gating/pipeline tests.
- **Repository-owned producers.** C2, C3, and C4 are fixed scenarios implemented in this repository and selected by a closed command registry. The outer CLI invokes the reviewed producer itself; an operator supplies only an allowlisted scenario-input document and target identity, never an arbitrary executable, SQL, URL, or shell fragment. The packet binds the invoked producer's repository-relative path, full source commit, Git-blob SHA-256, command ID, redacted/allowlisted argument object, and argument hash. The source path and hash must match the actual invoked file at that commit. Raw argv, environment values, secret-shaped flag names, and opaque values are never copied into evidence.
- **Fixed observations.** The runner reads workload/replacement/readiness observations with exact `kubectl` calls and performs C3 tuple/allocator measurements by executing repository-owned, fixed aggregate-only SQL with `psql` inside the PostgreSQL pod; no SQL comes from scenario input. Physical reclamation is reported through a short-lived Dapr-enabled adapter reporter Job using the deployed reviewed image, `automountServiceAccountToken: false`, no Kubernetes RBAC, and the adapter-only Dapr identity. It submits only the fixed C3 aggregate evidence to the authenticated physical-evidence route. C3 uses an exclusive, append-only, resumable journal under the external evidence root so the 1h/24h/168h run can resume without accepting rewritten or skipped observations.
- **Bounded command capture.** Producers use streaming process capture with deterministic stdout/stderr byte limits and a bounded timeout, terminate the child on the first exceeded limit, and emit an immutable secret-safe rejection packet. Timeout and Git timeout exceptions are translated into the same bounded validation error path. A producer MUST execute, or directly control, every child command used to decide a checkpoint and create its own transcript; it MUST NOT promote operator-authored booleans, counters, timestamps, hashes, commit identifiers, SQL, or command metadata into `passed` evidence.
- **C1 predecessor.** C1 means the complete canonical 25-gate packet or 25 individually registered immutable successors. Every gate has a unique gate ID and its own attributable result plus source/artifact identity; one artifact/hash cannot discharge multiple gates. Every accepted source commit resolves locally, every artifact/source hash is recomputed from the bytes read, and every command result is inside the producer-controlled run. Packet start/finish/result times are integral UTC milliseconds, ordered, no more than one second in the future, no older than 15 minutes when accepted, and the run duration is no more than 24 hours. Boolean-as-integer and floating-point integer lookalikes fail closed.
- **C2 scenario.** The fixed workload is exactly two Server writers at the ADR cluster total of 250 accepted records/s while the component sustains at least 500 operations/s during purge, for a 30-minute steady-state window. The producer records per-writer attempted/acknowledged/persisted/conflicted counts; exact transaction acknowledgements; every Server, lifecycle-service, clock-service, Dapr-sidecar, actor-activation, Placement-member, Scheduler-member, and approved adapter replacement; and a non-empty command/result observation for each replacement. It proves zero loss of acknowledged records, reconstruction/reconnect, JSON-console continuity, and, when OTLP is configured, explicit OTLP continuity. An aggregate boolean or workload hash alone cannot pass.
- **C3 scenario.** Three distinct cohorts prove exact 1-hour, 24-hour, and 168-hour retention measured from `emittedAtUtc`. Each cohort binds an integer horizon, stable cohort ID, database/schema/table, emitted/accepted/expires/purged UTC milliseconds, pre/post tuple counts, expiry/purge/reclamation command IDs and output hashes, logical absence, preservation of named newer records, interruption/restart recovery, and physical bytes returned to the PostgreSQL allocator within 24 hours. Multi-day waiting occurs with the qualification gate disabled, and a final 125-record control proves newer data survives the 168-hour purge. The producer attributes every measurement to its command transcript; assertion booleans and one shared cohort cannot pass.
- **C4 scenario.** The closed failure inventory includes Dapr outage, application outage, state outage, clock outage, stale attestation, actor failover, reminder delay, queue-record exhaustion, queue-byte exhaustion, transaction/ETag/TTL rejection, capacity pressure, invalid configuration/key material, profile drift, reconnect, retry exhaustion, shutdown, degraded rollback, and the approved adapter fault. Each item has non-empty command/result observations and exact bounded accounting. The packet also proves JSON-console and optional-OTLP continuity, bounded signal/label sets, Unhealthy-first health precedence, NoData behavior, least-privilege inspection, raw-value absence, cross-tenant denial, privacy denial before dependency access, and absence of a tenant-facing read route.
- **Closed C0-C6 chain.** C0 validates the full adapter-profile packet, source, commands, immutable profile, and artifact hashes; C1 validates all 25 gates; C2-C4 validate the scenarios above; C5 is a named post-C2-C4 Platform Operations approval; C6 is a separately named post-C2-C4 security approval. C5 and C6 each bind the exact C0-C4 artifact hashes and profile hash, cannot share an approver, and cannot reuse the earlier C1 approvals. Terminal validation has its own closed schema, source/command/time validation, exact C0-C6 set, zero skips/failures, and recomputed hashes; three-field stubs never pass.
- **Evidence roots.** Live packets and snapshots reside under an explicit absolute `--evidence-root` outside the repository; all packet artifact paths are relative to that root and are rejected on traversal, symlinks, aliases, or escape. Reviewed source paths remain repository-relative and resolve through Git. Repository cleanliness therefore does not conflict with external immutable evidence. Packet writes are exclusive creation with closed schemas, canonical UTF-8, per-file limits, and a 4 MiB aggregate chain/snapshot limit.
- **A41 mutation integrity.** The verifier owns a non-empty exact semantic transition registry for every allowed A41 path; a manifest cannot weaken it with empty `required`/`forbidden` arrays. Preflight reads inventory and snapshot bytes once from the authenticated source HEAD Git blobs, verifies their hashes, enforces the aggregate bound before writing, records the exact index/worktree state, and protects historical Epic 20/Story 20.5 plus `sprint-status.yaml` bytes. Postflight accepts only the authentic preflight packet and exact allowed index/worktree mutation. Concurrent-edit, TOCTOU, staged-tree, and semantic drift reject without repairing files.
- **Remote publication.** Preflight binds the intended branch, exact remote name, and SHA-256 of the normalized remote URL. Publish verification revalidates those values, fetches the advertised branch into a temporary ref without mutating working files, proves the postflight commit is an ancestor of the fetched remote tip (including an advanced descendant), and rejects wrong/unfetched/remapped/unpublished commits. Repository tests use isolated local remotes; implementation performs no real push or other remote mutation.
- **Profile identity.** The approved `dc19485835a050395cf73238524d98d735dd84540cdb7cb938512e73c2a63d14` remains SHA-256 of the ADR-defined canonical identity/capability/workload object only. Rendered manifests and running node/storage/resource/TLS/image observations are separately hash-bound evidence; the appendix must not claim that those fields implicitly alter the canonical hash. A canonical-object change requires a governing decision and new approved hash.
- **Runtime observability.** Every dashboard series has a production code producer. Capacity utilization is retained-record count divided by the ADR admitted logical capacity (`250 × 3,600 × configured retention hours`) and is distinct from queue depth. A present physical-reclamation timestamp is accepted only through the adapter-evidence authority from verified C3 output; otherwise it is absent/NoData. Queue-oldest, attestation, purge-cohort, and physical-evidence ages are observable gauges derived at collection time from stored timestamps and a testable clock, so they advance while idle. Effective health is centrally aggregated with `Unhealthy > Degraded > NoData > Healthy`; runtime-gate expiry is evaluated when observed and cannot be overwritten by a later healthy subsystem update. Profile queries retain a healthy `matched` series and map only missing data to NoData. Queue record/byte/oldest gauges refresh after every mutation, including partial acknowledgement.
- **Verification.** Parse inputs fail closed for unknown fields, non-canonical UTF-8, oversized values, normalized secret aliases, invalid bounded labels, path aliases, non-finite numbers, and non-exact integer types. Every CLI mode runs through subprocess tests. Isolated-Git tests exercise the complete C0-C6/preflight/postflight/publish chain, remote-descendant success, and tampered hashes, stale/future time, dirty/staged/racing trees, wrong branch/remote URL, and unpublished commits. `MeterListener` tests collect each new instrument and prove advancing ages, profile match/expiry, health precedence, physical-evidence NoData/present, capacity utilization, and partial-ack queue refresh. Offline tests never contact a target.

## Tasks & Acceptance

**Execution:**
- [x] `tools/verify-access-telemetry-lifecycle.py`, `tools/verify_access_telemetry_lifecycle.py`, and repository-owned C2-C4 producer modules -- implement the closed command registry and host-side qualification runner: C1/profile/source/RBAC/shared-system preflight, namespace Lease, expiring gate, zero-to-running-to-zero scaling, fixed `kubectl` orchestration, one fault at a time, bounded streaming capture, actual-source identity, exact/fresh packet parsing, C3 append-only resume journal, scenario-specific proof, external evidence root, and immutable blocker/pass packets; remove every dependency on `/operations/qualification/{checkpoint}/{command}`.
- [x] `tools/verify_access_telemetry_lifecycle.py` close-out modes -- validate closed C0-C6 packets and post-evidence approvals; implement verifier-owned semantic mutations, single-read bounded Git snapshots, exact index/worktree postflight, preflight-bound remote identity, safe fetched-descendant publication proof, and bounded timeout translation.
- [x] `tests/tooling/access_telemetry_lifecycle/` -- invoke every CLI/producer through subprocesses and cover exact integer/time/schema/secret/output limits, 25 attributable C1 gates, all C2 replacements, three C3 cohorts, the full C4 inventory, C0-C6 stubs/tampering, evidence-root paths, TOCTOU/aggregate bounds, and isolated-Git remote descendant/remap/unpublished cases.
- [x] `src/Hexalith.Memories.AccessTelemetry*/**`, `src/Hexalith.Memories.Server/**`, and focused tests -- implement centrally aggregated effective lifecycle health, collection-time age/profile-expiry gauges, admitted logical-capacity utilization, adapter-authority physical-evidence ingestion, queue gauge refresh after every mutation, and the single qualification-only fixed-workload Server surface through the normal telemetry pipeline; prove the endpoint gate/accounting plus every instrument and precedence rule with focused tests, `MeterListener`, and a controllable clock.
- [x] `deploy/kubernetes/overlays/qualification/**` and deployment contract tests -- keep lifecycle/clock at zero by default; add only the narrow qualification configuration, namespace-scoped Lease/RBAC requirements for the external operator, and short-lived Dapr adapter reporter with no service-account token or Kubernetes RBAC; do not introduce a controller, CRD, long-lived qualification service, cluster-admin service account, or application-side Kubernetes client.
- [x] `docs/operations/access-telemetry-lifecycle.md`, `docs/operations/access-telemetry-adapter-production.md` -- publish the neutral runbook and exact PostgreSQL 18.4 appendix, including the non-Production qualification transition, repository-owned commands, external evidence root, profile-hash boundary, C0-C6 approvals, monitoring/NoData, physical proof, rollback, recovery, RPO/RTO limits, rotation, and decommissioning.
- [x] `docs/dev/telemetry.md`, `docs/operations/{deployment-configuration,capacity-planning,monitoring-alerting-thresholds,incident-response,failure-recovery,disaster-recovery,upgrade-migration,rate-limiting}.md` -- add narrow lifecycle projections/cross-links and reconcile rate-limit wording without changing Story 20.5 quota behavior.
- [x] `deploy/grafana/dashboards/memories-operability.json` and dashboard tests -- add privacy-safe queue/retry, live age, logical capacity, expiry/purge, physical evidence, aggregated health, and profile panels; keep `matched` visible, make only absence NoData, and bind every query to a real runtime producer.
- [x] `tests/Hexalith.Memories.Server.Tests/Architecture/AccessTelemetryRetentionDecisionTests.cs`, `tests/Hexalith.Memories.Server.Tests/Deployment/AccessTelemetryOperationsContractTests.cs`, and `tests/Hexalith.Memories.Server.Tests/Architecture/AccessTelemetryA41CloseOutTests.cs` -- structure-bind the corrected runbooks, immutable profile boundary, trusted evidence chain, open-until-published wording, and protected historical/orchestrator bytes.
- [x] `_bmad-output/implementation-artifacts/tests/27-4-retention-verification-evidence.md` -- create the one canonical C0-C6 matrix with repository validations recorded and every unexecuted live observation/approval explicitly `operator-pending`; templates are never executed evidence.
- [x] `_bmad-output/implementation-artifacts/deferred-work.md` and other A41 sources -- make no closure mutation without an authentic passing chain; in this repository-only run preserve `carried-forward`/open semantics and record no Production enablement.

**Acceptance Criteria:**
- Given an offline checkout with no target credentials, when focused tests run, then every fixed producer/validator, metric, runbook contract, dashboard binding, and pending canonical-matrix schema passes without target access, Production enablement, A41 closure, or `sprint-status.yaml` mutation.
- Given any incomplete C0-C6 chain, reused C1 artifact, stale/future/non-integral observation, unreviewed producer, oversized output, secret-shaped input, omitted replacement/fault/cohort, absent tenant denial, missing post-evidence approval, path escape, racing tree, or source/profile/remote mismatch, when a mode runs, then it emits a bounded immutable rejection packet, exits nonzero, restores the qualification target disabled, and leaves Production disabled and A41 open.
- Given a passing C1 packet and authorized exact-profile non-Production qualification target, when the fixed C2-C4 host-side producers run, then they acquire one bounded Lease, open only an expiring qualification gate, invoke the fixed workload separately on two Server pods through the real telemetry pipeline, execute only repository-owned PostgreSQL aggregate SQL, run one fault at a time, use the unprivileged Dapr reporter for physical evidence, attribute every command needed for the complete workload/retention/fault/privacy/observability proof, and finish with the gate disabled and lifecycle/clock replicas at zero.
- Given verified C0-C4 artifacts plus separate post-evidence C5/C6 approvals and a closed terminal packet, when preflight/postflight/publish verification runs, then only verifier-owned exact A41 semantics are eligible, the snapshot and index remain authentic and bounded, a fetched same-remote descendant proves publication, and historical Epic 20/Story 20.5 plus `sprint-status.yaml` remain byte-identical.
- Given idle queues, aging attestations/evidence, runtime-profile expiry, partial acknowledgements, or competing subsystem health updates, when metrics are collected, then ages advance from timestamps, the effective gate and Unhealthy-first precedence are current, every capacity/physical/profile series has its defined producer or explicit NoData, and no label contains tenant/content identity.

## Spec Change Log

- 2026-09-02 — Review loop 1 replaced the self-attested evidence design with a trusted-producer contract. Production modes now have to execute controlled commands or remain `operator-pending`; all source and artifact identity is recomputed; C1 means the complete 25-gate predecessor; C2-C4 schemas carry the scenario-specific proof; and A41 preflight, postflight, and remote publish verification form one immutable chain. The amendment also makes evidence writes non-overwriting, strengthens input/privacy/path parsing, binds dashboard semantics to runtime metrics, and requires subprocess plus isolated-Git end-to-end tests. Known-bad approaches avoided: accepting operator-authored pass flags, treating a synthetic C1 summary as discharge, snapshotting an already-mutated tree, accepting hand-written chain packets, equating a local commit with publication, or deriving evidence age from scrape time. KEEP: `sprint-status.yaml` read-only; Production disabled and A41 open; neutral runbook plus PostgreSQL appendix; bounded privacy-safe labels; canonical pending-state matrix; no live target, secret, or historical-record mutation during repository validation.
- 2026-09-05 — Review loop 2 found that the first re-derivation still had no repository-owned executable scenarios, did not bind the invoked producer, buffered unbounded output, under-specified C1/C2/C3/C4 and the C0-C6 approval chain, conflicted external evidence with repository cleanliness, admitted vacuous A41 semantics and snapshot/remote races, misstated the canonical profile-hash boundary, and exposed dashboard series without reliable producers or time/health semantics. The trusted-evidence contract and tasks now define an isolated non-Production qualification transition with guaranteed disable cleanup; a closed source-bound producer registry and streaming limits; exact fresh C1 gates, C2 replacements/workload, C3 cohorts/measurements, C4 faults/privacy; post-evidence C5/C6 approvals; external evidence roots; verifier-owned semantic mutations; single-read bounded snapshots; preflight-bound fetched publication; and collection-time, centrally aggregated metrics with explicit producers/NoData. Known-bad states avoided: claiming C1 requires enabled Production writes, accepting arbitrary `/approved-tools` commands or raw argv, trusting aggregates/booleans/stubs, storing live evidence in a clean repository, accepting empty semantic rules, checking an unfetched/remapped remote, changing the profile hash through out-of-object observations, freezing ages, or allowing healthy last-writer telemetry to hide an expired/unhealthy gate. KEEP: Dapr-only authority; immutable exclusive packets; exact pending C0-C6 matrix; Production disabled and A41 open outside the bounded qualification lane; neutral plus PostgreSQL runbooks; privacy-safe bounded labels; recomputed source/profile/workload hashes; protected Epic 20/Story 20.5 and `sprint-status.yaml` bytes; and the focused verification lanes that previously passed.
- 2026-09-05 — Accepted advanced elicitation made the qualification lane implementable without adding a privileged in-cluster control plane. A reviewed host-side Python runner now owns fixed Kubernetes actions and PostgreSQL aggregate measurement; Server receives only one qualification-gated fixed-workload emitter; physical proof crosses the existing Dapr authority boundary through a short-lived reporter with no Kubernetes token; and C3 continuity uses an external append-only resume journal. The overlay remains zero-by-default and the runner uses preflight RBAC/approval checks, one namespace Lease, an expiring gate, one fault at a time, and guaranteed scale-to-zero cleanup. Explicit non-goals are a CRD/controller, long-lived qualification service, cluster-admin service account, generic command endpoint, application-side Kubernetes client, or evidence database.

## Review Triage Log

- Review loop 1 — `intent_gap: 0`; `bad_spec: 9` (`high: 8`, `medium: 1`); `patch: 0`; `defer: 0`; `reject: 4` (`low: 4`). Addressed findings: (1, high) checkpoint decisions relied on self-attested JSON and an incomplete C1 aggregate; (2, high) source/artifact/command/time/type/privacy provenance was not independently verified; (3, high) C2-C4 omitted required scenario attribution and observations; (4, high) preflight did not authenticate the complete evidence/approval chain or start from a clean tree; (5, high) postflight did not bind an authentic preflight snapshot, index, and worktree; (6, high) publish verification bypassed the chain and treated a local commit as published; (7, high) evidence could be overwritten and A41 content reconciliation was only path-based; (8, medium) dashboard age/capacity/NoData semantics were not bound to runtime metrics; (9, high) CLI and close-out chain coverage lacked subprocess, negative, and remote-publication tests. Rejected as non-defects: the workflow-transient `in-review` status, absence of final operator actions before finalization, unchanged orchestrator bookkeeping, and minor wording/style observations made moot by the redesign. KEEP: the read-only sprint-status boundary, open A41/disabled Production posture, runbook split and cross-links, bounded privacy-safe dashboard intent, canonical pending evidence matrix, and the earlier focused Python/.NET verification targets.

### Review loop 2 finding verdicts

| Finding | Verdict | Verification evidence | Route |
| :------ | :------ | :-------------------- | :---- |
| BH-01 | false | The saved baseline predates unrelated committed BMAD and submodule work; `git diff HEAD` shows those files are not part of Story 27.4's worktree change. | reject |
| BH-02 | high | The runbook keeps Production disabled through C2-C6 while `_validate_predecessor` requires C1 to report writes enabled before C2; the qualification transition is undefined. | bad_spec |
| BH-03 | high | carried: only the generic verifier and an operator-supplied `/approved-tools/*-producer` example exist; no repository-owned C2-C4 scenario producer controls a target. | bad_spec |
| BH-04 | high | carried: source hashes may name any committed blob and are never matched to the executable or script in `producer_command`. | bad_spec |
| BH-05 | false | The trusted-producer contract explicitly makes a reviewed, identity-bound producer responsible for its child-command transcript; independent recapture of every child is not required. | reject |
| BH-06 | high | `subprocess.run(..., capture_output=True)` buffers producer stdout/stderr for up to an hour before the 1 MiB JSON bound is applied. | bad_spec |
| BH-07 | high | carried: C2 accepts a workload hash plus aggregate booleans/counts, without observed 500 events/s duration or per-replacement command/result attribution. | bad_spec |
| BH-08 | high | `REQUIRED_FAILURE_SCENARIOS` omits `application-outage` and `queue-record-exhaustion`, both required by the ADR and runbook. | bad_spec |
| BH-09 | high | carried: C3 accepts one cohort and assertion booleans without independently binding the three horizons, purge time, tuple accounting, and reclamation measurements to command outputs. | bad_spec |
| BH-10 | high | carried: the terminal set omits C6 and reuses C1 approvals made before C2-C5 rather than requiring post-evidence operations/security acceptance. | bad_spec |
| BH-11 | high | carried: C0, C5, and terminal accept three-field stubs; the passing end-to-end fixture proves no closed schema or command/source validation is required. | bad_spec |
| BH-12 | high | The runbook stores packets under `/approved-evidence`, but terminal validation requires every artifact to be repository-relative and inside a clean repository. | bad_spec |
| BH-13 | high | carried: mutation semantics allow empty `required` and `forbidden` arrays, making semantic close-out verification vacuous. | bad_spec |
| BH-14 | maybe-false | Same-packet SHA-256 proves consistency, not cryptographic origin, but the approved evidence-author threat model is unspecified; a signing/trust-root decision would settle whether this is a high defect. | defer (high if verified) |
| BH-15 | high | Remote name and URL are selected only at publish time, so a newly configured local remote can satisfy containment without being the preflight-approved authority. | bad_spec |
| BH-16 | high | Descendant containment runs `merge-base` against an advertised remote OID that may not exist locally, falsely rejecting a valid advanced branch. | bad_spec |
| BH-17 | medium | The appendix says node/storage/resources/TLS and image changes alter the profile hash, while the canonical hash object omits most of those fields. | bad_spec |
| BH-18 | medium | The pre-existing verifier enforces durability multiplier `2` while the governing ADR and new appendix require `1`; operators can receive a false capacity rejection. | defer (pre-existing) |
| BH-19 | medium | Every `RecordCapacity` caller omits admitted capacity, so the newly added utilization gauge remains absent and its panel cannot observe thresholds. | bad_spec |
| BH-20 | medium | No runtime caller records physical evidence as present with a timestamp, leaving the new last-evidence gauge permanently absent. | bad_spec |
| BH-21 | medium | The profile-drift query filters out `matched` while mapping an empty result to Unhealthy/NoData, so a healthy profile looks unhealthy. | patch |
| BH-22 | medium | carried: attestation, queue-oldest, and purge-cohort ages are stored snapshots instead of being recomputed from timestamps when observed, so they freeze between events. | bad_spec |
| BH-23 | high | `Current` fails an expired runtime gate closed, but metrics update only in `Publish`, allowing stale matched/healthy telemetry after authorization expires. | bad_spec |
| BH-24 | high | Gate and processor health overwrite one shared value; a later Healthy processor update can erase an Unhealthy capability result, violating required precedence. | bad_spec |
| VG-01 | medium | Pre-verified: existing tests observe only the records counter and do not collect the new profile/health gauges, so inverted or missing measurements survive. | patch |
| VG-02 | medium | Pre-verified: the publish test pushes the exact close-out head and never exercises the descendant-containment branch. | bad_spec (grouped with BH-16) |
| VG-03 | medium | The complete call search confirms no caller supplies `admittedCapacity`; the utilization series has no producer. | bad_spec (grouped with BH-19) |
| VG-04 | high | `Current` can synthesize expiry without invoking either recorder, so the dashboard can disagree with the effective gate. | bad_spec (grouped with BH-23) |
| EC-01 | high | All 25 C1 gates may reuse one artifact path/hash even though the contract requires individually attributable evidence. | bad_spec |
| EC-02 | high | C2 records only `otlp_configured`; configured OTLP continuity is never required or represented. | bad_spec |
| EC-03 | medium | JSON floats `1.0`, `24.0`, and `168.0` compare equal to the integer list and pass because only Boolean elements are rejected. | bad_spec (grouped with BH-09) |
| EC-04 | high | Checkpoint timestamps are checked only for order and a 24-hour span, never for freshness or future skew relative to verification time. | bad_spec |
| EC-05 | high | The executed producer is not required to be one of the recomputed reviewed source paths. | bad_spec (grouped with BH-04) |
| EC-06 | high | Producer output is fully buffered before bounded parsing. | bad_spec (grouped with BH-06) |
| EC-07 | high | The outer command argv is copied verbatim into immutable evidence; secret flags and separate opaque values evade the value regex. | bad_spec |
| EC-08 | high | C0, C5, and terminal artifacts have no checkpoint-specific closed-schema validator. | bad_spec (grouped with BH-11) |
| EC-09 | high | Terminal approvals carry no hashes for the C2-C5 artifacts they purport to accept. | bad_spec (grouped with BH-10) |
| EC-10 | high | Empty semantic fragment lists are accepted. | bad_spec (grouped with BH-13) |
| EC-11 | medium | Inventory hashes and worktree snapshot bytes are gathered separately and never compared, so a concurrent edit can corrupt the recovery baseline. | bad_spec |
| EC-12 | medium | Preflight writes snapshots without enforcing the 4 MiB aggregate bound that postflight later requires. | bad_spec (grouped with EC-11) |
| EC-13 | high | The authoritative remote identity is not bound at preflight. | bad_spec (grouped with BH-15) |
| EC-14 | high | An unfetched descendant remote OID makes local `merge-base` fail even when containment is true. | bad_spec (grouped with BH-16) |
| EC-15 | medium | `_git_checked` does not translate `TimeoutExpired`; callers therefore cannot emit the required bounded rejection packet. | patch |
| EC-16 | high | Expiry changes the effective gate without refreshing profile or health metrics. | bad_spec (grouped with BH-23) |
| EC-17 | high | Last-writer-wins health recording does not implement Unhealthy-first aggregation. | bad_spec (grouped with BH-24) |
| EC-18 | medium | Queue oldest age is calculated only during enqueue/drain activity and does not advance while an idle queue ages. | bad_spec (grouped with BH-22) |
| EC-19 | medium | Attestation age is calculated once at receipt and remains near zero if refresh stops. | bad_spec (grouped with BH-22) |
| EC-20 | medium | Partial acknowledgements mutate the queue but refresh gauges only on a full-success response, leaving stale record/byte/age values. | patch |
| EC-21 | medium | Capacity utilization has no caller-supplied denominator. | bad_spec (grouped with BH-19) |
| EC-22 | medium | The physical-evidence timestamp instrument has no present-evidence ingestion path. | bad_spec (grouped with BH-20) |
| EC-23 | high | No executable repository-owned scenario producers were added; fixture-printing demonstrates only parser acceptance. | bad_spec (grouped with BH-03) |

Review loop 2 grouped the surviving findings into trusted-producer identity/bounds, complete C1-C6 evidence, C2-C4 schema attribution, A41 path/snapshot/remote integrity, immutable profile consistency, and runtime observability root causes. `bad_spec` entries take precedence; patch and defer routes are moot until re-derived code is reviewed again. KEEP: the Dapr-only authority boundary; immutable exclusive packets; exact pending C0-C6 matrix; Production disabled and A41 open outside an explicitly bounded qualification transition; neutral plus PostgreSQL runbooks; privacy-safe bounded labels; source/profile/workload hash recomputation; protected Epic 20/Story 20.5 and sprint-status bytes; and the passing focused verification lanes.

### Review loop 3 finding verdicts

| Finding | Verdict | Verification evidence | Route |
| :------ | :------ | :-------------------- | :---- |
| BH3-01 | high | Kubernetes projects a ConfigMap key through a symlink, while `AccessTelemetryQualificationGate` rejects any non-null `LinkTarget`; the deployed route is therefore closed. | bad_spec |
| BH3-02 | high | C0's adapter-profile path requires at least one lifecycle replica, but the qualification lane is zero until a later C1-authorized transition. | bad_spec |
| BH3-03 | high | A 45-minute renewal command exists but C2 and C4 never invoke it; their bounded work can outlive the initial Lease/gate. | bad_spec |
| BH3-04 | high | Qualification identity checks only five Kubernetes objects and does not bind the Server images, Dapr component/configuration, or workload identity used for the first write. | bad_spec |
| BH3-05 | medium | A killed owner cannot be recovered by `--disable-only` until its Lease expires, so replicas can remain nonzero for the remaining Lease duration even though the gate eventually expires. | bad_spec |
| BH3-06 | medium | Cleanup validates only the ConfigMap API object, not the projected gate observed by the Server pods; a stale mounted gate can outlive the claimed final state. | bad_spec |
| BH3-07 | high | Preflight checks only three permissions, while later fixed commands require ConfigMap, exec/log, workload, Job, and shared-system mutations after the target is enabled. | bad_spec |
| BH3-08 | medium | The namespace Role grants unnecessary create/delete verbs across all pods, ConfigMaps, and Leases rather than the smallest fixed runner surface. | bad_spec |
| BH3-09 | high | Delivery-worker expiry removes qualification records without updating qualification accounting, so exact fault-lane disposition can never reconcile. | bad_spec |
| BH3-10 | high | The validator requires rejection/drop in every C4 lane, which rejects valid zero-loss reconnect, failover, and controlled-shutdown recovery. | bad_spec |
| BH3-11 | high | C4 labels `/ready` as a business request and hard-codes its result; no fixed authenticated business operation is measured. | bad_spec |
| BH3-12 | high | Audit continuity accepts any JSON-looking recent log plus any metric sample and does not bind either observation to the fault interval or access telemetry. | bad_spec |
| BH3-13 | high | Configured OTLP continuity is inferred from Prometheus samples without observing an OTLP collector or exported item. | bad_spec |
| BH3-14 | high | The privacy lane runs local tests and synthesizes zero target dependency calls; it never observes a deployed tenant-mismatch denial. | bad_spec |
| BH3-15 | high | `alerts_passed` is derived from current metrics and local test names without observing an alert-rule state transition. | bad_spec |
| BH3-16 | high | C2 hard-codes 30 minutes and derives component operations/s from record totals; command time and component/purge counters do not substantiate either claim. | bad_spec |
| BH3-17 | high | Replacement validation never proves every replacement interval overlaps both writer intervals. | bad_spec |
| BH3-18 | medium | Process-global accounting attributes all concurrent typed access events to the qualification delta, so unrelated traffic can corrupt exact workload counts. | bad_spec |
| BH3-19 | high | A lost response causes an unmeasured segment retry even if the first segment persisted, allowing uncounted duplicate workload records. | bad_spec |
| BH3-20 | high | Resumed journal entries are matched only by command ID and are not bound to target, profile, workload, reviewers, or producer source. | bad_spec |
| BH3-21 | high | C3 SQL measures PostgreSQL record rows and its TTL index, not the lifecycle expiry catalog, Dapr Delete, and strong absent read required for logical proof. | bad_spec |
| BH3-22 | high | Newer-record aliases are regenerated from row ordinals and represent only 16 of 125 rows, so a different survivor set can satisfy the same aliases. | bad_spec |
| BH3-23 | high | The reporter receipt hashes one post-VACUUM aggregate, not the immutable cohort/transcript evidence described by the receipt contract. | bad_spec |
| BH3-24 | medium | Cohort packets omit the configuration epoch, actor epoch, profile hash, and workload hash required by the runbook. | bad_spec |
| VG3-01 | medium | Pre-verified: existing tests map the endpoint in a custom app or fake kubectl; deleting the real `Program` mapping leaves them green. | patch |
| VG3-02 | high | Pre-verified: neither runner tests nor C2 validation require the writer observations to span a 30-minute steady-state window. | bad_spec |
| VG3-03 | medium | Pre-verified: no test supplies a current wrong-profile gate, so deleting the profile comparison is not detected. | patch |
| VG3-04 | medium | Pre-verified: aggregate accounting is unit-tested separately, but preflight has no oversized nested-artifact rejection test. | patch |
| VG3-05 | high | The ConfigMap symlink incompatibility is confirmed by the mounted volume and regular-file-only tests. | bad_spec (grouped with BH3-01) |
| VG3-06 | high | Terminal validation re-runs the 15-minute C1 freshness rule after C3's mandatory 168-hour observation, so the chain deterministically expires. | bad_spec |
| EC3-01 | high | ConfigMap projection and the gate's `LinkTarget` rejection make the deployed endpoint unavailable. | bad_spec (grouped with BH3-01) |
| EC3-02 | high | The Server gate has no upper expiry bound; a manually patched far-future gate violates the promised bounded authorization. | bad_spec |
| EC3-03 | high | `retention-controls` reads a nonexistent ConfigMap form of the Dapr Configuration and sends the app token under the wrong header name. | bad_spec |
| EC3-04 | high | C1's 15-minute acceptance freshness is re-applied after the seven-day C3 wait. | bad_spec (grouped with VG3-06) |
| EC3-05 | high | Terminal validation also requires C2-C4 wrapper capture times to be fresh at close-out, making sequential retained evidence expire. | bad_spec |
| EC3-06 | high | The defined renewal path has no C2/C4 caller. | bad_spec (grouped with BH3-03) |
| EC3-07 | false | Another runner cannot acquire the Lease until cleanup's release step, which follows gate disable and scale-to-zero; the cited ordering does not disable an already-active successor. | reject |
| EC3-08 | high | Runtime image/component/configuration drift is not re-authenticated before enable and first write. | bad_spec (grouped with BH3-04) |
| EC3-09 | high | The normal Server deployment contains `ConnectionStrings__redis`; substring scanning therefore reports a direct backend dependency and makes C2 reject. | bad_spec |
| EC3-10 | high | A persisted segment whose response is lost is retried without an identity reconciliation mechanism. | bad_spec (grouped with BH3-19) |
| EC3-11 | medium | Per-segment acknowledgement delay and process overhead introduce gaps, while evidence reports a fixed 30-minute 250/s window from totals alone. | bad_spec (grouped with BH3-16) |
| EC3-12 | high | Sequential replacements can continue after either writer future completes because no overlap assertion exists. | bad_spec (grouped with BH3-17) |
| EC3-13 | high | Replacement loss uses total retained counts while writers insert concurrently, so inserts can mask acknowledged loss. | bad_spec |
| EC3-14 | high | Ordinal aliases do not bind the identity of the 125 newer control records. | bad_spec (grouped with BH3-22) |
| EC3-15 | medium | The journal's internal hash chain can be recomputed offline; this conflicts with the spec's statement that rewritten observations are not accepted, although the overall assurance boundary disclaims tamper evidence. | bad_spec |
| EC3-16 | high | C4 derives business continuity from readiness without a business request. | bad_spec (grouped with BH3-11) |
| EC3-17 | high | Requiring fail-closed loss for every failure scenario rejects successful zero-loss recovery lanes. | bad_spec (grouped with BH3-10) |
| EC3-18 | high | Prometheus availability is not evidence of configured OTLP delivery. | bad_spec (grouped with BH3-13) |
| EC3-19 | medium | Observability accepts any non-empty health-state set and does not require exactly one current effective state. | bad_spec |
| EC3-20 | medium | Aggregate budget accounting stats a path and later hash/parsing reopens it, leaving a replace/grow race between bound and consumption. | bad_spec |
| EC3-21 | high | Reporter validation accepts any non-placeholder digest rather than the image digest approved by C1. | bad_spec |
| EC3-22 | medium | If the reporter Job completes before its journal append, the existing Job cannot be rerun and current RBAC cannot perform the bounded recreation needed for resume. | bad_spec |
| EC3-23 | high | `ThreadPoolExecutor` waits for both 30-minute writers before leaving its context after a replacement error, delaying the `finally` cleanup. | bad_spec |
| EC3-24 | high | Expired queue records update lifecycle metrics but not qualification disposition accounting. | bad_spec (grouped with BH3-09) |
| EC3-25 | medium | C3 validation requires `accepted <= expires` but does not require `emitted <= accepted`, so impossible timing passes. | patch |
| EC3-26 | medium | The separate-open stat/read sequence confirms the claimed racing-tree coverage is incomplete for terminal evidence. | bad_spec (grouped with EC3-20) |
| EC3-27 | high | Regular temporary files in tests do not exercise the deployed ConfigMap projection. | bad_spec (grouped with BH3-01) |
| EC3-28 | high | The nonexistent ConfigMap and wrong header falsify the claim that the controlled C3 producer is executable. | bad_spec (grouped with EC3-03) |
| EC3-29 | medium | An exclusive writer lock prevents concurrent mutation only while owned; it does not support the stronger no-rewrite claim across resumptions. | bad_spec (grouped with EC3-15) |
| EC3-30 | false | As with EC3-07, the release is the last mutation in the disable vector, so the cited race cannot disable an already-owned successor session. | reject |

Review loop 3 groups the surviving findings into executable qualification identity/authorization, time and crash semantics, C2 exact workload/replacement accounting, C3 resumable logical/physical evidence, C4 target-observed behavior, and terminal immutable-evidence validation. The `bad_spec` groups take precedence, so direct test patches are moot until the implementation is re-derived. KEEP: the reviewed host runner rather than an in-cluster controller; the single no-input qualification endpoint; fixed repository-owned kubectl/SQL actions; the short-lived unprivileged Dapr reporter; the external resumable journal; zero-by-default Production and qualification overlays; open A41 and read-only sprint status; privacy-safe bounded evidence; the canonical pending matrix; and all already passing focused verification lanes.

## Design Notes

The repository change produced by this run prepares and verifies evidence machinery; it is not the A41-closing commit. Close-out is a second, tightly bound mutation after live evidence and post-evidence approvals exist. The qualification lane uses the exact approved deployment profile in a non-Production namespace and always restores lifecycle writes disabled; it never weakens the checked-in Production zero-replica overlay. The canonical matrix distinguishes `repository-validated`, `operator-pending`, `passed`, and `rejected`; only authentic `passed` external checkpoints satisfy C1-C6.

## Verification

**Commands:**
- `PYTHONHASHSEED=0 python3 -m unittest discover -s tests/tooling/access_telemetry_lifecycle -p 'test_*.py' -v` -- expected: exact fixture inventories pass with zero failures/errors/skips.
- `dotnet build tests/Hexalith.Memories.Server.Tests/Hexalith.Memories.Server.Tests.csproj --configuration Release --disable-build-servers -m:1 /nr:false -p:NuGetAudit=false -p:MinVerVersionOverride=1.0.0` -- expected: zero warnings/errors.
- `DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.Server.Tests/bin/Release/net10.0/Hexalith.Memories.Server.Tests.dll -class Hexalith.Memories.Server.Tests.Architecture.AccessTelemetryRetentionDecisionTests -parallel none -noLogo` -- expected: nonzero exact discovery, zero failed/errors/skipped/not-run.
- `git diff --check` -- expected: no whitespace errors; `sprint-status.yaml` absent from the changed path set.
