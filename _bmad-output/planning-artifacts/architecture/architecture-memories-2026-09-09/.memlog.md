---
scope: Hexalith.Memories product architecture
purpose: Build substrate for epics and implementation units
altitude: feature
updated: 2026-09-14T09:20
---

- (event) Architecture update started by migrating the legacy 2026-03-24 architecture.md; no architecture-specific memlog existed, so the rendered legacy artifact is source evidence rather than decision authority.
- (constraint) Deliverable is the architecture spine only; scope is the current Hexalith.Memories product, reconciled against the current PRD and codebase at feature altitude.
- (decision by architecture-update) AD-1 adopts Hexalith.Memories as a technical platform module: AppHost, ServiceDefaults, deployment composition, and reusable client/contract packages are intentional platform responsibilities. Binds project and deployment placement; prevents domain modules from depending on orchestration hosts; rule: product consumers depend on published contracts, clients, and Aspire integration packages, never AppHost or Server.
- (decision by architecture-update) AD-2 adopts Hexalith.EventStore as domain mutation truth while separating three contracts: domain command/event truth, future CloudEvent indexing and causal integration, and source-versus-package build evidence. Binds accepted mutations and replay; prevents Redis, FalkorDB, workflow state, or package topology from becoming domain truth; rule: acknowledge a domain mutation only after EventStore acceptance and rebuild every derived store from authoritative events.
- (decision by architecture-update) AD-3 adopts source-versioned rebuildable projections in Redis and FalkorDB with Dapr-owned orchestration. Binds ingestion completion and recovery; prevents partial fan-out from being reported as Indexed; rule: mark Indexed only when syntactic, vector, and graph projections acknowledge the same source version, otherwise remain Projecting or Failed with actionable state.
- (decision by architecture-update) AD-4 adopts Dapr workflows as deterministic process managers, activities as idempotent external-I/O boundaries, and actors only for serialized stateful concerns. Binds retries, recovery, and concurrency; prevents actors from becoming queues or workflow code from performing nondeterministic I/O; rule: capture mutable inputs at start and make each activity safe under at-least-once execution.
- (decision by architecture-update) AD-5 adopts exact, case-sensitive tenant identity from normalized authenticated claims plus an explicit system-principal allowlist, with backend-scoped credentials as the target isolation boundary. Binds every API, Dapr ingress, Redis, and FalkorDB operation; prevents caller-supplied tenant or case metadata and key prefixes from serving as authorization; rule: authorize before infrastructure access and resolve tenant-scoped backend credentials server-side.
- (decision by architecture-update) AD-6 adopts tenant lifecycle workflows as the sole owner of tenant indexes, graphs, backend identities, verification, and cleanup. Binds provisioning, deprovisioning, and repair; prevents ingestion hot paths or application startup from implicitly creating tenant resources; rule: lifecycle changes are explicit, idempotent, observable, bounded, and compensating.
- (decision by architecture-update) AD-7 adopts an adapter-level infrastructure boundary: Dapr APIs for state, pub/sub, actors, workflows, secrets, and conversation; direct typed clients only inside approved Redis search/vector and FalkorDB graph adapters, with a narrow atomic Redis reservation exception. Binds infrastructure access and test seams; prevents arbitrary data-plane clients in endpoints, workflows, domain code, or contracts; rule: inject keyed abstractions and keep provider SDK types behind adapter interfaces.
- (decision by architecture-update) AD-8 adopts independent syntactic, semantic, and graph retrieval followed by deterministic weighted reciprocal-rank fusion. Binds result ranking; prevents a missing graph start node from silently disabling required hybrid retrieval; rule: use k=10 and default weights 0.30/0.35/0.35, natural-language reranking at 0.20 and default-off, tie-break by opaque MemoryUnitId, and auto-seed graph traversal from the union of top-five syntactic and semantic hits when no explicit start is supplied.
- (decision by architecture-update) AD-9 adopts bounded, tenant-scoped, parameterized graph traversal with phase-scoped edge semantics. Binds graph construction and query safety; prevents user values in query text, unbounded traversal, cross-tenant paths, and premature automatic causal edges; rule: enforce tenant scope across the complete path, closed relationship labels, server-side depth/result/time limits, P1 explicit edges, and P1.5 EventStore-derived causal edges.
- (decision by architecture-update) AD-10 adopts Contracts.V1 plus the Evidence Packet as the cross-surface semantic contract for REST, Dapr invocation, CLI, MCP, and future Web hosting. Binds identifiers, routes, statuses, evidence, and errors; prevents surfaces from inventing divergent semantics or leaking persistence models; rule: evolve V1 additively, keep contracts persistence-neutral, and preserve deliberate legacy JSON names until a versioned break.
- (decision by architecture-update) AD-11 adopts immutable provenance and opaque identity end to end. Binds origin, actor, confidence, source, freshness, explanation, and MemoryUnitId handling; prevents external callers from asserting trusted actor identity or code from parsing IDs for GUID, ULID, or time semantics; rule: bind external actor provenance to normalized subject claims, allow system origins only through an allowlist, and authorize before explaining evidence.
- (decision by architecture-update) AD-12 adopts tenant-scoped provider and schema evolution through versioned create-backfill-verify-switch-retire migrations. Binds embedding model/dimension changes, key/index families, and provider replacement; prevents in-place destructive migration and provider SDK leakage; rule: maintain active and staging versions, reindex the full tenant corpus before atomic activation, and keep embeddings and LLM implementations behind strategies.
- (decision by architecture-update) AD-13 adopts OpenBao through Dapr secret stores as the product secret boundary, with separate bootstrap, application, and operator scopes. Binds runtime secret acquisition and rotation; prevents secrets in source, settings, logs, contracts, and direct Vault SDK usage; rule: applications request named secrets through Dapr and deployment supplies only the minimum bootstrap material.
- (decision by architecture-update) AD-14 adopts access telemetry as bounded infrastructure telemetry with an independent owner and lifecycle, not a product audit trail, and evidence-qualified observability claims. Binds activation, sanitization, trace continuity, and operational evidence; prevents telemetry failure from corrupting domain outcomes or unverified production claims; rule: writes are non-blocking and sanitized while activation gates fail closed until retention, durability, and delivery evidence is approved.
- (decision by architecture-update) AD-15 adopts centrally pinned dependencies and independent source-mode and package-mode release evidence. Binds reproducible builds, package inventory, and protected releases; prevents local version drift or one evidence lane standing in for the other; rule: take versions from the selected Hexalith.Builds catalog, package only tools/release-packages.json entries, and require pristine restore/build/contract/integration evidence for each supported lane.
- (constraint by architecture-update) Current code does not yet route ordinary ingestion through EventStore before workflow scheduling, does not keep incomplete three-axis projection out of Indexed, accepts caller-controlled IngestedBy, and does not create tenant backend ACL principals. The spine records the adopted target and these are implementation gaps, not alternative architectural decisions.
- (constraint by architecture-update) Kubernetes manifests do not deploy EventStore although AppHost composes an EventStore gateway. Production must supply a version-compatible external gateway or add an owned deployment before qualification.
- (version by architecture-update) .NET SDK 10.0.400, net10.0, and C# 14 are pinned by global.json and Directory.Build.props; official support verification: https://dotnet.microsoft.com/en-us/platform/support/policy/dotnet-core
- (version by architecture-update) Aspire 13.5.3, Dapr .NET SDK 1.18.5, CommunityToolkit Aspire Dapr 13.5.0-preview.1.260825-0345, Hexalith.EventStore 3.103.0, Redis Stack Server 7.4.0-v8, NRedisStack 1.7.4, StackExchange.Redis 3.1.31, FalkorDB 4.12.0, NFalkorDB 1.2.0, OpenBao 2.6.0, MCP SDK 2.2.0, Kreuzberg 4.10.2, FrontComposer 4.4.0, Fluent UI Blazor 5.0.0-rc.5-26219.1, and OpenTelemetry 1.18.0 are repository-pinned; public package versions were cross-checked against official NuGet package records.
- (deferred by architecture-update) Requalify or replace Redis as the production Dapr actor/workflow state store before production launch or SLO approval because current Dapr guidance does not recommend Redis for production actor state. Revisit when selecting the production state-store durability tier.
- (deferred by architecture-update) A Python/Dapr Agents sidecar remains optional rather than architectural. Revisit only when a selected feature requires a non-.NET agent runtime and supplies ownership, deployment, and contract evidence.
- (deferred by architecture-update) Cross-case references, a hosted product Web surface, generated OpenAPI, alternate search/graph backends, and multi-region topology remain deferred. Revisit only through their PRD phase gates, measured capacity/SLO evidence, or an approved feature spec.
- (reconciliation by reviewer-gate) Reviewer gate returned REVISE/FAIL/CHANGES REQUIRED. Accepted all critical and high convergence findings; the spine remains draft until corrected and re-reviewed.
- (decision by reviewer-gate) AD-3 projection completion is owned by one durable Dapr-state checkpoint per tuple (tenantId, caseId, memoryUnitId, authoritative EventStore sourceVersion, schemaGeneration, embeddingConfigurationEpoch). SourceVersion is not a workflow attempt. Per-axis acknowledgements advance monotonically with ETag/CAS, stale acknowledgements are rejected, and replay/repair uses the same protocol.
- (decision by reviewer-gate) AD-4 durable idempotency uses the accepted immutable request identity: V1 ingestion IdempotencyToken or CloudEvent id, namespaced by tenant and case, at EventStore/workflow acceptance; every projection is an idempotent upsert for its projection tuple. Redis preflight reservation is a fail-open admission optimization only, is released when scheduling fails, and otherwise expires by configured TTL.
- (decision by reviewer-gate) AD-5 separates user, workload, and channel authorization. REST/CLI and MCP preserve validated bearer tenant and subject authority; trusted internal callers require deny-by-default Dapr workload policy plus app-token channel protection and an operator-owned finite app-id to system-principal and tenant-grant mapping. Tenant and case IDs are validated opaque tokens compared ordinally without case folding or Unicode normalization after issuance.
- (decision by reviewer-gate) AD-5 also fixes backend and case isolation: Redis uses per-tenant ACL principals resolved server-side; FalkorDB selects a separate tenant database/graph; every MemoryUnit has exactly one authoritative case and all search seeds, graph nodes/edges, mutations, annotations, and deletions enforce that case. Cross-case references remain forbidden until explicitly adopted.
- (decision by reviewer-gate) AD-7 makes Server and EventStore integration the physical adapter assemblies but confines provider SDK references to composition roots and named internal Adapters.Redis or Adapters.FalkorDb namespaces, enforced by architecture tests. Direct Redis atomics are allowed only behind ports for reservations/idempotency, leases/fences, registries, and migration CAS/transactions when Dapr cannot express the primitive; Hexalith.Memories.Redis stays compatibility-only.
- (decision by reviewer-gate) AD-8 exact fusion convention: raw RRF contribution 1/(10+rank), tied raw axis scores share rank, candidates deduplicate by exact MemoryUnitId, composite normalizes by top-rank contribution times weights of axes that returned results, sort descending score then ascending StringComparer.Ordinal ID. Graph auto-seed union top-five syntactic/semantic with traversal depth at most 2. Query outage serves any safe available axis with Evidence Packet degradation; ingestion completion still requires all axes.
- (decision by reviewer-gate) AD-9 Phase 1 graph sources are contains, annotates, references from explicit links or AI-inferred content similarity, and caused_by/correlated_with only from supplied ingestion metadata. Automatic EventStore stream causal population and dual event embeddings activate in Phase 1.5. A graph kill switch remains required.
- (decision by reviewer-gate) AD-10 capability subsets may vary operations, never trust-envelope semantics. Every evidence-bearing surface carries tenant/case scope, source/origin, confidence and per-axis contribution, degradation/excluded axes, omitted-detail handles, freshness, and recovery with identical meaning. MCP and EventStore CloudEvent capabilities remain inactive until Phase 1.5 L1-L3 gates pass; deployment assets may exist earlier.
- (decision by reviewer-gate) AD-16 adopts EventStore crypto-shredding for tenant erasure: tenant deletion is incomplete until projections and access-telemetry mappings are purged and EventStore tenant keys are irreversibly invalidated/deleted with verification. Retain content-free tombstone/evidence to reject replay and ID reuse; restored backups quarantine unreadable tenant payloads and never rehydrate them. Current integration is an implementation gap.
- (decision by reviewer-gate) AD-17 adopts tenant-partitioned admission, concurrency, and backpressure. Per-tenant and global quotas have separate owners; bounded durable queues and workflow timers honor provider Retry-After; migrations/repair cannot starve interactive work; numeric budgets remain in PRD/configuration and require noisy-neighbor/capacity evidence.
- (constraint by reviewer-gate) OpenBao 2.6.0 and chart 0.28.5 are accurate repository pins but are not production-qualified because upstream security-fixed 2.6.2 and chart 0.28.6 are available. Production qualification is blocked until upgrade and requalification or an explicitly approved time-bounded security exception. Sources: https://github.com/openbao/openbao/blob/main/CHANGELOG.md and https://github.com/openbao/openbao-helm/releases
- (constraint by reviewer-gate) Redis Stack 7.4.0-v8 is the repository production pin but its maintenance line ended; qualify Redis 8 migration before the next production support window. FalkorDB 4.12.0 is qualified-but-behind upstream and needs an upgrade trigger. Sources: https://github.com/redis-stack/redis-stack and https://github.com/FalkorDB/FalkorDB/releases
- (reconciliation by reviewer-gate) Correct V1 status wire names to queued/extracting/embedding/indexing/indexed/failed; map product semantics pending to queued and projecting to indexing; reserve renaming for a versioned breaking-change decision.
- (reconciliation by reviewer-gate) Current additional gaps accepted into the ledger: missing EventStore replay-to-all-projections, provider SDK leakage outside target adapter namespaces, direct Kubernetes Redis/Falkor secrets, incomplete source-lane tests, floating local/Aspire images, missing graph auto-seeding and kill switch, incomplete G1 control/harness, non-runnable Web conformance specimen, and absent EventStore crypto-shredding integration.
- (reconciliation by reviewer-gate) OpenBao topology wording corrected to three-voter pod/process failover on one Kubernetes node; no node or site HA claim. The real traceability companion is _bmad-output/test-artifacts/traceability/traceability-matrix.md.
- (reconciliation by reviewer-gate) Second reviewer pass resolved the initial critical/high findings but found four integrity ambiguities plus one ranking ambiguity; spine remains draft.
- (decision by reviewer-gate) AD-7 distinguishes query scope from ownership: case-scoped queries filter all candidates/results/paths to one case; tenant-wide FR34 search may return independently ranked units from multiple cases with mandatory attribution, but graph seeding/traversal partitions by each seed's authoritative case and never crosses cases.
- (decision by reviewer-gate) AD-4 CloudEvent durable identity is (tenantId, caseId, canonical CloudEvent source, CloudEvent id); V1 IdempotencyToken is additionally scoped by command/operation. Redis preflight remains advisory.
- (decision by reviewer-gate) AD-16 tenant deletion records a durable access-telemetry erasure mapping/handoff but does not purge retained opaque telemetry early or wait for normal TTL expiry. Retained telemetry follows AD-17 TTL/purge; accelerated early purge requires a separate adopted operation.
- (decision by reviewer-gate) AD-8 finite direct-Redis exception registry contains only IPreflightDedupStore TryReserveAsync/ReleaseAsync using tenant/case-scoped SET NX plus TTL, fail-open to durable EventStore/workflow dedup, best-effort release, and race/outage evidence. Search/vector provider data-plane work remains direct only behind named adapters; coordination registries, leases, fences, and migration state use Dapr state unless a new AD adds a finite exception.
- (decision by reviewer-gate) AD-9 ranking preprocesses each axis by dropping blank IDs and non-finite scores, retaining the first provider-ordered occurrence of an exact ID, then assigns competition ranks (1,1,3). Null means unavailable; empty means available with no hits; both contribute no scoring denominator weight but remain distinct in Evidence Packet health. This is target behavior and current preprocessing differences are an alignment gap.
- (decision by reviewer-gate) AD-9 raw-score ties use exact finite Double.Equals semantics with no epsilon. For tenant-wide graph search, partition seeds by ordinal case ID; within a case retain each MemoryUnit's maximum graph score across seeds; merge cases by descending graph score, ascending case ID, then ascending MemoryUnitId before competition-rank allocation.
- (reconciliation by reviewer-gate) Third integrity pass passed; adversarial pass requested exact raw-score equality and tenant-wide graph merge ordering. Both are adopted before finalization.
- (event by architecture-update) Reviewer gate passed: rubric/security/data-integrity, version/repository reality, and adversarial divergence reviews report PASS with no unresolved architecture blockers. Full evidence is under reviews/.
- (event by architecture-update) spine finalized
- (event by architecture-update) Update run started 2026-09-12 from the architecture-validation-2026-09-12 reviewer gate: six lenses, FAIL verdict, 85 raw observations consolidated to 10 critical and 17 high. No adopted decision is reversed; every amendment tightens a Rule, corrects a stale fact, or records a decision. AD IDs stay stable; AD-20 is added.
- (decision by architecture-update) AD-14 is phase-qualified and RATIFIED by Jerome Piquot on 2026-09-12, closing PRD Open Question 9 and the architecture half of G6. Binds MVP FR43, Phase 2 embedding/schema migration, Phase 3 backend replacement. Prevents the unphased rule silently rebaselining MVP onto a Phase 2/3 contract the product has not funded. Rule: MVP permits only a tenant-scoped explicitly acknowledged degraded rebuild that never claims zero downtime; Phase 2 uses versioned create-backfill-verify-switch-retire with disjoint active/staging and full reindex before atomic activation; Phase 3 applies the same staged cutover at the adapter boundary; strategy ports, epoch-carrying evidence and execution-time secret resolution bind every phase.
- (decision by architecture-update) AD-3 gains a tuple comparison relation. Binds projection checkpoints, migration backfill, and reported ingestion state. Prevents a compliant epoch-lexicographic reading permanently stalling a unit in Indexing and two incompatible public status semantics. Rule: (embeddingConfigurationEpoch, schemaGeneration) is the identity discriminator and sourceVersion the only monotonic dimension; one checkpoint record per (tenant, case, unit, generation, epoch); an acknowledgement for a different generation or epoch is never stale; the reported public state is always the active tuple's while staging completion reports only as migration progress; AD-14 retire deletes retired records.
- (decision by architecture-update) AD-3 fences projection writes, not only acknowledgements. Prevents a late write from an older attempt overwriting newer projected content, or stale tuple-keyed documents remaining searchable. Rule: derived stores hold exactly one current document per (tenantId, caseId, MemoryUnitId) carrying the tuple it was projected from; every write is conditional on that stored tuple and is a no-op when the incoming tuple is older; deletion writes a fenced tombstone under the same rule.
- (decision by architecture-update) AD-4 forbids tenant content in durable workflow history. Binds workflow history, activity payloads, and actor state. Prevents crypto-shredding completing while extracted text and embeddings survive in the state store. Rule: workflow history and activity payloads carry identifiers, references, and non-secret configuration only; tenant content and content-derived material are read from an erasure-scoped store at execution time and never persisted into workflow history.
- (decision by architecture-update) AD-5 gains an identifier issuance grammar. Binds tenant, case, and MemoryUnit ID issuance and every composed key, ACL pattern, index name, and state key. Prevents a tenant ID containing a Redis ACL glob metacharacter turning its mandated per-tenant principal into a cross-tenant wildcard, and ambiguous key composition letting one tenant's checkpoint be consumed by another. Rule: one documented bounded ASCII grammar excluding every metacharacter, delimiter, or wildcard significant to Redis ACL patterns, Redis/RediSearch/FalkorDB key and index names, Dapr key and component names, URL path segments, or Kubernetes object names; unambiguous composition; comparison stays ordinal because issuance is what makes ordinal comparison sufficient.
- (decision by architecture-update) AD-5 splits allowlist and grant ownership. Prevents two compliant writers with opposite failure directions at tenant create and delete, a default principal on an allowlist miss, and a wildcard tenant grant. Rule: the app-ID-to-system principal allowlist is a deployment-time artifact with exactly one writer, the operator, held in the AD-15 operator secret scope, versioned and changed only through a reviewed observable procedure, granting principal identity and never tenant scope; the per-tenant grant is a tenant resource whose sole writer is AD-6's lifecycle workflow, created by verified provisioning and revoked as an AD-16 completion condition; every internal call requires allowlisted app ID and an explicit grant for the requested tenant and that tenant being active and verified, any one absent failing closed; grants are enumerated and a wildcard requires its own architecture decision.
- (decision by architecture-update) AD-5 states tenant authority for the two paths that had none. Prevents the Dapr pub/sub ingress trusting the CloudEvent envelope's tenant field, and resumed durable work writing projections for an inactive or erased tenant. Rule: /events/ingest derives tenant authority from the delivery channel, not the envelope, and an envelope tenant the channel does not authorize is rejected; work with no human caller carries an explicit tenant authority captured at initiation, revalidated against active tenant state on every resume and activity boundary, failing closed when the tenant is inactive, erased, or ungranted; captured configuration is not authority.
- (decision by architecture-update) AD-5 and AD-7 state the case-authority position explicitly: case is a data-partition and attribution boundary, not an authorization boundary. Prevents one implementation letting any tenant principal read any case while another enforces per-case membership, both compliant, and stops product documentation presenting cases as access control. Per-case authorization is deferred.
- (decision by architecture-update) AD-9 binds the graph axis's inputs. Prevents an unassigned result-limit application point changing rank membership, a silently incomplete wall-clock-dependent traversal reporting healthy, and 'top five hits' being read as five ranks. Rule: auto-seed from exactly the first five entries of each canonicalized list, taking entries not ranks; apply AD-11's result limit once to the merged graph list before rank allocation and never per case partition; apply the time limit once to the whole fan-out; a partition that does not complete makes the axis a third state, truncated - available, carrying its hits, contributing denominator weight, and named in the Evidence Packet with the count of uncompleted partitions.
- (decision by architecture-update) AD-9 fixes candidate depth and pagination. Prevents two conforming implementations returning different result sets and ranks for one query, and page-2's top result carrying page-1's composite score. Rule: a server-owned configuration-validated per-axis candidate depth identical across surfaces; fusion is defined over each axis's complete server-limited list; result limit and caller offset apply only to the fused output after rank allocation, normalization, and ordering, never pushed into an axis; consecutive pages concatenate to exactly the head of the single fused ordering.
- (decision by architecture-update) AD-9 resolves nl as a fourth ranked axis rather than a post-fusion rerank. Prevents a 1.20-versus-1.00 denominator split and, more seriously, two different result SETS across surfaces. Rule: when requested and configured for Phase 1.5 event units, nl is canonicalized, competition-ranked, and contributes 1/(10+rank) at weight 0.20 inside the same weighted sum; its weight enters the denominator only when it returned results; it appears in Evidence Packet axis health and in explain output on every surface under the wire name nl and is a selectable axis-control value; where NL is inactive it is reported as an excluded axis rather than omitted.
- (decision by architecture-update) AD-10 defines 'safely', the predicate that selects AD-9's denominator. Prevents each adapter team applying a private safety doctrine and moving every composite score by a factor of about 1.54. Rule: an axis can respond safely if and only if its adapter applied the request's authoritative tenant and case scope, returned within configured limits without truncation, and read only the tenant's active generation and epoch; staleness, low recall, and empty results are safe and disclosed; unverified scope, an exhausted limit, and a generation or epoch mismatch are unsafe and null the axis; safety is decided once per query by the server's axis-selection step and is the single input to both the denominator and Evidence Packet axis health.
- (decision by architecture-update) AD-12 makes Evidence Packet equivalence apply to the serialized document, not only to intent. Prevents an omit-nulls REST surface and an explicit-null MCP surface both complying while no single deserializer or golden vector can serve both. Rule: every listed element is always present on every surface; an unavailable axis or absent value is an explicit JSON null and never an omitted property; available-with-no-hits is an empty collection; no surface enables null-omitting serialization for evidence-bearing types; the packet state is a single value from the versioned vocabulary (complete, partial, weak, empty, stale, degraded, unauthorized, pendingExpansion) with a set-valued reason list; one golden-vector document per case is byte-comparable across REST, Dapr invocation, CLI JSON, and MCP.
- (decision by architecture-update) AD-12 names an owner for Contracts.V1 and a wire-name register. Prevents two purely additive changes colliding on one wire name and becoming unresolvable, because AD-12 forbids the corrective rename outside a versioned break. Rule: Contracts.V1 has one named owning component with change-approval authority; additive evolution is admissible only after the new wire name, JSON shape, and nullability are reserved in the versioned name register in the same change; a reserved name may not be reused with a different shape; collisions are resolved before merge, never by post-hoc rename; architecture tests fail the build when a public evidence-bearing type carries an unregistered wire name.
- (decision by architecture-update) AD-12 acquires the active-surface output contract that gives NFR37 an architectural home. Binds NFR37 and FR56. Prevents an exit-code map, JSON envelope shape, and cross-format parity guarantee - all public contract surface - having no owner. Rule: each active surface publishes a stable presentation order, a text label for every state, axis, score meaning, omission, progress stage and recovery, a machine-readable envelope and exit-code map, and identical semantics across every emitted form; colour, glyph, motion and screen position are supplementary only.
- (decision by architecture-update) AD-12 constrains omitted-detail handles. Prevents a mandatory handle acting as an existence oracle for content the caller cannot see. Rule: a handle is emitted only for detail the caller is authorized to retrieve; detail withheld for authorization reasons is omitted with no handle and is indistinguishable from absence; handles are opaque, non-enumerable, scoped to the issuing caller and request, and carry no score, count, or scope information about unauthorized content.
- (decision by architecture-update) AD-15 bounds the Kubernetes-secret exception and makes rotation effective. Prevents an unbounded temporary gap standing in for the only stated NFR8 isolation mechanism, and a rotated credential remaining valid. Rule: the direct-injection gap carries a named owner and a dated expiry recorded in the Stack note, and Production qualification is blocked until per-tenant backend principals replace it or a dated time-bounded security exception is approved; a rotated credential is revoked at the provider within a stated bound and no connection established under a superseded credential outlives that bound; rotation of a tenant data-plane credential is an AD-6 lifecycle operation with verification that the prior credential no longer authenticates.
- (decision by architecture-update) AD-16 enumerates every erasure target and names one durable register. Prevents erased content surviving in workflow history, actor state, projection backups, or FR71 export bundles, and prevents the non-reuse tombstone inheriting a telemetry TTL. Rule: deletion completes only after every store that can hold tenant content or content-derived material is purged - product projections, durable workflow history and activity payloads, actor state, caches, embeddings and index terms; every restore path consults a durable erased-tenant register before admission and refuses to rehydrate any record whose tenant appears there regardless of payload readability; export bundles are erasure-scoped artifacts invalidated by tenant erasure; the register is held outside any tenant-scoped keyspace, is not encrypted under a destroyed tenant key, is the sole authority for replay rejection, ID non-reuse and restore admission, carries the TTL-free non-reuse tombstone, and is retained indefinitely as content-free evidence.
- (decision by architecture-update) AD-16 defines verification and completion evidence. Prevents 'with verification' admitting any check an implementer chooses while the only durable evidence surface is one AD-17 refuses to certify. Rule: verification is a recorded reproducible check per enumerated target - evidence that tenant content is absent or unreadable, and for the EventStore tenant key that key material is destroyed and a sampled ciphertext no longer decrypts; completion evidence is a content-free record in the erased-tenant register carrying the target list, per-target outcome, and completion time; it is domain evidence under AD-2, not access telemetry.
- (decision by architecture-update) AD-16 records its own scope limitation: cross-tenant references held in other tenants' units are an application responsibility documented as a limitation, so the rule no longer over-claims complete erasure.
- (decision by architecture-update) AD-17 defines sanitized and brings access telemetry inside AD-5 and AD-6. Prevents AD-16's opaque-telemetry carve-out resting on an undefined word, and a second per-tenant datastore answering for whatever tenant a request names. Rule: sanitized means content-free - records carry tenant, case, principal, operation, outcome and timing as opaque or enumerated values and never query text, snippets, extracted content, or anything derived from them, and a record that cannot be written content-free is not written; telemetry reads and writes derive tenant authority under AD-5 independently of the calling workload's channel identity, and the telemetry store is a tenant-isolated resource under AD-6 with a stated per-tenant principal or partition provisioned and erased by the tenant lifecycle.
- (decision by architecture-update) AD-19 makes source-lane identity re-derivable and assigns container-default ownership. Prevents two release lanes being evidenced at different source revisions and two surfaces qualifying against different engine builds. Rule: the source-lane gitlink is re-derived from the superproject at every spine revision and must correspond to the package-lane version as part of lane evidence; Hexalith.Memories.Aspire is the single owner of qualified container image digests and AppHost consumes those same defaults rather than declaring its own.
- (decision by architecture-update) AD-20 is added: treat release gates as evidence contracts. Binds G1-G6, L1-L3, the alignment-gap ledger, phase exceptions, and every claim of current qualification. Prevents historical done tracking state, phase-inactive surfaces, or an unowned alignment gap being read as current gate evidence. Rule: every governed requirement and every gap classified active-foundation critical carries exactly one of a current evidence path or a dated phase exception approved by product and architecture, each with a named owner and tracker entry; phase-inactive surfaces earn no gate credit even when their assets exist; a row leaves the ledger only on a confirmed-resolved verdict backed by re-runnable evidence.
- (decision by architecture-update) The Direct Redis Exception Registry gains amendment rules. Prevents two coordination stores guarding one resource, a fail-open lease inheriting the fail-open precedent of an admission optimization, and two rows claiming one key prefix. Rule: a later AD-n may add a row only when it names the single coordination store for the protected resource and migrates any existing guard in the same decision; mutual-exclusion primitives fail closed and only admission optimizations with a durable fallback may fail open; every row declares its reserved Redis key prefix, which no other row may claim.
- (decision by architecture-update) Conventions corrected for two cross-reference contradictions and three under-specified rows. App tokens protect service-to-service channels, not only local ones, matching AD-5. Ordinal ordering is imposed at AD-9's specified merge and tie-break points rather than only the final tie-break. AD-13 provenance names the issuer plus subject claim, normalized exactly once at the authentication boundary and compared ordinally thereafter, and carries correlation and causation identifiers as opaque preserved provenance distinct from W3C trace context. The error-code catalogue is declared in Contracts/V1, is append-only under AD-12, is mapped rather than redefined by every surface, and a code's meaning may not change without a versioned break; authorization and existence failures on scoped resources return one indistinguishable response.
- (decision by architecture-update) The spine gains the dimensions the altitude owns but left silent: a recovery taxonomy promoted from the gap ledger into a Rule (authoritative replay is the only rebuild that may set projection truth; derived-store restore is an availability optimization that must invalidate affected checkpoints and re-verify through AD-3; export restore is an application-level import subject to normal ingestion rules), an enumerated test-tier vocabulary carrying the PRD's no-sidecar unit-test constraint, an environment taxonomy with a parity invariant, and a per-unit sizing clause in AD-18 covering NFR14.
- (constraint by architecture-update) Stack table corrected against verified reality: the EventStore source-lane gitlink becomes 6b0247acc0b3ef60eb00c0f9ac9cbd367f85da20 re-derived from HEAD (the recorded b1c00a79 was superseded by the very commit that published the spine and resolves to nothing); pins are stated as read from the committed Hexalith.Builds gitlink a32cb422; PostgreSQL 18.4-trixie is added as the access-telemetry production store; the embedding provider, model, and dimension AD-14 binds are recorded.
- (constraint by architecture-update) Caveat corrected on four counts: .NET 10.0.400 / runtime 10.0.11 predates the 2026-09-08 security release 10.0.12 / SDK 10.0.401 fixing six CVEs and is now named as a security blocker alongside OpenBao; the OpenBao remediation target becomes at least 2.6.2 with the current chart line 0.29.x rather than the already-stale 0.28.6; FalkorDB 4.12.0 is quantified as eight minor lines behind v4.20.4; the production-bound CommunityToolkit Aspire Dapr preview (no stable 13.5.x exists upstream) and the Fluent UI RC (no GA exists) receive an explicit accepted-because-no-stable-alternative verdict; Redis Stack 7.4.0-v8 carries its hard 2026-11-30 EOL date.
- (constraint by architecture-update) The alignment-gap ledger is restructured for G6 with two added columns - Disposition (owner / evidence path / resolved verdict / approved exception id) and Active-foundation critical - the classification column preceding the owner column so the spine does not over-scope G6 beyond the architecture-critical active-foundation gaps prd.md:189 names. Five rows corrected: the Web row was factually false (a runnable Playwright-driven CI-gated specimen host has existed since 2026-07-06); the fusion row understates competition ranks as missing when they are implemented and omits that passing tests lock the anti-AD-9 NaN behaviour; the status row omits that the read path defaults an unpersisted status to Indexed; row 5's convergence asks for separate Falkor graphs that already exist; the MCP row claims an ingress that does not exist, the real residue being an ungated replicas 2. A new row carries the AD-8 namespace and architecture-test creation.
- (constraint by architecture-update) AD-8's enforcement claim is corrected from present to future tense across all three places it appears. The namespaces Adapters.Redis and Adapters.FalkorDb have zero occurrences in src/, no architecture test enforces the provider-SDK boundary, and 105 files under Hexalith.Memories.Server hold the provider SDKs. Of seventeen invariants the spine leans on architecture tests to enforce, six do not exist, one is enforced backwards, and two cover only a subset.
- (constraint by architecture-update) Structural Seed corrected: tools/ operator utilities are added (MigrateEmbeddingVectors project-references Server and takes a direct StackExchange.Redis dependency, so it is an AD-8 surface, and the release-inventory gate enumerates src/ only); samples/ and docs/ are added as launch-prerequisite and G3-evidence locations; the Web entry is corrected to a non-packable RCL with its runnable specimen host under tests/; the Client* entry drops the ports claim, since only Client.Rest exists and exposes no port interface.
- (constraint by architecture-update) Sources and companions pruned: architecture.md and the pre-FR71 traceability matrix move from active inputs to historical reference, since a final document may not list superseded artefacts as current derivation sources.
- (decision by architecture-update) The Deferred hosted-Web row no longer hands authorization to a future host: the host owns authentication and session integration, navigation, global state, render mode, and activation of conformance components, while server-derived authority remains AD-5's.
- (constraint by architecture-update) Upstream correction owed and offered in the same pass: prd.md:927 still permits Kubernetes Secrets for direct pod inputs that DAPR cannot provide, contradicting its own tightened NFR9 and AD-15. The spine is right and the PRD is wrong; the 2026-09-12 reconciliation landed in NFR9 and the addendum but missed this line.
- (reconciliation by architecture-update) Binding header advanced to FR1-FR75, NFR1-NFR37, G1-G6. FR75's substance was already fully adopted in AD-3 and AD-4, so for FR75 the header and Binds lines were the only gap; NFR37 and G6 required new normative text. The spine has no FR-to-AD trace map to extend - traceability is per-AD Binds lines plus the capability map - so capability-map rows were added instead.
- (event by architecture-update) Reviewer gate re-run on the amended spine (four lenses): closure 24/27 CLOSED with 3 PARTIAL and 0 REGRESSED, AD IDs verified stable; adversarial 4 closed / 7 partially closed / 0 still open plus 17 new pairs; version PASS-WITH-FINDINGS with 4 newly-wrong facts introduced by the update itself; security FAIL with 11 closed / 8 partial plus 17 new. The update closed the validated findings but introduced a second generation, chiefly by under-specifying mechanism and by asserting three Stack facts that were never reality-checked.
- (constraint by architecture-update) Second amendment pass corrected four facts the update itself got wrong: the embedding model is gemini-embedding-001, not text-embedding-004 (the latter was shut down by Google on 2026-01-14 and appears nowhere in src/; the PRD still carries the stale value and now owes a correction); PostgreSQL 18.4-trixie precedes the 18.6 release fixing 28 CVEs and is a fourth Production blocker; the 2026-11-30 date is Redis Software 7.4's EOL, not Redis Stack's, whose maintenance ended December 2025 so the image is already unpatched; FalkorDB is four released minor lines behind, not eight. Also corrected: rollForward floats across feature bands, and a stable release exists for both prerelease pins outside their pinned line.
- (decision by architecture-update) AD-9 and AD-10 reconciled after the update put them in direct contradiction. Reaching the configured per-axis candidate depth is not truncation and the depth-limited list is complete; truncated applies only when a graph case partition did not complete while scope was verified on those that did, and is safe, disclosed, and denominator-contributing; a limit exhausted such that scope cannot be verified remains unsafe and nulls the axis. Without this the amendment reopened the same 1.54x score divergence it was written to close.
- (decision by architecture-update) AD-3's write fence is scoped to a single generation and epoch: a write whose generation or epoch differs is never merely older - it replaces the active document only when it carries the tenant's active pair, and otherwise targets AD-14's disjoint staging resources, so an active and a staging write cannot alternate in one document and mix embedding dimensions. A Phase 1 MVP degraded rebuild has one active epoch and writes under it.
- (decision by architecture-update) AD-5 exempts lifecycle work from its own revalidation clause: AD-6 provisioning and AD-16 erasure act on tenants being created, deactivated, or erased, are authorized by the operator principal, and revalidate against the lifecycle state machine rather than tenant-active, so erasure is not deadlocked by its own subject. AD-17 likewise continues retention accounting for an erased tenant under Platform Operations authority. The issuance grammar is additionally single-case, closing the Kubernetes DNS-1123 case-collapse variant, and its excluded-surface list gains OpenBao paths, SQL identifiers, and object-storage paths.
- (decision by architecture-update) The erased-tenant register is given a home and a failure posture: Dapr state under AD-8 in a platform-scoped component outside any tenant keyspace, with a stated durability and restore posture, existing from platform provisioning rather than first erasure, failing closed and resumable when unavailable, and consulted by AD-6 provisioning so an erased tenant ID cannot be re-issued. Hexalith.Memories.Contracts is named as the Contracts.V1 owning component. The erasure-scoped store AD-4 relocates content to is defined as one covered by AD-16's purge targets and the tenant key.
- (decision by architecture-update) AD-20 now obliges rather than describes: active-foundation critical is defined as violating a rule binding an MVP-active surface where the violation can produce incorrect product behaviour, cross-tenant exposure, irreversible data loss, or an unverifiable gate claim; a row must carry an owner plus evidence or a dated exception before it may be cited as qualification evidence, and a row lacking both is an open gate blocker. The ledger states plainly that every row is currently in that state.
- (constraint by architecture-update) Ledger grew to 24 rows with seven added by the re-run: PostgreSQL CVEs, the absent enforcement point for AD-4's content prohibition plus pre-rule history, pre-grammar identifiers with no validation-on-read or import rule, the unprovisioned access-telemetry tenant partition, the unimplemented erased-tenant register on an unqualified component, the missing Contracts.V1 name register and guard, and AD-1's two unguarded dependency invariants. The floating-image row was reclassified active-foundation critical for consistency with the other Production blockers.
- (constraint by architecture-update) Open after two amendment passes and owed to a third: roughly two dozen medium and low findings from the re-run, the AD-5 heading now carrying two decisions in 379 words, the undeclared composition delimiter and unconstrained CloudEvent source interaction, and every named owner and dated expiry in the ledger - which are organizational facts the architecture cannot invent. PRD Open Questions 9 and 10 were closed in this pass; prd.md still carries the stale text-embedding-004 model and owes that correction.
- (event by architecture-update) Update closed 2026-09-12: spine amended over two passes, AD IDs stable at AD-1..AD-20, linter clean, PRD corrected in three places (second secret path, Open Questions 9 and 10). A third reviewer pass is owed before the spine is treated as clean; the original spine took four adversarial passes to converge and this one has had two.
- (decision by architecture-update) Jerome Piquot is recorded as the named owner of every Current Alignment Gaps row, supplied by the user on 2026-09-12. Dated expiries are anchored to the PRD's [DERIVED] 2026-10-31 G1-prerequisite checkpoint (prd.md:1234) rather than independently negotiated, and move with it if the sprint-change D1 decision resets the gate schedule. AD-20's third requirement, a tracker entry in sprint-status.yaml, remains owed because the 2026-09-12 sprint-change correction freezes that file; until those entries land no row may be cited as satisfied qualification evidence.
- (event by architecture-update) Third reviewer-gate pass: closure 10 CLOSED / 10 PARTIAL / 0 REGRESSED; adversarial PASS-WITH-FINDINGS with 4 of 17 pass-2 pairs closed, 8 unchanged, 15 new; version PASS-WITH-FINDINGS with 1 newly-wrong and 3 still-wrong; security FAIL again at 2 CLOSED / 14 PARTIAL / 9 NOT CLOSED plus 16 new. Diagnosis recorded by the adversarial lens and accepted: the second amendment scoped three of its four highest-value fixes to the exact case the prior finding exhibited, and the narrowing un-covered the general case.
- (constraint by architecture-update) Third-pass errata corrected: AD-10's opening biconditional still required completion without truncation and kept the 1.54x composite-score divergence alive for three passes; truncated was scoped graph-only and is now generalised to any axis; the registry key prefix memories:preflight: was invented rather than read from code and is replaced by the shipped dedup:<tenant>:<case>:<sha256> family with the prefix recorded as owed; the PostgreSQL CVE count was four and is fourteen at CVSS 8.8; the claim that container base images do not roll forward is false because Directory.Build.targets:3 uses a floating 10.0-alpine tag for all four production images, now ledgered as its own AD-19 violation; the AD-19 gitlink annotation was requested in pass 2 and never applied and now records v3.103.0-40-g6b0247ac. PRD line 62 and the embedding provider table were corrected upstream.
- (constraint by architecture-update) Ledger honesty restored after the owner/date fill-in created a false managed-programme impression: AD-20 admits exactly two states, a current evidence path or an approved dated exception, and no row is in either - every row says evidence path owed and none carries an exception id. The ledger now says so plainly, notes the 2026-10-31 anchor derives from a checkpoint whose owning sprint-change D1 decision is OPEN with a reset recommendation, attributes the sprint-status.yaml tracker requirement to PRD G6 rather than AD-20, and records that AD-20's two-party exception route collapses into self-approval where one person holds owner, product, and architecture. Five further rows added: floating base image, the unwritten grammar and delimiter, the undefined tenant lifecycle state machine, the unnamed erasure-scoped store, and the unauthorized and uncreated erased-tenant register.
- (event by architecture-update) Spine editing frozen pending approval of sprint-change-proposal-2026-09-12-architecture-convergence.md, which records five structural decisions the reviewer gate proved an editing pass cannot close alone: the erased-tenant register's owner, creation point, authorization and store; the scope and tenant-binding of AD-5's lifecycle exemption; MVP epoch and staging semantics between AD-3 and AD-14; where the three-state axis contract lives and how it serializes; and whether AD-20's two-party exception route survives when one person holds owner, product and architecture. The proposal is a successor to the 2026-09-12 Sprint-Readiness Recovery proposal's section 5, whose three architecture asks are all now executed.
- (event by architecture-update) Convergence pass started 2026-09-12 on the frozen spine (sha256 f5516a70). Sprint-change-proposal-2026-09-12-architecture-convergence.md section 3 decisions D1-D5 were recorded by Jerome Piquot and are logged below verbatim before any spine edit. Pass is class-level by explicit instruction: the prior two passes failed by scoping each fix to the case its finding exhibited.
- (decision by Jerome Piquot) D1 RATIFIED - EVENTSTORE-REGISTER. The erased-tenant register is a platform-tenant EventStore stream created at platform bootstrap, never held under a tenant key, with a new AD owning its authorization and its readers. Rationale: resolves the AD-2 contradiction outright (the register was simultaneously 'Dapr state under AD-8' and 'domain evidence under AD-2', on the very Redis component AD-2's Prevents forbids as a system of record), inherits durability and replay semantics already adopted, and detaches the erasure guarantee from the unqualified state store the Deferred table still owes. Costs a new AD and a bootstrap ordering rule. Closes SEC3-05, ADV3-01, ADV3-08, ADV3-12, P3-03, P3-11.
- (decision by Jerome Piquot) D2 RATIFIED - AD-5 lifecycle exemption is enumerated and tenant-bound. Enumerate the exempt operations exhaustively; bind each invocation to exactly one tenant recorded in lifecycle evidence; and make 'no enumerated operation may create or restore resources for a tenant present in the erased-tenant register' an explicit clause. Rationale: the exemption as drafted bound its holder to no tenant by any mechanism, was the sole writer of the grants every other path consumes, and widened as AD-6 was honoured. Closes SEC3-01 (critical), SEC3-02, ADV3-04 (critical).
- (decision by Jerome Piquot) D3 RATIFIED - MVP allocates a new epoch, writes under it, and activates atomically per tenant; the AD-3 fence's staging clause is Phase-2-only by explicit statement rather than by silence. Rationale: 'one active epoch and writes under it' was satisfiable two ways, both bad - bump-and-activate flips every unit in the tenant to indexing and fails tenant-wide queries, keep-epoch produces mixed embedding dimensions under one epoch, which is precisely what AD-14 exists to prevent. Closes ADV3-03 (critical).
- (decision by Jerome Piquot) D4 RATIFIED - a new AD owns the three axis states AND their single canonical encoding together, so no future amendment can scope one state in one AD and leave another AD's encoding behind. Rationale: the axis-state contract spanned AD-9, AD-10 and AD-12; AD-12 defined two encodings while the spine named three states and used a fourth; and the adapter reporting contract was unowned after three passes. Closes ADV2-17, ADV-05, ADV-08 (encoding half), ADV3-02 residue, P3-15.
- (decision by Jerome Piquot) D5 RATIFIED - drop AD-20's two-party approval language; the evidence path is the only route. Rationale: one person holds owner, product and architecture, so 'approved by product and architecture' collapses into self-approval and states a control that does not exist. Keeping only the evidence-path route makes the ledger's real state legible. Closes SEC3-07 (governance half), ADV3-05, P3-04.
- (decision by architecture-update) AD-21 is added under D1: 'Hold platform-scoped authority outside every tenant key'. It owns the platform-tenant EventStore partition (domain memories-platform, never encrypted under any tenant key, never crypto-shredded, never carrying tenant content), the erased-tenant register held on it, the platform-bootstrap step that creates and initializes both before any tenant may be provisioned, an explicit initialized marker plus a monotonic register sequence, authorization (writes only from the erasure workflow's operator-scoped principal, append-only, tombstones not deletable by any application principal; reads enumerated to the consult paths and operator principals), and the availability rule that an absent marker, an unreachable partition, or any read error is unavailable and never an empty register. Binds tenant provisioning, deletion, authoritative replay, every restore path, and export re-import - all of which fail closed on unavailability.
- (decision by architecture-update) AD-22 is added under D4: 'Report one axis state with one encoding'. Four states in a closed set - unavailable, available, truncated, excluded - with one definition site, one wire encoding reserved in AD-12's name register, and the adapter-to-server reporting contract that AD-10's safety decision consumes: every adapter returns, alongside its hits, a machine-readable statement of whether it applied the request's tenant and case scope, whether it truncated, and the scope units it did not cover; an adapter that does not supply that statement is treated as scope-unverified and nulled. AD-9 and AD-10 now cite AD-22 instead of restating states, so a state cannot be scoped in one AD and left unencoded in another.
- (decision by architecture-update) AD-23 is added by the section 5.4 split: AD-5 keeps its number and its first decision (identity and authority derivation) and AD-23 takes the second (identifier issuance, the grammar, the reserved delimiter, and injective key composition). AD-5 at 470-plus words carried two decisions and the most findings of any AD. AD-23 declares the grammar lowercase ASCII, a strict subset of every naming scheme the platform composes into, bounded in length, conformance asserted by a test rather than by inspection; declares the single reserved delimiter; requires every non-grammar component of a composed key - CloudEvent source and id, IdempotencyToken, principal identifiers, handle scopes - to be length-bounded and reversibly encoded or hashed into the grammar alphabet before composition; and adds validation on read at every trust boundary including import, which previously existed only in a ledger convergence cell.
- (constraint by architecture-update) Brownfield reality checked before writing, three findings that change what the spine may claim. (1) Contracts.V1 already ships a TenantStatus enum with Provisioning, Active, Deleting, Failed, CompensationFailed (src/Hexalith.Memories.Contracts/V1/TenantStatus.cs:12) and an AD-2-committed TenantLifecycleStatusUpdatedEvent on EventStore domain memories-tenants, so the tenant lifecycle state machine the spine treated as nonexistent partly exists; the architecture ratifies that enum as the single definition site and extends it additively rather than inventing a parallel vocabulary. (2) The pub/sub tenant authority the spine described abstractly is shipped as TenantEventRoutingOptions.SourceToTenantMap, a deployment-time CloudEvent source-prefix to tenant map, longest-prefix wins - but StringComparer.OrdinalIgnoreCase, which contradicts AD-5's ordinal single-case rule. (3) The preflight reservation key is dedup:{tenantId}:{caseId}:{sha256(sourceUri)} (EventStoreDedupKey.cs:17), so the source is already hashed before composition and the SEC3-06 delimiter-injection scenario is mitigated in code for source though not for tenant or case; the spine records the shipped family rather than the invented memories:preflight: prefix, which has zero occurrences in src/.
- (constraint by architecture-update) AD-19 lane correspondence re-derived at this revision as the rule requires: the EventStore source-lane gitlink moved from 6b0247acc0b3ef60eb00c0f9ac9cbd367f85da20 (v3.103.0-40-g6b0247ac) to fe05a796faa46f6486f615c119809148347c9a25 (v3.103.0-43-gfe05a796) in commit a51080a5, so the divergence from package-lane 3.103.0 is now 43 commits, not 40. The prior value was correct when written and is stale by three commits; this is the second consecutive revision at which re-derivation changed the value, which is itself evidence that the rule is doing its job.
- (decision by architecture-update) Class sweep 1 - every referenced-but-nonexistent artifact gets a definition or an explicit deferral with a revisit condition. Five were named in the proposal and the sweep found two more. Defined: the issuance grammar and its reserved delimiter (AD-23, as a tracked artifact owned by this architecture and named from the decision); the tenant lifecycle state machine (AD-6, ratifying Contracts.V1 TenantStatus, states enumerated, transitions AD-2-committed, the shred-surviving projection of it held on AD-21's platform partition); the erasure-scoped store (AD-4, named as the tenant content store, provisioned as an AD-6 tenant resource with a stated authorization and quota posture); the erased-tenant register (AD-21); the Contracts.V1 name register (AD-12, a tracked file inside the owning project with a build-failing guard). Also defined rather than left dangling: the operator principal and the Platform Operations principal, both of which authorized privileged work while being derivable from nothing.
- (decision by architecture-update) Class sweep 2 - all 20 ADs swept for 'names an owner, store, or state that does not exist' rather than fixing reported instances. Beyond the five known artifacts the sweep found: AD-5's operator principal (authorizes the whole exempt class, derivable from nothing), AD-17's Platform Operations principal (a third undefined privileged identity), AD-16's export-bundle registration index (named, homeless), AD-12's active-surface exit-code map owner, AD-20's classifier and confirmed-resolved verdict issuer, and AD-19's digest set consumers beyond AppHost. Each is now either defined in the AD that relies on it or carries a ledger row naming the missing mechanism. The rule that generalises the sweep is added to the preamble: a rule that names a mechanism the architecture has not defined carries a ledger row naming that mechanism, and a reader may treat an obligation absent from both rule text and ledger as not yet governed.
- (decision by architecture-update) Class sweep 3 - one definition site and one encoding per state vocabulary. Axis states: AD-22, four values, closed set, encoding reserved once. Packet states: AD-12 remains the definition site and is reconciled with the shipped Contracts.V1 EvidencePacketState enum, which already carries exactly the eight values the spine lists. Status wire values: the Conventions V1 status row remains the single site and AD-3 cites it rather than restating it. Ledger dispositions: SEC3-08's three-word state - blocker, evidenced, excepted - becomes the first token of every Disposition cell, defined once in the ledger preamble. Tenant lifecycle states: Contracts.V1 TenantStatus is the single site per sweep 1. No vocabulary is now defined in two places or encoded in two ways.
- (decision by architecture-update) Class sweep 5 - items never reached rather than judged closed are closed as classes. ADV2-06 and ADV3-09 (delimiter and non-grammar key components) close together in AD-23's injective-composition rule, which covers every non-grammar component rather than CloudEvent source alone. ADV2-08 and ADV-08 (byte-comparability without a canonical form) close in AD-12 with a canonical serialization plus transport-envelope exclusion. ADV2-10 closes in AD-14 with a single resolution path and synchronous cache invalidation inside the activation commit. ADV2-11 closes in AD-9 with deployment-wide depth that admission control may not shrink. ADV2-12 closes in AD-9 with offset-overrun semantics and a reported fused length. ADV2-13 closes in AD-3 with an explicit reprojectionRequired checkpoint state rather than a deleted record. ADV2-15 closes in AD-19 by binding every digest consumer, not only AppHost. ADV2-17 closes in AD-22. ADV-03 closes in AD-5 with grant membership, a writer for a live tenant, and a revocation propagation bound matching AD-15's credential bound. ADV-05 closes in AD-22's adapter reporting contract. The nine NOT CLOSED security items reduce to six distinct defects because SEC-04, SEC-11 and SEC-19 are the pass-1 identities of SEC2-07, SEC2-09 and SEC2-15.
- (decision by architecture-update) Class sweep 4 executed as authorized and no further: AD-5 keeps its number and its identity/authority decision, AD-23 takes issuance, the grammar, the delimiter, and injective key composition. AD-5's Rule nonetheless grew from 470 to 883 words rather than shrinking, because D2 plus SEC3-01, SEC3-02, SEC2-15/SEC-19 and ADV-03 all land in the authority half and every one of them was a finding that the text gestured at a mechanism instead of enumerating it - the ten exempt operations, the three exemptions, the three internal-call checks, the operator artifact's three contents, and the revocation bound are now exhaustive where they were previously implied. Recorded as a tension rather than resolved: the concentration AD-5 was split to relieve is still there by word count, and a further split of the privileged-authority half into its own AD would relieve it but is beyond the recorded section 5.4 decision. Flagged to the user rather than taken unilaterally.
- (decision by architecture-update) One artifact the sweep found and did NOT define, recorded as an explicit deferral rather than a bare mention: per-case authorization stays deferred and AD-7 keeps case as a data-partition and attribution boundary only, with the Deferred row now also forbidding product documentation from presenting cases as access control. Everything else the proposal listed is defined: the grammar and delimiter (AD-23), the tenant lifecycle state machine (AD-6, ratifying the shipped Contracts.V1 TenantStatus enum), the erasure-scoped store (AD-4, named the tenant content store), the erased-tenant register (AD-21), and the Contracts.V1 name register (AD-12, a tracked file inside the owning project with a build-failing guard).
- (constraint by architecture-update) Ledger restructured to 44 rows from 29. Every Disposition now opens with its AD-20 state word and every row reads blocker, because no row is evidenced and the exception register AD-20 and AD-15 share does not exist yet. Fifteen rows added for obligations that were ungoverned, chiefly the mechanisms this and the prior passes named: the operator artifact, the OrdinalIgnoreCase routing map, the revocation propagation bound, the NFR8 suite's blindness to principals with no tenant claim, the export-bundle index and pre-mechanism sweep, demonstrative erasure verification, AD-22's adapter reporting statement, the canonical packet serialization, the single epoch resolution path, the reprojectionRequired state, the Phase 1 rebuild epoch and per-store staging, the exception register, the shared dedup: prefix, the access-telemetry /v1 plane, and the telemetry representation mapping's purge. Two rows gained a Production block per SEC3-13. The register row lost its dependency on the unqualified Dapr component, which is the one place D1 made the ledger smaller rather than larger.
- (event by architecture-update) Convergence pass edits complete: spine 369 to 422 lines, AD-1..AD-20 unchanged in number and none retired, AD-21/AD-22/AD-23 added, lint_spine.py reports 0 findings, CRLF normalized. Reviewer gate re-run next as four parallel lenses.
- (constraint by architecture-update) Upstream correction owed and NOT taken unilaterally: prd.md:189 defines G6 as requiring 'a phase-specific exception approved by product and architecture', which is the exact two-party route D5 just dropped from AD-20 as a control that does not exist where one person holds both roles. The spine and the PRD now diverge on the exception route. This is a product gate definition, and the sprint-change proposal states it changes no product requirement, so the edit is offered to Jerome rather than made here. The spine's own ledger text already attributes the sprint-status.yaml tracker requirement to PRD G6 rather than to AD-20, so that half is consistent. Verified corrected upstream already and needing nothing further: prd.md:927 (the second secret path, now reading 'Direct Redis/FalkorDB credential injection is an alignment gap, not an approved second path'), prd.md:62, and the prd.md:799 embedding provider table (gemini-embedding-001).
- (event by reviewer-gate) Pass-4 reviewer gate, four parallel lenses. SECURITY: PASS-WITH-FINDINGS with ZERO CRITICAL - the convergence criterion's security half, met for the first time in four passes; Part A 33 CLOSED / 7 PARTIAL / 1 NOT CLOSED across 41 judgments, with the pass-3 critical SEC3-01 and all six pass-3 highs closed by mechanism rather than by rationale; Part B 0 critical / 4 high / 7 medium / 3 low. ADVERSARIAL: PASS-WITH-FINDINGS, 38 of 40 carried items Closed against 8 of 24 in pass 3, 0 still open, 0 regressed, plus 20 new pairs of which 5 critical. CLOSURE: REVISE, 21 CLOSED / 6 PARTIAL / 5 NOT CLOSED / 1 REGRESSED, AD id constraint verified intact at AD-1..AD-23. VERSION: REVISE, 8 newly-wrong and 1 still-wrong.
- (constraint by reviewer-gate) Convergence NOT reached, and the reason is precise rather than general. Criterion one (security PASS-WITH-FINDINGS, zero critical) is MET. Criterion two (no lens reports a contradiction introduced by this pass) is NOT met: 14 of the adversarial lens's 20 new pairs sit in sentences this pass wrote. The adversarial diagnosis, accepted: the complement test WAS run on the reported findings, which is why 38 of 40 carried items closed, but it was not run on the new text written to close them - so the failure signature moved from 'narrowing the fix' to 'not propagating the fix into the rule beside it'. This is a different and smaller failure than the one that froze the spine, and unlike pass 3 the finding count moved with the underlying state rather than staying flat while it changed.
- (decision by architecture-update) Correction round applied to this pass's own errors, decision-free items only. Facts re-derived: the Builds gitlink was stale at a32cb422 and is cf52f74c (v4.27.3-3-gcf52f74), carrying four pins with it - Dapr 1.18.5 to 1.18.7, CommunityToolkit preview to 13.5.1-beta.751, Kreuzberg 4.10.2 to 4.10.3, StackExchange.Redis 3.1.31 to 3.2.0; Microsoft.AspNetCore.* is already 10.0.12 so the package-side CVE exposure is closed and only global.json and the floating base image remain; the PostgreSQL ledger row still said four CVEs at 8.8 against the paragraph's fourteen. The class lesson: AD-19 re-derived only the EventStore gitlink when every gitlink the spine reads facts from needs re-deriving, and that rule is now stated. Contradictions this pass introduced, corrected: AD-23's blanket lowercase grammar outlawed the shipped uppercase ULID case and MemoryUnit identifiers and is now declared per identifier class, single-case within each; AD-3's document identity now carries the generation and epoch so D3's rebuild write has a target and the Phase-2-only clause is scoped to the staging-resource machinery rather than to the fence; AD-22 states the denominator rule once as returned-results rather than by state name, closing the truncated-with-zero-hits divergence; AD-5's routing map is re-keyed onto authenticated channel identity because a source-keyed map lets a publisher choose its tenant, which AD-5's own Prevents forbids; AD-21 authorizes writes per record type so the lifecycle projection, bundle index, and purge completion are writable while tombstone authority stays with erasure alone, and every consult path now tests effective state so a reversal actually restores issuability.
- (constraint by architecture-update) Six ledger rows added from the gate, all verified in code first: the shipped dedup identity is (tenant, case, sha256(cloudEventId)) because EventIngestionService.cs:144 passes envelope.Id into a parameter named sourceUri, so source is absent from the key and this spine's prior claim that the shipped key hashed source was wrong in three places; tenant identifiers are caller-supplied and validated only by ^[a-zA-Z0-9-]+$ at TenantProvisioningInput.cs:9, falsifying the issued-not-chosen premise AD-16, AD-17 and AD-21 all rest on; Contracts.V1 enables null-omitting serialization in 70 places plus a global default at CliJsonContext.cs:19,59 against AD-12; the Evidence Packet carries axis health as two string buckets that cannot express AD-22's four states; AD-16's closed purge enumeration omits the tenant-keyed direct-Redis coordination stores the ledger already records; and no consumer shares one image digest set, the harnesses running redis-stack:latest against the manifests' redis-stack-server:7.4.0-v8. Ledger now 50 rows, every one blocker. Linter clean.
- (event) Architecture convergence update resumed 2026-09-13 from the unfinished pass-4 correction round; deterministic lint reports zero findings and the independent reviewer gate is being rerun before finalization.
- (constraint) Administrator confirmed the architecture spine is the only deliverable for this update; no additional human-facing rendering is in scope.
- (version) Pass-5 reality check re-derived every fact-source gitlink from committed HEAD e532936d: Hexalith.Builds is bf8adb73cec1ddb04b7d74b113cbfbb726cccf8c (v4.27.3-9-gbf8adb7) and retains the recorded catalog values; Hexalith.EventStore is 4502913cafbb6151a17544923b5b1ba76ea5e5ec (exact tag v3.104.0), 47 commits after v3.103.0, while the package lane remains pinned to 3.103.0. AD-19 lane correspondence remains unmet.
- (constraint) Pass-5 repository reality correction: the existing digest guard covers Kubernetes and OpenBao deployment literals but not the owned ContainerBaseImage or the single cross-consumer digest set AD-19 requires.
- (constraint) Pass-5 brownfield correction: AutoProvisionRoutedTenants schedules AD-6's TenantProvisioningWorkflow, so it does not bypass the lifecycle mechanism; the gap is that startup initiates provisioning without the operator authorization AD-5 and AD-6 require, while Development validation may defer unknown tenants independently of the flag.
- (decision) Pass-5 clear-fix sweep amends AD-5 without a new choice: the operator artifact carries an authenticated channel-identity tuple routing map, never a CloudEvent source-prefix map; the map selects a candidate tenant but does not grant it, so pub/sub admission also requires the publisher app ID allowlist entry, the tenant grant, and Active lifecycle state. This propagates AD-5's already-adopted no-publisher-selected-tenant rule into the artifact schema.
- (constraint) Pass-5 clear-fix sweep removes AD-4's stale claim that the shipped reservation key hashes CloudEvent source. Verified reality remains: it hashes id and omits source, so it is an alignment gap against the adopted (tenant, case, canonical source, id) identity.
- (decision) Pass-5 clear-fix sweep applies ratified D2 to AD-17: every retention-accounting invocation is bound at initiation to exactly one erased tenant, cannot change tenant on resume or retry, and may not run as one cross-tenant sweep.
- (decision) Pass-5 clear-fix sweep removes AD-12's omission ambiguity: authorization-withheld detail uses the same present canonical-null properties and bytes as ordinary absence; no handle is emitted.
- (decision) Pass-5 clear-fix sweep defines the AD-18/AD-22 boundary: capacity rejects or delays only before axis fan-out; after admission a provider cutoff or timeout is reported truthfully through AD-22, while backpressure may not silently shrink the contract or forge an axis state.
- (constraint) Redis Stack 7.4.0-v8 is already on an ended-maintenance line and is therefore an active Production qualification gap, not merely a future upgrade choice; the target Redis 8 migration design stays Deferred until qualification work begins.
- (decision) Pass-5 clear-fix sweep removes the remaining AD-10/AD-22 scoring contradiction: AD-22 alone owns the returned-results denominator rule; candidate-depth completion is not truncation, while traversal, result, provider, or time cutoffs after verified partial work are truncation.
- (decision) Pass-5 distillation executes already-ratified D5 consistently: a current re-runnable evidence path is the only qualification-satisfied state. A dated risk acceptance or security exception may document ownership but never satisfies a gate or closes a ledger row. The conflicting PRD G6 two-role exception route remains an upstream correction owed and is not edited because this run is spine-only.
- (event) Reviewer gate pass 5 completed with REVISE/FAIL: rubric found 2 critical, adversarial found 2 critical, security/data-integrity found 2 critical, and version/reality found 1 high stale-fact mismatch. Clear corrections were applied for current gitlinks, pub/sub source-routing contradiction, shipped dedup fact, D2 retention scope, Evidence Packet null semantics, admission/truncation semantics, active Redis support status, and ratified D5 evidence-only qualification. The spine remains draft pending user decisions and re-review.
- (question) Choose AD-21 tombstone semantics: retain terminal tenant-ID non-reuse by forbidding reversal after any erasure tombstone (recommended), or permit a tightly authorized pre-completion reversal that can prove no erasure completion evidence exists and define its lifecycle transition and writer.
- (decision) Administrator ratified terminal erasure semantics on 2026-09-13: any AD-21 erasure tombstone is irreversible and permanently retires that tenant identifier. AD-21 permits no reversal record, correction procedure, or effective-state restoration; every provisioning, replay, restore, and import consult treats tombstone presence as permanently erased. This preserves AD-6's terminal irrevocable Erased state and FR39/NFR16 non-reuse guarantees, and resolves pass-5 R5-02/ADV5-03 by removing restored issuability rather than adding a reversal writer.
- (question) Choose the concurrent-erasure protocol for AD-3/AD-16: adopt a tenant-wide lifecycle generation fence rotated by the committed transition to Deleting, require every derived-store write to compare that generation atomically at write time, drain or invalidate work admitted under the prior generation before the final purge, and CAS completion against the fence (recommended); or adopt a drain-only registry that waits for every admitted activity before purge, accepting stronger liveness dependence on exhaustive activity tracking.
- (decision) Administrator adopted the fence-led concurrent-erasure protocol for AD-3/AD-16. Committing Deleting advances a per-tenant lifecycle write generation; every writer to an AD-16 purge target must atomically compare its captured generation at the backend write boundary or be fenced by confirmed revocation of its tenant write authority. Erasure waits for every target to acknowledge the fence, then purges and verifies, and appends completion evidence plus the irreversible tombstone only through CAS against the same generation. Stale work cannot commit; draining or cancellation is an operational optimization, not the safety boundary. Binds concurrent mutation, projection, purge, verification, and completion; prevents previously admitted work from resurrecting tenant data after erasure.
- (decision) Pass-5 clear-fix sweep propagates AD-16's already-adopted closed purge-set rule across the spine's own store inventory. The closed enumeration explicitly includes every tenant-keyed direct-Redis and Dapr coordination record - preflight and durable deduplication, failed-unit registries, import leases, write fences, migration/rebuild state, and projection checkpoints - plus active, staging, and retired projection epochs. Their implementation remains blocker-ledgered. Operational backup treatment remains unresolved because purge versus key-destruction-and-retention is a load-bearing choice.
- (question) Choose AD-21 disaster-continuity semantics for the erased-tenant register. Pragmatic recommendation: one non-rollback authoritative lineage per tenant population, using the ordered EventStore stream revision as its sequence; ordinary a backup-restored or continuity-unproven copy unavailable and forbid provisioning/re, replay, restore, or, and import until the original lineage is recovered. A clean-room replacement is a distinct population with a disjoint identifier namespace, not recovery. Alternative: add an independently durable synchronous high-water/hash-chain witness for every register append so a restored primary can prove and reconstruct every record through the witnessed head, accepting another critical component and write availability coupling.
- (question) Correction and replacement for the immediately preceding malformed question: choose AD-21 disaster-continuity semantics for the erased-tenant register. Pragmatic recommendation: one non-rollback authoritative lineage per tenant population, using the ordered EventStore stream revision as its sequence; treat any backup-restored or continuity-unproven copy as unavailable and forbid provisioning, deletion, replay, restore, and import until the original lineage is recovered. A clean-room replacement is a distinct population with a disjoint identifier namespace, not recovery. Alternative: add an independently durable synchronous high-water/hash-chain witness for every register append so a restored primary can prove and reconstruct every record through the witnessed head, accepting another critical component and write-availability coupling.
- (decision) Administrator adopted AD-21's single non-rollback register lineage. One dedicated append-only erased-tenant register stream exists per tenant population; its immutable population identifier defines the lineage and its EventStore stream revision is the only register sequence. Unrelated platform records never advance that sequence. Only the live lineage maintained through qualified EventStore continuity is authoritative; an application backup restore, empty replacement, counter advance, or operator assertion can never establish continuity. If lineage cannot be proven, register-dependent operations fail closed indefinitely. A new population is not recovery and uses a disjoint identifier namespace. A synchronous independently durable full tombstone mirror - not a high-water-only witness - is deferred until a finite RPO/RTO, total-lineage-loss recovery, or multi-region requirement is adopted. Binds register ordering, restore admission, provisioning safety, and disaster continuity; prevents a rolled-back register from reissuing an erased tenant ID.
- (question) Choose AD-16 operational-backup erasure semantics. Option A physically deletes or rewrites every backup containing tenant or tenant-derived material before erasure completes, including shared Redis/FalkorDB snapshots. Option B (recommended) retains only backup content protected by the tenant key for bounded operational retention, proves sampled ciphertext unreadable after key destruction, does not back up rebuildable shared projections, and makes any future non-tenant-key-protected backup a purge-and-verification target that blocks erasure until removed.
- (decision) Administrator adopted AD-16's pragmatic operational-backup erasure model. Authoritative EventStore and tenant-content backups may remain only for bounded operational retention when each tenant payload is protected exclusively by tenant key material that erasure destroys; completion evidence must demonstrate on a sampled pre-erasure backup ciphertext that the payload no longer decrypts. Rebuildable shared Redis/FalkorDB projections and tenant-keyed coordination state have no operational backups and recover by AD-2 replay. Any future backup or replica containing tenant content or content-derived material without tenant-key protection is automatically a physical purge-and-verification target and blocks Erased until every copy is removed. Backup manifests retain only content-free opaque identifiers, and every restore still consults AD-21. Binds backup policy, restore, purge enumeration, and NFR16 evidence; prevents immutable/shared backups from retaining readable erased-tenant data without requiring backup rewriting machinery.
- (question) Choose FR71 export/erasure semantics. Option A keeps a source-managed, tenant-key-wrapped and indexed bundle that becomes unusable after erasure, but it is not independently portable unless the decryption key leaves the source - which defeats revocation. Option B (recommended) treats completed export as an explicit custody transfer: deliver self-contained portable JSON or recipient-encrypted bytes, purge only platform-held staging copies, remove the permanent bare-content-digest and source-key-escrow requirements, refuse same-population restore when the signed origin tenant is tombstoned, and state honestly that an external holder can resubmit copied content as new ingestion outside the source platform's erasure boundary. Stage and finalize the bundle under the tenant key, revalidate the tenant lifecycle generation before first release, and cancel/purge any export that has not begun release when Deleting commits.
