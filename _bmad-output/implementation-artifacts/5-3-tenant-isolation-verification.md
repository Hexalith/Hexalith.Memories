# Story 5.3: Tenant Isolation Verification

Status: ready-for-dev

## Story

As an operator,
I want to run automated tenant isolation checks,
so that I can verify zero cross-tenant data leakage with confidence.

## Acceptance Criteria

1. **Given** two tenants (A and B) each with indexed memory units, **when** I run `tenant verify` on tenant A (FR40), **then** automated checks confirm:
   - Search from tenant A context returns zero results from tenant B across all axes (syntactic, semantic, graph)
   - Ingestion into tenant A is not visible from tenant B's context
   - Graph traversal from tenant A returns zero nodes from tenant B

2. **Given** identical graph structures created in tenant A and tenant B, **when** traversal is executed from tenant A, **then** zero nodes from tenant B appear even if edge IDs collide (NFR8)

3. **Given** malformed, empty, or swapped tenant IDs, **when** used in search, ingestion, or graph traversal requests, **then** the system rejects them with clear error messages — never falls through to a default tenant or cross-tenant access

4. **Given** a verification run completes, **when** results are returned, **then** they include per-check pass/fail status with details for any failures

## Tasks / Subtasks

- [ ] Task 1: Create verification contracts (AC: #4)
  - [ ] 1.1 `Contracts/V1/TenantIsolationCheckResult.cs` — sealed record: `CheckName` (string), `Passed` (bool), `Details` (string?), `Remediation` (string?), `DurationMs` (double). `Remediation` contains actionable operator guidance on failure (e.g., "Run `memories tenant delete --id ghost-tenant` to clean up orphaned database"). Apply `[JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]` on both `Details` and `Remediation`.
  - [ ] 1.2 `Contracts/V1/TenantIsolationVerificationResult.cs` — sealed record: `TenantId` (string), `VerifiedAt` (DateTimeOffset), `AllPassed` (bool), `Summary` (string), `Checks` (IReadOnlyList\<TenantIsolationCheckResult\>). `Summary` provides a human-readable overview (e.g., "7 of 7 checks passed" or "3 of 7 checks failed: SyntacticIsolation, GraphIsolation, OrphanedDatabases"). Generated by the verifier at aggregation time.
  - [ ] 1.3 Register both types in `MemoriesJsonContext.cs`
  - [ ] 1.4 Create serialization tests for both contract types in `Contracts.Tests/V1/`

- [ ] Task 2: Create TenantIsolationVerifier service (AC: #1, #2, #3)
  - [ ] 2.1 `Server/Tenants/TenantIsolationVerifier.cs` — sealed partial class (for `[LoggerMessage]`)
    - Constructor-inject: `TenantRegistryService`, `IConnectionMultiplexer` (keyed `"redis"`), `IConnectionMultiplexer` (keyed `"falkordb"`), `ILogger<TenantIsolationVerifier>`
    - Public method: `VerifyAsync(string tenantId, CancellationToken ct) -> Task<TenantIsolationVerificationResult>`
  - [ ] 2.2 Implement individual check methods (each returns a `TenantIsolationCheckResult`):
    - `CheckSyntacticIsolationAsync` — FT.SEARCH `{otherTenantId}:memories:idx` with `@tenantId:{targetTenantId}` to detect data in wrong index; also FT.SEARCH `{targetTenantId}:memories:idx` with `@tenantId:{otherTenantId}` to detect leaked entries
    - `CheckSemanticIsolationAsync` — same pattern against `{tenantId}:memories:vec` indexes
    - `CheckGraphIsolationAsync` — FalkorDB uses **database-level isolation**, so graph nodes do NOT have a `tenantId` property. Verification approach: (a) execute `GRAPH.LIST` via Redis command to enumerate all graph databases; (b) confirm tenant's database exists; (c) **connection-path test:** attempt `GRAPH.QUERY {otherTenantId} "MATCH (n) RETURN count(n)"` using the tenant's own FalkorDB connection — if it returns results instead of an error, database-level isolation is broken; (d) for each other tenant, confirm that `GRAPH.LIST` shows separate databases (structural isolation proof). Do NOT use `IGraphQueryBuilder` for this — `GRAPH.LIST` and `GRAPH.QUERY` here are Redis infrastructure commands for isolation testing, not application data queries.
    - `CheckOrphanedDatabasesAsync` — compare `GRAPH.LIST` results against `TenantRegistryService.ListTenantsAsync`. Flag any FalkorDB databases that do not correspond to a registered tenant as `ORPHANED_DATABASE` findings (catches manually provisioned or incompletely cleaned-up infrastructure).
    - `CheckIndexExistenceAsync` — verify `FT.INFO` succeeds for both `{tenantId}:memories:idx` and `{tenantId}:memories:vec`; verify FalkorDB database responds via `GRAPH.LIST` containment check
    - `CheckInputValidationAsync` — attempt operations with malformed tenant IDs (`""`, `null`, `"../escape"`, `"tenant with spaces"`, reserved names from `TenantIdGuard`) and verify rejection. Note: `TenantIdGuard` is `internal` in `Server/Activities/Indexing/` but accessible from `Server/Tenants/` since both are in the same `Hexalith.Memories.Server` assembly — do NOT create a duplicate validation method.
  - [ ] 2.3 **Resilience pattern:** Each check method must catch `RedisConnectionException` and `RedisServerException` internally and return a failing `TenantIsolationCheckResult` with `Details: "Backend unavailable: {exceptionMessage}"` and `Remediation: "Check Redis/FalkorDB connectivity and retry"` — never let backend unavailability propagate as an unhandled exception. The verifier reports infrastructure issues as check failures, not crashes.
  - [ ] 2.4 **Cross-check tenant filtering:** When iterating other tenants for cross-checks, skip tenants with non-`Active` status (`Provisioning`, `Deleting`, `Failed`, `CompensationFailed`). Report skipped tenants as `CheckName: "CrossCheck-{tenantId}", Passed: true, Details: "Skipped — tenant status: {status}"`.
  - [ ] 2.5 Aggregate all check results with per-check timing (use `Stopwatch.Elapsed.TotalMilliseconds`)
  - [ ] 2.6 Register `TenantIsolationVerifier` as singleton in DI

- [ ] Task 3: Create verification endpoint (AC: #1, #4)
  - [ ] 3.1 `POST /api/tenants/{tenantId}/verify` — validates tenantId via `TenantIdGuard`, checks tenant exists via `TenantRegistryService`, runs `TenantIsolationVerifier.VerifyAsync`, returns 200 with `TenantIsolationVerificationResult`. Wrap in top-level try-catch: `DaprException` → 503 with `ErrorResponse("DAPR_UNAVAILABLE", ...)`, `RedisException` → 503 with `ErrorResponse("BACKEND_UNAVAILABLE", ...)`.
  - [ ] 3.2 Return 404 with `ErrorResponse("TENANT_NOT_FOUND", ...)` if tenant doesn't exist
  - [ ] 3.3 Register endpoint in `Program.cs` following existing minimal API delegate pattern

- [ ] Task 4: Unit tests for TenantIsolationVerifier (AC: #1, #2, #3)
  - [ ] 4.1 `tests/Server.Tests/Tenants/TenantIsolationVerifierTests.cs` — mock `IConnectionMultiplexer` (keyed), `TenantRegistryService`
  - [ ] 4.2 Test cases:
    - Verify returns all checks passed when no cross-tenant data exists
    - Verify detects syntactic index leakage (foreign tenantId in index)
    - Verify detects semantic index leakage
    - Verify detects graph database-level isolation breach (cross-database query returns data)
    - Verify detects orphaned FalkorDB databases not in tenant registry
    - Verify rejects malformed tenant IDs
    - Verify rejects reserved tenant IDs
    - Verify handles non-existent tenant gracefully (TENANT_NOT_FOUND)
    - Verify includes per-check timing in results
    - Verify `AllPassed` is false when any check fails
    - Verify endpoint returns 500 with meaningful error when `RedisException` is thrown (not unhandled)
    - Verify single-tenant scenario: cross-checks report "Skipped — no other tenants to check against" (not vacuously passing)
    - Verify empty-tenant scenario: tenant with zero memory units passes with Details indicating checks were vacuously true
    - Verify non-Active tenants are skipped in cross-checks with appropriate skip message
    - Verify backend-unavailable scenario: check returns `Passed: false` with connectivity error, not unhandled exception

- [ ] Task 5: Integration tests for end-to-end isolation verification (AC: #1, #2)
  - [ ] 5.1 `tests/IntegrationTests/Tenants/TenantIsolationIntegrationTests.cs` — requires Aspire AppHost fixture
  - [ ] 5.2 Test cases:
    - Provision tenant A and B, ingest memory units into both, run verify on A, confirm all checks pass
    - Create identical graph structures in A and B, verify zero cross-tenant nodes (edge ID collision test per NFR8). **Note:** AC #2 (edge ID collision) can only be validated by integration tests — the verifier service checks structural isolation, not data-level collision. This test is the AC #2 validation mechanism.
    - Ingest into A, search from B context, confirm zero results across all axes
    - Run verify with malformed/empty/swapped tenant IDs, confirm rejection
    - Delete tenant B, run verify on A, confirm A isolation is unaffected
    - **Negative test (false-pass prevention):** Deliberately plant cross-tenant data (e.g., manually write a hash with tenant B's key prefix into tenant A's RediSearch index), run verify on A, confirm the verifier **detects** the planted leakage. This prevents false-pass bugs in `FT.SEARCH` query construction.
  - [ ] 5.3 **NOTE:** Integration tests may be deferred for initial PR merge if Aspire AppHost fixture is not yet extended (consistent with 5-1 deferral). Create test file with `[Fact(Skip = "Requires Aspire AppHost fixture")]` if needed. However, these integration tests are **required before Gate 2 sign-off** — NFR8 (zero cross-tenant data leakage) is a hard gate and cannot be validated by unit tests alone.

## Dev Notes

### Dependencies

- **Story 5-1 (Tenant Provisioning):** Required — provides `TenantRegistryService`, provisioning activities, `IndexSchemaDefinitions`, `TenantIdGuard`, and all tenant contracts. Already implemented (status: review).
- **Story 5-2 (Tenant Deletion):** NOT required — story 5-3 can be implemented independently. Do NOT assume `TenantStatusGuard` exists (it is planned for 5-2 but not yet built).
- **Story 5-4 (Tenant Context Enforcement):** Separate concern. This story verifies **data isolation** (no cross-tenant data leakage in indexes/databases). Story 5-4 verifies **endpoint-level enforcement** (rejecting cross-tenant API requests). The two are complementary but independent.

### Implementation Priority

Implement in this order to satisfy ACs incrementally:
1. **Core checks first (AC-satisfying):** `CheckIndexExistence`, `CheckSyntacticIsolation`, `CheckSemanticIsolation`, `CheckGraphIsolation`, `CheckInputValidation`
2. **Enhancements second (beyond AC, justified for Gate 2):** `CheckOrphanedDatabases`, connection-path test, actionable remediation messages

### Architecture Compliance

- **NFR8 (Hard Gate):** This story implements the verification side of the zero cross-tenant data leakage gate. The verification must cover all three axes: syntactic (RediSearch), semantic (Redis Vector), graph (FalkorDB).
- **Physical isolation model:** RediSearch/Redis Vector use tenant-namespaced indexes (`{tenantId}:memories:idx`, `{tenantId}:memories:vec`). FalkorDB uses **separate databases per tenant** (not label/namespace separation). This means graph isolation is at the connection level, not the query level.
- **D9 (`IGraphQueryBuilder`):** All Cypher data queries MUST go through `IGraphQueryBuilder` — no raw Cypher string construction. However, `GRAPH.LIST` is a Redis infrastructure command (not a Cypher query) and is executed directly via `IConnectionMultiplexer` — this does not violate D9.
- **D1 (FalkorDB):** FalkorDB isolation is database-level. Graph nodes do NOT have a `tenantId` property — there is no need for one within a physically isolated graph. Isolation verification must be structural: confirm separate databases exist via `GRAPH.LIST`, not by searching for foreign `tenantId` values on nodes.

### Index Naming (from `IndexSchemaDefinitions.cs`)

| Backend | Index Name | Key Prefix |
|---------|-----------|------------|
| RediSearch (syntactic) | `{tenantId}:memories:idx` | `{tenantId}:mu:` |
| Redis Vector (semantic) | `{tenantId}:memories:vec` | `{tenantId}:vec:` |
| FalkorDB (graph) | database = `{tenantId}` | N/A (database-level) |

Use `IndexSchemaDefinitions.GetSyntacticIndexName(tenantId)` and `IndexSchemaDefinitions.GetSemanticIndexName(tenantId)` — do NOT hardcode index names.

### Existing Infrastructure (from Story 5-1)

| Component | Location | Usage |
|-----------|----------|-------|
| `TenantRegistryService` | `Server/Tenants/TenantRegistryService.cs` | `GetTenantAsync`, `ListTenantsAsync` to find other tenants for cross-checks |
| `TenantIdGuard` | `Server/Activities/Indexing/TenantIdGuard.cs` | `Validate(tenantId)` for format + reserved name checks. Internal class. |
| `IndexSchemaDefinitions` | `Server/Infrastructure/IndexSchemaDefinitions.cs` | `GetSyntacticIndexName`, `GetSemanticIndexName` for correct index names |
| `IGraphQueryBuilder` | `Server/Graph/IGraphQueryBuilder.cs` | 17 existing methods; no changes needed for this story (graph isolation verified via `GRAPH.LIST`, not Cypher) |
| `VerifyTenantActivity` | `Server/Activities/Tenants/VerifyTenantActivity.cs` | Verifies indexes exist and are accessible (post-provisioning check). Different from isolation verification. |
| `TenantInfo` | `Contracts/V1/TenantInfo.cs` | Sealed record with `Id`, `DisplayName`, `Status`, `CreatedAt` |
| `TenantStatus` | `Contracts/V1/TenantStatus.cs` | Enum: `Provisioning`, `Active`, `Deleting`, `Failed`, `CompensationFailed` |

### Redis Connection Pattern

Keyed DI injection for Redis connections:
- `IConnectionMultiplexer` keyed `"redis"` — for RediSearch and Redis Vector (same Redis Stack instance)
- `IConnectionMultiplexer` keyed `"falkordb"` — for FalkorDB graph database

Access via `[FromKeyedServices("redis")] IConnectionMultiplexer redis` in constructor.

### Error Response Pattern

Use `ErrorResponse("CODE", "message", "suggestion")` pattern for all 4xx/5xx responses (consistent with existing endpoints). Known error codes in tenant domain:
- `TENANT_NOT_FOUND` (404)
- `TENANT_ALREADY_EXISTS` (409)
- `TENANT_DELETING` (409)
- `TENANT_PROVISIONING` (409)

### Code Conventions

- Sealed records for all contract types (positional ctor for required fields, init for optional)
- `[JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]` on nullable optional fields
- Sealed partial class for services using `[LoggerMessage]` source generator
- File-scoped namespaces: `namespace Hexalith.Memories.Server.Tenants;`
- Feature-based namespace organization: `Server.Tenants` for services
- Singleton DI registration for stateless services
- `Stopwatch.Elapsed.TotalMilliseconds` (not `.Milliseconds`) for timing
- xUnit + Shouldly assertions + NSubstitute mocking
- Test naming: `{ClassName}Tests.cs` with descriptive method names

### Anti-Patterns to Avoid

1. **Do NOT use raw Cypher strings** — all Cypher data queries through `IGraphQueryBuilder`. `GRAPH.LIST` is a Redis command, not Cypher, and is executed directly via `IConnectionMultiplexer`.
2. **Do NOT hardcode index names** — use `IndexSchemaDefinitions` methods
3. **Do NOT assume `TenantStatusGuard` exists** — it does not exist yet (planned for Story 5-2). Use `TenantRegistryService.GetTenantAsync` directly and check for null.
4. **Do NOT use Redis `KEYS` command** — use `SCAN` if iterating keys
5. **Do NOT search for `tenantId` on graph nodes** — they don't have one. FalkorDB isolation is database-level (see D1). Verify structurally via `GRAPH.LIST`.
6. **Do NOT create `Hexalith.Memories.Redis.Tests` project** — architecture planned this but it doesn't exist; put tests in `Server.Tests` and `IntegrationTests`
7. **Do NOT duplicate `VerifyTenantActivity` logic** — that activity checks post-provisioning (indexes exist, are empty). `TenantIsolationVerifier` checks cross-tenant leakage (different concern).
8. **Do NOT duplicate `TenantIdGuard` validation** — `TenantIdGuard` is `internal` in `Server/Activities/Indexing/` but is accessible from `Server/Tenants/` since both are in the same `Hexalith.Memories.Server` assembly. Reuse it directly.
9. **Do NOT add `IGraphQueryBuilder` methods for isolation checks** — graph isolation is structural (database-level), not data-level. No Cypher-based verification is needed.

### Previous Story Learnings

- **FalkorDB `RedisServerException` for graph-not-found** must be caught gracefully (from 5-1 compensation activities)
- **FalkorDB `collect()` result types vary** by NFalkorDB driver version — parse defensively
- **ETag-based CAS retry loop** (max 3 retries) used for concurrent DAPR state updates
- **Tuples don't serialize well for DAPR** — use sealed records instead
- **`DaprException` wrapping** — catch and return 503 `DAPR_UNAVAILABLE` for sidecar issues
- **1051 total tests currently passing** — run full suite before and after implementation to catch regressions

### Verification Strategy

The `TenantIsolationVerifier` is a **diagnostic tool that confirms architectural guarantees hold** — it does not enforce isolation at runtime. Isolation is enforced by the architecture itself (physically separate indexes and databases). If the verifier ever reports a failure, it means the architecture has a bug. Do NOT over-engineer this as middleware, a guard, or a continuous enforcement mechanism.

The verifier does **proactive cross-tenant leakage detection**, not just "are my indexes there" checks. The verification must:

1. **Find all other tenants** via `TenantRegistryService.ListTenantsAsync`
2. **For each other tenant**, check that:
   - No data from the target tenant appears in their indexes
   - No data from them appears in the target tenant's indexes
3. **Test connection-path isolation** by attempting `GRAPH.QUERY {otherTenantId}` on the tenant's FalkorDB connection — must fail or return empty
4. **Detect orphaned infrastructure** by comparing `GRAPH.LIST` against the tenant registry — flag unregistered databases
5. **Validate input boundaries** by attempting operations with known-bad tenant IDs
6. **Report per-check results** with timing for operator visibility

This is distinct from `VerifyTenantActivity` (which just confirms indexes exist post-provisioning).

**Scope boundary:** This story verifies **data isolation outcomes** — that data ends up in the correct tenant's indexes after ingestion. It does NOT verify that the ingestion API rejects cross-tenant write requests (that's endpoint-level enforcement in Story 5-4). AC #1's "ingestion into tenant A is not visible from tenant B's context" is validated by checking index contents, not by testing API rejection behavior.

### Edge Cases

- **Single tenant:** If only one tenant exists, cross-tenant checks cannot run. Report as `"Skipped — no other tenants to check against"` rather than silently passing. Index existence, input validation, and orphaned database checks still run.
- **Empty tenant:** A tenant with zero memory units will pass all cross-checks vacuously. Include `Details: "Tenant has zero indexed memory units — isolation checks are vacuously true"` so the operator knows the check was not meaningfully exercised.

### Gate 2 Sign-off Criteria

Gate 2 sign-off requires ALL of the following:
1. All unit tests for `TenantIsolationVerifier` pass (Task 4)
2. All integration tests pass on real infrastructure — not deferred, not skipped (Task 5)
3. The negative integration test confirms the verifier detects planted cross-tenant data (Task 5.2, negative test)
4. Operator runs `POST /api/tenants/{tenantId}/verify` on at least 2 tenants with real data and sees `AllPassed: true`

### Known MVP Limitations

- **Scalability:** Cross-tenant checks are O(n) per tenant verified (checking against all other tenants). For the MVP target of 10 tenants (NFR12), this is acceptable. For deployments with 100+ tenants, a sampling strategy or tenant-pair limit should be added in a future enhancement.
- **Unauthenticated endpoint:** `POST /api/tenants/{tenantId}/verify` has no authentication (D8 defers `TenantAuthorizationMiddleware` to Phase 1.5). Any caller can trigger verification on any tenant and enumerate all tenant IDs via cross-check results. Acceptable for MVP since the API is not externally exposed, but must be addressed in Phase 1.5.

### Project Structure Notes

New files go in:
- `src/Hexalith.Memories.Contracts/V1/` — contract records
- `src/Hexalith.Memories.Server/Tenants/` — `TenantIsolationVerifier.cs` (alongside `TenantRegistryService.cs`)
- `tests/Hexalith.Memories.Contracts.Tests/V1/` — serialization tests
- `tests/Hexalith.Memories.Server.Tests/Tenants/` — unit tests
- `tests/Hexalith.Memories.IntegrationTests/Tenants/` — integration tests

### References

- [Source: _bmad-output/planning-artifacts/epics.md — Epic 5, Story 5.3]
- [Source: _bmad-output/planning-artifacts/architecture.md — NFR8, D1, D9, Tenant Isolation]
- [Source: _bmad-output/planning-artifacts/prd.md — FR40, NFR8, Journey 5 (Kenji)]
- [Source: _bmad-output/implementation-artifacts/5-1-tenant-provisioning-workflow.md — Infrastructure created]
- [Source: _bmad-output/implementation-artifacts/5-2-tenant-deletion-workflow.md — TenantStatusGuard design]
- [Source: src/Hexalith.Memories.Server/Infrastructure/IndexSchemaDefinitions.cs — Index naming]
- [Source: src/Hexalith.Memories.Server/Graph/IGraphQueryBuilder.cs — D9 contract]
- [Source: src/Hexalith.Memories.Server/Tenants/TenantRegistryService.cs — Tenant CRUD]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
