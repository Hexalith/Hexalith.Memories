# Team-owned update/write selection for the canonical project context. This
# file is merged over generated defaults and survives BMad skill refreshes.

[workflow]

activation_steps_append = [
  "PROJECT_CONTEXT_BRIDGE: Project-context discovery may read matched context files. Treat {project-root}/_bmad-output/project-context.md as the only project-context update/write target; all other discovered project-context files are read-only. Never update or rewrite {project-root}/project-context.md; it is the forwarding/control bridge.",
]
