---
title: 'Refresh the audited release-tooling lock graph'
type: 'bugfix'
created: '2026-08-04'
status: 'done'
baseline_commit: '908ed7a6c20af86fbf95dc115f7cea634ad98485'
review_loop_iteration: 1
context:
  - '{project-root}/_bmad-output/project-context.md'
  - '{project-root}/docs/dev/release-runbook.md'
---

**Status:** done

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** GitHub Actions Release run `30887412413` and CI run `30887412677` both fail at the blocking root `npm audit --audit-level=low` gate because `package-lock.json` resolves `undici` `7.28.0`. Five advisories published on 2026-08-03 affect the 7.x line below `7.29.0`, so both workflows stop before semantic-release verification or the unit/contract lane.

**Approach:** Refresh only the transitive `undici` lock entry to the first patched 7.x version, update the repository's exact reviewed-graph assertion, and prove the unchanged CI and release audit gates pass. Preserve Memories' stricter vulnerability evidence even though EventStore and Tenants currently omit that audit and carry the same vulnerable lock entry.

## Boundaries & Constraints

**Always:** Keep the root graph development-only and lockfile-driven; retain `npm ci`, `npm audit --audit-level=low`, semantic-release configuration verification, the exact direct dependency set, and the test that pins reviewed transitive versions. Demonstrate that the generated lock diff is limited to `node_modules/undici` `7.28.0 -> 7.29.0` plus the matching test expectation.

**Ask First:** Any direct dependency or override change; any edit to CI/release triggers, jobs, permissions, audit thresholds, publication behavior, package/container inventories, or shared Hexalith.Builds workflows; any broader EventStore/Tenants parity migration.

**Never:** Remove, soften, ignore, or make the audit non-blocking; add `undici` as an application dependency; update unrelated packages; edit EventStore, Tenants, Builds, or other submodule contents; treat the CI artifact-upload cascade as an independent test failure.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|---------------|---------------------------|----------------|
| Patched locked graph | `@semantic-release/github@12.0.8` permits `undici ^7.0.0` | Lock resolves exactly `7.29.0`; `npm ci` reproduces it and the low-level audit passes | Any remaining advisory fails the task |
| Unexpected lock drift | Lock refresh changes another package or root dependency metadata | Reject the generated diff and regenerate narrowly | Do not accept opportunistic upgrades |
| Reviewed-graph mismatch | Lock entry and C# inventory assertion disagree | Focused contract test fails | Update both only to the advisory-patched identity |

</frozen-after-approval>

## Code Map

- `package-lock.json` -- deterministic root release-tooling dependency graph used by both failing audit steps.
- `tests/Hexalith.Memories.Cli.Tests/Ci/CiTestInventoryTests.cs` -- exact manifest/lock allowlist, dependency-edge assertion, and reviewed artifact-source/integrity contract.
- `.github/workflows/ci.yml` -- unchanged blocking install, audit, configuration, build, and unit/contract lane.
- `.github/workflows/release.yml` -- unchanged blocking install/audit gate before any publish-capable step.

## Tasks & Acceptance

**Execution:**
- [x] `package-lock.json` -- run the dated narrow transitive update for `undici`, reject any resolution other than `7.29.0`, and retain only its resolved URL/integrity/version change.
- [x] `tests/Hexalith.Memories.Cli.Tests/Ci/CiTestInventoryTests.cs` -- assert the reviewed `undici` version, npm-registry URL, SHA-512 integrity, and `@semantic-release/github` ownership edge without changing other graph policy.
- [x] Verification -- reproduce the locked install, pass the low-level audit and semantic-release verifier, prove the exact lock diff and CRLF policy, then build and execute the focused CI inventory contract.

**Acceptance Criteria:**
- Given the same root commands used by both linked workflows, when the refreshed lock is installed and audited, then no low-or-higher npm vulnerability remains and semantic-release configuration verification passes.
- Given the reviewed dependency-graph test, when it reads `package.json` and `package-lock.json`, then it requires exact `undici 7.29.0`, the reviewed registry URL and integrity digest, and the expected parent dependency edge while continuing to reject undeclared root dependencies and the npm CLI bundle.
- Given the baseline-to-candidate diff, when lock drift is audited, then only the three reviewed `undici` identity fields differ and every other lock entry remains unchanged.
- Given comparison with EventStore and Tenants, when the incident is handed off, then the record states that their shared workflows are structurally different and omit this vulnerability audit; no submodule is changed to manufacture superficial parity.

## File Scope

**Allowed to modify:**
- `_bmad-output/implementation-artifacts/spec-gh-30887412413-30887412677-fix-ci-cd.md`
- `package-lock.json`
- `tests/Hexalith.Memories.Cli.Tests/Ci/CiTestInventoryTests.cs`

### File List

- `_bmad-output/implementation-artifacts/spec-gh-30887412413-30887412677-fix-ci-cd.md`
- `package-lock.json`
- `tests/Hexalith.Memories.Cli.Tests/Ci/CiTestInventoryTests.cs`

## Change Log

| Date | Phase | Change | Test count | File List reconciliation |
| :--- | :---- | :----- | :--------- | :----------------------- |
| 2026-08-04 | create-story | Adopted the project phase ledger before re-derivation; captured the existing focused graph contract as the comparison baseline. | Phase `+0`; cumulative `+0`; focused method `1 -> 1`; `DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.Cli.Tests/bin/Release/net10.0/Hexalith.Memories.Cli.Tests.dll -method Hexalith.Memories.Cli.Tests.Ci.CiTestInventoryTests.ReleaseTooling_ManifestAndLockPinSecureSemanticReleaseGraph -parallel none -noLogo`. | Matched 1/1 current artifact against baseline `908ed7a6c20af86fbf95dc115f7cea634ad98485`; the three-entry File List includes the two planned implementation paths. |
| 2026-08-04 | dev-story | Refreshed `undici` to `7.29.0` and strengthened the focused contract with exact registry URL, SHA-512 integrity, and owning dependency-edge assertions. | Phase `+0`; cumulative `+0`; focused method `1 -> 1`; focused execution passed 1/1. | Matched 3/3 changed files against the File List: the spec, `package-lock.json`, and `CiTestInventoryTests.cs`; no out-of-scope file changed. |
| 2026-08-04 | code-review | Triaged the blind, edge-case, and verification-gap layers; added fail-closed lock-diff proof plus reproducible toolchain and sibling-module evidence. No implementation patch was required. | Phase `+0`; cumulative `+0`; focused method remained `1 -> 1`; the exact linked-run Node/npm audit and verifier also passed. | Matched 3/3 changed files against the File List; EventStore and Tenants remained read-only. |

## Spec Change Log

- 2026-08-04: Refreshed the audited `undici` lock entry and matching reviewed-graph assertion to `7.29.0`; all specified verification gates passed.
- 2026-08-04 (review loop 1): Adversarial review found that the spec lacked the required File Scope and that the version-only graph assertion did not protect the changed registry URL, integrity digest, or parent edge. Added exact File Scope/File List and phase evidence, strengthened the test task and acceptance text, and made lock-diff/line-ending proof reproducible. This avoids a story-scope rejection and unreviewed artifact substitution. KEEP: the lock-only `7.29.0` repair, unchanged blocking audit gates, exact direct dependency set, and zero-vulnerability/configuration verification.
- 2026-08-04 (review pass): Recorded the exact linked-run Node/npm reproduction, a fail-closed hash for the complete lock diff, and fixed EventStore/Tenants gitlink and workflow evidence. Broader whole-graph and publish-fixture suggestions were not caused by this lock-only repair.

## Design Notes

The two linked failures have one root cause. The later CI artifact-upload error is only `if: always()` fallout after the audit skipped every test-producing step. At baseline, EventStore gitlink `7854f8e51ce9b852bb6c3cac6012670122e93792` and Tenants gitlink `323baf8871e70be3fde92072f32b758af950bc8c` both lock `undici 7.28.0`. Their CI entry points delegate to `Hexalith.Builds/domain-ci.yml`; EventStore adds a semantic-release `npm ci` fixture lane, while neither checked-in sibling workflow invokes `npm audit`. Both releases delegate publication to pinned `Hexalith.Builds/domain-release.yml` revisions. Copying those shapes would remove Memories' blocking vulnerability evidence. Full shared-core CI and guarded-release migration remains the separately sequenced Epic 30 scope.

The durable advisory set is `GHSA-8xcm-r25x-g524`, `GHSA-4cwx-7wf7-3272`, `GHSA-m8rv-5g2x-5cg5`, `GHSA-jr45-8vmc-qm54`, and `GHSA-v3r7-h72x-cjcm`; all identify `7.29.0` as the first patched 7.x release. “Unrelated graph drift” is bounded to the candidate lock diff, while the permanent C# contract protects the selected release-tooling graph, forbidden bundle absence, artifact identity, and owning dependency edge.

## Verification

**Commands:**
- `npm update --package-lock-only --ignore-scripts --no-audit undici` -- expected on 2026-08-04: only the top-level locked `undici` entry advances to exactly `7.29.0`; reject a later resolver result rather than accepting broader drift.
- `npx -y -p node@24.18.0 -p npm@11.16.0 -c 'node --version && npm --version && npm ci && npm audit --audit-level=low && npm run verify:semantic-release-config'` -- expected: reproduce the Release run's `lts/*` resolution exactly, install deterministically, report zero low-or-higher advisories, and pass the verifier plus negative self-tests.
- `dotnet build tests/Hexalith.Memories.Cli.Tests/Hexalith.Memories.Cli.Tests.csproj --configuration Release --disable-build-servers -m:1 /nr:false -p:NuGetAudit=false -p:MinVerVersionOverride=1.0.0` -- expected: zero warnings and errors.
- `DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.Cli.Tests/bin/Release/net10.0/Hexalith.Memories.Cli.Tests.dll -method Hexalith.Memories.Cli.Tests.Ci.CiTestInventoryTests.ReleaseTooling_ManifestAndLockPinSecureSemanticReleaseGraph -parallel none -noLogo` -- expected: focused reviewed-graph contract passes.
- `test "$(git diff --numstat 908ed7a6c20af86fbf95dc115f7cea634ad98485 -- package-lock.json)" = $'3\t3\tpackage-lock.json' && test "$(git diff --unified=0 908ed7a6c20af86fbf95dc115f7cea634ad98485 -- package-lock.json | sha256sum | cut -d' ' -f1)" = d00575e8fd637a516bef22e428a9c58ef3b10bf398f9d7d2fb473d7d3b6648ca` -- expected: mechanically fail closed unless the complete lock diff is exactly the reviewed three-field replacement.
- `git ls-tree HEAD references/Hexalith.EventStore references/Hexalith.Tenants && ! rg -n 'npm audit' references/Hexalith.EventStore/.github/workflows/{ci,release}.yml references/Hexalith.Tenants/.github/workflows/{ci,release}.yml && rg -n 'uses: Hexalith/Hexalith.Builds/.github/workflows/domain-(ci|release).yml@|npm ci' references/Hexalith.EventStore/.github/workflows/{ci,release}.yml references/Hexalith.Tenants/.github/workflows/{ci,release}.yml && rg -n -A1 '"node_modules/undici":' references/Hexalith.EventStore/package-lock.json references/Hexalith.Tenants/package-lock.json` -- expected: fixed gitlinks and `7.28.0` locks, shared CI/release delegation, EventStore's extra install fixture, and no sibling `npm audit` invocation.
- `perl -0777 -ne 'exit 1 if /(?<!\r)\n/' package-lock.json tests/Hexalith.Memories.Cli.Tests/Ci/CiTestInventoryTests.cs _bmad-output/implementation-artifacts/spec-gh-30887412413-30887412677-fix-ci-cd.md` -- expected: all changed files satisfy the CRLF working-tree policy.
- `git diff --no-index --check /dev/null _bmad-output/implementation-artifacts/spec-gh-30887412413-30887412677-fix-ci-cd.md >/dev/null; test $? -eq 1` -- expected: the untracked spec differs from `/dev/null` but has no whitespace error.
- `git diff --check` -- expected: no whitespace errors.

**Executed Results (2026-08-04):**
- `npm update --package-lock-only --ignore-scripts --no-audit undici` exited 0 and resolved exactly `7.29.0`.
- The exact Release-run toolchain reproduction (`node v24.18.0`, `npm 11.16.0`) exited 0; npm audited 306 packages with 0 vulnerabilities, and the offline verifier plus fail-closed negative cases passed.
- The specified Release `dotnet build` exited 0 with 0 warnings and 0 errors.
- The specified focused xUnit invocation exited 0 with 1 passed, 0 failed, and 0 skipped.
- The fail-closed baseline lock-diff command exited 0 with exact `3/3` line counts and SHA-256 `d00575e8fd637a516bef22e428a9c58ef3b10bf398f9d7d2fb473d7d3b6648ca` for the complete diff.
- The parity command recorded EventStore `7854f8e51ce9b852bb6c3cac6012670122e93792` and Tenants `323baf8871e70be3fde92072f32b758af950bc8c`; both locks contain `undici 7.28.0`, both CI/release shapes delegate to Hexalith.Builds, and neither sibling workflow contains `npm audit`.
- The CRLF policy command exited 0 for all three changed files.
- The untracked-spec whitespace command produced its expected checked-difference status and exited 0 as a compound command.
- `git diff --check` exited 0 with no whitespace errors.

## Suggested Review Order

**Scope and rationale**

- Start with the approved lock-only response and retained audit boundary.
  [`spec-gh-30887412413-30887412677-fix-ci-cd.md:17`](spec-gh-30887412413-30887412677-fix-ci-cd.md#L17)

**Audited lock repair**

- Review the sole release-tooling graph mutation: three `undici` identity fields.
  [`package-lock.json:3704`](../../package-lock.json#L3704)

**Regression guard**

- Confirm the selected version, artifact identity, and owning dependency edge.
  [`CiTestInventoryTests.cs:237`](../../tests/Hexalith.Memories.Cli.Tests/Ci/CiTestInventoryTests.cs#L237)
