---
baseline_commit: 272c33bc5d30d71ac46f20e703b9d5456e75a093
creation_commit: 6d7fd8aaa0a2fc58de741e31f38544fc15a10c08
creation_sprint_status_sha256: f2cc7a2520634d2ca280622faf1494477c0ec5500e24ae0c8b7baf36f1dc13c8
creation_sprint_status_sha256_note: 'pre-create value; the sprint-status blob at creation_commit hashes to cb351de28a5ac78667cfdd57d67a45d6e5c4ab53a429cf50985819126be23116'
creation_path: _bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md
renamed_to_current_path_by: f474db15
provenance_note: 'creation_commit created the story at creation_path; commit f474db15 renamed it to this path under the approved 2026-07-20 course correction. Recorded 2026-07-26 by code review, chunk 3b.'
creation_scope_evidence: _bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md
predecessor_status_at_creation: in-progress
---

# Story 27.3: Production Adapter and Deployment Profile

Status: in-progress

Execution gate: C0 passed on 2026-07-19 against Story 27.2's completed, reviewed handoff. The first approved 2026-07-20 course correction narrows this story to C1 qualification; the later approved 2026-07-20 on-premises correction replaces `PG-AZ-1` with `PG-ONPREM-1`. C1 blocks Production lifecycle writes and Story 27.4 until the exact Production profile passes every capability, security, durability, capacity, fault, backup/restore, and physical-reclamation gate. A41 remains open and is not mutable under Story 27.3.

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a Platform Operations and security review pair,
I want one immutable Production state-store profile qualified against every C1 gate,
so that deployment-shaped lifecycle verification starts only on an atomic, durable, capacity-funded, and approved adapter.

## Acceptance Criteria

1. **Given** qualification target `PG-ONPREM-1`,
   **When** the running target is captured,
   **Then** its exact runtime, component, PostgreSQL 18.4 backend, Dapr control plane, application images, component/config manifests, actor/Scheduler identities, configuration epoch, profile hash, node/storage capacity, and operating cost are immutable evidence.

2. **Given** the exact captured profile,
   **When** the complete C1 probe runs,
   **Then** CRUD, strong reads, ETags, rollback-atomic multi-key transactions, TTL, actor reactivation, Placement/Scheduler/reminder recovery, request bounds, two-writer 500 events/s throughput, 150,000-record purge catch-up, isolation, encryption, capacity, and cohort-attributable physical reclamation all pass without skip.

3. **Given** the declared backend fault,
   **When** the PostgreSQL container/process is forcibly lost and its StatefulSet pod is replaced while the single node and retained local volume remain healthy,
   **Then** every Dapr-acknowledged record remains present; node, local-volume, control-plane, and site loss are explicitly outside profile; and backup/restore evidence plus the resulting nonzero RPO/RTO are published without an HA claim or overstatement.

4. **Given** complete immutable C1 evidence,
   **When** approvals are recorded,
   **Then** Hexalith Platform Operations separately approves node/storage capacity, operating cost, operation, bounded fault, backup/restore, upgrade, rollback, and reclamation and acknowledges the absence of node/disk/site HA, while a security reviewer approves identity, secrets, TLS, network, authorization, encryption, privacy, and evidence integrity.

5. **Given** any missing digest, placeholder, profile drift, failed probe, missing approval, or unreserved capacity,
   **When** C1 evaluates the profile,
   **Then** Production writes remain disabled, Story 27.3 remains `in-progress`, Story 27.4 remains `backlog`, and A41 remains open.

## Tasks / Subtasks

- [x] Task 0 - Pass the Story 27.2 implementation handoff before beginning Story 27.3 evidence work (AC: 1, 2)
  - [x] Require Story 27.2 to be `done` with its C1-C6 checkpoints reviewed and complete, its `dev-story` and `code-review` ledger rows present, its actual runner-derived counts reconciled, and its cumulative File List matched. The creation-time `in-progress` sprint row, `ready-for-dev` story header, unchecked tasks, and pending C1-C6 rows are not completion evidence.
  - [x] Re-read every Story 27.2 implementation, test, Dapr component, deployment, dashboard, and documentation path after that handoff. Record the actual handoff inventory in a dedicated C0 section of the Dev Agent Record; keep acceptance criteria, task definitions, planned-delta history, and other frozen sections unchanged. Reconcile actual counts only in the Change Log, File List, Dev Agent Record, or another phase-ledger-permitted section before making an implementation edit.
  - [x] Re-run the predecessor's portable checkpoint class through the **Checkpoint Execution Contract** below, using the canonical selector `-class Hexalith.Memories.IntegrationTests.Telemetry.AccessTelemetryLifecycleIntegrationCheckpointTests -parallel none -noLogo`.
  - [x] If Story 27.2 is incomplete or the class/path differs, record the exact blocker and return ownership to Story 27.2. Do not implement missing portable runtime behavior, claim pending/planned evidence as actual, or mutate A41 records under Story 27.3.

- [ ] Task 1 - Qualify, pin, approve, and capacity-gate the exact `PG-ONPREM-1` Production Dapr component profile (AC: 1, 2, 3, 4, 5)
  - [ ] Use only profile ID `postgresql-v2-dapr-1.18.1-postgresql-18.4-onprem-k8s1-openebs-local-retain-400g-v1`: Dapr `state.postgresql/v2`; the official PostgreSQL 18.4 Trixie image pinned to index digest `sha256:3a82e1f56c8f0f5616a11103ac3d47e632c3938698946a7ad26da0df1334744a` and `linux/amd64` manifest `sha256:d93de42662696f278fb34354b06fdaa90ad7ca3106d6f72fbd01d16da006d2cf`; one StatefulSet replica; 4 CPU/8 GiB requests and 8 CPU/16 GiB limits; one 400 GiB `openebs-hostpath-retain` PVC local to `node1`; ClusterIP-only TLS `verify-full`; and the dedicated `memories_access_telemetry` database/`access_telemetry` schema/runtime role. This is explicitly non-HA. Any substitution changes the profile hash and requires another approved course correction.
  - [ ] Before exercising PostgreSQL v2, replace the unsupported `QueryStateAsync`/`queryIndexes` expiry scan with deterministic explicit minute/shard bucket state committed through Dapr transactions. Prove strong reads/ETags, bounded traversal, ordering, two-writer collision behavior, stale-index cleanup, and cross-tenant denial without adding a PostgreSQL SDK or connection string to product code.
  - [ ] Record the exact Dapr runtime and component implementation identity from the running deployment, not from .NET package pins: runtime version, sidecar image digest, component type/API version/capabilities, Scheduler connections, actor types, enabled features, component manifest/profile hash, backend identity/version, configuration epoch, and any alpha opt-in.
  - [ ] Run the complete ADR behavioral probe against the exact candidate: strong CRUD/reads, ETags, record-plus-expiry-index atomicity, deterministic conflict behavior, actor state/reactivation/placement/reminders, effective TTL, request bounds, two-writer throughput during purge, durability, isolation/encryption, capacity, and physical-evidence hooks.
  - [ ] Fault-inject a later operation in the multi-key transaction and prove no partial record or expiry-index commit. Do not certify Redis from Dapr's capability table: the official Dapr 1.18 Redis implementation warns that transactions have no rollback and are unsuitable as a Production actor store unless the exact profile proves the ADR's atomicity contract.
  - [ ] Publish the adapter's declared single-component fault and prove zero acknowledged-record loss for that exact fault. A candidate that cannot prove zero is ineligible. Publish RPO/RTO only for failures outside the declared single-component profile, plus the physical-reclamation collector/bound, upgrade and rollback owner, and an approval by Hexalith Platform Operations plus the security reviewer.
  - [ ] Calculate `records × (measuredRecordBytes + measuredIndexBytes) × durabilityMultiplier + controlBytes + reclamationWorkspace` for 1 hour, the configured 24-hour target, and the 7-day software maximum. Normalize every operand to integer bytes/counts before arithmetic; reject missing units, fractional counts, negative values, `NaN`/infinity, values above signed 64-bit input bounds, overflow, and mixed decimal/binary units; use checked/arbitrary-precision arithmetic and round required capacity upward. Include actor/index state, WAL/snapshot overhead, allocator fragmentation, rewrite/vacuum workspace, shared-node headroom, Scheduler storage, operating cost, and decommission headroom. Admit only durations whose measured result fits the exact 400 GiB profile; reject a higher duration until a new capacity result/profile is approved. Treat the PVC request as non-reserving local storage and capture host filesystem headroom.
  - [ ] Enforce steady state <=70%, reclamation peak <=80%, critical at 80%, Unhealthy at 90%, and the exact ADR two-writer workload at 500 events/s while purge runs: per writer, 200 search, 6 ingest, 10 traverse, 16 case-access, 2 delete, 0.2 tenant-lifecycle, 0.8 tenant-config, 4 case-member, and 11 annotation events/s, using the ADR's deterministic 90/10 success/error fixture and bounded P95 payloads. Require a 30-minute steady-state window plus the 10-minute 150,000-due-record purge backlog, zero acknowledged loss/unclassified errors, p99 transaction latency below the configured 3-second Dapr client timeout, no more than 10% p95 latency regression from the same-profile no-purge baseline, and complete queue/backlog drain within five minutes with oldest-due age below 15 minutes. The committed single 20 GiB Redis StatefulSet cannot be presumed eligible: the ADR's 24-hour canonical payload is already 20.60 GiB before overhead.
  - [ ] Keep Production lifecycle writes disabled/fail-closed until this checkpoint passes. Memories and the lifecycle service remain Dapr-only; no backend SDK, connection string, backend credential, or orchestrator API enters application code.
  - [ ] Prove the declared pod/process-loss fault with zero acknowledged-record loss while `node1` and the retained volume remain healthy. Publish node, local-volume, control-plane, and site loss as outside profile, attach a named backup destination and successful restore evidence, publish the resulting nonzero RPO/RTO, and never claim node/disk/site HA.
  - [ ] If `PG-ONPREM-1` cannot pass every atomicity, zero-loss, configured-retention capacity, security, backup/restore, and physical-reclamation gate, preserve its rejection and exact failed command, keep Production writes disabled and A41 open, and run a new correct-course decision. Never weaken a gate or silently substitute another profile.

### Scope Transferred to Story 27.4

The approved 2026-07-20 course correction moves former Tasks 2-8 and checkpoints C2-C6 to Story 27.4: multi-writer/replacement/durability proof; expiry, purge, newer-record preservation, and cohort-attributable reclamation; failure/privacy/authority/health/metrics/alerts; lifecycle runbook and exact-adapter operations appendix; closure-owned residual reconciliation; evidence-backed A41 mutation; and terminal governed validation. None of that scope is executable under Story 27.3, and Story 27.4 remains `backlog` until this story is `done` with an immutable approved C1 packet.

### Scope Transferred to Stories 30.1 and 31.1 (2026-07-26)

The approved 2026-07-26 course correction executes the two splits the Administrator approved on 2026-07-21 during code review.

**To Story 30.1 (Epic 30, DW 27.3-CR5)** — the four-image container release and partial-recovery pipeline, with its CI wiring, tooling, tests, and the release-runbook four-image expansion.

**To Story 31.1 (Epic 31, DW 27.3-CR6)** — the OpenBao `hexalith-keys` platform and the runtime Dapr `secretstore` migration from Kubernetes Secrets to `hashicorp.vault` for the `eventstore` and `memories` scopes. Story 27.3 retains the access-telemetry-specific secret components and the `PG-ONPREM-1` secret backing, which remain in adapter scope.

**Environment prerequisite, not a story dependency:** the `PG-ONPREM-1` connection string, including `sslmode=verify-full`, resolves from the deployed OpenBao platform. That platform is already running — the 2026-07-20 live probe loaded the store — so Story 31.1 formalizes ownership and hardening of an operational platform rather than standing one up. Story 27.3's C1 probe is not blocked on Story 31.1. **Corrected 2026-07-26 by code review (chunk 3b), per the Administrator decision recorded below:** a forward dependency on Story 30.1 *does* exist and is accepted. The 27.3-retained `tools/verify-production-deployment.ps1` declares `-AccessTelemetryArchive` and `-AccessTelemetryClockArchive` as `[Parameter(Mandatory)]`, and both `.tar.gz` archives are produced only by the transferred `tools/publish-containers.ps1` (Story 30.1); the C1 reopen trigger recorded in the Debug Log additionally requires signed registry publication for all four images, which is Story 30.1 acceptance scope. Story 30.1 must therefore not regress this verification lane. The earlier blanket claim that "no forward dependency is created" was wrong and is superseded by this paragraph.

Neither transferred block is executable under Story 27.3, and neither may be claimed as Story 27.3 evidence. Both target stories are `backlog`.

### Review Findings

The completed rows below are append-only evidence from the pre-split Story 27.3. Findings about former Tasks 2-8 or C2-C6 now describe transferred Story 27.4 scope and are not active Story 27.3 requirements.

**Anchor resolution note (added 2026-07-26 by code review, chunk 3b).** 28 of the retained rows below anchor at `_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:NNN`. That path does not exist at HEAD: commit `f474db15` renamed the story to `27-3-production-adapter-and-deployment-profile.md` under the approved 2026-07-20 course correction. The anchors are append-only history and are not rewritten; to resolve one, read the file at its last existing revision with `git show f474db15^:_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md`. The count is 28 line anchors across 32 filename mentions and 37 retained rows; an earlier finding stating "cited 37 times" conflated the three.

- [x] [Review][Patch] [High] Append the missing blocked `dev-story` ledger row for the recorded C0 attempt [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:258]
- [x] [Review][Patch] [High] Map Story 27.2's live external discovery deltas before any Story 27.3 count agreement [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:173]
- [x] [Review][Patch] [High] Prevent A41 mutation until Task 8 and every final validation gate pass [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:90]
- [x] [Review][Patch] [High] Make every checkpoint command build fresh binaries and fail on zero discovery or execution [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:106]
- [x] [Review][Patch] [High] Require every mandatory checkpoint to pass without an approved-skip escape hatch [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:89]
- [x] [Review][Patch] [High] Keep C0 handoff repairs out of frozen task and planning sections [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:40]
- [x] [Review][Patch] [High] Add required completion dates to the approved umbrella checkpoint table [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:104]
- [x] [Review][Patch] [High] Classify the reverified runbook and dashboard test mechanics separately from anti-template Story 26.5 [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:111]
- [x] [Review][Patch] [High] Define one canonical evidence matrix and closure anchor across C1-C6 and A41 records [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:112]
- [x] [Review][Patch] [High] Add a close-out preflight, exact mutation set, postconditions, and recovery procedure [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:90]
- [x] [Review][Patch] [High] Make discovery of every current A41 summary exhaustive and reproducible [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:91]
- [x] [Review][Patch] [High] Verify purge interruption between record and index deletion across restart [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:63]
- [x] [Review][Patch] [High] Revalidate the live component profile immediately before evidence-backed closure [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:89]
- [x] [Review][Patch] [Medium] Make the creation-scope verifier assert monotonic status, unique YAML keys, artifact existence, status parity, and the 3/3 baseline-relative File List [_bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md:51]
- [x] [Review][Defer] [Medium] Reconcile the stale Aspire AppHost SDK version in canonical project context [_bmad-output/project-context.md:24] — deferred, pre-existing
- [x] [Review][Patch] [High] Re-run C0 against Story 27.2's completed, reviewed handoff and replace every stale predecessor blocker/current-state claim [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:156]
- [x] [Review][Patch] [High] Bind this review to an immutable current diff/blob manifest and explicitly cover the post-401-line review delta [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:313]
- [x] [Review][Patch] [High] Exclude and attribute the same-file `deferred-work.md` hunks owned by commit `042dcba6` and `spec-run-tests-and-fix-failures` [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:330]
- [x] [Review][Patch] [High] Enforce monotonic Story 27.3 transitions and epic/story status parity against immutable prior workflow state [_bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md:138]
- [x] [Review][Patch] [Medium] Reject multiple or conflicting story `Status:` lines instead of accepting the first match [_bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md:135]
- [x] [Review][Patch] [High] Validate every required task and subtask before review/done instead of checking top-level Task rows only [_bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md:143]
- [x] [Review][Patch] [High] Parse the exact checkpoint table as unique ordered C0-C6 rows; the current whole-document selector matches non-checkpoint rows [_bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md:144]
- [x] [Review][Patch] [High] Parse chronological successful phase-ledger rows so marker text and blocked pre-implementation rows cannot satisfy review/done [_bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md:147]
- [x] [Review][Patch] [High] Require the resolved A41 residual/action, canonical evidence-anchor parity, and successful postflight before `done` [_bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md:149]
- [x] [Review][Patch] [Medium] Name exact Python unittest suites, discovery commands, and expected case inventories for C1, C3, and C4 [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:132]
- [x] [Review][Patch] [High] Require exact deployment-control, replacement, fault-injection, and evidence-collection commands in addition to local test assemblies [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:138]
- [x] [Review][Patch] [High] Bind the 500 events/s admission gate to the ADR operation/payload mix, two-writer concurrency, purge/backlog duration, error, latency, and drain limits [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:51]
- [x] [Review][Patch] [High] Make the embedded verifier fail when Python assertions are optimized away [_bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md:57]
- [x] [Review][Patch] [Medium] Re-hash reviewed source and assemblies after discovery/execution to detect concurrent evidence drift [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:130]
- [x] [Review][Patch] [Medium] Fault-inject the crash window after record/index deletion but before cohort completion accounting [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:65]
- [x] [Review][Patch] [High] Prove old, transition, new, and rollback retention-epoch cohorts during duration changes [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:62]
- [x] [Review][Patch] [High] Reject non-finite, negative, unit-mixed, or overflowing capacity inputs before adapter admission [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:50]
- [x] [Review][Patch] [High] Include intended untracked A41 artifacts in exhaustive inventory and fail on every unclassified untracked match [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:92]
- [x] [Review][Patch] [High] Bind postflight's approved mutation manifest to the staged and eventually published repository tree [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:95]
- [x] [Review][Patch] [High] Map the current external Server +11, Integration +1, and AccessTelemetry +8 discovery drift in the new code-review evidence and ledger row [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:328]
- [x] [Review][Patch] [High] Apply every preflight-approved A41 reconciliation mutation before terminal postflight so postflight covers the complete closure set [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:94]
- [x] [Review][Patch] [Medium] Reclassify Story 27.2's whole-story handoff as historical dependency evidence and reserve `current-narrow-pattern` for the reverified checkpoint seam [_bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md:244]

<!-- Code review 2026-07-21 — chunk 1 of 3 (adapter code + tests: 3 src + 9 test files). Chunks 2 (deploy manifests + docs) and 3 (tooling + CI) are not yet reviewed; this intermediate chunk does not finalize the phase ledger or advance status. -->

- [x] [Review][Action] Split the four-image release/publish pipeline (`CiTestInventoryTests.cs` + `tests/tooling/publish_containers/*`) into a newly numbered story via correct-course, and shrink the 27.3 File List + ledger to adapter/qualification scope. (Split approved 2026-07-21 by Administrator during code review; execution is a separate correct-course activity, not applied by this review. DW 27.3-CR5) [tests/Hexalith.Memories.Cli.Tests/Ci/CiTestInventoryTests.cs:1] — executed 2026-07-26 by approved Sprint Change Proposal 2026-07-26; scope moved to Epic 30 / Story 30.1.
- [x] [Review][Defer] Cumulative Server.Tests story-vs-external count attribution excludes 4 Story-27.3 C1 methods in story-owned files (`AccessTelemetryRetentionDecisionTests.cs` 6→10 + `ProductionDeploymentArtifactsTests.cs` +2, booked under external rather than the +1 story delta) [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:430] — deferred to the final-chunk ledger reconciliation, where the Server delta is recomputed (+1→+5 story / +30→+26 external) with live discovery. Fail-closed blocker for `done` until then. (DW 27.3-CR4)
- [x] [Review][Patch] Align within-minute due-entry ordering across both `IAccessTelemetryStateStore` implementations to `ExpiresAtUtc`→`Shard`→`RecordId` — change `InMemoryAccessTelemetryStateStore.GetDueEntriesAsync` (currently `ExpiryMinute`→`Shard`→`RecordId`) to match the Dapr adapter; re-run the 27.2/lifecycle tests that assert purge order. (Parity approved 2026-07-21.) [src/Hexalith.Memories.AccessTelemetry/Lifecycle/InMemoryAccessTelemetryStateStore.cs:100] — applied 2026-07-26: ordering is now `ExpiryMinute`→`ExpiresAtUtc`→`Shard`→`RecordId`, which is exactly the Dapr adapter's observable order (minute-major outer traversal, then the within-minute tuple). Guarded by the new three-method `AccessTelemetryStateStoreOrderingParityTests`, which drove a genuine RED (2 failures on the pre-fix shard-major order) → GREEN.
- [x] [Review][Patch] Add missing failure-path unit tests for `DaprAccessTelemetryStateStore` (only 4 happy-path tests exist) — cover `Conflict` (same RecordId / different envelope; bucket anti-resurrection guard), `StaleIndex` (live-record protection + stale-entry prune), `VerificationFailed` (post-delete strong re-read still present), `AlreadyAbsent` (TTL-reaped record + lingering bucket), `GetDueEntriesAsync` limit-truncation and not-due exclusion, and a no-partial-commit negative test (stale ETag → transaction throws → nothing committed); extend the `TransactionalDaprState` fake to model empty-etag `FirstWrite` insert semantics. [tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/DaprAccessTelemetryStateStoreTests.cs:25] — applied 2026-07-26: all eight requested negatives added (8 new methods; the class is now 12 — **corrected 2026-07-26 by code review, chunk 3b: live discovery gives 13 methods**, i.e. +9 across `358bef35`, the eight negatives plus the cross-tenant denial; the Change Log's `+12` AccessTelemetry phase delta is unaffected and correct). The fake was extracted to `TransactionalDaprState.cs` and extended with empty-ETag `FirstWrite` insert semantics (an empty ETag under first-write concurrency may not clobber an existing key), `ExpireByTtl` (component TTL reaping one key and orphaning its bucket entry), `UndeletableKeys` (backend acknowledges a delete the strong re-read contradicts), and `BeforeTransaction` (a concurrent writer interleaved between the adapter's ETag read and its commit).
- [x] [Review][Patch] Relabel mixed test-count units and add the method-discovery snapshot in the 2026-07-20 ledger row (`61/61` and `2,742/2,743` are case totals recorded in the method lane without labels or a method snapshot/hash). [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:431] — applied 2026-07-21 (append-only Code Review Evidence note)
- [x] [Review][Patch] Correct the malformed 66-char AccessTelemetry method-set hash in the 2026-07-19 ledger row (canonical is the 64-char `…a2b99a14`) via an append-only correction note. [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:428] — applied 2026-07-21 (append-only Code Review Evidence note)
- [x] [Review][Patch] Record create-story baselines for the `Cli.Tests` and `publish_containers` tooling lanes modified this chunk (phase delta +0; no baseline exists to compute a cumulative delta against). [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:433] — applied 2026-07-21 (append-only Code Review Evidence note)
- [x] [Review][Defer] State-store concurrency retry / empty-etag first-creation clobber in a rare actor-failover split-brain window [src/Hexalith.Memories.AccessTelemetry/Lifecycle/DaprAccessTelemetryStateStore.cs:123] — deferred to the pending C1 two-writer/partial-commit deployment probe; not reachable under the single global turn-based actor and fail-closed (ETag throw → at-least-once retry; orphan bounded by record TTL).
- [x] [Review][Defer] Live test-count totals predate current HEAD; re-run live discovery before `done` [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:432] — chunk files verified unchanged since the recorded snapshot; full recount not runnable in this sandbox.
- [x] [Review][Defer] `recordId` interpolated into the state key without charset validation [src/Hexalith.Memories.AccessTelemetry/Lifecycle/DaprAccessTelemetryStateStore.cs:346] — verify the record contract constrains `RecordId` (no `/`); harden if unconstrained.


<!-- Code review 2026-07-21 - chunk 2 of 3 (deployment manifests + docs: 18 File-List paths). Chunk 1 (adapter code + tests) reviewed 2026-07-21; chunk 3 (tooling + CI) not yet reviewed. This intermediate chunk emits findings only; it does not finalize the phase ledger or advance status. -->

- [x] [Review][Action] Split the OpenBao `hexalith-keys` secrets platform + runtime `secretstore` migration (k8s -> Vault, scopes `eventstore`/`memories`) into a newly numbered story via correct-course, and shrink 27.3 to the PG-ONPREM-1 adapter/secret-backing scope. (Split approved 2026-07-21 by Administrator during code review; execution is a separate correct-course activity, not applied by this review. DW 27.3-CR6) [deploy/openbao/values.yaml:140] — executed 2026-07-26 by approved Sprint Change Proposal 2026-07-26; scope moved to Epic 31 / Story 31.1.
- [x] [Review][Patch] Add `tools/validate-production-deployment-evidence.ps1` to the File List (27.3-owned production-deployment-evidence tooling, commit `f474db15`) and reconcile it in the chunk-3 final row. (Ownership decided 2026-07-21: own it.) [tools/validate-production-deployment-evidence.ps1:1]
- [x] [Review][Patch] Remove the dead `sslRootCert` metadata from `access-telemetry-store.yaml` (not a recognized `state.postgresql/v2` field; silently ignored); verify-full is carried by the OpenBao connection string (`sslrootcert`/`sslmode=verify-full`) - confirm with the operator and note it in the ADR component block. (Decided 2026-07-21: remove dead field + confirm connstring.) [deploy/kubernetes/base/dapr/access-telemetry-store.yaml:15]
- [x] [Review][Patch] Base lifecycle+clock `replicas: 2` is fail-open (scale-to-zero lives only in the out-of-chunk production overlay); set the base to `replicas: 0` so the fail-closed invariant lives in the base and `kubectl apply -k base` cannot schedule broken pods. [deploy/kubernetes/base/access-telemetry-deployments.yaml:9]
- [x] [Review][Patch] Operator contract omits every access-telemetry Secret the shipped manifests hard-require; add `access-telemetry-postgresql-bootstrap` (keys `admin-password`,`runtime-password`), `access-telemetry-postgresql-tls` (`tls.crt`/`tls.key`/`ca.crt`), `access-telemetry-clock-key`, `access-telemetry-clock-sources`, and `access-telemetry-marker-key` to the Required-external-inputs table. [docs/operations/deployment-configuration.md:28]
- [x] [Review][Patch] Postgres init script reads `runtime-password` under `set -Eeuo pipefail`; a missing key aborts AFTER `initdb`, trapping a half-initialized `Retain` PVC (initdb.d never re-runs). Add an explicit pre-flight existence/non-empty guard with a fatal message before the `psql` block. [deploy/kubernetes/base/access-telemetry-postgresql.yaml:31]
- [x] [Review][Patch] Add `access-telemetry-postgresql` to `memories-access-telemetry-config` `allowedSecrets` as defense-in-depth/forward-compat (component-metadata secret resolution is not currently gated by Configuration secret scopes - the 2026-07-20 live probe loaded the store - but listing it documents intent and survives a Dapr behavior change). [deploy/kubernetes/base/dapr/access-telemetry-lifecycle-config.yaml:14]
- [x] [Review][Patch] Re-point the malformed 66-char AccessTelemetry hash correction note (spec:350) at its actual contaminated row (2026-07-19 dev-story, not the code-review row), canonical `...216081a2b99a14`; fold into the final chunk-3 code-review row. [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:446]
- [x] [Review][Patch] Add named File-List exclusions (owners `c7c2ca21`/`93af830c` = Story 27.2; `1242e774` = CI rollout lane) for the access-telemetry manifests referenced by 27.3's `kustomization.yaml` but excluded from the 47-path File List; fold into the final chunk-3 reconciliation. [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:451]
- [x] [Review][Defer] FAIL-CLOSED blockers for `done`: chunk 3 (tooling + CI) unreviewed; live method/case recount not runnable in this sandbox; Server story/external split +1/+30 -> +5/+26 (DW 27.3-CR4) depends on that recount. Status must stay `in-progress`. [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:115] - deferred, blocker
- [x] [Review][Defer] Clock NetworkPolicy egress to TCP/443 is unrestricted (no `to:` selector); tighten to the real UTC-source CIDRs before enablement (sources are `.example.invalid` placeholders now). [deploy/kubernetes/base/access-telemetry-network-policy.yaml:99] - deferred, pre-enablement
- [x] [Review][Defer] `maxConns: 64` x 2 lifecycle replicas (128) can exceed PostgreSQL `max_connections=100` under the C1 two-writer load; reconcile pool math (lower maxConns, raise max_connections, or cap replicas) at/before the C1 load probe. [deploy/kubernetes/base/dapr/access-telemetry-store.yaml:25] - deferred, pre-enablement
- [x] [Review][Defer] Verification coverage: no static guard binds `access-telemetry-secrets` `skipVerify:"false"`, pg_hba `hostnossl...reject` + init-SQL least-privilege grants, the new RBAC secret-reader Roles, `actorStateStore:"true"`, or the telemetry-lifecycle ACL (deny-default + exact ops) - a drift ships green; add assertions to the chunk-1 guard tests. [tests/Hexalith.Memories.Server.Tests/Deployment/ProductionDeploymentArtifactsTests.cs:1] - deferred, test-hardening
- [x] [Review][Defer] Pre-enablement operational hardening: exec probes require `wget`+`/bin/sh` in the (placeholder `:0.0.0`) images; no metrics/monitoring ingress (Prometheus NoData); startup 60s vs store `initTimeout:1m` cold-start race; Postgres `terminationGracePeriodSeconds:120` + PDB `minAvailable:1` on a single replica blocks node drain; DB/OpenBao password + TLS-CA rotation need manual sidecar restart (HotReload off). [deploy/kubernetes/base/access-telemetry-deployments.yaml:98] - deferred, pre-enablement
- [x] [Review][Defer] Docs: release-runbook four-image expansion should ride the approved DW 27.3-CR5 split; ADR byte-bucket boundaries overlap (64KiB/1MiB/10MiB in adjacent labels) and `edgeTypeCount>16` clamp-vs-reject is unspecified; verify attribution of the ADR `Story 27.2 C1 mapping` block. [docs/dev/release-runbook.md:1] - deferred, docs
<!-- Code review 2026-07-26 - chunk 3a of 3 (never-reviewed tooling/CI + the post-2026-07-21 review-patch delta: 13 paths, 2,219 diff lines, manifest SHA-256 1ee36f77ef4446741e545663c6e386122ce4c93653f7a4448a258cdc9594aac3 over `git diff 272c33bc..159d7216`). Chunks 1 and 2 were reviewed 2026-07-21. Chunk 3b (8 governance/planning record paths, 2,076 lines) is NOT reviewed here. This intermediate chunk emits findings only; it does not finalize the phase ledger or advance status. -->

- [x] [Review][Decision] Three shipped, independently provable lanes map to no approved checkpoint - the story declares one outcome (C0 + C1), but the PG-v2 adapter/units lane, the PG-ONPREM-1 manifest-guard lane, and the kind-based four-image deployment-verification harness each ship, prove, and close findings on their own runner with no dependency on C1, which has been externally blocked since 2026-07-19. Either add named checkpoints with individual owner/evidence/review/completion state, or split the deployment-verification harness into a newly numbered story - note `_bmad-output/planning-artifacts/epics.md:5104` currently assigns its three tools to Story 27.3, so a split needs an epics amendment. - **Decided 2026-07-26 by Administrator during code review:** add named checkpoints. Story 27.3 remains one story; each of the three lanes gains its own checkpoint row with individual owner, evidence command, review state, and completion state, per the `story-scope-guard.md` umbrella exemption. No `epics.md` amendment required. Routed to patch. — patch work applied 2026-07-26 by dev-story: the PG-v2 adapter/unit lane and the PG-ONPREM-1 manifest-guard lane are both covered by named C1 gate rows; the kind-based deployment-verification harness remains tracked as `DW 27.3-CR15` pending its approved correction, and is not claimed as Story 27.3 evidence.
- [x] [Review][Decision] The declared C1 evidence command cannot produce the observation 12 of its 14 gate rows require - `run_adapter_profile_checkpoint` issues five read-only `kubectl get -o json` calls, derives one `reason` string, and has a single `return 1` with no pass path; there is no CRUD, strong-read, ETag, transaction-fault, TTL, actor/Scheduler, request-bound, throughput, purge-backlog, isolation, encryption, or reclamation probe anywhere in the module. Choose: make the verifier a real behavioural prober, or narrow its declared role to identity capture plus fail-closed rejection and give each C1 row its own operator-executed command. [tools/verify_access_telemetry_lifecycle.py:617] - **Decided 2026-07-26 by Administrator during code review:** narrow the verifier's declared role to identity capture plus fail-closed rejection, and give each C1 gate row its own operator-executed command and observation. The single shared `adapter-profile` command stops being cited as the producer for gates it cannot observe. Routed to patch. — patch work applied 2026-07-26 by dev-story: `run_adapter_profile_checkpoint`'s docstring now states its narrowed role - identity capture plus fail-closed rejection - and explicitly disclaims being the producer for the behavioural gate rows, each of which names its own operator-executed command in the C1 Gate Evidence Table.
- [x] [Review][Decision] `maxConns` is `"40"` in the reviewed manifest but the ADR's immutable `PG-ONPREM-1` component block still pins `"64"` - Task 1 states any substitution changes the profile hash and requires another approved course correction. Decide whether this is an approved profile change (correction + ADR update + new profile hash + ledger entry) or an ADR documentation repair. [deploy/kubernetes/base/dapr/access-telemetry-store.yaml:29] - **Decided 2026-07-26 by Administrator during code review:** `40` is the correct value (it is what makes the ADR two-writer probe fit `max_connections=100`); update the ADR's immutable component block to `40`, recompute the profile hash, and record it as an approved profile correction with a ledger entry. Routed to patch. — the manifest value stays `40` and the canonical profile pins `"40"` (fixture `test_canonical_pg_onprem_profile_hash_is_pinned`). The ADR update, the recomputed `profile_sha256` and the superseding correction are `DW 27.3-CR14`, a correct-course activity outside dev-story.
- [x] [Review][Decision] The 27.3-retained verification tool now hard-requires artifacts only Story 30.1 produces - `-AccessTelemetryArchive` and `-AccessTelemetryClockArchive` are `[Parameter(Mandatory)]`, and both `.tar.gz` archives come only from the transferred `tools/publish-containers.ps1`. This contradicts the story's own claim that no forward dependency is created. Decide: accept the dependency and correct that statement, or restructure so 27.3's lane runs without a `backlog` story's output. [tools/verify-production-deployment.ps1:9] - **Decided 2026-07-26 by Administrator during code review:** accept the dependency. Record an explicit Story 27.3 -> Story 30.1 artifact dependency so Story 30.1 cannot regress 27.3's verification lane, and correct the story's "no forward dependency is created" statement. Routed to patch. — the dependency is accepted and recorded; the 'no forward dependency is created' statement was corrected by the chunk-3b review and the Story 27.3 -> Story 30.1 artifact dependency is stated in **Scope Transferred to Stories 30.1 and 31.1**.
- [x] [Review][Decision] `.github/workflows/ci.yml` has three hunk owners, not the two the File List note assumes - `1242e774` (NU19xx restore repair), `af821aba` (release/publish steps, Story 30.1), and `bf911d5c` (tenant-isolation evidence gate). The only adapter-lane wiring retained by 27.3 is two argument lines. Decide whether 27.3 retains the path on that basis, and confirm the three-way attribution. [.github/workflows/ci.yml:1] - **Decided 2026-07-26 by Administrator during code review:** Story 27.3 retains the path, and all three hunk owners are named explicitly - `1242e774` (NU19xx restore repair), `af821aba` (release/publish steps, Story 30.1), and `bf911d5c` (tenant-isolation evidence gate) - as named exclusions with owner and reason in the chunk-3 reconciliation. Routed to patch. — patch work applied 2026-07-26 by dev-story: the three hunk owners are named as exclusions with owner and reason in this phase's File List reconciliation, and Story 27.3 retains the path - this phase adds three adapter-lane discovery steps to it.
- [x] [Review][Decision] Does the `+1` OpenBao Server method follow Story 31.1 with the artifact it guards? `OpenBaoDeploymentProfile_IsPinnedTlsOnlyPersistentAndInternal` asserts `deploy/openbao/values.yaml`, which DW 27.3-CR6 transferred to Story 31.1, yet the method stays in the 27.3-retained test file and its `+1` stays in 27.3's cumulative delta. Both readings balance against the live 2,190 total (`2,157 + 7 + 26` if retained; `2,157 + 6 + 1 + 26` if transferred). The final `code-review` row cannot be written until this is decided. [tests/Hexalith.Memories.Server.Tests/Deployment/ProductionDeploymentArtifactsTests.cs:1] - **Decided 2026-07-26 by Administrator during code review:** the method follows Story 31.1 with the artifact it guards. The authoritative Server equation becomes `2,157 create + 6 Story 27.3 + 1 Story 31.1 + 26 external = 2,190`. Routed to patch. — applied: the authoritative Server equation `2,157 create + 6 Story 27.3 + 1 Story 31.1 + 26 external = 2,190` is carried by the chunk-3b `code-review` row and restated in this phase's `dev-story` row.
- [x] [Review][Decision] Concurrent Story 30.2 claims the CI lane topology that two of this chunk's findings target - an independent `correct-course` session introduced Story 30.2 ("Shared CI Core and Module-Specific Verification Lanes") while this review was running, and its acceptance criteria state that tooling fixtures and production-deployment verification "remain named local jobs or companion workflows", making the lane topology Story 30.2's scope. The two findings above that register `Hexalith.Memories.AccessTelemetry.Tests` in `tools/test-projects.unit-contract.txt` and add a `tests/tooling/access_telemetry_lifecycle` discovery step to `ci.yml` therefore have contested ownership. Decide whether Story 27.3 fixes its own untested surface now (accepting that Story 30.2 will migrate the lane later), or whether both findings transfer to Story 30.2 - noting that transferring leaves all 55 AccessTelemetry methods and the four-case C1 inventory executed by no workflow until Story 30.2 completes, while Story 30.2 is `backlog` and its proposal is still untracked. [_bmad-output/planning-artifacts/epics.md:1] - **Decided 2026-07-26 by Administrator during code review:** Story 27.3 wires both lanes itself now, accepting that Story 30.2 may later migrate them to the shared CI core. The story's own 55 AccessTelemetry methods and four-case C1 inventory begin being enforced immediately rather than waiting on a `backlog` story. Routed to patch. — patch work applied 2026-07-26 by dev-story: Story 27.3 wired both lanes itself. `Hexalith.Memories.AccessTelemetry.Tests` is registered in the unit-contract lane via the three-way sync, and `tests/tooling/access_telemetry_lifecycle` has its own CI discovery step. Story 30.2 may later migrate them to the shared CI core.
- [x] [Review][Patch] `StaleIndex` deletes the live record's only expiry-index entry, making that record permanently unpurgeable - `bucketContainsEntry` matches on `RecordId` alone, so any superseded or foreign entry sharing `(ExpiryMinute, Shard)` with the live record prunes the live entry before returning `StaleIndex`; `GetDueEntriesAsync` reads candidates only from buckets, so the record can never be rediscovered. Both new tests assert this orphaning as correct. Match the full entry identity before pruning, mirror the fix in the in-memory adapter, and correct the two tests. [src/Hexalith.Memories.AccessTelemetry/Lifecycle/DaprAccessTelemetryStateStore.cs:216] — applied 2026-07-26: `DeleteAndVerifyAsync` now matches the complete entry identity (`candidate == entry`, record value equality over all five members) in the bucket-contains check, in `CreateBucketRemoval`, in `RemoveBucketEntryAsync`, and in the post-delete verification re-read. Mirrored in `InMemoryAccessTelemetryStateStore`, whose entry key now carries the full identity. Genuine RED: both tests that asserted the orphaning failed on the fix, and both now assert the opposite - the live entry survives and `GetDueEntriesAsync` still returns it.
- [x] [Review][Patch] Record the `StaleIndex` index-orphaning defect as a `remediation-runtime-checklist.md` Category 3 (owner-checked cleanup and dedup) item on this story - it is a review-found cleanup/dedup defect that happy-path coverage missed, which the policy's review gate requires be carried onto the reviewed story. [_bmad/custom/remediation-runtime-checklist.md:62] — applied 2026-07-26: recorded as a Category 3 item in the Completion Notes remediation-runtime-checklist record for this phase, with its guarding tests named.
- [x] [Review][Patch] Register `Hexalith.Memories.AccessTelemetry.Tests` in a CI lane - all 55 methods, including every negative this chunk added, are executed by no workflow; the project is absent from both `tools/test-projects.unit-contract.txt` and `tools/test-projects.integration-fast.txt`, and `grep -rn "AccessTelemetry.Tests" .github/ tools/` returns nothing. Reverting this chunk's ordering fix or its cross-tenant denial leaves CI fully green. Requires the repo's established three-way sync (lane file, `ExpectedDockerFreeProjects`, `line-coverage-gate.json`); note the guard file is Story 30.1-owned, so record the cross-story edit. [tools/test-projects.unit-contract.txt:1] — applied 2026-07-26 via the established three-way sync: `tools/test-projects.unit-contract.txt`, `ExpectedDockerFreeProjects` in the Story 30.1-owned `CiTestInventoryTests.cs` (cross-story edit recorded in the File List), and `requiredReportProjects` in `tests/tooling/coverage_gate/line-coverage-gate.json`. The `Hexalith.Memories.AccessTelemetry` source assembly is deliberately NOT added to `requiredAssemblies`: that is a separate coverage-policy decision with its own owner.
- [x] [Review][Patch] Add a `tests/tooling/access_telemetry_lifecycle` unittest discovery step to CI - eight tooling suites are wired at `ci.yml:212-233` and this one is missing, so the four-case C1 inventory the story cites as mandatory evidence runs in no workflow. [.github/workflows/ci.yml:233] — applied 2026-07-26. The sweep that this finding prompted also found `tests/tooling/story_scope` and `tests/tooling/integration_stub_closure` running in no workflow, so both were wired too, and a new `CiWorkflow_RunsEveryToolingFixtureSuiteThatGuardsAShippedTool` guard now fails whenever any `tests/tooling/*` suite is executed by no workflow (it scans every workflow, because `release_preflight` runs from `release.yml`).
- [x] [Review][Patch] `_normalize_bytes` accepts unit-less values as bytes, defeating Task 1's explicit reject-missing-units requirement - a bare `int` short-circuits at line 168 and a unit-less string defaults to `"B"` at line 176, so `"400"` meaning 400 GB is silently read as 400 bytes and `"8b"` as 8 bytes; the mixed-unit check then excludes `"none"`, so mixing `"1KiB"` with bare `1024` passes. Also harden: an empty `horizons` mapping silently substitutes the three defaults, zero measured record/index bytes yields a `required_bytes = 0` "proof", and operands above the 28-digit decimal context round to integral and bypass the fractional-rejection path. The fixture itself feeds bare ints and must be updated. [tools/verify_access_telemetry_lifecycle.py:168] — applied 2026-07-26: `_normalize_bytes` now rejects every bare number and every unit-less string, so the `"none"` escape from the mixed-unit check is gone; the fixture was rewritten to feed united operands. Also hardened: an explicitly empty `horizons` mapping raises instead of substituting the three defaults, zero measured record/index bytes raises, and a local 80-digit decimal context makes a 19-digit operand compare exactly instead of rounding to integral. Mutation-proven (restoring the bare-int acceptance failed 2 cases).
- [x] [Review][Patch] Capacity arithmetic omits ADR-mandated operands and every admission gate - no `schedulerBytes` term, no `reclamationWorkspace` floor of `max(137438953472, ceil_div(base, 4))`, no `durabilityMultiplier` pin, no comparison against the exact profile's 70/80/90% thresholds, no rejection of the 7-day horizon, and no host-filesystem headroom despite the PVC being non-reserving local storage. [tools/verify_access_telemetry_lifecycle.py:223] — applied 2026-07-26: `calculate_capacity` now requires `scheduler_bytes` and `host_filesystem_headroom_bytes`, applies the reclamation floor `max(supplied, 137438953472, ceil(base/4))`, pins `durability_multiplier` to `APPROVED_DURABILITY_MULTIPLIER`, and its result is judged by `evaluate_capacity_admission` against the 70/80/90% table with the 7-day horizon non-admissible. Mutation-proven (dropping the scheduler term, and dropping the reclamation floor, each failed a case).
- [x] [Review][Patch] Remove the dead Story 31.1 OpenBao residue and the inert redaction path from the retained verification tool - `$OpenBaoImage`, `$openBaoNamespace`, `$openBaoPod`, `$openBaoMaterialPath`, `$openBaoRootToken`, `$openBaoUnsealKey`, `$openBaoRuntimeToken`, and `$openBaoAccessTelemetryToken` each occur exactly once (declaration only); `$sensitiveEvidenceValues` is appended into `Protect-EvidenceText` but never populated, so the new redaction branch protects nothing; and `openbao-resources.txt`, `openbao-status.json`, `dapr-secret-components.yaml` are listed as owned evidence that nothing writes. [tools/verify-production-deployment.ps1:24] — applied 2026-07-26: `$OpenBaoImage`, `$openBaoNamespace`, `$openBaoPod`, `$openBaoMaterialPath`, `$openBaoRootToken`, `$openBaoUnsealKey`, `$openBaoRuntimeToken`, `$openBaoAccessTelemetryToken` and the never-populated `$sensitiveEvidenceValues` are removed, along with the three owned-evidence names (`openbao-resources.txt`, `openbao-status.json`, `dapr-secret-components.yaml`) that nothing writes.
- [x] [Review][Patch] `kubectl` and summary-helper exceptions escape before any evidence packet is written - `subprocess.run(..., timeout=60)` handles only `json.JSONDecodeError`, so a missing binary (`FileNotFoundError`) or an unresponsive API server (`TimeoutExpired`) produces a traceback and no packet, which is precisely the fail-closed situation the packet exists to record; the `_*_summary` helpers likewise `AttributeError` on a null `spec`/`metadata` or a non-mapping component entry, and an empty or unusable `--evidence` path raises `IsADirectoryError`/`NotADirectoryError` instead of returning 2. [tools/verify_access_telemetry_lifecycle.py:293] — applied 2026-07-26: every read-only command runs through `_run_command`, which turns `FileNotFoundError` (127), `TimeoutExpired` (124) and `OSError` (126) into recorded observations; the six `_*_summary` helpers route every nested lookup through `_mapping`/`_sequence` so a null `spec`, a scalar where a mapping is expected, or a non-mapping component entry cannot raise; and an empty, directory, or unwritable `--evidence` path returns 2 before the target is contacted. Covered by two new fixture cases and mutation-proven.
- [x] [Review][Patch] The cited cross-tenant denial evidence is not cross-tenant - both records in `DeleteAndVerifyAsync_EntryCarryingAnotherTenantMarker_IsDeniedAndLeavesTheRecordIntact` use `RecordId` `01K0A000000000000000000001`, so both resolve to one state key (`GetRecordKey` has no tenant dimension) and the test re-exercises the envelope-hash mismatch already covered by the preceding test; its "leaves the record intact" claim also omits that the foreign entry pruned the live index entry. Separately, `tools/tenant-isolation-surfaces.txt` contains no `src/Hexalith.Memories.AccessTelemetry/**` glob, so the gate wired in by this very diff never fires for these files. Add the surface and real physical-isolation negative evidence, or stop citing the project-context tenant-isolation rule as satisfied. [tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/DaprAccessTelemetryStateStoreTests.cs:1] — applied 2026-07-26 per the Administrator decision (2026-07-26) not to widen `tools/tenant-isolation-surfaces.txt`, whose own header makes widening a gated 'Ask First' decision. The test is renamed `..._IsDeniedAndLeavesTheRecordPurgeable` and now states in code that it is NOT physical cross-tenant isolation evidence - the record key carries no tenant dimension because this is a single global infrastructure store under one fixed actor - and that gate C1.11 is not discharged by it. It also now asserts the denial does not cost tenant A its index entry. The project-context tenant-isolation rule is no longer cited as satisfied for this lane.
- [x] [Review][Patch] Wire or remove `_run_dapr_version` and `calculate_capacity` - both are defined and never called from any evidence path (`calculate_capacity` has exactly one caller, the test fixture), so AC1's running Dapr runtime/control-plane identity is never captured and gate C1.13 has no producer while the `adapter-profile` command is cited as its source. [tools/verify_access_telemetry_lifecycle.py:319] — applied 2026-07-26: `_run_dapr_version` is invoked against the first Running labelled pod and its result becomes the packet's `Runtime and Control-Plane Identity (AC1)` section (gate C1.15's producer); `calculate_capacity` and `evaluate_capacity_admission` are invoked through the new `collect_capacity_evidence`, which becomes gate C1.13's producer and records `blocked` with the exact missing operand names when the operator has supplied none.
- [x] [Review][Patch] Two of the six required Production-Shaped identity values are synthesized rather than supplied - `DECLARED_SINGLE_COMPONENT_FAULT` defaults to the literal `"unconfigured"`, which is non-empty and therefore satisfies the required-field check and is stamped into the immutable packet as a reviewed declaration, and `EVIDENCE_ROOT` is derived from `Path(args.evidence).resolve().parent` instead of the reviewed value. [tools/verify-access-telemetry-lifecycle.py:49] — applied 2026-07-26: the CLI takes `--declared-single-component-fault` and `--evidence-root` (env-defaulted, never synthesized), `EVIDENCE_ROOT` is no longer derived from the evidence path's parent, and a placeholder fault value (`unconfigured`/`unknown`/`tbd`/`none`) exits 2 instead of being stamped into the immutable packet as a reviewed declaration.
- [x] [Review][Patch] `EXPECTED_KUBE_CONTEXT = "jpiquot@local"` hardcodes a developer kubeconfig context as the approved production identity and a fixture asserts the literal, so no other operator can run the checkpoint; a client-side context name is also not an identity control. The five read-only queries additionally execute against the target *before* the approved-identity comparison, and their output is written into the packet regardless. [tools/verify_access_telemetry_lifecycle.py:123] — applied 2026-07-26: the reviewed context is now `DEFAULT_REVIEWED_KUBE_CONTEXT` overridable through `REVIEWED_KUBE_CONTEXT`, it is recorded as an observation rather than used as a gate, and the approved-identity preflight (workload profile, envelope, namespace, profile ID) runs to completion **before** any query is issued against the target. The fixture no longer asserts the literal; it asserts the default and the override.
- [x] [Review][Patch] The rejection packet cannot represent the fields its own execution contract requires - `_write_rejection_evidence` emits only command line, exit code, and stdout/stderr hashes: no stdout/stderr content, no per-command UTC timestamps, no pre/post workload identity, no reviewed-source or immutable-artifact hashes, and no fields for backup destination, restore result, RPO/RTO, the out-of-profile statement, or the two separately named approvals. [tools/verify_access_telemetry_lifecycle.py:460] — applied 2026-07-26: the packet now carries per-command UTC start/finish timestamps and redacted stderr excerpts, reviewed-source SHA-256 hashes for both verifier files, pre/post workload identity with an `unchanged_during_run` flag, the runtime/control-plane identity section, the capacity section with its admission table, and the seven AC3/AC4 attestation fields (backup destination, restore result, RPO, RTO, out-of-profile statement, and the two separately named approvals), each defaulting to `unrecorded` and never to a synthesized claim.
- [x] [Review][Patch] The evidence packet is written with LF while `.gitattributes` materializes the tracked packet as CRLF, so every run produces bytes - and any hash over them - that differ from the committed form, undermining the immutable-evidence and evidence-integrity claims; CI's `line_endings` suite guards this policy. [tools/verify_access_telemetry_lifecycle.py:516] — applied 2026-07-26 (already carried by commit `a6753c11`, verified this phase): `_write_rejection_evidence` joins with `\r\n` and writes with `newline=""`, and every run also lands an immutable per-run copy under `adapter-profile-runs/`.
- [x] [Review][Patch] Redaction was moved onto the health-decision path - `Protect-EvidenceText` now sanitizes before the status regex and `Get-HealthJsonBody`/`ConvertFrom-Json` run, using unanchored `String.Replace` over `$env:HEXALITH_ZOT_USERNAME`/`_API_KEY`, values this script does not control. A short or common username value corrupts the parsed status text the gate depends on. Redact at the evidence write only, where `Save-HealthResponseEvidence` already does it. [tools/verify-production-deployment.ps1:254] — applied 2026-07-26: `Get-HealthResponse` parses the raw transcript and redaction happens only at the evidence write, where `Save-HealthResponseEvidence` already applies it. Guarded by an assertion that the sanitize-first form is absent.
- [x] [Review][Patch] The evidence validator makes legitimate failure-path packets unvalidatable - it requires `statusCode` in `@(200, 503)` and a JSON-parsable `body` for every `health-*.json` regardless of `$result.status`, while `Get-HealthJsonBody` falls back to the raw transcript and `statusCode` can be `$null` on a failed probe. Gate the parse and status-code rules on `succeeded` and record a `probeError` field instead. [tools/validate-production-deployment-evidence.ps1:55] — applied 2026-07-26: the envelope rules (`schemaVersion`, `stage`) still apply to every packet, but the parsable-body and 200/503 status-code rules are now gated on `$result.status -eq 'succeeded'`. A new fixture writes a failed-run packet with a null `statusCode` and a raw transcript body and requires it to validate.
- [x] [Review][Patch] A failed or timed-out `wget` no longer falls back to netcat - `exit "$wgetExit"` fires whenever the output carries no `HTTP/... 503` line, so the slow-but-healthy case the removed `nc -w 6` probe existed for now dies on the stage deadline. Run the fallback on any non-zero wget exit, not only when a 503 line was already parsed. [tools/verify-production-deployment.ps1:236] — applied 2026-07-26: the fallback runs on any non-zero wget exit, not only when a 503 line was already parsed. A new fixture stubs a wget that times out with no status line at all and requires the netcat fallback to run; mutation-proven by restoring the 503 gate.
- [x] [Review][Patch] Health evidence is keyed by stage and rewritten on every poll, so only the final attempt survives and the validator's "must include both HTTP 200 and HTTP 503" rests on incidental final-poll timing rather than a recorded transition; two stage names differing only outside `[0-9A-Za-z_.-]` also collide onto one filename. [tools/verify-production-deployment.ps1:391] — applied 2026-07-26: every poll writes `health-<stage>-<attempt>.json` with an `attempt` field, so the observed transition is recorded rather than the final poll's incidental timing, and two stage names that differ only outside `[0-9A-Za-z_.-]` are disambiguated by an eight-character SHA-256 suffix.
- [x] [Review][Patch] Add negative fixtures for the four new health-evidence rules - only the all-files-deleted case is covered, so nothing exercises a malformed `body`, a `statusCode` outside 200/503, a wrong `schemaVersion`, or a `succeeded` run whose only health file is the 200 (the rule that makes a successful rollout prove it observed a real 503). [tests/tooling/production_deployment_evidence/production_deployment_evidence_test.py:1] — applied 2026-07-26: four new cases cover a malformed `body`, a `statusCode` outside 200/503, a wrong `schemaVersion`, and a succeeded run whose only health file is the 200. Mutation-proven (dropping the succeeded-run body rule failed 12 cases).
- [x] [Review][Patch] `Get-HealthJsonBody`, 55 new lines of string-aware brace balancing, is pinned only by source-text `assertIn("function Get-HealthJsonBody", verifier)` - no test ever calls it with input, so dropping the escape/in-string handling or keeping the first rather than the last status-bearing object passes every suite and the live kind job, failing only on the noisy transcripts the function was written for. [tools/verify-production-deployment.ps1:271] — applied 2026-07-26: `Get-HealthJsonBody` and the newly extracted `Get-HealthStatusCode` moved to the dot-sourceable `tools/production-deployment-health.ps1` and are now driven with real transcripts (diagnostic braces, escaped quotes inside strings, two status-bearing objects, a brace inside a string). Mutation-proven: keeping the first rather than the last object failed 2 cases, and dropping the in-string handling failed 4.
- [x] [Review][Patch] `render-production-deployment.ps1` validates placeholder counts and the `:$Version` suffix but never repository identity or argument distinctness, so transposed or duplicated image arguments render a valid-looking manifest whose workloads run the wrong image; its new mandatory-parameter and single-placeholder guards also have no negative fixture anywhere. [tools/render-production-deployment.ps1:83] — applied 2026-07-26: each image argument is bound to the repository suffix it must carry relative to the Server image's repository (`-mcp`, `-access-telemetry`, `-access-telemetry-clock`), and all four must be distinct. Bound to the *structure* rather than to a hardcoded `registry.hexalith.com/memories` literal: the first form was correct against the finding but broke the registry/prefix parameterization that `publish-containers.ps1` relies on, which turned 20 `tests/tooling/publish_containers` cases red - caught and corrected before handoff, and that lane is green again at 38/38. New fixtures cover a Server/MCP transposition, a duplicated image, and a missing mandatory parameter; mutation-proven by removing the repository binding.
- [x] [Review][Patch] `LastTransactionOperationCount` is a hardcoded `2` asserted as `ShouldBe(2)` in two checkpoints - a tautology that also misstates the adapter it stands in for (the Dapr store commits 2 or 3 operations, and `DeleteAndVerifyAsync` never updates the property), and the getter reads outside `lock (_gate)` unlike every sibling member. Derive it from the actual operation set or drop the assertion. [src/Hexalith.Memories.AccessTelemetry/Lifecycle/InMemoryAccessTelemetryStateStore.cs:84] — applied 2026-07-26: the count is derived from the committed operation set (3 when the write opens a new expiry minute, 2 otherwise - exactly what the Dapr adapter commits), the getter now reads under `lock (_gate)` like its siblings, and a `TransactionOperationCounts` history replaces the tautology in both checkpoints. The 500-writer integration checkpoint now asserts the deterministic distribution 501 = 2x3 + 499x2 instead of a single racy value.
- [x] [Review][Patch] The ordering-parity tests never exercise the `Shard` or `RecordId` tiebreakers they claim to pin - every fixture gives each record a distinct `ExpiresAtUtc`, so deleting `.ThenBy(entry => entry.Shard)` keeps all three tests green while production tie order diverges (dictionary insertion order versus shard-ascending bucket scan). Add fixtures with entries sharing `ExpiresAtUtc`, and sharing both `ExpiresAtUtc` and `Shard`. [tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/AccessTelemetryStateStoreOrderingParityTests.cs:42] — applied 2026-07-26: two fixtures added - entries sharing `ExpiresAtUtc` with disagreeing shards (6 vs 21), and entries sharing both `ExpiresAtUtc` and `Shard` (two identifiers that both hash to shard 1), written in descending identifier order. Mutation-proven: removing `.ThenBy(Shard)` fails the first and removing `.ThenBy(RecordId)` fails the second, while the three pre-existing tests stay green in both mutations - exactly as this finding predicted.
- [x] [Review][Patch] The `TransactionalDaprState` fake does not model three of the contract properties it is documented to model - `Apply` never inspects `operation.Metadata`, so dropping the `ttlInSeconds` or `partitionKey` entries stays green; reads are served identically for `Strong`, `Eventual`, and `null`, so removing `ConsistencyMode.Strong` from the verification re-read that makes `VerificationFailed` meaningful stays green; `UndeletableKeys` suppresses only Delete operations, leaving the Upsert-path `VerificationFailed` and `RemoveBucketEntryAsync` `InvalidOperationException` branches unreachable; and `ExpireByTtl`/`AdvanceETag` silently no-op or throw bare `KeyNotFoundException` on a mistyped key. [tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/TransactionalDaprState.cs:99] — applied 2026-07-26: `Apply` now enforces the metadata contract (every operation must carry `partitionKey: access-telemetry`; a `records/` upsert must carry `ttlInSeconds` and a non-record operation must not), reads throw unless `ConsistencyMode.Strong` is requested, a new `UnappliedUpsertKeys` hook makes the Upsert-path `VerificationFailed` and the `RemoveBucketEntryAsync` `InvalidOperationException` branches reachable (both now covered), and `ExpireByTtl`/`AdvanceETag` throw a `KeyNotFoundException` naming the key and listing the stored keys. Mutation-proven three ways (dropping Strong from the verification re-read: 4 failures; dropping the record TTL metadata: 21; dropping the bucket partition metadata: 21).
- [x] [Review][Patch] The in-memory adapter diverges from the adapter it substitutes for on four behaviours the lifecycle checkpoints depend on - a duplicate entry key throws `ArgumentException` *after* `_records` was mutated (a non-atomic write where the Dapr store returns `Conflict`), `VerificationFailed` is unreachable so the durability defect the enum exists for is never exercised, `ttlInSeconds` is accepted and discarded with no validation, and the due-scan has no catalog or empty-minute side effect so repeat scans diverge. [src/Hexalith.Memories.AccessTelemetry/Lifecycle/InMemoryAccessTelemetryStateStore.cs:82] — applied 2026-07-26, all four: a duplicate entry slot now returns `Conflict` decided **before** any mutation instead of throwing `ArgumentException` after `_records` was written; `VerificationFailed` is reachable through a `SuppressRecordDeletion` hook and the same strong post-delete verification the Dapr adapter performs; `ttlInSeconds` is validated (non-positive raises) and observable via `LastTtlInSeconds`; and the due scan is minute-major over an active-minute catalog that prunes drained minutes and leaves untraversed minutes alone. Seven new cases in `InMemoryAccessTelemetryStateStoreContractTests` pin each behaviour.
- [x] [Review][Patch] The deployment-guard helpers are structurally unsound, so several guards assert less than they claim - `ReadComponentMetadata` takes the next `value:` anywhere after the name with no item bound, returning a neighbour's value for any `secretKeyRef`-backed key; `ReadYamlSequence` runs to end-of-document when the terminator is absent and inspects only the first key occurrence; `max_connections` and `reserved_connections` are searched only in the StatefulSet document and only at the first occurrence, so setting either through `access-telemetry-postgresql-config` voids the check; the pg_hba assertions are presence-only and ignore first-match-wins ordering, so a bare `host ... scram-sha-256` line inserted before the reject rules accepts plaintext TCP while all four assertions pass; and the ACL guards are case-sensitive substring counts, so `httpVerb: ["delete"]` bypasses `ShouldNotContain("DELETE")`. [tests/Hexalith.Memories.Server.Tests/Deployment/ProductionDeploymentArtifactsTests.cs:442] — applied 2026-07-26, all five: `ReadComponentMetadata` binds to its own `- name:` item and requires exactly one inline value (with a separate `ReadComponentMetadataSecretRef` for `secretKeyRef`-backed entries); `ReadYamlSequence` binds by indentation, requires the key to occur exactly once, and handles a key that is itself a sequence item; `max_connections`/`reserved_connections` are searched across the whole rendered manifest and must occur exactly once; the pg_hba rules are compared as an exact ordered list so a `host` rule inserted above the reject rules fails; and the ACL verb exclusions are case-insensitive. Mutation-proven: a plaintext-capable `host` rule above the rejects, a lowercase `delete` verb, and a repointed `connectionString` secret each fail the class.
- [x] [Review][Patch] The connection-pool guard hardcodes `c1WriterReplicas = 2` while the rendered overlay is `replicas: 0` with no `strategy:` block, so the constant is bound to nothing - a scale to three replicas or a default RollingUpdate surge gives `3 x 40 + 13 = 133` against `max_connections=100` and the test still passes. Read `spec.replicas` and add the surge ceiling. [tests/Hexalith.Memories.Server.Tests/Deployment/ProductionDeploymentArtifactsTests.cs:326] — applied 2026-07-26: the guard reads `spec.replicas` and `maxSurge` out of the rendered overlay and computes peak concurrent pods as `max(declared, C1 probe) + maxSurge`. Because the Kubernetes default surge is 25% and would round up to a third sidecar pool (3 x 40 + 13 = 133 > 100), the lifecycle Deployment now pins `strategy.rollingUpdate.maxSurge: 0` with `maxUnavailable: 1`. Mutation-proven by setting `maxSurge: 1`.
- [x] [Review][Patch] `-p:WarningsNotAsErrors=NU1901..NU1904` on four CI restore steps sets a *global* MSBuild property, which a `<PropertyGroup>` cannot override, so `Directory.Build.props:18`'s own definition is inert during CI restore - the two lists are identical today, but anything added there later works locally and is silently dropped in CI. The exception is also widened from the single documented advisory to all four audit codes across the whole restore graph, submodules included, with no allowlist or expiry, and no assertion pins the four steps in sync. [.github/workflows/ci.yml:178] — applied 2026-07-26 as a sync guard rather than a removal: the global property is genuinely required because it is the only way the exception reaches the Hexalith.FrontComposer submodule's separate `Directory.Build.props` chain, so `CiWorkflow_KeepsEveryRestoreAuditExceptionInSyncWithDirectoryBuildProps` now parses the declared list out of `Directory.Build.props`, requires exactly four injected restore steps, and requires each to inject exactly that list. Owner, consequence and reopen trigger are recorded in the test's own comment.
- [x] [Review][Patch] The `sslRootCert` removal is correct but its replacement guard binds nothing - the assertion is now absence plus a comment, and no producer of `sslmode=verify-full` or `sslrootcert` exists anywhere in `deploy/`, `tools/`, or `src/`, so a connection string provisioned with `sslmode=require` leaves every guard green. Bind what is statically bindable and record an explicit accepted blocker for the OpenBao-resident connection-string content with owner, consequence, and reopen trigger. [tests/Hexalith.Memories.Server.Tests/Deployment/ProductionDeploymentArtifactsTests.cs:139] — applied 2026-07-26: what is statically bindable is now bound structurally - `connectionString` must resolve from a `secretKeyRef` (never an inline literal), from secret `access-telemetry-postgresql` key `connectionString`, through `secretStore: access-telemetry-secrets` - and the OpenBao-resident connection-string content is recorded as an accepted blocker in the guard itself, with owner (Hexalith Platform Operations plus the AC4 security reviewer), consequence (gate C1.12 cannot be discharged by any test in this assembly) and reopen trigger. Mutation-proven by repointing the secret.
- [x] [Review][Patch] Restate the Server story/external split as `2,157 + 7 + 26 = 2,190` and close DW 27.3-CR4 - independent live per-class recount at HEAD `159d7216` gives `AccessTelemetryRetentionDecisionTests` 6 -> 10 and `ProductionDeploymentArtifactsTests` 5 -> 9, so 8 methods were added inside 27.3-declared Server files, of which one (`ProductionOverlay_AccessTelemetryDeploymentsAreScaledToZero`) is already attributed to external commit `1242e774`, leaving 7 methods in 27.3-declared files. Per the 2026-07-26 decision above, `OpenBaoDeploymentProfile_IsPinnedTlsOnlyPersistentAndInternal` follows Story 31.1 with the artifact it guards, so the authoritative equation is `2,157 create + 6 Story 27.3 + 1 Story 31.1 + 26 external = 2,190`. The recorded `+3/+30` is wrong and CR4's own `+5/+26` target was correct only at Server 2,188. The blocked-evidence gate is discharged: discovery ran successfully in this invocation. [_bmad-output/implementation-artifacts/deferred-work.md:2289] — applied 2026-07-26: the authoritative equation is `2,157 create + 6 Story 27.3 + 1 Story 31.1 + 26 external = 2,190` per the 2026-07-26 Administrator decision, restated in this phase's `dev-story` row, and DW 27.3-CR4 is closed in `deferred-work.md`. Live discovery this phase confirms Server 2,190 at hash `bd27c3da...a28b4d0`, unchanged by this phase.
- [x] [Review][Patch] Add `tests/tooling/production_deployment_evidence/production_deployment_evidence_test.py` to the File List and record its unledgered lane - it is the fixture suite for the already-declared `tools/validate-production-deployment-evidence.ps1`, was last modified by the same commits (`f474db15`, `58b509bb`), is CI-enforced at `ci.yml:233`, and moved 14 -> 17 Python unittest cases (+3) with no story delta, no external delta, and no `+0` recorded anywhere. The File List baseline becomes 37: `.github/workflows/ci.yml` is retained per the 2026-07-26 decision above, and this fixture suite is the only path added. [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:499] — applied 2026-07-26: declared in both `## File Scope` and the File List. Its lane is ledgered in this phase's row: 17 -> 27 Python unittest cases (+10), all Story 27.3.
- [x] [Review][Patch] Record `matched 37/37` with the exact `git diff --name-status` command - the 52 -> 36 split was executed by `correct-course`, which is not a canonical phase and appended no row, so the ledger's most recent reconciliation claim is still `matched 52/52` while the File List enumerates 36. Independent verification at HEAD returns exactly 36 rows (21 `A`, 15 `M`) with no declared path unchanged or missing, so `36/36` is true but unrecorded. [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:464] — applied 2026-07-26 (already carried by the chunk-3b `code-review` row, which records `matched 37/37` against baseline `272c33bc` with the exact command). This phase's row supersedes it with `matched 45/45`, naming the eight paths that joined.
- [x] [Review][Patch] Add the three missing named File-List exclusions with owner and reason - the `bf911d5c` tenant-isolation evidence-gate `ci.yml` steps together with `tools/check-tenant-isolation-evidence.py`, `tools/tenant-isolation-surfaces.txt`, and `tests/tooling/tenant_isolation_evidence/tenant_isolation_evidence_test.py`; `deploy/kubernetes/overlays/production/kustomization.yaml` (owner `93af830c`, Story 27.2); and `deploy/dapr/config.yaml` plus `deploy/dapr/access-telemetry-lifecycle-config.yaml` (owners `6779f7cb`/`c7c2ca21`), both of which fall outside the chunk-2 `deploy/dapr/components/*` glob. [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:421] — applied 2026-07-26: recorded as named exclusions with owner and reason in this phase's File List reconciliation - the `bf911d5c` tenant-isolation evidence-gate steps together with `tools/check-tenant-isolation-evidence.py`, `tools/tenant-isolation-surfaces.txt` and `tests/tooling/tenant_isolation_evidence/tenant_isolation_evidence_test.py`; `deploy/kubernetes/overlays/production/kustomization.yaml` (owner `93af830c`, Story 27.2); and `deploy/dapr/config.yaml` plus `deploy/dapr/access-telemetry-lifecycle-config.yaml` (owners `6779f7cb`/`c7c2ca21`).
- [x] [Review][Patch] Append-only ledger repairs for two historical rows - the 2026-07-20 row at line 520 records no `matched N/N`, no declared baseline, and no command (unlike its neighbours), and claims Server/Integration `+0` while recording only `2,742/2,743` cases with no method snapshot although 2,188/297 were available; the 2026-07-20 row at line 522 carries `38/38`, `384/384`, and `57/57` as unlabeled execution counts for `publish_containers` and `Cli.Tests` lanes that the 2026-07-26 split transferred to Story 30.1. The final `code-review` row must not restate those as Story 27.3 lanes. [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:520] — applied 2026-07-26 as append-only notes in this phase's row: the 2026-07-20 row that records no `matched N/N` is named with its missing baseline/command and its available-but-unrecorded method snapshot (Server 2,188 / IntegrationTests 297 / AccessTelemetry.Tests 43), and the 2026-07-20 row carrying `38/38`, `384/384` and `57/57` is named as `publish_containers` and `Cli.Tests` execution counts for lanes the 2026-07-26 split transferred to Story 30.1. Neither historical row is rewritten and neither is restated as a Story 27.3 lane.
- [x] [Review][Patch] Record the derivation pipeline for the Python lane's sorted-case SHA-256 or drop the hash - six rows cite `e2e1c469...cbf6dae` while recording only the discovery command, never the pipeline that turns its output into the hash, unlike the three xUnit lanes whose exact `sed | LC_ALL=C sort | sha256sum` pipelines are recorded. An independent reconstruction yields a different value, so the case-set identity can be neither confirmed nor refuted; the case *count* (4/0/0) is verified. [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:524] — applied 2026-07-26: this phase's row records the full pipeline, not just the discovery command, for every Python lane hash it publishes.
- [x] [Review][Patch] Close DW 27.3-CR2 and correct its stale figures - it states a full live runner recount could not be executed in this sandbox and must be re-run at the `done` gate, citing Server 2,188 / Integration 297 / AccessTelemetry 43. The recount ran successfully in this invocation at HEAD `159d7216` with a clean tree, and every current ledger total matches live discovery hash-for-hash (2,190 / 297 / 55). This discharges CR2's evidence gate but not the attribution defect recorded above. [_bmad-output/implementation-artifacts/deferred-work.md:2274] — applied 2026-07-26: closed in `deferred-work.md`. The recount ran successfully again this phase at Server 2,190 / IntegrationTests 297 / AccessTelemetry.Tests 55 -> 67; the entry's stale 2,188 / 297 / 43 figures are corrected in its closure note.
- [x] [Review][Patch] Add the two missing Historical Context Classification rows - the story's own superseded pre-split artifact `27-3-retention-verification-operations-runbook-and-a41-close-out.md` is cited 37 times as the anchor for retained findings and its creation-time planned delta is still carried verbatim, yet it has no row despite meeting the policy's own anti-template tests; and the `ProductionDeploymentArtifactsTests` / `*-production-deployment*.ps1` harness this diff extends by roughly 207 lines originates in Story 26.1 (commit `7217ef89`), which the table classifies as `anti-template`, with no `current-narrow-pattern` row or re-verification citation. The reuse itself is legitimate narrow current-source reuse; the missing classification is the defect. Also give the 37 transferred finding rows an explicit `transferred -> 27.4` disposition rather than a bare `[x]`. [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:283] — applied 2026-07-26 (already carried by the chunk-3b review: both rows exist in **Historical Context Classification**, and the 37 transferred finding rows carry their disposition). Verified present this phase; no further change required.
- [x] [Review][Patch] Record `InMemoryAccessTelemetryStateStore.cs` as a Story 27.2-owned file with a Story 27.3-owned line range - the baseline-relative diff renders it as `new file mode` with all 138 lines scoring as 27.3 content, while 27.3 owns only the `GetDueEntriesAsync` ordering block and its comment. [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:523] — applied 2026-07-26: recorded in the File List ownership notes. The same treatment is now applied to the two further Story 27.2-owned files this phase touched, `LifecycleActorCheckpointTests.cs` and `AccessTelemetryLifecycleIntegrationCheckpointTests.cs`, each with its Story 27.3-owned line range named.
- [x] [Review][Defer] FAIL-CLOSED blockers for `done`: chunk 3b (8 governance/planning record paths, 2,076 diff lines) is unreviewed, so this invocation must not append the final `code-review` row or advance status; the six `[Review][Decision]` items above must be resolved first; and the DW 27.3-CR4 attribution restatement depends on that resolution. Status must stay `in-progress`. Path-level coverage was verified complete and disjoint: 13 (chunk 3a) + 8 (chunk 3b) + 15 paths whose last commit predates the 2026-07-21 chunk-1/2 reviews = 36. [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:464] - deferred, blocker
- [x] [Review][Defer] `InMemoryAccessTelemetryStateStore` is a test double by its own XML doc yet ships inside the product container assembly (`EnableContainer=true`, `ContainerRepository memories-access-telemetry`), reachable only through `InternalsVisibleTo`; a DI misregistration would satisfy the lifecycle contract with zero durability and no expiry. Moving it to a shared test-support project is a Story 27.2-origin structural change beyond this review's scope. [src/Hexalith.Memories.AccessTelemetry/Lifecycle/InMemoryAccessTelemetryStateStore.cs:8] - deferred, pre-existing
- [x] [Review][Defer] Commit `358bef35` is not a Conventional Commit (`Add tests for AccessTelemetryStateStore ordering and transactional Dapr state`, no type prefix) and its body lists only the three new test files, omitting the `InMemoryAccessTelemetryStateStore.cs` purge-ordering product change it also carries. Already published on `main`; correcting it needs a history rewrite, so the durable fix is the commit-message gate rather than this commit. [src/Hexalith.Memories.AccessTelemetry/Lifecycle/InMemoryAccessTelemetryStateStore.cs:1] - deferred, pre-existing


<!-- Code review 2026-07-26 - chunk 3b of 3 (the 8 governance/planning record paths: 2,264 diff lines at HEAD c9dfb06f, manifest SHA-256 605152e597357936680f5f171d9a87e09dfcb7887e21ecd953bfab6d550d6344 over `git diff 272c33bc..HEAD -- <the 8 paths>`). Chunks 1 and 2 were reviewed 2026-07-21; chunk 3a on 2026-07-26 against 159d7216. All six review layers returned non-empty; zero failed layers. Two findings were dismissed after verification. This chunk completes path-level review coverage, but it does NOT finalize the phase ledger or advance status: the 45 open chunk-3a items and the 7 decisions below remain unresolved, so no final `code-review` row is appended here. -->

- [x] [Review][Decision] Which approved record carries the `PG-ONPREM-1` profile after the `maxConns` 64 -> 40 change — decision `:159` directs the repair at the ADR only, but the approved 2026-07-20 course correction is a third artifact pinning `"64"` (`sprint-change-proposal-2026-07-20-story-27-3-on-prem-postgresql-18-4.md:176`, `docs/dev/adr-27.1-001-access-telemetry-lifecycle.md:884`, shipped manifest `deploy/kubernetes/base/dapr/access-telemetry-store.yaml:29` = `"40"`). Task 1 (`:54`) states any substitution changes the profile hash and requires another approved course correction. Choose: (a) amend the approved dated proposal in place, (b) supersede it with a new correction, or (c) treat it as ADR-only documentation repair — and name which artifact carries the recomputed `profile_sha256`, given the profile ID string (`...-openebs-local-retain-400g-v1`) does not encode `maxConns` and so cannot detect the substitution. - **Decided 2026-07-26 by Administrator during code review:** a new approved course correction supersedes the 2026-07-20 pinning. Raise a correct-course that approves `40`, recomputes `profile_sha256`, and states which artifact carries the new hash; approved dated proposals remain append-only and are not edited in place. This is the strict reading of Task 1. Execution is a separate correct-course activity, not applied by this review. Recorded as DW 27.3-CR14. — resolved 2026-07-26 during this review.
- [x] [Review][Decision] Which capacity admission threshold governs — the story (`:60`) admits durations "whose measured result fits the exact 400 GiB profile" (429,496,729,600 bytes, 100%), while the approved correction admits only results fitting its thresholds (70% steady state = 300,647,710,720). A measured 24-hour result between those two values is admitted by one authority and rejected by the other, with no precedence rule. Two adjacent gaps need the same decision: 80% is simultaneously "reclamation peak <=80%" and "critical at 80%" (`:61`, proposal `:142`), and no rule states the consequence when the *configured* 24-hour target itself fails capacity — "reject a higher duration" does not apply to the configured duration, and the 1-hour horizon has no stated consequence at all. - **Decided 2026-07-26 by Administrator during code review:** the approved correction's 70/80/90% threshold table governs. Amend the story's "fits the exact 400 GiB profile" wording to cite that table as the admission gate, resolve the 80% peak-vs-critical collision, and define the disposition when the configured 24-hour target itself fails capacity. Routed to patch. — resolved 2026-07-26 during this review.
- [x] [Review][Decision] AC4's separation of approvers is defeated by the only reviewer assignment on record — `:413` names "Platform Operations owner Jérôme Piquot (`jpiquot`) and security reviewer Murat TEA for Jérôme", i.e. one human plus a persona acting for that same human, while `:109` and `:113` simultaneously list separately-named Platform Operations and security reviewers as still absent. AC4 (`:39`) requires an independent security reviewer. Name a real independent security approver, or record an accepted blocker with owner, consequence and reopen trigger. - **Decided 2026-07-26 by Administrator during code review:** record an accepted blocker. No independent security approver is available, so the AC4 gates stay `pending` behind a recorded blocker naming owner, consequence and reopen trigger; the persona assignment is removed rather than treated as satisfying independence. Routed to patch. — resolved 2026-07-26 during this review.
- [x] [Review][Decision] The kind-based production-deployment-verification lane has no acceptance criterion, no task, and no checkpoint, yet `_bmad-output/planning-artifacts/epics.md:5137` assigns its three tools to Story 27.3 and `.github/workflows/ci.yml:387` runs it as a standalone job with no dependency on the externally-blocked C1. The approved 2026-07-26 correction (`:161` of that proposal) simultaneously freezes Story 27.3's acceptance criteria, so a checkpoint alone would prove an outcome no AC declares. Choose: (a) a new correct-course adding an AC plus its checkpoint, or (b) a newly numbered story plus the `epics.md:5137` amendment. - **Decided 2026-07-26 by Administrator during code review:** a new course correction adds the acceptance criterion and its checkpoint. The lane and its three tools stay in Story 27.3; the 2026-07-26 AC freeze is lifted only far enough to declare the deployment-verification outcome, after which the checkpoint row is added with owner, evidence command, review state and completion state. Execution is a separate correct-course activity. Recorded as DW 27.3-CR15. — resolved 2026-07-26 during this review.
- [x] [Review][Decision] Stories 30.1 and 31.1 as newly written reproduce the anti-template shape the 2026-07-26 split was executed to cure — Story 30.1 (`epics.md:5076-5147`) has seven Given/When/Then blocks, names eight "separate reviewable checkpoints" with no owner, evidence command, review state or completion state, and gates the whole story on an unshipped external Hexalith.Builds revision; Story 31.1 (`epics.md:5188-5216`) bundles the OpenBao platform hardening and the runtime `secretstore` migration as two independently deployable outcomes with no checkpoint table. Both are `backlog`, so `epics.md:555` and `story-scope-guard.md:30-31` do not bind yet. Decide whether to split now or to require the full four-field checkpoint table as a condition of `ready-for-dev`. - **Decided 2026-07-26 by Administrator during code review:** split both now via correct-course, before either is selected. Story 30.1's independently shippable lanes (manifest `packageId`->`id` migration, four-image publication mapping, partial-recovery workflow, cutover rehearsal/rollback, registry-authorization blocker) and Story 31.1's two outcomes (OpenBao platform hardening; runtime `secretstore` migration) become separately numbered stories. Execution is a separate correct-course activity. Recorded as DW 27.3-CR16. — resolved 2026-07-26 during this review.
- [x] [Review][Decision] Who repairs the external content now committed inside Story 27.3-declared paths — `architecture.md` D31 (`4d2e4e2f`) and the D17 rewrite (`c9dfb06f`), `epics.md` Epic 28/29/31 blocks and the five 2026-07-25 Cerebras placeholders (`1f95a72b`), `sprint-status.yaml` epic-28/29/30/31 registration and two Epic 21 action closures, `deferred-work.md` `21.10-A4-VERIFY` plus four `REL-*` entries. The substantive defect is D17: `architecture.md:564`/`:596` state as decided that pushes to `main` use `domain-ci.yml@main` and that publication is operator-dispatched into a protected environment invoking `domain-release.yml`, while `.github/workflows/ci.yml` never calls `domain-ci.yml` and `release.yml` is push-triggered with no `environment:` — a target state recorded as current, with D17 also silently re-phased from `MVP` to `Engineering/Operational Readiness` and no superseded row preserved. Decide: Story 27.3 patches these, or they route to the owning `correct-course` sessions and appear here only as named exclusions. - **Decided 2026-07-26 by Administrator during code review:** route to the owning sessions. Story 27.3 records named exclusions with owner and reason only; the D17 target-state-as-current defect, its missing superseded row, and the remaining Epic 28/29/31, Cerebras, Epic 21 and `REL-*` content are repaired by the owning `correct-course` sessions / Story 30.2, not by this story. Routed to patch (the exclusions). — resolved 2026-07-26 during this review.
- [x] [Review][Decision] Where the A41 `done` gate lives — the create-story scope verifier's `done` branch (`_bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md:239-265`) requires `20.5-A41-ACCESS-TELEMETRY-RETENTION` to be `Status: resolved`, the sprint action to be `done`, and a postflight-passed matrix. Story 27.3 forbids exactly that in four places (`:13`, AC5 at `:43`, `:68`, `:276`) and reserves the mutation to Story 27.4, which stays `backlog` until Story 27.3 is `done` (`epics.md:4921`). The only `done` transition the verifier admits therefore requires an action reserved to a story that cannot start until that transition happens. Decide whether the `done` branch is Story 27.4-scoped and must be removed from Story 27.3's verifier, or whether Story 27.3 carries a different `done` definition. - **Decided 2026-07-26 by Administrator during code review:** the `done` branch is Story 27.4-scoped and is removed from Story 27.3's verifier. Story 27.3's `done` gate becomes C0 + C1 plus ledger and File List reconciliation, with A41 explicitly asserted to remain open. This breaks the closed cycle without weakening A41. Folded into the verifier rewrite below. Routed to patch. — resolved 2026-07-26 during this review.

- [x] [Review][Patch] Record the post-split File List reconciliation in a phase row — `matched 36/36` (37 once the fixture suite below is declared) at HEAD `c9dfb06f` with the exact `git diff --name-status` command; independently verified this review as 36 rows, 21 `A` / 15 `M`, no declared path unchanged or untracked. The newest ledger claim is still `matched 52/52` against a 36-path File List because the 52 -> 36 cut ran under `correct-course`, which appends no row [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:584] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Correct the chunk-3b scope declaration — it is 2,264 diff lines at HEAD `c9dfb06f` (manifest SHA-256 `605152e5...d6344`), not the recorded 2,076 lines at `159d7216`; commit `c9dfb06f` added 188 lines across five of the eight paths, and the `13 + 8 + 15 = 36` partition claim is bound to the older HEAD [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:155] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Correct the chunk-3a triage totals in three places — `grep -c` at HEAD gives 7 open `[Review][Decision]` and 38 open `[Review][Patch]` rows (45, not 44), and the uncounted seventh decision (Story 30.2 lane ownership, `:163`) is itself a fail-closed `done` blocker missing from the story's own blocker list [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:202] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Renumber the duplicate deferred-work ID — two different open entries are both `27.3-CR7` (`:2318` "create-story scope verifier is inert after the 27.3 split rename", minted by `dev-story`; `:2377` "chunk 3b unreviewed; fail-closed for `done`", minted by chunk-3a). Both are cited as `done` blockers, so "CR7 closed" is ambiguous and closing either discharges the reference for the other [_bmad-output/implementation-artifacts/deferred-work.md:2377] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Give the chunk-2 and chunk-3a deferred entries the register's structured schema (`ID`/`Status`/`Source story`/`Target artifact`/`Re-open trigger`/`Rationale`) — they are bold-prose bullets, so they fall outside the `deferred-work.md:92` classification sweep over "every active `open` / `carried-forward` structured entry" and can never be marked resolved; the stale chunk-2 bullet at `:2309` has no field to update now that chunk 3a and the recount are done [_bmad-output/implementation-artifacts/deferred-work.md:2305] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Re-file the six entries misplaced under the "chunk 2 (2026-07-21)" heading — `21.10-A4-VERIFY` is an Epic 21 retro action and the four `REL-*` entries are sourced from `spec-gh-30146368778-fix-tenants-release-startup-failure.md` and surfaced 2026-07-25; `DW 27.3-CR7` was minted 2026-07-26 by `dev-story`, not by the 2026-07-21 chunk-2 review, and `CR7` is filed ahead of `CR6` [_bmad-output/implementation-artifacts/deferred-work.md:2305] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Declare or name-exclude `sprint-change-proposal-2026-07-26-memories-ci-cd-alignment.md` — the story calls it "untracked" and owned by "that course-correction workflow", but commit `c9dfb06f` added it as a tracked path and its own front matter declares `prepared by: Correct Course workflow` with `artifacts_modified: [epics.md, architecture.md, sprint-status.yaml]`. It needs a named exclusion with owner and reason in the phase row, and it is covered by no review chunk [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:475] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Add named exclusions with owner and reason for the external content committed inside the four declared governance paths — `architecture.md` D31 (`4d2e4e2f`) and the D17 rewrite (`c9dfb06f`); `epics.md` Epic 28/29/31 blocks, the NFR9/NFR24 rewrites, the five 2026-07-25 Cerebras placeholder stories (`1f95a72b`) and Story 30.2; `sprint-status.yaml` `readiness_accounting`/`epic_metadata`/`story_execution_order` expansion and the two Epic 21 `action_items` closures; `deferred-work.md` `21.10-A4-VERIFY` and the four `REL-*` entries. The `:202` coverage claim is path-complete but not ownership-complete [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:584] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Add a Completion-state column to the C1 Gate Evidence Table and define the `passed` transition — all 14 rows carry `pending | —` under a single `Review state` column, unlike the umbrella table at `:230` which carries review state, completion state and completion date; AC5 as restated at `:236` triggers on "any row not in `passed` state", a value the schema cannot hold, and no rule names who records the transition or on what evidence [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:240] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Add per-gate rows for AC1, AC3 and AC4 — the table's own preamble states "Every row below is an AC2 gate" and all 14 rows are tagged `AC2`, leaving AC1's ~12 separately capturable identities, AC3's zero-loss/out-of-profile/backup/restore/RPO-RTO evidence, and AC4's sixteen separate approvals covered only by the single C1 umbrella row. `epics.md:555`, strengthened by this same diff, forbids exactly that shape and names Story 27.3 as a known instance [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:236] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Reword the C1 row's Completion-state cell — it opens with "approved planning and server installation may proceed after C0" for a gate whose Review state is `pending` and whose named evidence artifact records `status: rejected`; a completion column that leads with "approved" for the story's single blocking gate is the cell most likely to be misread [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:232] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Add the two missing Historical Context Classification rows and re-derive the table — it is a byte-for-byte copy of the superseded pre-split story's table (verified identical against `f474db15^` lines 260-281) and was never updated across either split; the superseded `27-3-retention-verification-operations-runbook-and-a41-close-out.md` meets three of the policy's own anti-template triggers (superseded, umbrella, checkpoint-heavy) and has no row, and the Story 26.1-origin `ProductionDeploymentArtifactsTests` / `*-production-deployment*.ps1` harness (commit `7217ef89`) has no `current-narrow-pattern` row. `Testing Baseline and Planned Delta` at `:299-330` is likewise verbatim and still carries transferred Story 27.4 delta breadth [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:332] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Repoint the 28 unresolvable review-finding anchors — every retained pre-split row ends `[...27-3-retention-verification-operations-runbook-and-a41-close-out.md:NNN]` and that path does not exist at HEAD, so the evidence behind 28 closed `[x]` findings cannot be re-checked; the open item at `:200` also misstates the count as "cited 37 times" (28 line anchors, 32 filename mentions, 37 retained rows) [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:86] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Correct the story's frontmatter provenance — `creation_commit: 6d7fd8aa` names a commit that created `27-3-retention-verification-operations-runbook-and-a41-close-out.md`, not this path, and `creation_sprint_status_sha256: f2cc7a25...` is the pre-create value (the creation-commit blob hashes to `cb351de2...`, its parent to `ce5b010c...`). Record the rename lineage rather than leaving machine-readable fields that do not describe this file [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:3] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Add both 2026-07-26 proposals to the Course Correction Record and References — the record lists only the 2026-07-19 and 2026-07-20 proposals and its "Artifacts changed" bullet names no Epic 30/31 work, while the References block lists neither 2026-07-26 file; the bare citation "approved Sprint Change Proposal 2026-07-26" used at `:522`, `:126` and `:140` is ambiguous between `...readiness-coherence-and-27-3-splits.md` and `...memories-ci-cd-alignment.md` [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:511] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Correct the authorizing proposal's normative path count — §2 (`:130`) and Edit 14 (`:618`) both state "13 paths move out of Story 27.3" while the enumerated set at `:620-641` is 16; the correction exists only in the approval note at `:962`, so an auditor reconstructing scope from the normative text computes 52 - 13 = 39, not the 36 the story now uses as its reconciliation baseline [_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-26-readiness-coherence-and-27-3-splits.md:618] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Delete or correct the "no forward dependency is created" sentence — decision `:160` accepted the dependency on Story 30.1 and ordered this statement corrected; it stands unamended in the section a reader treats as normative. The C1 reopen trigger recorded at `:423`/`:427` additionally requires "signed registry publication for all four images", which is Story 30.1 acceptance scope, itself gated on an unshipped external Hexalith.Builds revision [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:78] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Repair the stale ownership sentence in `architecture.md` — "Stories 27.2 and 27.3 are unblocked to implement **and verify** the accepted target" survives the 2026-07-20 transfer of verification to Story 27.4, and `grep "27\.4"` over the file returns nothing. The path is Story 27.3-declared, so the stale claim sits inside 27.3-owned scope [_bmad-output/planning-artifacts/architecture.md:227] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Bind the new `PG-ONPREM-1` non-HA assurance sentence to a guard — the only test reading that section (`AccessTelemetryRetentionDecisionTests`, `Security Architecture`) asserts eight substrings, none of them the new profile ID, in-profile fault, or "no HA claim" text, so the sentence can be deleted or rewritten into a node-redundancy claim with Server.Tests green while the ADR-side guard keeps passing and the two records diverge silently [_bmad-output/planning-artifacts/architecture.md:227] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Update the decision-registry caption — "Captured in Decision Registry D1-D28" understates a registry that already carried D29 and D30 and now carries the D31 added by this diff; `:1646` ("28 decisions (D1-D28)") and `:1711` ("all 27 architectural decisions (D1-D28)") are also stale and disagree with each other [_bmad-output/planning-artifacts/architecture.md:536] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Correct the blanket "No Story 27.3 mutation" claims about A41 — `:276` and `:518` both deny any A41 change while `:517` discloses "A41 backlog-home/owner references", and `git log -S` attributes the `deferred-work.md` `Backlog home: Epic 27, Stories 27.1-27.3 -> 27.1-27.4` edit to Story 27.3's own commit `f474db15`. The residual/action state was indeed not mutated; the blanket wording is what is false [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:276] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Extend the Change Log's transfer disclaimer past 2026-07-20 — it covers only rows "dated before 2026-07-20" and mentions only the Story 27.4 transfer, while the 2026-07-20 row at `:582` and Completion Note `:494` still book publish-container `38/38`, Cli.Tests `384/384`, `CiTestInventoryTests` `57/57`, SBOM production and four-image release orchestration as Story 27.3 evidence — all transferred to Story 30.1 [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:565] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Re-baseline the planned test delta after the split — the plan is Server +8..12, Integration +22..32, AccessTelemetry +10..16 and new tooling +8..12 Python cases; cumulative actuals are +6, +0, +16 and +4. The header at `:301` re-baselines only Story 27.4's deltas, so the narrowed Story 27.3 never got a revised plan, and the Python lane it wholly owns delivered half its floor with no recorded explanation [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:299] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Correct "the class is now 12" — live discovery gives 13 methods in `DaprAccessTelemetryStateStoreTests` (4 -> 13 across `358bef35`, i.e. +9: eight negatives plus the cross-tenant denial). The Change Log's `+12` AccessTelemetry phase delta is correct; only this finding prose is wrong [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:129] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Give the C1 evidence packet a per-run path or an append-only history — a single fixed path is rewritten on every invocation, so the rejection history AC5 depends on is destroyed by the run that would claim approval, and an approval packet and a rejection packet are indistinguishable by path despite AC1 requiring immutable evidence and AC4 hash-bound approvals [_bmad-output/implementation-artifacts/tests/27-3-adapter-profile-evidence.md:1] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Record what `profile_sha256` covers and reconcile the three profiles now in play — the packet's `components` observation still shows the running store carrying `sslRootCert` (removed from the manifest by the chunk-2 patch) and the pre-change `maxConns`, `:425` confirms the cluster has not been re-applied, and `:493` still cites the 2026-07-20 live capability results as Task 1 progress although they were captured against that different profile. `01f6ce9f...` is published with no statement of whether it hashes the reviewed manifests or the divergent live state [_bmad-output/implementation-artifacts/tests/27-3-adapter-profile-evidence.md:21] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Replace the tautological immutability case — `test_profile_and_mutation_manifest_are_immutable` calls `profile.manifest()` twice on a synthetic profile and asserts the two results are equal, which holds under any canonicalization change; nothing pins the canonical `PG-ONPREM-1` profile to its expected `profile_sha256`, and `grep` confirms no test or tool reads the committed packet at all [tests/tooling/access_telemetry_lifecycle/test_adapter_profile.py:78] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Make gate C1.13's cited proof capable of failing — `test_capacity_inputs_fail_closed` is four `assertRaises(CapacityInputError)` calls with no successful computation, no `required_bytes` value, and no comparison against 429,496,729,600 or the 70/80/90% thresholds, so a 500 GiB requirement or a `required_bytes = 0` result satisfies every assertion in the one gate whose evidence is claimed runnable today [tests/tooling/access_telemetry_lifecycle/test_adapter_profile.py:38] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Rewrite the create-story scope verifier for the post-split story and wire it into CI — verified independently this review: it exits `1` at its first check with `missing governed artifact: ...27-3-retention-verification-operations-runbook-and-a41-close-out.md`, and `grep` confirms no workflow, tool or test invokes it, so every "hardened by the create-story verifier" finding recorded in this story is unenforced. DW 27.3-CR7's prescribed "repoint the three constants" repair cannot reach exit 0: `:148` requires `range(7)` C0-C6 against a two-row table; `KEY`/`STORY` serve both immutable creation-commit reconstruction and current status parity, so repointing fails at "creation sprint must contain exactly one ready-for-dev row" or "creation name-status mismatch"; the `MATRIX` repoint demands `postflight_status`/`approved_mutation_manifest_sha256`/`publish_verify_status` fields the packet schema lacks; `:257` requires an `evidence:` key absent from the sprint action-item schema; and `:251`'s anchored `- Status: resolved` rejects the register's own closure style (`- Status: resolved 2026-07-26 — ...`). Per the 2026-07-26 decision above, the rewrite also removes the Story 27.4-scoped `done` branch (`:239-265`) and asserts instead that A41 remains open [_bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md:69] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Fix the verifier's `successful_phase` classifier and its unguarded `prior_status` lookup — the five-word blocklist scores both 2026-07-26 HALT rows as *successful*, satisfying the `review` precondition on fail-closed evidence, while an honest final row containing "blocked" scores as *unsuccessful* and defeats `done`; and `prior_status` is indexed into `STATUS_RANK` without the membership check applied to `current`, so a `backlog` or missing key raises an uncaught `KeyError` instead of the intended non-monotonic-transition failure [_bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md:167] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Add a `## File Scope` section to this story — `tools/check-story-file-scope.py` would raise `Story file has no parseable File Scope section` for it, and its CI job carries `if: github.event_name != 'push' || github.ref_name != 'main'`, which is false for the pushes that landed `159d7216`, `358bef35` and `c9dfb06f`, so the changed-file governance gate has never evaluated this story's declared paths [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:520] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Write the evidence packets with the CRLF the repository materializes — `.gitattributes` declares `* text=auto eol=crlf`, and `git ls-files --eol` shows `27-3-adapter-profile-evidence.md` as `w/crlf` while `27-3-create-story-scope-evidence.md` is `w/lf`; the `line_endings` suite covers only razor, LF and binary patterns, so `*.md` under `_bmad-output/**` is unguarded and any hash taken over working-tree bytes is checkout-dependent, undermining the evidence-integrity claim in AC4 [_bmad-output/implementation-artifacts/tests/27-3-adapter-profile-evidence.md:1] — applied 2026-07-26 (chunk 3b).

- [x] [Review][Patch] Bind the capacity admission gate to the approved 70/80/90% threshold table per the 2026-07-26 decision — replace the story's "fits the exact 400 GiB profile" wording, resolve the 80% peak-vs-critical collision, and state the disposition when the configured 24-hour target itself fails capacity [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:60] — applied 2026-07-26 (chunk 3b).
- [x] [Review][Patch] Replace the AC4 reviewer assignment with a recorded accepted blocker per the 2026-07-26 decision — remove "security reviewer Murat TEA for Jérôme", name owner, consequence and reopen trigger, and keep every AC4 gate `pending` until a real independent security approver exists [_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md:413] — applied 2026-07-26 (chunk 3b).
- [ ] [Review][Action] Raise the course correction that approves `maxConns: 40`, recomputes `profile_sha256`, and supersedes the 2026-07-20 pinning (Decision approved 2026-07-26 by Administrator during code review; execution is a separate correct-course activity, not applied by this review. DW 27.3-CR14) [_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-20-story-27-3-on-prem-postgresql-18-4.md:176]
- [ ] [Review][Action] Raise the course correction that adds the deployment-verification acceptance criterion and its named checkpoint to Story 27.3 (Decision approved 2026-07-26 by Administrator during code review; execution is a separate correct-course activity, not applied by this review. DW 27.3-CR15) [_bmad-output/planning-artifacts/epics.md:5137]
- [ ] [Review][Action] Split Stories 30.1 and 31.1 into separately numbered stories via correct-course before either is set `ready-for-dev` (Decision approved 2026-07-26 by Administrator during code review; execution is a separate correct-course activity, not applied by this review. DW 27.3-CR16) [_bmad-output/planning-artifacts/epics.md:5076]

- [x] [Review][Defer] No executable integrity check exists for `sprint-status.yaml` — no tool, test or workflow parses it; nothing validates duplicate `development_status` keys, allowed status values, `story_execution_order` membership, or `epics.md` parity, so every registration and status flip in this diff ships unchecked [_bmad-output/implementation-artifacts/sprint-status.yaml:1] — deferred, pre-existing repo-wide gap
- [x] [Review][Defer] Epic 21 retro action closed `done` while its enforcing gate has never fired — the five `EmbeddingVectorMigrationRedisIntegrationTests` facts did execute and pass in run 29798593273, so the action's "executes in an approved CI lane" criterion is met, but `verify-integration-fast-coverage.py` asserts class presence rather than per-class outcome and is `success()`-gated, and that run was red so the verifier step never ran; hardening remains open as `21.10-A4-VERIFY` [_bmad-output/implementation-artifacts/sprint-status.yaml:591] — deferred, external Epic 21 lane with hardening already tracked
- [x] [Review][Defer] `DW 27.3-CR3`'s reopen trigger describes the defect rather than an observable event — "a `RecordId` containing `/` or other key-delimiter characters can reach `GetRecordKey`/`GetBucketKey`" is observed by no guard, test or gate, so the entry can only reopen after the key collision it exists to prevent has already occurred [_bmad-output/implementation-artifacts/deferred-work.md:2283] — deferred, chunk-1 item
- [x] [Review][Defer] `epics.md` planning-convention drift — Stories 29.x, 30.1, 30.2 and 31.1 duplicate `**Status:** backlog` inside `epics.md` while Epic 27's stories carry none, and `epics.md` presents Epic 30 as 30.1 then 30.2 while `sprint-status.yaml:163` sequences 30-2 before 30-1 with a stated reason that Story 30.1's own activation gate never mentions [_bmad-output/planning-artifacts/epics.md:5079] — deferred, owned by the Epic 29/30 planning sessions


## File Scope

Added 2026-07-26 by code review (chunk 3b). This section is the machine-readable form of the cumulative File List below and exists so `tools/check-story-file-scope.py` can evaluate this story; the File List remains the reconciliation record of authority. The two must stay identical.

Allowed files for this story:

- `.github/workflows/ci.yml`
- `_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-20-story-27-3-on-prem-postgresql-18-4.md`
- `_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-26-readiness-coherence-and-27-3-splits.md`
- `_bmad-output/planning-artifacts/epics.md`
- `_bmad-output/planning-artifacts/architecture.md`
- `_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md`
- `_bmad-output/implementation-artifacts/deferred-work.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `_bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md`
- `deploy/kubernetes/base/access-telemetry-deployments.yaml`
- `deploy/kubernetes/base/access-telemetry-network-policy.yaml`
- `deploy/kubernetes/base/access-telemetry-postgresql.yaml`
- `deploy/kubernetes/base/dapr/access-telemetry-store.yaml`
- `deploy/kubernetes/base/dapr/access-telemetry-lifecycle-config.yaml`
- `deploy/kubernetes/base/dapr/access-telemetry-secrets.yaml`
- `deploy/kubernetes/base/dapr/config.yaml`
- `deploy/kubernetes/base/service-accounts-rbac.yaml`
- `deploy/kubernetes/base/kustomization.yaml`
- `docs/operations/deployment-configuration.md`
- `docs/dev/adr-27.1-001-access-telemetry-lifecycle.md`
- `src/Hexalith.Memories.AccessTelemetry/Lifecycle/AccessTelemetryExpiryBucket.cs`
- `src/Hexalith.Memories.AccessTelemetry/Lifecycle/AccessTelemetryExpiryCatalog.cs`
- `src/Hexalith.Memories.AccessTelemetry/Lifecycle/DaprAccessTelemetryStateStore.cs`
- `src/Hexalith.Memories.AccessTelemetry/Lifecycle/InMemoryAccessTelemetryStateStore.cs`
- `tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/DaprAccessTelemetryStateStoreTests.cs`
- `tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/AccessTelemetryStateStoreOrderingParityTests.cs`
- `tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/AccessTelemetryStateStoreTestRecords.cs`
- `tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/TransactionalDaprState.cs`
- `tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/InMemoryAccessTelemetryStateStoreContractTests.cs`
- `tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/LifecycleActorCheckpointTests.cs`
- `tests/Hexalith.Memories.IntegrationTests/Telemetry/AccessTelemetryLifecycleIntegrationCheckpointTests.cs`
- `tests/Hexalith.Memories.Server.Tests/Deployment/ProductionDeploymentArtifactsTests.cs`
- `tests/Hexalith.Memories.Server.Tests/Architecture/AccessTelemetryRetentionDecisionTests.cs`
- `tests/Hexalith.Memories.Cli.Tests/Ci/CiTestInventoryTests.cs`
- `_bmad-output/implementation-artifacts/tests/27-3-adapter-profile-evidence.md`
- `tests/tooling/access_telemetry_lifecycle/test_adapter_profile.py`
- `tests/tooling/production_deployment_evidence/production_deployment_evidence_test.py`
- `tests/tooling/coverage_gate/line-coverage-gate.json`
- `tests/tooling/coverage_gate/coverage_contract_test.py`
- `_bmad-output/implementation-artifacts/tests/adapter-profile-runs/**`
- `tools/render-production-deployment.ps1`
- `tools/verify-production-deployment.ps1`
- `tools/production-deployment-health.ps1`
- `tools/validate-production-deployment-evidence.ps1`
- `tools/verify-access-telemetry-lifecycle.py`
- `tools/verify_access_telemetry_lifecycle.py`
- `tools/test-projects.unit-contract.txt`

Read/verify only:

- `docs/dev/adr-27.1-001-access-telemetry-lifecycle.md` — the immutable `PG-ONPREM-1` profile authority; edited only through an approved course correction.
- `_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-19-story-27-3-c1-production-adapter.md`
- `_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-26-memories-ci-cd-alignment.md`

## Implementation Checkpoints

Story 27.3 is an explicitly approved C0/C1 qualification story. Every row is mandatory; completion qualifies only the immutable Production adapter profile and never closes A41.

### Checkpoint Execution Contract

Every C# checkpoint command below is a two-stage command contract: first build the named test project from the reviewed source with `DOTNET_CLI_USE_MSBUILD_SERVER=0 dotnet build <project> --configuration Release --disable-build-servers -m:1 /nr:false -p:NuGetAudit=false -p:MinVerVersionOverride=1.0.0`; then hash the reviewed source/profile and fresh assembly, discover the exact selected classes with `DiffEngine_Disabled=true dotnet exec <assembly> -list methods -noLogo`, and execute that same assembly with the table's selectors, `-parallel none -noLogo`. Immediately after execution, re-hash the source/profile and assembly and require byte-for-byte equality with the pre-execution values. The evidence must prove every selector discovers at least one unique method, `Total` equals the selected discovery count and is greater than zero, and `Errors`, `Failed`, `Skipped`, and `Not Run` are all zero. A stale or concurrently changed source/profile/assembly, missing selector, zero discovery, count mismatch, or nonzero result fails the checkpoint even when the process exit code is zero.

The C1 Python checkpoint runs its exact `unittest` discovery command below with `PYTHONHASHSEED=0`, records the sorted fully qualified case IDs and their SHA-256, requires the listed inventory exactly once with zero failures/errors/skips, and then invokes the verifier against the same reviewed source/profile and immutable adapter evidence packet.

| Checkpoint | Exact Python discovery command | Required case inventory |
| :--------- | :----------------------------- | :---------------------- |
| C1 | `PYTHONHASHSEED=0 python3 -m unittest discover -s tests/tooling/access_telemetry_lifecycle -p 'test_adapter_profile.py' -v` | `AdapterProfileTests.test_environment_identity_is_complete`; `AdapterProfileTests.test_capacity_inputs_fail_closed`; `AdapterProfileTests.test_capacity_result_is_admitted_against_profile_thresholds`; `AdapterProfileTests.test_workload_matches_adr_operation_envelope`; `AdapterProfileTests.test_profile_and_mutation_manifest_are_immutable`; `AdapterProfileTests.test_canonical_pg_onprem_profile_hash_is_pinned` |

**Inventory changed 2026-07-26 by code review (chunk 3b): 4 -> 6 cases.** Two cases were added because the original four could not fail on the gates they were cited for. `test_capacity_result_is_admitted_against_profile_thresholds` judges a computed capacity result against the approved 70/80/90% threshold table (the pre-existing `test_capacity_inputs_fail_closed` only proves bad *inputs* raise). `test_canonical_pg_onprem_profile_hash_is_pinned` pins the reviewed `PG-ONPREM-1` profile to `profile_sha256 dc19485835a050395cf73238524d98d735dd84540cdb7cb938512e73c2a63d14` and `mutation_manifest_sha256 2983ccdebedbd12e34bb1aec363335eb825301ce92d1c4ed87f8956d9c176b84` (the pre-existing `test_profile_and_mutation_manifest_are_immutable` hashes a synthetic profile twice and holds under any canonicalization or profile change). Both new cases were mutation-proven: making 80% admissible, changing `maxConns` in the canonical profile, accepting a zero requirement, and admitting the 7-day horizon each failed the class, with the baseline returning to `6/0/0` after every revert. **Note:** clear `tools/__pycache__` between mutation runs — a same-second edit/restore can be served from stale bytecode and mask the result.

### Production-Shaped Execution Contract

Set non-empty `KUBE_CONTEXT`, `KUBE_NAMESPACE`, `DEPLOYMENT_ID`, `PROFILE_ID`, `EVIDENCE_ROOT`, and `DECLARED_SINGLE_COMPONENT_FAULT` values for the reviewed `PG-ONPREM-1` environment. The verifier must record its expanded `kubectl`/Dapr/backend-evidence child commands, stdout, stderr, exit codes, UTC timestamps, pre/post workload identities, source/profile hashes, and immutable artifact hashes. Any target other than `jpiquot@local/hexalith-memories`, mocked control plane, missing expansion transcript, skipped fault/probe, or zero discovered observation fails C1.

```bash
python3 tools/verify-access-telemetry-lifecycle.py --checkpoint adapter-profile --kube-context "$KUBE_CONTEXT" --namespace "$KUBE_NAMESPACE" --deployment-id "$DEPLOYMENT_ID" --profile-id "$PROFILE_ID" --workload-profile adr-27.1-two-writer-500eps --steady-state-minutes 30 --purge-backlog-records 150000 --evidence "$EVIDENCE_ROOT/27-3-adapter-profile-evidence.md"
```

| Checkpoint | Accountable owner | Required evidence artifact and command | Review state | Completion state | Completion date |
| :--------- | :---------------- | :------------------------------------- | :----------- | :--------------- | :-------------- |
| C0 - Story 27.2 completion handoff | Story 27.2 owner + code reviewer | Reconciled Story 27.2 C1-C6/ledger/File List plus the IntegrationTests build/discovery/run contract for `-class Hexalith.Memories.IntegrationTests.Telemetry.AccessTelemetryLifecycleIntegrationCheckpointTests -parallel none -noLogo` | reviewed 2026-07-19 | complete; Story 27.2 is `done`, its reviewed 155/155 File List and ledger reconcile, and the fresh portable class passed 8/8 | 2026-07-19 |
| C1 - Exact `PG-ONPREM-1` Production adapter, capability, capacity, fault, backup/restore, and separated approval | Deployment adapter owner + Hexalith Platform Operations + independent security reviewer | `_bmad-output/implementation-artifacts/tests/27-3-adapter-profile-evidence.md`; run the exact C1 Python inventory and `adapter-profile` Production-shaped command above against the immutable profile in ADR 27.1 | pending | not complete — C1 is rejected; the shared evidence packet records `status: rejected` and Production lifecycle writes remain disabled, Story 27.4 remains `backlog`, and A41 remains open. (Planning and server installation were separately approved after C0; that approval is not C1 completion and confers none of C1's gates.) | — |

#### C1 Gate Evidence Table

Every row below is an independent C1 gate and must pass on its own evidence. Per `epics.md` checkpoint-guard policy one row per gate is required, a gate cannot be marked complete because the C1 umbrella row is complete, and per AC5 any row not in `passed` completion state keeps Production writes disabled, Story 27.3 `in-progress`, Story 27.4 `backlog`, and A41 open. All rows begin `pending` review state and `not complete` completion state; no completion is claimed by adding or extending this table.

**Transition rule (added 2026-07-26 by code review, chunk 3b).** A row leaves `not complete` only when its named observation exists in the referenced evidence packet with `evidence_is_approval: true`, its accountable owner records the transition, and the code reviewer confirms the observation against the packet. `passed` is the only admissible completion state; `pending` review state and `passed` completion state cannot coexist. A row whose named producer cannot emit its observation stays `not complete` regardless of any other evidence.

**Capacity admission (added 2026-07-26 by code review, chunk 3b, per the Administrator decision).** The approved 2026-07-20 course correction's threshold table governs admission: steady state <=70% (300,647,710,720 bytes), reclamation peak <=80%, critical at 80% (343,597,383,680), Unhealthy at 90% (386,547,056,640). Where the narrative elsewhere in this story reads that a duration is admissible if it "fits the exact 400 GiB profile", the threshold table is the controlling gate and 100% occupancy is not admissible. A measured result at exactly 80% is treated as critical, not as an admissible peak. If the configured 24-hour target itself fails the 70% steady-state threshold, `PG-ONPREM-1` is rejected for that retention duration and a new correct-course decision is required; a passing 1-hour horizon licenses nothing on its own. Amending Task 1's own wording is a task-definition change and remains with the story owner.

Shared evidence packet for every row: `_bmad-output/implementation-artifacts/tests/27-3-adapter-profile-evidence.md`, produced by the Production-Shaped Execution Contract command above against the immutable `PG-ONPREM-1` profile. Each row names the observation within that packet that proves it. Rows whose producer is a named test rather than the packet say so explicitly.

| Gate | AC | Accountable owner | Required evidence observation and command | Review state | Completion state | Completion date |
| :--- | :-- | :---------------- | :---------------------------------------- | :----------- | :--------------- | :-------------- |
| C1.1 CRUD | AC2 | Deployment adapter owner | `adapter-profile` probe: create/read/update/delete round trip on `state.postgresql/v2` | pending | not complete | — |
| C1.2 Strong reads | AC2 | Deployment adapter owner | `adapter-profile` probe: post-write strong-consistency read observation | pending | not complete | — |
| C1.3 ETags | AC2 | Deployment adapter owner | `adapter-profile` probe: ETag match/mismatch and `FirstWrite` insert semantics | pending | not complete | — |
| C1.4 Rollback-atomic multi-key transactions | AC2 | Deployment adapter owner | `adapter-profile` probe: fault injected on a later operation; no partial record or expiry-index commit | pending | not complete | — |
| C1.5 TTL | AC2 | Deployment adapter owner | `adapter-profile` probe: effective TTL expiry observation | pending | not complete | — |
| C1.6 Actor reactivation | AC2 | Deployment adapter owner | `adapter-profile` probe: actor state survives deactivation/reactivation | pending | not complete | — |
| C1.7 Placement / Scheduler / reminder recovery | AC2 | Deployment adapter owner | `adapter-profile` probe: Placement and Scheduler reconnection and reminder firing after control-plane disruption | pending | not complete | — |
| C1.8 Request bounds | AC2 | Deployment adapter owner | `adapter-profile` probe: request size/count bound enforcement | pending | not complete | — |
| C1.9 Two-writer 500 events/s throughput | AC2 | Deployment adapter owner + Hexalith Platform Operations | `adapter-profile` probe `--workload-profile adr-27.1-two-writer-500eps --steady-state-minutes 30`: ADR operation mix, zero acknowledged loss, p99 below the 3s Dapr client timeout, <=10% p95 regression vs same-profile no-purge baseline | pending | not complete | — |
| C1.10 150,000-record purge catch-up | AC2 | Deployment adapter owner + Hexalith Platform Operations | `adapter-profile` probe `--purge-backlog-records 150000`: 10-minute backlog drained within five minutes, oldest-due age below 15 minutes | pending | not complete | — |
| C1.11 Isolation | AC2 | Deployment adapter owner + security reviewer | `adapter-profile` probe: physical cross-tenant denial observed against the running profile, per the project-context tenant-isolation negative-evidence rule. The existing `DeleteAndVerifyAsync_EntryCarryingAnotherTenantMarker_IsDeniedAndLeavesTheRecordIntact` unit test does not satisfy this row: both its records resolve to one state key, so it re-exercises envelope-hash mismatch rather than tenant isolation | pending | not complete | — |
| C1.12 Encryption | AC2 | Deployment adapter owner + security reviewer | `adapter-profile` probe: TLS `verify-full` in force and at-rest encryption posture | pending | not complete | — |
| C1.13 Capacity | AC2 | Deployment adapter owner + Hexalith Platform Operations | `AdapterProfileTests.test_capacity_inputs_fail_closed` and `AdapterProfileTests.test_capacity_result_is_admitted_against_profile_thresholds` (input rejection and computed-result admission), plus the checked-arithmetic capacity result evaluated against the 70/80/90% threshold table above at 1h / 24h / 7d | pending | not complete | — |
| C1.14 Cohort-attributable physical reclamation | AC2 | Deployment adapter owner + Hexalith Platform Operations | `adapter-profile` probe: collector bound and cohort-attributable physical space reclamation | pending | not complete | — |
| C1.15 Runtime and control-plane identity | AC1 | Deployment adapter owner | Packet observation: Dapr runtime version, sidecar image digest, Scheduler connections, actor types, enabled features and any alpha opt-in, captured from the running deployment rather than .NET package pins | pending | not complete | — |
| C1.16 Component and backend identity | AC1 | Deployment adapter owner | Packet observation: component type, API version, capabilities, backend identity and PostgreSQL 18.4 version | pending | not complete | — |
| C1.17 Image, manifest and epoch identity | AC1 | Deployment adapter owner | Packet observation: application image digests, component/config manifest identity, configuration epoch, and the component manifest/profile hash with a statement of what the hash covers | pending | not complete | — |
| C1.18 Node/storage capacity and operating cost | AC1 | Hexalith Platform Operations | Packet observation: node and storage capacity, host filesystem headroom for the non-reserving local PVC, and recorded operating cost | pending | not complete | — |
| C1.19 Declared-fault zero acknowledged-record loss | AC3 | Deployment adapter owner | Packet observation: PostgreSQL pod/process forcibly lost and its StatefulSet pod replaced with node and retained local volume healthy; every Dapr-acknowledged record still present | pending | not complete | — |
| C1.20 Out-of-profile statement | AC3 | Hexalith Platform Operations | Packet observation: node, local-volume, control-plane and site loss published as explicitly outside profile | pending | not complete | — |
| C1.21 Backup destination and successful restore | AC3 | Hexalith Platform Operations | Packet observation: named backup destination and a successful restore result | pending | not complete | — |
| C1.22 Published RPO/RTO without HA claim | AC3 | Hexalith Platform Operations | Packet observation: resulting nonzero RPO and RTO published for out-of-profile failures, with no node/disk/site HA claim | pending | not complete | — |
| C1.23 Platform Operations approval | AC4 | Hexalith Platform Operations | Packet observation: separate recorded approval of node/storage capacity, operating cost, operation, bounded fault, backup/restore, upgrade, rollback and reclamation | pending | not complete | — |
| C1.24 Non-HA acknowledgement | AC4 | Hexalith Platform Operations | Packet observation: explicit acknowledgement of the absence of node, disk and site HA | pending | not complete | — |
| C1.25 Security reviewer approval | AC4 | Independent security reviewer | Packet observation: separate recorded approval of identity, secrets, TLS, network, authorization, encryption, privacy and evidence integrity. **Accepted blocker (2026-07-26):** no independent security approver is currently assigned — see the AC4 approver blocker in the Dev Agent Record | pending | not complete | — |

## Dev Notes

### Scope and Authority

- The exact Story/AC text comes from current `epics.md`; the accepted ADR and its **Production Adapter Qualification — PG-ONPREM-1** appendix are the technical authority. Story 27.2 implements portable runtime behavior; Story 27.3 qualifies the exact Production adapter and immutable deployment profile; Story 27.4 owns deployment-shaped lifecycle evidence, operations guidance, and the remaining A41 close-out. [Source: _bmad-output/planning-artifacts/epics.md#Story-27.3-Production-Adapter-and-Deployment-Profile; docs/dev/adr-27.1-001-access-telemetry-lifecycle.md#Production-Adapter-Qualification--PG-ONPREM-1]
- This story has one independently demonstrable outcome: an immutable, fully probed, configured-capacity-funded, backup/restore-documented, and separately approved `PG-ONPREM-1` C1 packet. It does not claim lifecycle close-out or mutate A41.
- Run another correct-course decision if `PG-ONPREM-1` is rejected or discovery requires multiple adapters, a multi-node/replicated-storage platform, a general trusted-time/persistence/operations platform, a tenant-facing inspection product, or an independently deployable raw-console/OTLP privacy change. Keep writes disabled and A41 open.
- Bounded infrastructure telemetry remains separate from domain persistence. Do not add access telemetry to Hexalith.EventStore, repurpose domain state, or add direct infrastructure dependencies to product/lifecycle code.

### Current State, Required Updates, and Preservation Rules

| Path/surface | Current state at story creation | Story 27.3 change | Preserve |
| :----------- | :------------------------------ | :---------------- | :------- |
| Story 27.2 runtime/tests/deployment | Runtime projects, Server lifecycle folder, AccessTelemetry.Tests, and dedicated lifecycle components are absent; story status/checkpoints are unreconciled | C0 re-reads the actual completed handoff and binds exact evidence paths | Do not implement predecessor scope or count its planned tests as actual |
| `docs/dev/adr-27.1-001-access-telemetry-lifecycle.md` | Accepted portable contract and explicit verification handoff; A41 remains open | Record `PG-ONPREM-1` as the sole qualification target, exact non-secret profile, Redis rejection, bounded non-HA fault, backup/restore blocker, and separated approval gate | Accepted Dapr-only design, authorities, bounds, rollback, assurance limit |
| `docs/dev/telemetry.md` | Documents FR67 JSON/OTLP emission, accepted lifecycle target, and open A41 residual | No Story 27.3 change; Story 27.4 describes only implemented/proved lifecycle and links the runbook/evidence | FR67 event IDs/schema, provider separation, JSON/optional-OTLP continuity, privacy warnings |
| Existing operations docs | Deployment, capacity, monitoring, incident, recovery, disaster, and rate-limit procedures exist but have no proven lifecycle adapter contract | No Story 27.3 runbook expansion; Story 27.4 adds the lifecycle runbook and proven profile operations | Existing product/backend procedures and owners; avoid duplicated umbrella text |
| Production deployment | Two read-only-root Server replicas; shared `state.redis/v1`; one 20 GiB Redis Stack StatefulSet; lifecycle/clock images are placeholders and disabled | Provision and probe dedicated on-premises PostgreSQL 18.4 `PG-ONPREM-1`; keep lifecycle writes disabled until terminal C1 acceptance | Two writers, non-root/read-only posture, business readiness, existing Redis/FalkorDB and Keycloak workloads |
| `AccessTelemetryRetentionDecisionTests.cs` | Structure-aware tests guard the accepted ADR, current open residual, and anti-corruption rules | Story 27.3 guards the exact qualification-target contract; Story 27.4 owns closed-state reconciliation | Exact table/section binding and leaked-markup rejection |
| A41 deferred/action/current summaries | Deferred entry is `carried-forward`; sprint action is `open`; current summaries forbid premature closure | No Story 27.3 mutation of the A41 residual, action status, or current summaries; Story 27.4 alone may apply its verifier-approved mutation set after C2-C6 and terminal validation. **Corrected 2026-07-26 by code review (chunk 3b):** Story 27.3's own commit `f474db15` did edit the A41 entry's `Backlog home: Epic 27, Stories 27.1-27.3` reference to `27.1-27.4`, as the Course Correction Record discloses. That reference edit is the only A41 change Story 27.3 made; no residual, action, or closure state was mutated | Epic 20/Story 20.5 history and `done` statuses; original approval/evidence chronology |

Every future Story 27.2 UPDATE file must be read completely during C0. For each, record its state machine/data shape/API calls, the exact verification-only change, and behaviors to preserve before editing it. A planned filename in this story is not authority over the completed handoff.

### Architecture and Technology Guardrails

- Stable logical identities remain `memories-access-telemetry`, `AccessTelemetryLifecycleActor/global`, `memories-access-telemetry-clock`, `access-telemetry-store`, `access-telemetry-secrets`, and `access-telemetry-config` unless an accepted ADR amendment says otherwise.
- Repository pins control implementation: .NET SDK 10.0.302/net10.0/C# 14, Dapr .NET packages 1.18.4, Aspire AppHost SDK 13.4.6, StackExchange.Redis 3.0.17, xUnit v3 3.2.2, Shouldly 4.3.0, and NSubstitute 5.3.0. Do not upgrade dependencies in this story. The running Dapr runtime/component identity must be captured independently.
- Use current one-type-per-file, file-scoped namespace, XML documentation, source-generated logging, central package management, warnings-as-errors, `.slnx`, SDK containers, and existing composition roots.
- Preserve sanitization-before-enqueue, finite catalogs, bounded queues/batches/retries, signed independent UTC, source-age retention, transactional record/index writes, fixed-actor/reminder recovery, separate authorities, low-cardinality metrics, and business-readiness isolation.

### Latest Technical Information

Research snapshot: 2026-07-18. Repository pins remain authoritative.

- Dapr state-store feature tables are not behavioral certification. The official Dapr 1.18 Redis implementation states that Redis transactions lack rollback; the exact adapter must pass a partial-commit fault probe. [Source: https://docs.dapr.io/reference/components-reference/supported-state-stores/; https://raw.githubusercontent.com/dapr/components-contrib/v1.18.0/state/redis/redis.go]
- Dapr forwards `ttlInSeconds` to native stores but does not independently expire state; logical expiry and active purge remain authoritative and testable separately. [Source: https://docs.dapr.io/developing-applications/building-blocks/state-management/state-store-ttl/]
- Actor reminders survive activation failover through Dapr Scheduler; Production evidence must cover Scheduler/Placement persistence and replacement, not only the actor process. [Source: https://docs.dapr.io/developing-applications/building-blocks/actors/actors-timers-reminders/; https://docs.dapr.io/operations/hosting/kubernetes/kubernetes-persisting-scheduler/]
- Dapr metadata and health APIs expose runtime/component/actor/Scheduler identity needed in the evidence packet; do not infer runtime version from application packages. [Source: https://docs.dapr.io/reference/api/metadata_api/; https://docs.dapr.io/reference/api/health_api/]
- Actor state-store component changes require restart and component reload can transiently close/reinitialize the component; rollout/rollback must preserve manifests and prove epoch convergence. [Source: https://docs.dapr.io/operations/components/component-updates/; https://docs.dapr.io/reference/api/configuration_api/]
- Redis `everysec` AOF and `WAIT` do not establish a universal zero-loss guarantee. If Redis is selected, scope the tested fault/RPO precisely and separately prove dataset and AOF/disk reclamation for the test cohort. [Source: https://redis.io/docs/latest/operate/oss_and_stack/management/persistence/; https://redis.io/docs/latest/commands/wait/; https://redis.io/docs/latest/commands/memory-usage/; https://redis.io/docs/latest/commands/info/]
- The current local integration environment reported Dapr runtime 1.18.1 while application Dapr packages are 1.18.4. This is baseline evidence of identity drift, not the Production identity; C1 must capture and pin the running Production runtime/component profile explicitly.

### Testing Baseline and Planned Delta

This section preserves the creation-time pre-split baseline. After the approved 2026-07-20 course correction, only C1 adapter-profile tooling and qualification deltas belong to Story 27.3; lifecycle/failure/runbook/A41 deltas belong to Story 27.4 and must be re-baselined when its implementation file is created.

**Story 27.3 planned delta re-baselined 2026-07-26 by code review (chunk 3b).** The creation-time plan below (Server +8..12, IntegrationTests +22..32, AccessTelemetry +10..16, tooling +8..12 Python cases) was written for the pre-split umbrella and was never revised for the narrowed story; it is retained as history and is no longer the plan of record. The post-split Story 27.3 plan is: Server.Tests +6 (achieved), IntegrationTests +0 (the lifecycle/failure/continuity breadth that justified +22..32 is Story 27.4 scope), AccessTelemetry.Tests +16 (achieved), and `tests/tooling/access_telemetry_lifecycle` Python cases +4 at the creation-time inventory, revised upward by this review's capacity and immutability additions. The apparent Python shortfall against the pre-split +8..12 floor is a consequence of the split, not an unexplained miss.

Fresh comparable baselines were built on 2026-07-18 at commit `272c33bc5d30d71ac46f20e703b9d5456e75a093` with the pre-existing dirty Story 27.2 sprint transition and EventStore submodule work excluded from Story 27.3 ownership.

```bash
DOTNET_CLI_USE_MSBUILD_SERVER=0 dotnet build tests/Hexalith.Memories.Server.Tests/Hexalith.Memories.Server.Tests.csproj --configuration Release --disable-build-servers -m:1 /nr:false -p:NuGetAudit=false -p:MinVerVersionOverride=1.0.0
DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.Server.Tests/bin/Release/net10.0/Hexalith.Memories.Server.Tests.dll -list methods
DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.Server.Tests/bin/Release/net10.0/Hexalith.Memories.Server.Tests.dll -list methods -noLogo | awk '/^Hexalith\.Memories\.Server\.Tests\./ { total++; if ($0 ~ /\.Architecture\./) architecture++; if ($0 ~ /\.Telemetry\./) telemetry++; if ($0 ~ /\.Hosting\./) hosting++ } END { print total, architecture, telemetry, hosting }'
DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.Server.Tests/bin/Release/net10.0/Hexalith.Memories.Server.Tests.dll -list methods -noLogo | sed -n '/^Hexalith\.Memories\.Server\.Tests\./p' | LC_ALL=C sort | sha256sum
DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.Server.Tests/bin/Release/net10.0/Hexalith.Memories.Server.Tests.dll -class Hexalith.Memories.Server.Tests.Architecture.AccessTelemetryRetentionDecisionTests -parallel none -noLogo

DOTNET_CLI_USE_MSBUILD_SERVER=0 dotnet build tests/Hexalith.Memories.IntegrationTests/Hexalith.Memories.IntegrationTests.csproj --configuration Release --disable-build-servers -m:1 /nr:false -p:NuGetAudit=false -p:MinVerVersionOverride=1.0.0
DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.IntegrationTests/bin/Release/net10.0/Hexalith.Memories.IntegrationTests.dll -list methods
DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.IntegrationTests/bin/Release/net10.0/Hexalith.Memories.IntegrationTests.dll -list methods -noLogo | awk '/^Hexalith\.Memories\.IntegrationTests\./ { total++; if ($0 ~ /\.Telemetry\./) telemetry++ } END { print total, telemetry }'
DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.IntegrationTests/bin/Release/net10.0/Hexalith.Memories.IntegrationTests.dll -list methods -noLogo | sed -n '/^Hexalith\.Memories\.IntegrationTests\./p' | LC_ALL=C sort | sha256sum
```

- Server.Tests: build 0 warnings/errors; 2,157 xUnit methods total, including Architecture 24, Telemetry 141, and Hosting 17; sorted method-set SHA-256 `98440744599bf454ee991a3cad39f69dee9185f6d1fe86106223638c3a194ae2`. `AccessTelemetryRetentionDecisionTests` is 6 methods and ran 6/0/0.
- IntegrationTests: build 0 warnings/errors; 270 xUnit methods total, including Telemetry 9; sorted method-set SHA-256 `038e0a140092d0a7910c17566a5b87a05bdc417699f67c4335516777168a30c4`.
- `Hexalith.Memories.AccessTelemetry.Tests`: absent because Story 27.2 is incomplete. Its later first Story 27.2 discovery is an external `0 -> N` transition and must be mapped before Story 27.3 counts.
- `tests/tooling/access_telemetry_lifecycle`: absent. If created, its first Python discovery is a named Story 27.3 `0 -> N` transition.

Planned Story 27.3 additions are quantified by comparable named unit, not combined across runners:

- Server.Tests: +8 to +12 xUnit methods for exact evidence/doc/deployment/A41 guards.
- IntegrationTests: +22 to +32 xUnit methods for multi-writer, replacement, retention, fault, privacy, and continuity proof.
- AccessTelemetry.Tests after the external Story 27.2 baseline: +10 to +16 xUnit methods for adapter atomicity/capability/reclamation and clock/reminder edge evidence.
- New tooling lane: +8 to +12 Python `unittest` cases for environment identity, immutable evidence, capacity arithmetic, doc/status parity, and zero-unapproved-skip validation.

Actual create-story delta is +0 and cumulative +0 in every lane. At C0, capture post-Story-27.2 totals and record its external delta before subtracting any Story 27.3 result.

### Historical Context Classification

**Provenance note (added 2026-07-26 by code review, chunk 3b).** The rows above the two 2026-07-26 additions were carried over byte-for-byte from the superseded pre-split story and were not re-derived at either the 2026-07-20 or the 2026-07-26 split; they are verified identical to that file's table. They remain accurate as classifications, but the table is not evidence that a fresh classification pass was performed for the narrowed story. The two rows added on 2026-07-26 close the gaps that pass identified.

| Reference | Classification | Permitted influence on Story 27.3 |
| :-------- | :------------- | :-------------------------------- |
| Story 27.1 whole-story shape | `historical-reference-only` | Accepted decision provenance only; do not copy its long decision/review task shape. |
| Accepted ADR 27.1, especially the 27.3 handoff | `current-narrow-pattern` | Current technical/evidence authority. |
| Story 27.1 superseded Redis/Kubernetes iterations | `anti-template` | Never restore backend SDKs, Kubernetes clock/Pod identity, or backend/orchestrator application dependencies. |
| Story 27.2 whole-story handoff | `historical-reference-only` | Completed predecessor/dependency evidence only; do not reuse its task, checkpoint, AC, or File List breadth. |
| Current `AccessTelemetryLifecycleIntegrationCheckpointTests` seam | `current-narrow-pattern` | Reverified portable C0 implementation/test seam only; the fresh 2026-07-19 Release assembly discovered and passed the exact eight methods with no errors, failures, skips, or not-runs. |
| Story 7.5 | `anti-template` | Preserve FR67 emission only; its broad observability/stdout proof is not retention evidence. |
| Story 8.4 | `historical-reference-only` | Current-emission/test-helper provenance only; stdout evidence is not TTL/purge/reclamation proof. |
| Story 8.5 | `anti-template` | Do not copy its bundled operational scope. |
| Story 20.2 | `historical-reference-only` | Three reverified denial-before-dependency negatives and principal-derived identity only. |
| Story 20.5 | `anti-template` | Rate-limit/audit-emission provenance and residual ID only; never reopen or reuse the broad bundle. |
| Story 21.1 | `current-narrow-pattern` | Reverified structure-aware document-guard mechanics only. |
| Story 24.3 | `historical-reference-only` | Three verifier/tenant-marker negatives only; not its decision-plus-implementation shape. |
| Story 24.4 | `current-narrow-pattern` | Reverified finite metric/tag validation mechanics only. |
| Stories 26.1 and 26.5 | `anti-template` | Current manifest/doc facts only; never copy broad infrastructure/runbook/checkpoint scope. |
| Current `OperationalRunbookSetTests` and `MemoriesDashboardTests` mechanics | `current-narrow-pattern` | Current source-backed document/link/dashboard contract mechanics only. Code review reverified both current classes together at 13/0/0 on 2026-07-18; Story 26.5 task shape, AC/file breadth, checkpoint organization, and historical green results remain excluded anti-template material. |
| Story 26.6 | `current-narrow-pattern` | Reverified rollback and observable restoration mechanics only. |
| Story 26.8 and Epic 20/21 retrospectives | `historical-reference-only` | Close-out chronology/provenance only; numeric adjacency and old green evidence are irrelevant. |
| Retention visibility proposal | `historical-reference-only` | Durable premature-closure guard; its no-scheduling text was superseded by the approved implementation proposal. |
| Superseded pre-split `27-3-retention-verification-operations-runbook-and-a41-close-out.md` | `anti-template` | **Added 2026-07-26 by code review (chunk 3b).** This story's own superseded predecessor file, renamed away by `f474db15`. It meets three of the policy's anti-template triggers at once — superseded, umbrella, and checkpoint-heavy (it carried an approved umbrella checkpoint table with C1-C6 owners/evidence/review gates). Permitted influence: retained-finding provenance and anchor resolution only. Its task shape, AC density, C1-C6 checkpoint breadth, File List, and planned-delta breadth are explicitly not reusable, and the 28 retained rows anchored at it are historical evidence, not current requirements. |
| Story 26.1-origin `ProductionDeploymentArtifactsTests` / `render-production-deployment.ps1` / `verify-production-deployment.ps1` harness | `current-narrow-pattern` | **Added 2026-07-26 by code review (chunk 3b).** All three originate in commit `7217ef89` (Story 26.1), which the row above classifies `anti-template` at whole-story level. Only the focused guard/render/verify *mechanics* are reused, and they were re-verified against current source by executed runs this story: the connection-pool guard was driven by a genuine RED (`141 > 100`, 8 discovered / 1 failed) before the manifest fix, and the security-contract guard was mutation-proven by six drift injections each failing the class with the baseline returning to `9/0/0`. Story 26.1's broad infrastructure/runbook/checkpoint scope, AC breadth, and File List remain excluded. |

### Slice Proof

Story 27.3 is one approved qualification decision: prove or reject the single exact `PG-ONPREM-1` Production profile through C0/C1. **Re-derived 2026-07-26 by code review (chunk 3b):** the story remains one story under the `story-scope-guard.md` umbrella exemption only because every checkpoint now carries its own owner, evidence command/artifact, review state and completion state — C0 and C1 in the checkpoint table, and each of the 25 C1 gates in the gate evidence table. Three lanes ship and prove independently of the externally-blocked C1 (the PostgreSQL-v2 adapter/unit lane, the `PG-ONPREM-1` manifest-guard lane, and the kind-based deployment-verification harness); the first two are covered by named gates, and the third is tracked as `DW 27.3-CR15` pending the approved correction that gives it an acceptance criterion and its own checkpoint. Until that correction lands, the deployment-verification lane is not claimable as Story 27.3 evidence. A passing outcome requires every immutable image, capability, bounded fault, configured-retention capacity, backup/restore, security, and reclamation result plus separate Platform Operations and security approvals. Rejection is also bounded, but it keeps writes disabled and requires another course correction.

The split boundary is strict: 27.1 owns the accepted portable design, 27.2 owns portable runtime behavior, 27.3 owns exact-adapter qualification and the immutable deployment profile, 27.4 owns C2-C6/runbooks/reconciliation/A41 close-out, and Epic 28 owns EventStore runtime adoption. Any general platform, tenant-facing product, multiple-adapter rollout, or missing runtime feature returns to planning.

### Project Structure Notes

Likely **new** Story 27.3 paths after C0; bind names to the actual predecessor structure:

- Signed lifecycle and clock deployment image references and the non-secret `PG-ONPREM-1` PostgreSQL/component/profile manifests.
- C1 qualification tooling/tests and `_bmad-output/implementation-artifacts/tests/27-3-adapter-profile-evidence.md` for immutable environment, image, fault, capacity, and approval evidence.

Story 27.4, not this story, owns lifecycle/runbook/A41 evidence paths, C2-C6 test classes, dashboards/alerts, and the canonical close-out matrix.

Likely **updated** current paths:

- Story 27.2 lifecycle/clock/Server provider, component, and deployment paths only where C1 qualification exposes a harness/configuration defect within the narrowed scope.
- `docs/dev/adr-27.1-001-access-telemetry-lifecycle.md`, selected component/deployment overlays, exact profile/evidence guards, and the sprint/story artifacts approved by the course correction.
- `_bmad-output/implementation-artifacts/sprint-status.yaml`; do not mutate deferred A41 state or historical records.

Do not edit `tools/release-packages.json`, add Dockerfiles, change EventStore pins/submodules, reopen Epic 20/Story 20.5, or duplicate the canonical runbook across every operations document.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Epic-27-Access-Telemetry-Lifecycle-Hardening]
- [Source: _bmad-output/planning-artifacts/epics.md#Story-27.3-Production-Adapter-and-Deployment-Profile]
- [Source: docs/dev/adr-27.1-001-access-telemetry-lifecycle.md#Production-Adapter-Qualification--PG-ONPREM-1]
- [Source: docs/dev/adr-27.1-001-access-telemetry-lifecycle.md#Retention-Expiry-Purge-and-Clock]
- [Source: docs/dev/adr-27.1-001-access-telemetry-lifecycle.md#Capacity-Evidence-and-Admission-Envelope]
- [Source: _bmad-output/planning-artifacts/sprint-change-proposal-2026-07-16-access-telemetry-retention-implementation.md]
- [Source: _bmad-output/planning-artifacts/sprint-change-proposal-2026-07-19-story-27-3-c1-production-adapter.md]
- [Source: _bmad-output/planning-artifacts/sprint-change-proposal-2026-07-20-story-27-3-on-prem-postgresql-18-4.md]
- [Source: _bmad-output/planning-artifacts/sprint-change-proposal-2026-07-26-readiness-coherence-and-27-3-splits.md]
- [Source: _bmad-output/planning-artifacts/sprint-change-proposal-2026-07-26-memories-ci-cd-alignment.md]
- [Source: _bmad-output/implementation-artifacts/deferred-work.md#Story-20.5-Deferred-Retention-Slice-2026-07-04]
- [Source: docs/dev/telemetry.md#Retention-lifecycle-status]
- [Source: _bmad-output/project-context.md#Testing-Rules]
- [Source: _bmad/custom/story-scope-guard.md]
- [Source: _bmad/custom/story-phase-ledger.md]

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Implementation Plan

- Add a standard-library-only C1 adapter-profile contract with explicit Production identity validation, ADR workload constants, checked capacity arithmetic, and immutable profile/mutation hashes.
- Keep the deployment collector read-only and fail closed: collect redacted Kubernetes structure, reject disabled lifecycle writers or unproven `state.redis` capability, and emit a non-approval evidence packet without enabling Production writes.
- Provision and qualify only the exact `PG-ONPREM-1` target recorded in ADR 27.1; pin every live image by digest, including PostgreSQL and new lifecycle/clock CI digests, and reject profile drift.
- Replace the unsupported Dapr Query path with deterministic transactional expiry buckets before exercising PostgreSQL v2.
- Require zero skipped C1 probes, zero acknowledged loss for the declared pod/process fault, configured-retention capacity, backup/restore evidence, explicit non-HA limits, and separate hash-bound Platform Operations and security approvals before changing this story from `in-progress`.

### Debug Log References

- 2026-07-18: HALT at C0 / Task 0. Story 27.2 is not a completed handoff: its story header is `ready-for-dev`, sprint status is `in-progress`, Tasks 1-9 and checkpoints C1-C6 remain pending, and no `dev-story` or `code-review` ledger row exists. Exact required checkpoint command `DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.IntegrationTests/bin/Release/net10.0/Hexalith.Memories.IntegrationTests.dll -class Hexalith.Memories.IntegrationTests.Telemetry.AccessTelemetryLifecycleIntegrationCheckpointTests -parallel none -noLogo` exited 0 but discovered/executed `Total: 0`; the named class is absent from source. Ownership returns to Story 27.2; Story 27.3 implementation, Production-adapter certification, evidence claims, and A41 mutations remain blocked.
- 2026-07-19: HALT at C1 / Task 1 before the RED phase because the required Production-shaped deployment configuration is unavailable. Exact preflight `kubectl config current-context` exited 1 with `error: current-context is not set`; `kubectl config get-contexts -o name` exited 0 with zero contexts; non-empty tests for `KUBE_CONTEXT`, `KUBE_NAMESPACE`, `DEPLOYMENT_ID`, `PROFILE_ID`, `EVIDENCE_ROOT`, and `DECLARED_SINGLE_COMPONENT_FAULT` each exited 1; and `test -f tools/verify-access-telemetry-lifecycle.py` exited 1. Owner: Deployment adapter owner plus Hexalith Platform Operations. Consequence: the running Dapr/component/backend identity, security approval, atomicity/durability/capacity probes, and exact Production evidence cannot be captured; no C1 artifact, Production enablement, or A41 mutation is permitted. Reopen trigger: provide an accessible reviewed Production-shaped Kubernetes context and namespace, exact deployment/profile/fault identities, writable evidence root, and the accountable operations/security reviewers; then begin Task 1 with failing C1 tooling tests and execute the exact adapter-profile contract against that environment.
- 2026-07-19: HALT at C1 / Task 1 before RED after rechecking the prior reopen trigger. `kubectl config current-context` now exits 0 with `jpiquot@local`, `kubectl cluster-info` exits 0, and `kubectl config get-contexts -o name` exits 0 with that single context. Read-only discovery does not reveal a candidate: `kubectl get all -n hexalith-memories`, `kubectl get components.dapr.io -n hexalith-memories`, and `kubectl get configurations.dapr.io -n hexalith-memories` each exit 0 with `No resources found in hexalith-memories namespace`. Non-empty checks for `KUBE_CONTEXT`, `KUBE_NAMESPACE`, `DEPLOYMENT_ID`, `PROFILE_ID`, `EVIDENCE_ROOT`, and `DECLARED_SINGLE_COMPONENT_FAULT` still each fail, and `test -f tools/verify-access-telemetry-lifecycle.py` exits 1. The Python discovery command `PYTHONHASHSEED=0 python3 -m unittest discover -s tests/tooling/access_telemetry_lifecycle -p 'test_*.py' -v` exits 1 because the required start directory is absent. Owner: Deployment adapter owner plus Hexalith Platform Operations. Consequence: a reachable but empty target namespace cannot identify or approve the reviewed Production-shaped profile, start C1 tooling RED, or capture capability/security/durability/capacity/reclamation evidence; Production writes and A41 mutation remain prohibited. Reopen trigger: deploy or identify the reviewed Production-shaped target, configure all six required values, identify the accountable operations/security reviewers, and provide the C1 verifier/tooling seam before rerunning the exact adapter-profile contract.
- 2026-07-19: The C1 infrastructure reopen trigger is now satisfied, but C1 itself is not certified. The reviewed target is live at context `jpiquot@local`, namespace `hexalith-memories`, deployment `memories-production-2.11.0-c7c2ca21-hexalith-keys-r2`, profile `redis-state-v1-dapr-1.18.1-openbao-2.6.0-4183b741eac062d9`, evidence root `_bmad-output/implementation-artifacts/tests`, and declared fault `dapr-sidecar-restart`; the six values are stored in the gitignored `27-3-production-target.env`. Accountable reviewers are Platform Operations owner Jérôme Piquot (`jpiquot`) and security reviewer Murat TEA for Jérôme. **Corrected 2026-07-26 by code review (chunk 3b), per the Administrator decision:** that pairing does not satisfy AC4, which requires an independent security reviewer — it names one human plus a persona acting for that same human. The security-reviewer assignment is withdrawn and replaced by the accepted blocker recorded below; gates C1.23-C1.25 stay `pending`/`not complete`. The application target is 2/2 Ready for both Server and MCP. Internal service `hexalith-keys` runs pinned OpenBao 2.6.0/chart 0.28.5 with TLS verification, single-node Raft, retained data/audit PVCs, static auto-unseal, 2-of-3 recovery, a revoked initial root token, separate read-only Dapr prefixes, no Kubernetes API token, and no external Service. A controlled OpenBao pod replacement returned initialized/unsealed/Ready with retained secrets; Dapr secret API reads and application readiness remained Healthy. C1 capability, atomicity, zero-loss, capacity, physical-reclamation, and verifier evidence are still pending, so lifecycle replicas remain disabled and no A41 mutation is permitted.

 - 2026-07-19: Began Task 1/C1 with the required RED phase. `PYTHONHASHSEED=0 python3 -m unittest discover -s tests/tooling/access_telemetry_lifecycle -p 'test_adapter_profile.py' -v` failed with `ModuleNotFoundError: No module named 'verify_access_telemetry_lifecycle'` before the verifier implementation existed. Added the standard-library C1 profile primitives and reran the same command successfully at 4/0/0; the exact full Python discovery `PYTHONHASHSEED=0 python3 -m unittest discover -s tests/tooling/access_telemetry_lifecycle -p 'test_*.py' -v` passed 4/0/0 with sorted-case SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.
 - 2026-07-19: HALT at C1 after the GREEN tooling tests. With the reviewed target environment sourced from `_bmad-output/implementation-artifacts/tests/27-3-production-target.env`, the exact command `python3 tools/verify-access-telemetry-lifecycle.py --checkpoint adapter-profile --kube-context "$KUBE_CONTEXT" --namespace "$KUBE_NAMESPACE" --deployment-id "$DEPLOYMENT_ID" --profile-id "$PROFILE_ID" --workload-profile adr-27.1-two-writer-500eps --steady-state-minutes 30 --purge-backlog-records 150000 --evidence "$EVIDENCE_ROOT/27-3-adapter-profile-evidence.md"` exited 1 with `lifecycle deployment is disabled; Production writes remain fail-closed`. Read-only Kubernetes queries observed `memories-access-telemetry` at `0/0`, the `state.redis` adapter, and Dapr sidecar image `ghcr.io/dapr/daprd:1.18.1`; no atomicity, zero-loss, capacity, security, or physical-reclamation approval was claimed. The redacted rejection packet is `_bmad-output/implementation-artifacts/tests/27-3-adapter-profile-evidence.md`. Owner: deployment adapter owner plus Hexalith Platform Operations. Consequence: C1 and every dependent checkpoint remain blocked; no lifecycle enablement, A41 mutation, sprint transition, or commit/publish operation was performed. Reopen trigger: obtain an approved exact-profile probe result, scale lifecycle writers only for the reviewed test, and rerun the full C1 contract; any failed Redis atomicity/zero-loss/capacity/reclamation gate keeps Production disabled and A41 open.
- 2026-07-19: HALT at C1 before new RED or implementation work after revalidating the current deployment. The six non-secret target identity values remain configured, but `kubectl --context "$KUBE_CONTEXT" --namespace "$KUBE_NAMESPACE" get deployment memories-access-telemetry memories-access-telemetry-clock -o custom-columns='NAME:.metadata.name,GEN:.metadata.generation,DESIRED:.spec.replicas,READY:.status.readyReplicas,AVAILABLE:.status.availableReplicas,IMAGE:.spec.template.spec.containers[0].image' --no-headers` reports both services at `0` desired replicas with placeholder `0.0.0` images; `kubectl --context "$KUBE_CONTEXT" --namespace "$KUBE_NAMESPACE" get component access-telemetry-store -o custom-columns='NAME:.metadata.name,GEN:.metadata.generation,TYPE:.spec.type,VERSION:.spec.version,SCOPES:.scopes' --no-headers` reports `state.redis/v1`; and `kubectl --context "$KUBE_CONTEXT" --namespace "$KUBE_NAMESPACE" get configmap memories-production-config -o jsonpath='{.data.ACCESS_TELEMETRY_ENABLED}{"\n"}{.data.ACCESS_TELEMETRY_COMPONENT_PROFILE_HASH}{"\n"}'` exits 1 with `NotFound`. All three governed test-project Release builds pass with 0 warnings/errors, the exact C1 and full Python discovery commands pass 4/0/0, and the exact adapter-profile checkpoint exits 1 with `lifecycle deployment is disabled; Production writes remain fail-closed`; the refreshed rejection packet remains `_bmad-output/implementation-artifacts/tests/27-3-adapter-profile-evidence.md`. Owner: deployment adapter owner plus Hexalith Platform Operations. Consequence: required Production configuration, runnable lifecycle images, and the atomicity/zero-loss/capacity/physical-reclamation evidence remain unavailable, so C1 and Tasks 2-8 cannot proceed; A41 and sprint/story status remain unchanged. Reopen trigger: provide runnable reviewed lifecycle/clock images and Production configuration, approve an exact adapter capable of every C1 gate, and provide its complete probe evidence before scaling the reviewed test deployment. The current Redis profile must be rejected and correct-course initiated if its no-partial-commit and zero-loss gates cannot be proved.
- 2026-07-20: HALT at Task 1/C1 before a new RED phase because the approved `PG-AZ-1` target has not been provisioned. `az postgres flexible-server list` and `az aks list` each exit 0 with `[]`; the subscription reports `65` unused `standardDADSv5Family` cores in `francecentral`, but no capacity is reserved by an exact-profile server. The reachable `jpiquot@local` namespace still has `memories-access-telemetry` and its clock at `0` replicas with `:0.0.0` images, and `access-telemetry-store` remains `state.redis/v1` with `queryIndexes`. The exact read-only adapter-profile command was rerun with profile ID `postgresql-v2-dapr-1.18.1-azure-pg17.10-zrha-d32ds-v5-4095gib-40kiops-v1` and the declared PostgreSQL primary/AZ-loss fault; it exited 1 with `lifecycle deployment is disabled; Production writes remain fail-closed` and refreshed the immutable non-approval rejection packet. The local Aspire baseline also exposed a pre-existing lifecycle error from `DaprAccessTelemetryStateStore.GetDueEntriesAsync`: it calls the unsupported Dapr Query API, while the approved PostgreSQL v2 profile requires explicit transactional expiry-bucket keys. Owner: deployment adapter owner plus Hexalith Platform Operations; lifecycle/clock CI image owners; separate Platform Operations and security approvers. Consequence: the exact backend/runtime identity, live digests, quota/cost reservation, behavioral/load/fault/reclamation probes, and hash-bound approvals cannot be produced or honestly certified; no product/deployment implementation, scaling, Task checkbox, A41, or status mutation is permitted. Reopen trigger: provision the exact private Azure PostgreSQL 17.10 zone-redundant target and reviewed execution cluster, publish signed lifecycle/clock image digests, replace the unsupported query path within the narrowed lifecycle contract, and provide separately accountable reviewers so the complete no-skip C1 RED/GREEN/probe sequence can run.
- 2026-07-20: Executed the Administrator-approved `PG-ONPREM-1` fail-closed installation on `jpiquot@local/hexalith-memories`. The first empty-cluster start exposed an official-entrypoint/custom-peer-auth mismatch; the manifest now uses an explicit `pg_ident` map, and the missing empty database/schema/roles were provisioned in place without deleting the retained PVC. Final verification observed PostgreSQL 18.4 at the pinned digest, `1/1` Ready, a Bound 400 GiB retained PVC, checksums and durability settings on, TLS 1.3 `verify-full`, plaintext rejection, a non-superuser runtime role, allowed/denied network paths, and a probe row surviving StatefulSet pod replacement. Keycloak resource identities and versions were unchanged. A diagnostic displayed base64-encoded Secret data; all newly created PostgreSQL passwords and TLS material were immediately rotated and reverified. The pre-existing OpenBao access token was not mutated and must be rotated before its PostgreSQL connection secret is seeded. The live Dapr state component therefore remains Redis and lifecycle/clock remain zero replicas. C1 stays rejected pending actual Dapr behavior, load/capacity/reclamation, backup/restore, immutable application images, and separate approvals; Task 1, Story 27.4, A41, and story/sprint status remain unchanged.
- 2026-07-20: HALT at Task 1/C1 before new RED or implementation work after revalidating the approved `PG-ONPREM-1` target. The non-secret environment handoff still names the superseded profile, so the exact read-only command was run with the approved profile ID and declared bounded pod/process-loss fault supplied as command-local overrides. Live discovery observes the pinned PostgreSQL 18.4 StatefulSet at `1/1` Ready, but `memories-access-telemetry` and `memories-access-telemetry-clock` remain at zero replicas with placeholder `:0.0.0` images, and `access-telemetry-store` remains `state.redis/v1`. The exact C1 Python inventory passed 4/0/0; the exact adapter-profile verifier exited 1 with `lifecycle deployment is disabled; Production writes remain fail-closed` and refreshed `_bmad-output/implementation-artifacts/tests/27-3-adapter-profile-evidence.md`. The local Aspire development preflight reached healthy resources and bounded lifecycle responses before the AppHost was stopped, but it does not supply Production identity or evidence. The release publisher still has no reviewed, signed, and scanned lifecycle/clock image outputs; the OpenBao PostgreSQL connection, Production configuration and clock authority, complete no-skip behavioral/load/capacity/fault/reclamation probes, backup/restore destination and evidence, and separate Platform Operations and security approvals remain absent. Owner: lifecycle/clock CI image owners, deployment adapter owner plus Hexalith Platform Operations, backup owner, and separate Platform Operations/security approvers. Consequence: Task 1/C1 cannot be certified; Production writes, Task 1, Story 27.4, A41, and story/sprint status remain unchanged. Reopen trigger: publish and deploy the reviewed lifecycle/clock images by signed digest, refresh the exact-profile environment handoff, seed the rotated TLS-verified OpenBao PostgreSQL connection and Production authorities, run every required C1 probe without skips including backup/restore, and obtain separate named hash-bound approvals.
- 2026-07-20: Resumed Task 1/C1 against the running Docker, Dapr 1.18.1, and `PG-ONPREM-1` target. Expanded release and partial-recovery tooling from a two-image to the required four-image unit, built exact lifecycle/clock OCI archives, generated SPDX SBOMs, passed credential-free pinned-image HIGH/CRITICAL scans, imported the images into the Kubernetes node, and temporarily activated the reviewed PostgreSQL v2 profile. Live qualification verified the exact Dapr runtime and PostgreSQL component capabilities (`ETAG`, `TRANSACTIONAL`, `TTL`, `KEYS_LIKE`, and `ACTOR`), strong save/read with ETag, stale-ETag rejection, transaction rollback without a partial write, per-record TTL expiry, and a Dapr-acknowledged record surviving replacement of the PostgreSQL pod with the retained PVC. A same-site disposable `pg_dump`/restore rehearsal proved restore mechanics only. The clock returned 503 because all three configured authorities are `.example.invalid`, so actor recovery, the 30-minute 500-events/second two-writer run, the 150,000-record purge/reclamation run, and representative capacity arithmetic could not be certified. Registry credentials and a signing context are absent, no named off-site backup target exists, and separate Platform Operations/security approvals remain absent. A diagnostic exposed encoded Secret values; every affected credential, token, password, PostgreSQL leaf certificate/key, and clock signing key was rotated, scoped OpenBao access and denials were reverified, the temporary break-glass root was revoked, and hardened configuration was restored. Qualification lifecycle/clock replicas were returned to zero, the main application remains `ACCESS_TELEMETRY_ENABLED=false`, and the exact verifier again rejects at `lifecycle deployment is disabled; Production writes remain fail-closed`. Owner: clock-authority owner, release/signing owner, backup owner, Platform Operations, and security reviewer. Consequence: Task 1/C1, Story 27.4, A41, and story/sprint status remain unchanged. Reopen trigger: provide three independent authenticated UTC endpoints/tokens, signed registry publication capability for all four images, a named off-site backup destination, approved capacity/operating-cost inputs, and separate reviewers; then run the entire no-skip C1 workload, purge/reclamation, recovery, restore, and approval sequence.

- 2026-07-26: Applied the two approved chunk-1 `[Review][Patch]` items and re-derived C1, which remains rejected. Live discovery at HEAD `e8a91b19163b50123b5f1809438c0a9b1e98f638` shows the target has advanced since the 2026-07-20 record: `access-telemetry-store` is now `state.postgresql/v2` (was `state.redis/v1`), and `memories-access-telemetry`/`memories-access-telemetry-clock` now carry digest-pinned `registry.hexalith.com` images (`sha256:b3790e08…`, `sha256:50413b71…`) instead of `:0.0.0` placeholders. PostgreSQL 18.4 is `1/1` Ready at pinned digest `sha256:3a82e1f5…`. C1 nevertheless stays fail-closed: both lifecycle deployments remain at `0` desired replicas, `memories-production-config-4fk2cbg78f` still reports `ACCESS_TELEMETRY_ENABLED=false`, and all three clock authorities are still `.example.invalid` placeholders (`Clock__Sources__0..2__Endpoint`). The exact C1 Python inventory passed `4/0/0` with sorted-case SHA-256 `e2e1c469aae8f51e1d96c9fb5644bff815f854ca93eb17568cec3e201cbf6dae`, matching the recorded canonical value. The exact adapter-profile verifier — run with the approved profile ID `postgresql-v2-dapr-1.18.1-postgresql-18.4-onprem-k8s1-openebs-local-retain-400g-v1` and declared fault `postgresql-pod-process-loss-retained-local-volume` as command-local overrides, because the gitignored environment handoff still names the superseded Redis profile — exited `1` with `lifecycle deployment is disabled; Production writes remain fail-closed` and refreshed the immutable non-approval packet (`profile_sha256 01f6ce9f…`, `evidence_is_approval: false`). Owner: clock-authority owner, release/signing owner, backup owner, Hexalith Platform Operations, and the independent security reviewer. Consequence: the clock-dependent C1 gates — actor reactivation and Placement/Scheduler/reminder recovery, the 30-minute two-writer 500 events/s run, the 10-minute 150,000-record purge catch-up, and measured-byte capacity arithmetic — cannot be executed or honestly certified, so Task 1 stays unchecked, Production writes stay disabled, Story 27.4 stays `backlog`, and A41 stays open. Reopen trigger: unchanged from the 2026-07-20 record — three independent authenticated UTC endpoints and tokens, signed registry publication for all four images, a named off-site backup destination, approved capacity and operating-cost inputs, and separately named Platform Operations and security reviewers.
- 2026-07-26: SECURITY — credential exposure during discovery, rotation required. A read-only diagnostic (`kubectl get secret access-telemetry-clock-sources -o go-template=…base64decode`) printed the decoded values of `source-a-token`, `source-b-token`, and `source-c-token` into the assistant session transcript. This is the same diagnostic-exposure class recorded on 2026-07-20. No further secret read was performed and no value is reproduced in this story file or in any evidence packet. Owner: clock-authority owner plus the security reviewer. Consequence: the three clock-source tokens must be treated as disclosed. Mitigating context, not a substitute for rotation: all three endpoints are still `.example.invalid` placeholders, so the tokens authenticate against no real UTC authority today. Reopen trigger: rotate the three tokens in OpenBao and reverify scoped Dapr secret reads before any real clock authority is configured under `Clock__Sources__*__Endpoint`; C1 must not be certified against unrotated material.
- 2026-07-26: Live-versus-repository component drift observed, not corrected. The refreshed evidence packet shows the running `access-telemetry-store` component still carrying `sslRootCert` in its metadata, while `deploy/kubernetes/base/dapr/access-telemetry-store.yaml` no longer declares it (the chunk-2 review patch was applied to the manifest and is present in the repository). The cluster has therefore not been re-applied since that patch. No `kubectl apply` was performed: re-applying is a live deployment mutation outside this session's authority and is not required while the profile is fail-closed. Owner: deployment adapter owner. Reopen trigger: re-apply the reviewed base kustomization and re-capture the component identity as part of the C1 evidence run, so the certified profile hash reflects the reviewed manifests.

- 2026-07-26 (second dev-story invocation): HALT at Task 1/C1 after closing two C1 preconditions in the repository. Live re-derivation at HEAD `358bef352bf657bcd2a7c4e91cb0a6d298c3f7a4` (the earlier session's work is now committed and the worktree was clean at start) shows the target byte-identical to the morning capture: `access-telemetry-store` is `state.postgresql/v2`, PostgreSQL 18.4 is `1/1` Ready at pinned digest `sha256:3a82e1f5…`, both lifecycle deployments carry digest-pinned `registry.hexalith.com` images, and all five external C1 inputs are still absent — `memories-access-telemetry` and `memories-access-telemetry-clock` remain at `0` desired replicas, `memories-production-config-4fk2cbg78f` still reports `ACCESS_TELEMETRY_ENABLED=false`, and `Clock__Sources__0..2__Endpoint` are still `https://utc-source-{a,b,c}.example.invalid/v1/time` with empty `AuthenticationToken` values. The exact C1 Python inventory passed `4/0/0`. The exact adapter-profile verifier, run with the approved profile ID and declared fault as command-local overrides (the gitignored handoff still names the superseded Redis profile), exited `1` with `lifecycle deployment is disabled; Production writes remain fail-closed`; the refreshed packet differs from the morning capture only in `captured_utc`, which is independent confirmation that no external input changed. Owner: clock-authority owner, release/signing owner, backup owner, Hexalith Platform Operations, and the independent security reviewer. Consequence: the clock-dependent C1 gates — actor reactivation and Placement/Scheduler/reminder recovery, the 30-minute two-writer 500 events/s run, the 10-minute 150,000-record purge catch-up, and measured-byte capacity arithmetic — remain unexecutable, so Task 1 stays unchecked, Production writes stay disabled, Story 27.4 stays `backlog`, and A41 stays open. Reopen trigger: unchanged — three independent authenticated UTC endpoints and tokens, signed registry publication for all four images, a named off-site backup destination, approved capacity and operating-cost inputs, and separately named Platform Operations and security reviewers.
- 2026-07-26 (second dev-story invocation): The clock cannot be unblocked from inside the repository, and the fail-closed design is why. `src/Hexalith.Memories.AccessTelemetry.Clock/Program.cs:64` registers `DevelopmentAuthenticatedUtcSource` only when `!builder.Environment.IsProduction() && Clock:AllowDevelopmentSources`, so the three placeholder authorities cannot be substituted in the Production-shaped target without turning the trusted-time authority into a mock. The **Production-Shaped Execution Contract** fails C1 on a mocked control plane, so exercising the load/purge/reclamation gates against development clock sources would produce inadmissible evidence rather than progress. No such substitution was made.

- 2026-07-26 (second dev-story invocation): NEW FAIL-CLOSED BLOCKER FOR `done`, found while revalidating this story's own governance gates; pre-existing and not caused by this session. The embedded verifier in `_bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md` exits `1` with `missing governed artifact: _bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md`. Its executable constants at lines 69-76 (`KEY`, `STORY`, `MATRIX`) still name the pre-split story key, story path, and evidence matrix, but commit `f474db15` renamed the story to `27-3-production-adapter-and-deployment-profile.md` (and the matrix to `27-3-adapter-profile-evidence.md`) under the approved 2026-07-20 course correction. The verifier has therefore been inert since the split, so the story/sprint status-parity, monotonic-transition, unique-YAML-key, and baseline-relative File List assertions that earlier review findings added are not actually running. The recorded creation diff at lines 16-34 is append-only history and must not be rewritten; only the constants are stale. That file is clean in this session's working tree and was neither read into scope nor edited here. Owner: `create-story`/`code-review` phase owner for Story 27.3 — this is a create-story-phase governance artifact, and the story phase ledger does not authorize `dev-story` to rewrite another phase's evidence verifier, so ownership is routed rather than assumed. Consequence: `done` must not be granted while a required scope gate cannot execute; this does not affect any evidence recorded in this phase, which was verified by direct commands. Reopen trigger: repoint the three constants to the post-split key/path/matrix, re-run the embedded command to a `0` exit, and record the result in the owning phase's row.

- 2026-07-26 (third dev-story invocation): HALT at Task 1/C1 after applying every open chunk-3a review patch. Live re-derivation at HEAD `a6753c1152ba6a4688210e453cc567f3faca8720` (clean tree at start) shows the target byte-identical to the previous two captures on every C1-relevant axis: `access-telemetry-store` is `state.postgresql/v2`, PostgreSQL 18.4 is `1/1` Ready at pinned digest `sha256:3a82e1f5…`, both lifecycle deployments carry digest-pinned `registry.hexalith.com` images (`sha256:b3790e08…`, `sha256:50413b71…`), and all five external C1 inputs are still absent — `memories-access-telemetry` and `memories-access-telemetry-clock` remain at `0` desired replicas, `memories-production-config-4fk2cbg78f` still reports `ACCESS_TELEMETRY_ENABLED=false`, and `Clock__Sources__0..2__Endpoint` are still `https://utc-source-{a,b,c}.example.invalid/v1/time`. The exact C1 Python inventory passed `13/0/0`. The exact adapter-profile verifier, run with the approved profile ID and declared fault as command-local overrides (the gitignored handoff still names the superseded Redis profile), exited `1` with `lifecycle deployment is disabled; Production writes remain fail-closed`. Owner: clock-authority owner, release/signing owner, backup owner, Hexalith Platform Operations, and the independent security reviewer. Consequence: the clock-dependent C1 gates — actor reactivation and Placement/Scheduler/reminder recovery, the 30-minute two-writer 500 events/s run, the 10-minute 150,000-record purge catch-up, and measured-byte capacity arithmetic — remain unexecutable, so Task 1 stays unchecked, Production writes stay disabled, Story 27.4 stays `backlog`, and A41 stays open. Reopen trigger: unchanged — three independent authenticated UTC endpoints and tokens, signed registry publication for all four images, a named off-site backup destination, approved capacity and operating-cost inputs, and separately named Platform Operations and security reviewers.
- 2026-07-26 (third dev-story invocation): the refreshed evidence packet now states its own blockers rather than omitting them, which is a reporting improvement, not C1 progress. `Capacity (gate C1.13)` records `status: blocked` and names all nine missing measured operands; the seven AC3/AC4 attestation fields all read `unrecorded`; `runtime_matches_reviewed_profile` is `false` (the runtime-observed profile is not the reviewed canonical profile, and the packet now says which object its hash covers); and `daprd_version` records `not captured; kubectl exited 1` for probe pod `memories-b667844cf-6s9j7` instead of silently omitting AC1's runtime identity. `unchanged_during_run` is `true`: the pre-run and post-run workload identities are byte-identical, so nothing in the target was mutated by this invocation.
- 2026-07-26 (third dev-story invocation): repaired two pre-existing red/broken governance surfaces found while running this story's own gates, both caused by the immediately preceding commit `a6753c11` and neither by this session's work. (1) `Hexalith.Memories.Cli.Tests` was red at HEAD with 2 failures: `deferred-work.md` had gained closure statuses the register's own parser rejects (`Status: superseded …`, and `Status: resolved 2026-07-26 — …` with a dated rationale suffix). The parser now takes the leading token as the status — so the register's real closure style is accepted while an invented status such as `superseded` still fails — and the one `superseded` entry is corrected to `resolved`. (2) Nine structured register entries were missing required fields (`Evidence:` on four `resolved` entries, `Rationale:` on five `open` entries, `Target artifact:` on two); all are now complete. The assembly is 487/487.
- 2026-07-26 (third dev-story invocation): three tooling suites were executed by no workflow, not one. Closing the `access_telemetry_lifecycle` gap (chunk-3a finding) prompted a sweep of `tests/tooling/*` against every workflow, which found `story_scope` (42 cases, guarding `check-story-file-scope.py`) and `integration_stub_closure` (7 cases) equally unwired; `release_preflight` is wired, but from `release.yml` rather than `ci.yml`. All three gaps are now closed and a new `CiWorkflow_RunsEveryToolingFixtureSuiteThatGuardsAShippedTool` guard fails whenever any `tests/tooling/*` suite is executed by no workflow. Disclosed as a deliberate in-scope widening beyond the finding as written: narrowing the guard to the one named suite would have left the same defect class live in two other lanes.

- 2026-07-26 (third dev-story invocation): one self-inflicted regression, caught and corrected before handoff. The first form of the `render-production-deployment.ps1` image-identity guard pinned the literal `registry.hexalith.com/memories*` repositories, which is correct against the finding but breaks the registry and repository-prefix parameterization `tools/publish-containers.ps1` depends on; 20 of the 38 `tests/tooling/publish_containers` cases went red. Confirmed as mine by restoring the HEAD renderer and re-running one case (green), then rebound the guard to the repository *suffix* relative to the Server image's repository. Both lanes are green (`publish_containers` 38/38, `production_deployment_evidence` 27/27) and the transposition/duplication mutation still fails the guard, so the finding stays discharged.

#### Accepted blocker - AC4 independent security approver (recorded 2026-07-26, code review chunk 3b)

- **Blocker:** AC4 requires Hexalith Platform Operations approval and a *separate* security reviewer approval of identity, secrets, TLS, network, authorization, encryption, privacy and evidence integrity. No independent security approver is assigned. The only assignment on record named the Platform Operations owner plus a persona acting for that same human, which does not satisfy the separation requirement and has been withdrawn.
- **Owner:** Product Owner, to nominate an independent security approver; Hexalith Platform Operations remains the AC4 operations approver.
- **Consequence:** C1 gates C1.23 (Platform Operations approval), C1.24 (non-HA acknowledgement) and C1.25 (security reviewer approval) cannot leave `pending`/`not complete`. C1 therefore cannot pass, Production lifecycle writes stay disabled, Story 27.3 stays `in-progress`, Story 27.4 stays `backlog`, and A41 stays open. This blocker is independent of, and additional to, the five external C1 inputs and the `.example.invalid` clock authorities.
- **Reopen trigger:** a named human other than the Platform Operations owner is recorded as the accountable security reviewer for Story 27.3, and records their AC4 approval against the immutable evidence packet.
- **Not a substitute:** no persona, agent, workflow, or self-review may be recorded as the independent security approver.

### C0 Completion Handoff

- 2026-07-19: Story 27.2 is `done`; its C1-C6 rows are reviewed/complete, its final `dev-story` and `code-review` rows reconcile Server.Tests 2,181, IntegrationTests 279, and AccessTelemetry.Tests 39 methods, and its cumulative File List is matched 155/155. The handoff inventory was reread from the final File List: 155 readable files comprising 118 source, 12 test, 19 deployment, 2 documentation, and 4 governance/build artifacts. The sorted `sha256sum` manifest hash is `282c3c02005a9a319ccbf41803d26245457c0a7890271d61bcb2be4180dbf5ff`; no predecessor dashboard path exists, so dashboard/alert work remains entirely in Story 27.3.
- 2026-07-19: Fresh C0 command `DOTNET_CLI_USE_MSBUILD_SERVER=0 dotnet build tests/Hexalith.Memories.IntegrationTests/Hexalith.Memories.IntegrationTests.csproj --configuration Release --disable-build-servers -m:1 /nr:false -p:NuGetAudit=false -p:MinVerVersionOverride=1.0.0` passed with 0 warnings/errors at HEAD `c7c2ca21b9442962be92353a6677b5d083892462`. The fresh assembly SHA-256 was `e032bd6a356ee8cae42776ceb6635d0d44c245b4f42c9dc2a07872b10c210fad`; exact selected discovery found eight unique methods, and `DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.IntegrationTests/bin/Release/net10.0/Hexalith.Memories.IntegrationTests.dll -class Hexalith.Memories.IntegrationTests.Telemetry.AccessTelemetryLifecycleIntegrationCheckpointTests -parallel none -noLogo` passed 8/0/0 with zero skipped/not-run. Re-hashing after execution returned the same assembly SHA-256; the selected source blob was `0f826567e67fc6dcd948a96f5f21d3deadeb7a45`. C0 is complete; C1 is the next fail-closed gate.

### Code Review Evidence

- 2026-07-18: Review scope was one complete, non-chunked Story 27.3 group: the three creation File List paths against baseline `272c33bc5d30d71ac46f20e703b9d5456e75a093` (401 diff lines at review start). The user confirmed the group; all six active layers completed with non-empty results and zero failed layers. Triage retained 14 patches and one pre-existing defer, dismissed 14 findings, and the user selected all patches. The workflow-required deferred-work entry is the only added review path; no in-scope review chunk remains.
- 2026-07-18: Fresh Release builds passed with 0 warnings/errors for `Hexalith.Memories.Server.Tests.csproj`, `Hexalith.Memories.IntegrationTests.csproj`, and `Hexalith.Memories.AccessTelemetry.Tests.csproj`, using `DOTNET_CLI_USE_MSBUILD_SERVER=0 dotnet build <project> --configuration Release --disable-build-servers -m:1 /nr:false -p:NuGetAudit=false -p:MinVerVersionOverride=1.0.0`. The repository advanced concurrently to `bc2e4a135f86197c03ebc13c039a765d555bb16f`; all three builds, discoveries/hashes, and both focused selections were rerun against that HEAD with unchanged results.
- 2026-07-18: Current-source reuse proof passed 13/0/0 with `DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.Server.Tests/bin/Release/net10.0/Hexalith.Memories.Server.Tests.dll -class Hexalith.Memories.Server.Tests.Deployment.OperationalRunbookSetTests -class Hexalith.Memories.Server.Tests.Telemetry.MemoriesDashboardTests -parallel none -noLogo`. The C0 portable class now passes 8/0/0 with its canonical IntegrationTests selector, but C0 remains blocked because Story 27.2 is `review`, not `done`, and has no completed code-review handoff.
- 2026-07-18: The exact embedded Python command in `_bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md` exited 0, proving the reconstructed pre-create hash, post-create hash, immutable scoped creation 3/3 set, unique YAML keys, governed artifact existence, and current story/sprint parity.
- 2026-07-18: Post-patch discovery used the following exact comparable method commands; the second command in each lane hashes the same filtered/sorted method set.

  ```bash
  DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.Server.Tests/bin/Release/net10.0/Hexalith.Memories.Server.Tests.dll -list methods -noLogo | awk '/^Hexalith\.Memories\.Server\.Tests\./ { total++; if ($0 ~ /\.Architecture\./) architecture++; if ($0 ~ /\.Telemetry\./) telemetry++; if ($0 ~ /\.Hosting\./) hosting++ } END { print total, architecture, telemetry, hosting }'
  DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.Server.Tests/bin/Release/net10.0/Hexalith.Memories.Server.Tests.dll -list methods -noLogo | sed -n '/^Hexalith\.Memories\.Server\.Tests\./p' | LC_ALL=C sort | sha256sum
  DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.IntegrationTests/bin/Release/net10.0/Hexalith.Memories.IntegrationTests.dll -list methods -noLogo | awk '/^Hexalith\.Memories\.IntegrationTests\./ { total++; if ($0 ~ /\.Telemetry\./) telemetry++ } END { print total, telemetry }'
  DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.IntegrationTests/bin/Release/net10.0/Hexalith.Memories.IntegrationTests.dll -list methods -noLogo | sed -n '/^Hexalith\.Memories\.IntegrationTests\./p' | LC_ALL=C sort | sha256sum
  DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.AccessTelemetry.Tests/bin/Release/net10.0/Hexalith.Memories.AccessTelemetry.Tests.dll -list methods -noLogo | awk '/^Hexalith\.Memories\.AccessTelemetry\.Tests\./ { total++ } END { print total }'
  DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.AccessTelemetry.Tests/bin/Release/net10.0/Hexalith.Memories.AccessTelemetry.Tests.dll -list methods -noLogo | sed -n '/^Hexalith\.Memories\.AccessTelemetry\.Tests\./p' | LC_ALL=C sort | sha256sum
  ```

  Results: Server.Tests 2,170 methods (Architecture 27, Telemetry 150, Hosting 17), hash `1449199044ae36604668d364a656ede748911d90b7cc95e3e68830a5dcb55d12`; IntegrationTests 278 methods (Telemetry 17), hash `92f4cfeb683a0bb2cd90305e92f61f00eb53542e2aac231f3c61b7a9dcf96d55`; AccessTelemetry.Tests 31 methods, hash `4eea34c669021dc01446ddf0d44111c6aa38e482e92736ea155dcf84f5498460`.
- 2026-07-18: External same-lane mapping is explicit. Story 27.2 owns Server `+12`, Integration `+8`, and AccessTelemetry `0 -> 31` per its `dev-story` row. During this review, commit `1242e77479bfbf312c823a03e07508801a6f4bbf` independently added `ProductionOverlay_AccessTelemetryDeploymentsAreScaledToZero`, another Server `+1`; its owner is the CI/CD/production-rollout hardening lane. Therefore Server `2,157 + Story 27.3 0 + Story 27.2 12 + release lane 1 = 2,170`, Integration `270 + 0 + 8 = 278`, and AccessTelemetry `0 + 0 + 31 = 31`.
- 2026-07-18: `git diff --check 272c33bc5d30d71ac46f20e703b9d5456e75a093 -- <the four File List paths>` exited 0. `git diff --name-status` with the same baseline and paths returned exactly `A` story, `M` deferred-work, `M` sprint-status, and `A` create-scope evidence: matched 4/4. Story 27.2 same-file sprint changes, commit `1242e774` release-hardening paths, and root-declared submodule advances in `94958c22`/`bc2e4a13` remain externally owned and excluded. Concurrent commit `bc2e4a13` captured intermediate versions of the three review artifacts beside those external gitlinks; the remaining status/checkbox/evidence reconciliation stays in the Story 27.3 working diff and no path ownership was broadened.
- 2026-07-19: This invocation reviewed one complete, non-chunked four-file Story 27.3 scope. The user confirmed the A/M/M/A group; all six active layers returned non-empty results with zero failed layers. Triage retained 22 unambiguous patches (17 high, 5 medium), dismissed one request to rewrite an append-only historical review row, and retained no decision-needed or new defer item. The review-start diff was 611 lines with SHA-256 `6bb3598313b86af0987a698fd5be836fe885ba93a681f50042d21b64c9b0db89`; this final manifest covers every later patch and status-reconciliation line.
- 2026-07-19: Fresh C0 Release build passed with 0 warnings/errors. The selected source blob was `0f826567e67fc6dcd948a96f5f21d3deadeb7a45`; the IntegrationTests assembly SHA-256 was `e032bd6a356ee8cae42776ceb6635d0d44c245b4f42c9dc2a07872b10c210fad` both before and after the exact 8/0/0 C0 execution. The Story 27.2 155-path inventory manifest SHA-256 is `282c3c02005a9a319ccbf41803d26245457c0a7890271d61bcb2be4180dbf5ff`.
- 2026-07-19: Post-patch discovery reran the exact three `DiffEngine_Disabled=true dotnet exec <assembly> -list methods -noLogo` commands with the filtered/sorted method pipelines recorded above. Results remained Server 2,181 (Architecture 27, Telemetry 160, Hosting 17), Integration 279 (Telemetry 18), and AccessTelemetry 39; method-set hashes remained `62c935a6f13179d4f3e84912565279faffc0b84c4d223446a02be5c614659cd0`, `8af20c16b0d0a9f4b947357e232fb864f2bccba63aa33ccff3a81676979b9468`, and `0e79e40274ddf051f891e5bdfae2f25d1cb4498fd82d865b78216081a2b99a14`. The embedded verifier passed under both normal Python and `PYTHONOPTIMIZE=1`; `git diff --check` passed.
- 2026-07-19: Final File List reconciliation is A/M/M/A matched 4/4 against baseline `272c33bc5d30d71ac46f20e703b9d5456e75a093`. `deferred-work.md` blob `07d12e05b47a662f31704c5c5e07fb889ef6a2f4` includes the Story 27.3-owned entry at lines 2241-2245; label changes at lines 87/89 and entries at 2247-2263 are externally owned by commit `042dcba6`/`spec-run-tests-and-fix-failures`. Sprint blob `70e9f44d4519ec643f9869d3f2a874ed416b5e7a` includes Story 27.2's external `done` transition and Story 27.3's truthful `in-progress`/2026-07-19 synchronization. Scope-evidence blob is `93f4335842748c3430d0cf175414c8c747a9cc31`.
- 2026-07-19: Immutable final review manifest algorithm: run the exact baseline/four-path `git diff --no-ext-diff --unified=5`; replace every `index OLD..NEW [MODE]` header with `index <canonical>..<canonical> [MODE]`; replace the hexadecimal value following `review-manifest-sha256:` with `<canonical>`; then SHA-256 the resulting bytes. `post-patch-diff-lines: 762`; `review-manifest-sha256: 92985d717f5d7abb7521ade3941b5487ca1973be2cd2197e2e1173295938a39f`.

- 2026-07-21: Code review chunk 1 of 3 (adapter code + tests) recorded these append-only ledger corrections, to be consolidated into the final-chunk `code-review` row; this intermediate chunk does not finalize the ledger or advance status.
  - [P3] The 2026-07-19 `code-review` row's AccessTelemetry method-set hash was recorded as a malformed 66-character token `0e79e40274…a2b99b9468`; the canonical 64-character value is `0e79e40274ddf051f891e5bdfae2f25d1cb4498fd82d865b78216081a2b99a14` (as used in the 2026-07-19 `dev-story` and later rows). Append-only correction; the historical row is not rewritten.
  - [P2] The 2026-07-20 `dev-story` row's `61/61` and `2,742/2,743` figures are xUnit test-case execution counts (theory expansions), not the method-discovery lane; the comparable method-discovery snapshot for that state is Server.Tests 2,188 / IntegrationTests 297 / AccessTelemetry.Tests 43 methods. Named unit = test cases for the 61 / 2,742 figures, test methods for the 43 / 2,188 figures.
  - [P4] `Hexalith.Memories.Cli.Tests` (`CiTestInventoryTests`) and the Python `tests/tooling/publish_containers/*` suite have no create-story baseline (the baseline captured only Server.Tests and IntegrationTests); both are named `0 → N` external lanes with phase delta +0 this chunk (rename/strengthening only), so no cumulative story delta is computable against the create baseline for those lanes.


- 2026-07-21: Code review chunk 2 of 3 (deployment manifests + docs) recorded these append-only ledger corrections, to be consolidated into the final-chunk `code-review` row; this intermediate chunk does not finalize the ledger or advance status.
  - [P5] The malformed 66-character AccessTelemetry method-set hash canonicalization is re-pointed to its actual contaminated row: the 2026-07-19 `dev-story` RED/GREEN row, not the 2026-07-19 `code-review` row (which already carries the correct 64-character `...216081a2b99a14`). Canonical value `0e79e40274ddf051f891e5bdfae2f25d1cb4498fd82d865b78216081a2b99a14`. Append-only; no historical row is rewritten.
  - [P6] `tools/validate-production-deployment-evidence.ps1` (27.3-owned production-deployment-evidence tooling, commit `f474db15`) is added to the File List above and will be reconciled in the chunk-3 final row.
  - [P7] Named File-List exclusions for access-telemetry manifests referenced by 27.3's `kustomization.yaml` but excluded from the 47-path File List: `deploy/kubernetes/base/dapr/access-telemetry-clock-config.yaml` and `deploy/dapr/components/*` (owner `c7c2ca21`, Story 27.2); `access-telemetry-config-store.yaml`, `server-deployment.yaml`, `services.yaml` (owner `93af830c`, Story 27.2); `overlays/production/access-telemetry-disabled-patch.yaml` and `.github/workflows/nightly.yml` (owner `1242e774`, CI rollout lane). To be recorded in the chunk-3 final reconciliation.

- 2026-07-26: Code review chunk 3a of 3 (never-reviewed tooling/CI plus the post-2026-07-21 review-patch delta). This intermediate chunk emits findings only; it does not finalize the phase ledger, append the final `code-review` row, or advance status, because chunk 3b is unreviewed.
  - Scope and immutable manifest: 13 paths / 2,219 diff lines, composed as `git diff --no-ext-diff 272c33bc5d30d71ac46f20e703b9d5456e75a093 159d7216 --` over the ten never-reviewed code/tooling paths, plus `git diff --no-ext-diff e8a91b19 159d7216 --` over the three paths that chunks 1-2 reviewed on 2026-07-21 and that changed again on 2026-07-26 (`deploy/kubernetes/base/dapr/access-telemetry-store.yaml`, `tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/DaprAccessTelemetryStateStoreTests.cs`, `tests/Hexalith.Memories.Server.Tests/Deployment/ProductionDeploymentArtifactsTests.cs`). Review-diff SHA-256 `1ee36f77ef4446741e545663c6e386122ce4c93653f7a4448a258cdc9594aac3`; HEAD `159d7216c66d5795b86aa8bae27ff200752db602`. The working tree was clean when the manifest was bound and when live discovery ran, and the reviewed manifest is commit-to-commit, so it is unaffected by later working-tree activity. Named concurrent exclusion: after manifest binding, an independent `correct-course` session introduced uncommitted Story 30.2 / CI-CD alignment edits to `_bmad-output/planning-artifacts/epics.md` (new Epic 30 framing and Story 30.2), `_bmad-output/planning-artifacts/architecture.md` (Decision D17 rewritten to a Hexalith.Builds reusable CI core), and `_bmad-output/implementation-artifacts/sprint-status.yaml` (Story 30.2 registration and epic-30 execution reordering), plus the untracked `_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-26-memories-ci-cd-alignment.md`. Owner: that course-correction workflow. Those paths were neither reviewed, edited, nor relied upon by this chunk, and no Story 27.3 status or sprint row was synchronized here.
  - Chunk partition verified complete and disjoint against the post-split File List: 13 (chunk 3a) + 8 governance/planning record paths (chunk 3b, unreviewed) + 15 paths whose last commit predates the 2026-07-21 chunk-1/2 reviews = 36 declared paths. `git diff --name-status 272c33bc5d30d71ac46f20e703b9d5456e75a093 159d7216 -- <the 36 declared paths>` returns exactly 36 rows (21 `A`, 15 `M`), with no declared path unchanged and none untracked.
  - Six review layers ran with zero failed layers and no empty result: blind adversarial, edge-case, verification-gap, acceptance auditor, historical-slice guard, and story-phase-ledger auditor. Triage retained 6 `decision_needed` (all resolved by the Administrator this session and routed to patch), 38 `patch`, and 3 `defer`, and dismissed one finding (a claimed `Shard >= 64` ordering divergence: `AccessTelemetryExpiryIndex.GetShard` returns `hash[0] & 63`, so values outside `0..63` are unreachable through the production path).
  - Review-patch phase delta `+0` in every comparable lane: the Administrator elected to leave all 44 patch findings as story action items, so no product, tooling, CI, ADR, or test source changed in this review. The only writes are this evidence note, the `### Review Findings` entries, the recorded decisions, and the three `deferred-work.md` entries 27.3-CR7/CR8/CR9.
  - Independent live discovery was executed successfully in this sandbox at HEAD `159d7216`, which discharges the blocked-evidence gate that DW 27.3-CR2 and the 2026-07-26 rows recorded as not runnable. Named unit is xUnit test methods: Server.Tests `2,190` / `bd27c3da547f6efacc2fc9ce9abd2360794c77e52e4a5fd7c6a4a5e73a28b4d0` (Architecture 28, Telemetry 160, Hosting 17), IntegrationTests `297` / `7836151bdf59ff8712f59911ed138a2f7afc792a7c4d2415c64122695c163856` (Telemetry 18), AccessTelemetry.Tests `55` / `973244b8ebcdfd55eeaf01ba56b8f33a1836aee158a98906817b5a5b2e3e60ef`. Python `tests/tooling/access_telemetry_lifecycle` reports `4/0/0` matching the required inventory exactly. Every recorded total and method-set hash in the 2026-07-26 rows agrees with live discovery.
  - Counts agree but attribution does not, and one lane is unledgered. The Server story/external split is restated by live per-class recount: `AccessTelemetryRetentionDecisionTests` 6 -> 10 and `ProductionDeploymentArtifactsTests` 5 -> 9 give 8 added methods in 27.3-declared Server files, less the one already attributed to external commit `1242e774`, leaving 7; per this session's decision the OpenBao profile method follows Story 31.1 with the artifact it guards, so the authoritative equation is `2,157 create + 6 Story 27.3 + 1 Story 31.1 + 26 external = 2,190`, replacing the recorded `2,157 + 3 + 30`. Separately, `tests/tooling/production_deployment_evidence/production_deployment_evidence_test.py` is the fixture suite for the already-declared `tools/validate-production-deployment-evidence.ps1`, is CI-enforced at `.github/workflows/ci.yml:233`, and moved `14 -> 17` Python unittest cases (`+3`) with no delta recorded in any lane. Both repairs are retained as patch action items, not applied here.
  - Fail-closed status: chunk 3b unreviewed, 44 patch action items open, and the post-split `matched 36/36` (37 once the fixture suite is added) still unrecorded in any phase row because the 2026-07-26 split ran under `correct-course`, which is not a canonical phase and appends no row. Story 27.3 remains `in-progress`; Task 1 remains unchecked; C1 remains rejected; A41 remains open. No Production enablement, deployment mutation, approval claim, Story 27.4 artifact, or A41 mutation occurred.

#### Code review 2026-07-26 - chunk 3b of 3 (final chunk)

- Scope and immutable manifest: the 8 governance/planning record paths, `git diff --no-ext-diff 272c33bc5d30d71ac46f20e703b9d5456e75a093 c9dfb06f --` over them, 2,264 diff lines, manifest SHA-256 `605152e597357936680f5f171d9a87e09dfcb7887e21ecd953bfab6d550d6344`. HEAD `c9dfb06f`; working tree clean at manifest binding.
- **Correction to the chunk-3a scope declaration (P2).** Chunk 3a recorded chunk 3b as "8 governance/planning record paths, 2,076 diff lines". That figure was bound to HEAD `159d7216`. Commit `c9dfb06f` subsequently added 188 lines across five of the eight paths, so the reviewed chunk is 2,264 lines at HEAD `c9dfb06f`. The `13 + 8 + 15 = 36` partition claim was likewise bound to `159d7216`; it remains path-complete at HEAD.
- **Correction to the chunk-3a triage totals (P3).** Chunk 3a recorded "6 `decision_needed` ... 38 `patch`" and "all 44 patch findings", and its fail-closed defer referred to "the six `[Review][Decision]` items". `grep -c` at HEAD returns 7 open `[Review][Decision]` rows and 38 open `[Review][Patch]` rows, i.e. 45 open items, not 44. The uncounted seventh decision is the Story 30.2 lane-ownership contest, which is itself a fail-closed `done` blocker and was therefore missing from the story's own blocker list.
- Six review layers ran with zero failed layers and no empty result: blind adversarial, edge-case, verification-gap, acceptance auditor, historical-slice guard, and story-phase-ledger auditor. Triage retained 7 `decision_needed` (all resolved by the Administrator this session), 34 `patch`, and 4 `defer`, and dismissed 2 findings after verification: (a) a claimed "unowned CLI secret placeholder" at `epics.md` - the text in fact states that application and provider secrets are not CLI configuration and remain behind the Server's DAPR secret-store boundary, which is an ownership statement, not a gap; (b) a claimed missing `story_execution_order` entry for `epic-27` - no tool, test or workflow parses that key, and the 27.3 -> 27.4 gate is carried by the `epics.md` Predecessor Gate, AC5, and the `backlog` sprint row.
- One finding was downgraded rather than accepted as reported. Three layers framed the Epic 21 `21.10-A4` action closure as "closed on evidence that never executed". Independent check: the five `EmbeddingVectorMigrationRedisIntegrationTests` facts *did* execute and pass in run 29798593273; what never ran is the `verify-integration-fast-coverage.py` step, which is `success()`-gated on a lane that was red. Recorded as `DW 27.3-CR11`, not as an over-close.
- **All 34 chunk-3b patch findings were applied this session.** The 7 decisions were resolved by the Administrator and routed: 4 to patch (applied), 3 to `correct-course` actions recorded as `DW 27.3-CR14`, `DW 27.3-CR15` and `DW 27.3-CR16` and left open as `[Review][Action]` rows.
- Executed evidence for the applied patches:
  - **Create-story scope verifier repaired and proven.** It exited `1` at session start (`missing governed artifact: ...27-3-retention-verification-operations-runbook-and-a41-close-out.md`) and now exits `0`. The repair separated creation-time identity (`CREATION_KEY`/`CREATION_STORY`, used only to reconstruct the creation commit) from current identity (`KEY`/`STORY`/`MATRIX`), which is what made the post-split repoint possible; `DW 27.3-CR7`'s recorded "repoint the three constants" fix could not have reached `0` on its own. Mutation-proven: a story/sprint status mismatch failed with `story/sprint status mismatch: in-progress != review`, and removing the C0 row failed with `checkpoints must be unique, ordered and contiguous from C0: [1]`. The inverted A41 preservation assertion sits behind the "every task and subtask checked" gate and is therefore not reachable while Task 1 is unchecked; it was proven in isolation instead (current residual `- Status: carried-forward` passes; an injected `- Status: resolved` fails).
  - **C1 Python inventory 4 -> 6 cases**, both additions mutation-proven with `tools/__pycache__` cleared between runs: making 80% admissible, changing `maxConns` in the canonical profile, accepting a zero requirement, and admitting the 7-day horizon each failed the class; baseline returned to `6/0/0` after every revert.
  - **Architecture guard added and mutation-proven.** `AccessTelemetryRetentionDecisionTests` now binds the `PG-ONPREM-1` profile, its in-profile fault, the explicit no-HA statement, the backup/restore RPO/RTO requirement, and the Story 27.4 verification ownership. Removing the no-HA clause failed 1 of 10; restoring the stale "Stories 27.2 and 27.3 are unblocked to implement and verify" sentence failed 1 of 10; baseline `10/0/0`.
  - **`## File Scope` section added**, so `tools/check-story-file-scope.py` can evaluate this story for the first time: `Story file scope validation passed.`
- **Named exclusions (P7, P8), with owner and reason.** These paths or hunks are inside Story 27.3-declared files but are not Story 27.3's work, per the Administrator decision to route them to their owning sessions:
  - `_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-26-memories-ci-cd-alignment.md` - owner: the 2026-07-26 CI/CD-alignment `correct-course` session (its own front matter declares `prepared by: Correct Course workflow`). Reason: Epic 30 CI/CD planning artifact outside `PG-ONPREM-1` adapter qualification. It was committed by `c9dfb06f`, so the earlier description of it as "untracked" is superseded. Reviewed by no chunk.
  - `_bmad-output/planning-artifacts/architecture.md`: Decision D31 (owner `4d2e4e2f`, Epic 29 OpenBao lane) and the Decision D17 rewrite (owner `c9dfb06f`, Story 30.2 CI/CD alignment). D17 additionally states as decided a CI/CD contract the shipped workflows contradict - `ci.yml` never calls `domain-ci.yml`, and `release.yml` is push-triggered with no `environment:` - and was re-phased from `MVP` to `Engineering/Operational Readiness` with no superseded row. Owner: Story 30.2 / that `correct-course` session.
  - `_bmad-output/planning-artifacts/epics.md`: Epic 28/29/31 blocks and the NFR9/NFR24 rewrites (owner `4d2e4e2f`), the five 2026-07-25 Cerebras Phase-2 placeholder stories (owner `1f95a72b`), and Story 30.2 (owner `c9dfb06f`).
  - `_bmad-output/implementation-artifacts/sprint-status.yaml`: `readiness_accounting`, `epic_metadata` and `story_execution_order` expansion for epics 28-31, and the two Epic 21 `action_items` closures (owner: the Epic 21 retrospective and the 2026-07-26 `correct-course` sessions).
  - `_bmad-output/implementation-artifacts/deferred-work.md`: `21.10-A4-VERIFY` (owner: Epic 21 retrospective) and the four `REL-*` entries (owner: `spec-gh-30146368778-fix-tenants-release-startup-failure.md`, surfaced 2026-07-25). These five were re-filed under their own heading this session because they sat under a Story 27.3 chunk-2 code-review heading that does not own them.
  - `_bmad-output/implementation-artifacts/tests/27-3-production-target.env` - gitignored environment handoff, excluded as before.
- Two root-declared submodule gitlinks (`references/Hexalith.EventStore`, `references/Hexalith.Tenants`) moved during this session as a side effect of a Release build. They are unrelated to Story 27.3, were not reverted, and are excluded from this reconciliation.
- Still open and fail-closed for `done`: the 38 chunk-3a `[Review][Patch]` items, the 3 `[Review][Action]` correct-course items (`DW 27.3-CR14`/`CR15`/`CR16`), `DW 27.3-CR4` (Server attribution restatement), and the undeclared 38th path `tests/tooling/production_deployment_evidence/production_deployment_evidence_test.py`, which the open chunk-3a item owns.

### Completion Notes List

- 2026-07-26 (third dev-story invocation): Applied all 38 open chunk-3a `[Review][Patch]` items and the patch work behind the 7 chunk-3a `[Review][Decision]` items, then re-derived C1 as still rejected. Task 1 remains unchecked and Story 27.3 remains `in-progress`; no Production enablement, live deployment mutation (`kubectl apply` was not run), approval claim, Story 27.4 artifact, or A41 mutation occurred. The 3 `[Review][Action]` items stay open: they are `correct-course` activities (`DW 27.3-CR14`/`CR15`/`CR16`), not dev-story scope.
  - **The one genuine product defect: `StaleIndex` orphaned the record it was protecting.** `DeleteAndVerifyAsync` decided bucket membership on `RecordId` alone, so a superseded or foreign expiry entry sharing `(ExpiryMinute, Shard)` with the live record pruned the *live* entry before returning `StaleIndex`. `GetDueEntriesAsync` reads purge candidates only from buckets, so that record could never be rediscovered — permanently unpurgeable, while the caller saw a clean denial. The fix matches the complete five-member entry identity in the membership check, in `CreateBucketRemoval`, in `RemoveBucketEntryAsync`, and in the post-delete verification re-read, and is mirrored in `InMemoryAccessTelemetryStateStore`, whose entry key now carries the full identity. Driven by a genuine RED: both tests that asserted the orphaning as correct failed on the fix; both now assert the opposite and additionally require the record to still be returned by `GetDueEntriesAsync`.
  - **The in-memory adapter no longer diverges from the adapter it stands in for.** All four behaviours the portable lifecycle checkpoints can observe are aligned: a duplicate entry slot returns `Conflict` decided before any mutation (it used to throw `ArgumentException` *after* `_records` was written — a non-atomic write); `VerificationFailed` is reachable through a `SuppressRecordDeletion` hook plus the same strong post-delete verification; `ttlInSeconds` is validated and observable instead of silently discarded; and the due scan is minute-major over an active-minute catalog that prunes drained minutes and leaves untraversed minutes alone. Seven new cases pin each behaviour. `LastTransactionOperationCount` is now derived from the committed operation set (3 opening a new minute, 2 otherwise — exactly the Dapr adapter's counts) and read under the lock, replacing a `ShouldBe(2)` tautology in two checkpoints.
  - **Guards that could not fail are now bound.** The three deployment-guard helpers were structurally unsound in five distinct ways; each is fixed and mutation-proven, including the one that mattered most — pg_hba is first-match-wins, so a bare `host … scram-sha-256` line above the reject rules accepted plaintext TCP while all four presence assertions passed. The ordering-parity tests never exercised the `Shard` or `RecordId` tiebreakers they claimed to pin; two fixtures now do, and removing either tiebreaker fails exactly one of them. The `TransactionalDaprState` fake now enforces the partition/TTL metadata contract and strong-only reads, so an adapter that stopped sending either no longer stays green.
  - **A manifest defect the connection-pool guard was blind to.** The guard hardcoded two writers while the shipped overlay is `replicas: 0` with no `strategy:` block, so Kubernetes' default 25% surge would have added a third sidecar pool (3 x 40 + 13 = 133 against `max_connections=100`) mid-probe. The Deployment now pins `maxSurge: 0`/`maxUnavailable: 1`, and the guard derives peak pods from the rendered overlay instead of a constant.
  - **The C1 verifier now has real producers and an honest packet.** `_run_dapr_version` and `calculate_capacity` were defined and called from no evidence path, so AC1's runtime identity was never captured and gate C1.13 had no producer at all; both are wired. Capacity arithmetic gained the missing `schedulerBytes` and host-filesystem-headroom operands, the `max(137438953472, ceil(base/4))` reclamation floor, a pinned durability multiplier, and admission against the approved 70/80/90% table. `_normalize_bytes` now rejects every unit-less value, so `"400"` meaning 400 GB can no longer read as 400 bytes. Command failures become recorded observations instead of tracebacks that leave no packet. The packet carries per-command timestamps, reviewed-source hashes, pre/post workload identity, and the seven AC3/AC4 attestation fields — all defaulting to `unrecorded`, never to a synthesized claim.
- 2026-07-26 (third dev-story invocation): Remediation runtime defect checklist, applicability re-derived from this session's actual diff. Changed surfaces are the two `IAccessTelemetryStateStore` implementations, their test doubles and fixtures, the deployment-guard tests, one Dapr component-adjacent Deployment field (`strategy.rollingUpdate`), the C1 verifier tooling, the production-deployment PowerShell lane, and CI wiring. **Category 1 (Dapr workflow activity registration): not applicable** — no workflow, child workflow, activity, or DI registration changed. **Category 2 (observed child workflows): not applicable** — no orchestration changed. **Category 3 (owner-checked cleanup and dedup): applicable, and it is where this session's one real defect lived.** The review-found `StaleIndex` index-orphaning defect is recorded here as a Category 3 item per the policy's review gate: a cleanup path that mutated shared state on a match that was not the entry it was authorised to remove. Backed by executed passing evidence in the `85/85` AccessTelemetry.Tests run — owner-checked cleanup by `DeleteAndVerifyAsync_IndexEntryRefersToSupersededEnvelope_ReturnsStaleIndexAndProtectsLiveRecord` and `DeleteAndVerifyAsync_SupersededEntrySharingTheLiveSlot_LeavesTheLiveEntryPurgeable` (both assert the live entry survives *and* the record stays purgeable); duplicate-detector false positives by `WriteRecordAndIndexAsync_TtlReapedRecordWithLingeringBucketEntry_ReturnsConflictWithoutResurrection` and `WriteRecordAndIndexAsync_EntrySlotAlreadyOccupied_ReturnsConflictAndCommitsNothing`; owner-check races by `WriteRecordAndIndexAsync_ConcurrentCatalogWriteBetweenReadAndCommit_ThrowsAndCommitsNoPartialState`, `DeleteAndVerifyAsync_BackendKeepsRecordAfterAcknowledgedDelete_ReturnsVerificationFailed` and `DeleteAndVerifyAsync_BackendKeepsTheBucketEntryAfterAcknowledgedRewrite_ReturnsVerificationFailed`; and index repair under late events by `DeleteAndVerifyAsync_TtlReapedRecordWithLingeringBucketEntry_ReturnsAlreadyAbsentAndPrunesEntry` and `GetDueEntriesAsync_AfterAMinuteIsFullyPurged_PrunesThatMinuteOnTheNextScan`. Migration inventory miscounting and tenant index repair remain untouched: this is a single global infrastructure store keyed by `RecordId` under one fixed actor, not tenant-partitioned state. **Correction to the previous phase's record, per the 2026-07-26 Administrator decision:** the `project-context.md` tenant-isolation rule is **no longer cited as satisfied for this lane**. `DeleteAndVerifyAsync_EntryCarryingAnotherTenantMarker_IsDeniedAndLeavesTheRecordPurgeable` proves envelope-binding denial, not physical cross-tenant isolation — the record key carries no tenant dimension, so both of its records resolve to one state key by construction. Gate C1.11 remains the only thing that can discharge physical isolation, and it is `not complete`. **Category 4 (rollback marker and staging-artifact preservation): not applicable** — no migration, rollback, abort, or staging key, index, or alias changed. **Category 5 (File List): satisfied** by the phase-ledger cumulative reconciliation below.
- 2026-07-26 (second dev-story invocation): Closed the two chunk-2 `[Review][Defer]` items that are C1 preconditions, and re-derived C1 as still rejected. Task 1 remains unchecked and Story 27.3 remains `in-progress`; no Production enablement, live deployment mutation, approval claim, Story 27.4 artifact, or A41 mutation occurred.
  - **Connection-pool exhaustion, fixed.** `maxConns: 64` on `access-telemetry-store` against two ADR writers demanded 128 backend connections while `access-telemetry-postgresql.yaml` starts PostgreSQL with `max_connections=100`. The 500 events/s probe would have failed on connection exhaustion and been misread as a capacity limit of the profile. `maxConns` is now `40`: `2 x 40 + 3 superuser-reserved + 10 evidence sessions = 93 <= 100`. Driven by a genuine RED — the new `ProductionOverlay_AccessTelemetryConnectionPoolFitsPostgreSqlMaxConnections` failed at `141 > 100`, then passed after the fix. The guard parses both operands out of the rendered overlay rather than matching strings, and refuses to run its arithmetic if `reserved_connections=` is ever overridden, so the derivation cannot go stale silently.
  - **Profile security contracts, now bound.** AC4 requires an independent security reviewer to approve identity, secrets, TLS, authorization, and encryption for the exact profile, but six of those properties had no static guard and could drift green: `access-telemetry-secrets` `skipVerify`/`tlsServerName`, the store's `actorStateStore: "true"`, pg_hba `hostssl … scram-sha-256` + `hostnossl … reject`, the init-SQL least-privilege grants/revokes, both RBAC secret-reader Roles, and the deny-default lifecycle ACL. `ProductionOverlay_AccessTelemetryProfileSecurityContractsAreBound` binds all of them structurally — exact `resourceNames`/`resources`/`verbs` sequences and exact `allowedSecrets` membership, not substring presence.
  - **The new guard was mutation-proven, not RED-proven.** It passed on first authoring, which proves nothing about drift detection, so each binding was verified by injecting the drift it exists to catch: `skipVerify:"true"`, `actorStateStore:"false"`, a dropped `hostnossl … reject` line, RBAC verbs widened to `get,list`, ACL `defaultAction: allow`, and an extra secret smuggled into `allowedSecrets`. Every injection failed the suite (the RBAC widening tripped two tests), and the baseline returned to `9/0/0` after each revert.
- 2026-07-26 (second dev-story invocation): Remediation runtime defect checklist, applicability re-derived from this session's actual diff rather than any creation-time note. Changed surfaces are one Dapr component metadata value (`maxConns`, connection-pool sizing) and one deployment guard test. **Category 1 (Dapr workflow activity registration): not applicable** — no workflow, child workflow, activity, or DI registration changed; the component's `actorStateStore` registration property is newly *guarded* but not modified. **Category 2 (observed child workflows): not applicable** — no orchestration changed. **Category 3 (owner-checked cleanup and dedup): not applicable** — no cleanup, deletion, compensation, or dedup path changed; `cleanupInterval` is untouched and pool sizing carries no ownership semantics. **Category 4 (rollback marker and staging-artifact preservation): not applicable** — no migration, rollback, abort, or staging key, index, or alias changed. **Category 5 (File List): satisfied** by the phase-ledger cumulative reconciliation below. Recorded explicitly per the policy rather than left implied; the store manifest is adjacent to runtime surfaces, but this change alters neither dispatch, registration, cleanup, dedup, nor rollback behavior.
- 2026-07-26: Closed the two approved chunk-1 `[Review][Patch]` items, repaired a red Story 27.3 guard the chunk-2 patch had left behind, and re-derived C1 as still rejected. ✅ Resolved review finding [High]: within-minute due-entry ordering parity between the two `IAccessTelemetryStateStore` implementations. ✅ Resolved review finding [High]: missing failure-path unit coverage for `DaprAccessTelemetryStateStore`. Task 1 remains unchecked and Story 27.3 remains `in-progress`; no Production enablement, approval claim, Story 27.4 artifact, or A41 mutation occurred.
- 2026-07-26: Repaired a fail-closed regression inside Story 27.3's own scope. `ProductionDeploymentArtifactsTests.ProductionOverlay_RendersExactSecurityPersistenceAndResourceContracts` was failing at session start: commit `bf911d5c` applied the chunk-2 patch that removes the dead `sslRootCert` field from `deploy/kubernetes/base/dapr/access-telemetry-store.yaml`, but the guard still asserted `name: sslRootCert` and its `value: /mnt/access-telemetry-postgresql/ca.crt`. The manifest patch shipped without its guard update, so the story's own suite was red. The guard now asserts the dead field stays absent and binds what actually carries TLS verify-full — the OpenBao `connectionString` secret reference and `secretStore: access-telemetry-secrets`. The failure is provably independent of this session's other work: no `deploy/` path was modified here. Server.Tests returned to `2,743` cases with `0` failed and the single pre-existing `SubmoduleGuardTests` skip; discovery stayed `2,188` methods at hash `9768c8680c1e0e79ead81495bf7e3a9731a7687171fb48ce08eadd4376da251d`, so this is a `+0` behaviour-strengthening, not an added test.
- 2026-07-26: Remediation runtime defect checklist, applicability re-derived from this session's actual diff (not from a creation-time note). Touched surfaces are `InMemoryAccessTelemetryStateStore.GetDueEntriesAsync` (purge-candidate traversal ordering), four AccessTelemetry test files, one Server deployment-guard test, the adapter evidence packet, and this story record. **Category 1 (Dapr workflow activity registration): not applicable** — no workflow, child workflow, activity, or DI registration changed. **Category 2 (observed child workflows): not applicable** — no orchestration changed. **Category 3 (owner-checked cleanup and dedup): applicable**, and backed by executed passing evidence in the `73/73` AccessTelemetry.Tests run — duplicate-detector false positives are covered by `WriteRecordAndIndexAsync_TtlReapedRecordWithLingeringBucketEntry_ReturnsConflictWithoutResurrection` and `DeleteAndVerifyAsync_IndexEntryRefersToSupersededEnvelope_ReturnsStaleIndexAndProtectsLiveRecord`; owner-check races by `WriteRecordAndIndexAsync_ConcurrentCatalogWriteBetweenReadAndCommit_ThrowsAndCommitsNoPartialState` and `DeleteAndVerifyAsync_BackendKeepsRecordAfterAcknowledgedDelete_ReturnsVerificationFailed`; index repair and idempotence under late events by `DeleteAndVerifyAsync_TtlReapedRecordWithLingeringBucketEntry_ReturnsAlreadyAbsentAndPrunesEntry` and the empty-minute prune assertion. Migration inventory miscounting and tenant index repair are not touched: this lane is a single global infrastructure store keyed by `RecordId` under one fixed actor, not tenant-partitioned state. The `project-context.md` tenant-isolation rule is satisfied for the sub-surface that does exist via `DeleteAndVerifyAsync_EntryCarryingAnotherTenantMarker_IsDeniedAndLeavesTheRecordIntact`, cited once here rather than duplicated. **Category 4 (rollback marker and staging-artifact preservation): not applicable** — no migration, rollback, abort, or staging key, index, or alias changed. **Category 5 (File List): satisfied** by the phase-ledger cumulative reconciliation below; not separately re-specified.
- 2026-07-26: Partial progress on Task 1's PostgreSQL v2 compatibility subtask, recorded without checking it. Now proven at unit level against the transactional fake: bounded traversal and limit truncation, deterministic ordering, stale-index cleanup, two-writer collision behaviour with no partial commit, and cross-tenant denial (an expiry entry minted against a different `TenantMarker` yields `StaleIndex` and leaves the live record intact — the sealed envelope hash covers the marker). Strong reads, ETags, stale-ETag rejection, transactional rollback, and TTL were verified against the real PostgreSQL v2 profile on 2026-07-20. Still outstanding for that subtask, and therefore why it stays unchecked: the ADR two-writer 500 events/s load envelope, which is a C1 deployment probe and cannot run while the clock authorities are placeholders. No PostgreSQL SDK, connection string, or backend credential entered product code.
- 2026-07-20: Used the running Docker/Dapr/Kubernetes environment to complete the technically executable portion of C1. Four-image release/recovery tooling, exact lifecycle/clock archives, SBOMs/scans, PostgreSQL v2 capability and behavioral probes, retained-PVC PostgreSQL replacement, and a disposable backup/restore rehearsal passed. The security material exposed during diagnostics was fully rotated and reverified. C1 remains fail-closed because real clock authorities, signed publication, the full sustained-load/purge/reclamation/capacity evidence, a named off-site backup target, and separate approvals are still missing; lifecycle/clock replicas are zero, the main application flag is false, Task 1 is unchecked, and Story 27.3 remains `in-progress`.
- 2026-07-20: Revalidated the post-installation C1 boundary without changing product or deployment source. The governed Release builds and method inventories remained stable, all four adapter-profile unit tests passed, and the exact read-only verifier refreshed a rejected evidence packet at the expected lifecycle-disabled gate. Missing immutable lifecycle/clock images, live PostgreSQL Dapr/OpenBao configuration, full probes, backup/restore evidence, and separate approvals keep Production writes disabled and Story 27.3 `in-progress`.
- 2026-07-20: Installed the dedicated non-HA PostgreSQL 18.4 server for `PG-ONPREM-1` with the pinned official digest, 400 GiB retained OpenEBS PVC, ClusterIP-only TLS, runtime-role isolation, restricted ingress, and no changes to Keycloak. The pod-replacement persistence probe and focused tests passed. The PostgreSQL v2 expiry adapter and CA-mount manifests are ready, but live Dapr/OpenBao activation, backup/restore, load/capacity/reclamation evidence, application image digests, and separate approvals remain gated; Production writes stay disabled and the story remains `in-progress`.
- 2026-07-20: Administrator approved the on-premises correction in `_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-20-story-27-3-on-prem-postgresql-18-4.md`. `PG-ONPREM-1` replaces `PG-AZ-1` as the active qualification target: PostgreSQL 18.4 on `jpiquot@local`, one 400 GiB retained local PVC, bounded pod/process-loss proof, explicit non-HA limits, and backup/restore evidence before C1 approval. This authorizes fail-closed server installation and compatibility work; it does not enable lifecycle writes, complete Task 1, start Story 27.4, or close A41.
- 2026-07-20: Revalidated the narrowed `PG-AZ-1` C1 prerequisite and preserved fail-closed state. Azure contains no PostgreSQL Flexible Server or AKS target, the reachable local target remains disabled Redis with placeholder lifecycle images, and the exact collector refreshed a rejected non-approval packet under the locked PostgreSQL profile identity. No Task, Production, A41, or story/sprint status changed.
- 2026-07-20: Aligned `AccessTelemetryRetentionDecisionTests` to the approved 27.3/27.4 boundary and exact `PG-AZ-1` ADR contract. The focused Release build completed with 0 warnings/errors and the exact class passed 10/0/0 with zero skipped or not-run tests. This verifies document structure only; C1 runtime qualification remains pending.
- 2026-07-20: Administrator approved the C1 course correction. Story 27.3 is narrowed to `PG-AZ-1` Production-adapter qualification; former Tasks 2-8/C2-C6/runbooks/reconciliation/A41 close-out moved to backlog Story 27.4. Epic 27 remains `in-progress`, Production writes remain disabled, and A41 remains `carried-forward`/`open`. This planning transition ran no product tests and does not certify the profile.
- 2026-07-19: Revalidated Task 1/C1 against the current live target and preserved the fail-closed result. Fresh Release builds passed with 0 warnings/errors and C1 Python tests passed 4/0/0, but both lifecycle deployments remain disabled with placeholder images, the Production ConfigMap is absent, and the exact adapter-profile checkpoint rejected the unproved Redis profile. No implementation, Production enablement, task completion, A41 mutation, or status transition occurred.
- 2026-07-19: Deployed and pinned the Production-shaped application target plus internal `hexalith-keys` OpenBao service, configured all six execution values, assigned the accountable operations/security reviewers, disabled incompatible actor-state component hot reload, and migrated both Dapr secret-store components to TLS-verified isolated OpenBao prefixes. Helm lint, Kubernetes rendering, 23 focused deployment/runbook tests, live Dapr secret reads, negative cross-prefix reads, audit persistence, and OpenBao restart recovery passed. This removes the environment and secret-manager caveats; it does not claim C1 certification or enable lifecycle writes.
- 2026-07-19: Rechecked Task 1/C1. A local Kubernetes context is now reachable, but the `hexalith-memories` namespace contains no workloads or Dapr components/configuration, and the six required deployment/profile/evidence/fault values plus lifecycle verifier/tooling seam remain unavailable. The required-configuration HALT still applies before RED. Comparable xUnit discovery remains Server 2,181, Integration 279, and AccessTelemetry 39 methods; no Story 27.3 test, implementation, Production, or A41 state changed.
- 2026-07-19: Reopened and completed C0 against Story 27.2's final reviewed handoff. The 155-path predecessor inventory reconciles, the fresh portable checkpoint passed 8/0/0, and current discovery is Server 2,181, Integration 279, and AccessTelemetry 39 methods. C1 is now the active fail-closed gate; no Production certification or A41 mutation occurred.
- 2026-07-18: Adversarial code review completed all six layers and applied all 14 accepted patches. The review hardened C0-C6 execution, canonical evidence and A41 close-out atomicity, purge interruption proof, historical classification, checkpoint dates, scope verification, and the phase ledger. Story 27.3 remains `in-progress` and fail-closed at C0 until Story 27.2 reaches `done` with its reviewed handoff.
- 2026-07-18: Created Story 27.3 from current planning, the accepted ADR, the incomplete Story 27.2 handoff, live deployment/code/tests, official Dapr/Redis/.NET guidance, and the repository historical-slice/phase-ledger policies.
- 2026-07-18: Added C0 to prevent pending Story 27.2 work from being counted or implemented as Story 27.3, and added C1-C6 owners/evidence/review gates for the approved checkpoint breadth.
- 2026-07-18: Recorded the exact Redis atomicity, current 20 GiB capacity, Scheduler/reminder, durability, security, and physical-reclamation hazards that the exact Production adapter must prove rather than inherit.
- 2026-07-18: Ultimate context engine analysis completed - comprehensive developer guide created.

### Course Correction Record

- Approved by Administrator on 2026-07-20 in `_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-20-story-27-3-on-prem-postgresql-18-4.md`.
- Active effect: replace the Azure-only `PG-AZ-1` qualification target with the exact single-node on-premises PostgreSQL 18.4 `PG-ONPREM-1` profile; authorize fail-closed installation and the narrow Dapr PostgreSQL v2 compatibility correction; keep statuses and A41 unchanged.
- Approved by Administrator on 2026-07-20 in `_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-19-story-27-3-c1-production-adapter.md`.
- Approved by Administrator on 2026-07-26 in `_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-26-readiness-coherence-and-27-3-splits.md` (added 2026-07-26 by code review, chunk 3b).
- Active effect: execute the two 2026-07-21 approved splits, transferring 16 File List paths to Story 30.1 (Epic 30) and Story 31.1 (Epic 31), expand the C1 checkpoint into a gate evidence table, and shrink the cumulative File List from 52 to 36 paths. Acceptance criteria are not edited; no status advances and no evidence is claimed.
- Recorded 2026-07-26 by code review (chunk 3b) for completeness: `_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-26-memories-ci-cd-alignment.md` is a separate `correct-course` artifact that edited `epics.md`, `architecture.md` and `sprint-status.yaml` inside Story 27.3-declared paths. It is owned by that session, not by Story 27.3, and is carried as a named exclusion in the code-review reconciliation below.
- Citation note (added 2026-07-26 by code review, chunk 3b): where this story cites "approved Sprint Change Proposal 2026-07-26" without a path, it means `sprint-change-proposal-2026-07-26-readiness-coherence-and-27-3-splits.md`.
- Scope classification: Moderate; routed to Product Owner/Developer with separate Platform Operations and security C1 approvals still pending.
- Artifacts changed: Epic 27 story definitions, ADR 27.1 qualification appendix and focused structure guard, this current story scope, the sprint ledger, and A41 backlog-home/owner references without status mutation.
- No deployment, application, A41 residual/action, historical Epic 20/Story 20.5, commit, or publication state changed. **Corrected 2026-07-26 by code review (chunk 3b):** this bullet is scoped to A41 *state*; the A41 backlog-home/owner *references* were changed, as the preceding bullet records.

### File List

**Scope-change note (2026-07-26, approved Sprint Change Proposal 2026-07-26).** The two Administrator-approved splits were executed, transferring **16** paths out of Story 27.3: ten to Story 30.1 (Epic 30, DW 27.3-CR5) — `.github/workflows/recover-partial-release.yml`, `tests/Hexalith.Memories.Cli.Tests/Ci/CiTestInventoryTests.cs`, the four `tests/tooling/publish_containers/*` suites, `tools/complete-partial-release.ps1`, `tools/publish-containers.ps1`, `tools/verify-container-registry.ps1`, and `docs/dev/release-runbook.md` — and six to Story 31.1 (Epic 31, DW 27.3-CR6) — `deploy/openbao/values.yaml`, `namespace.yaml`, `service-account-hardening.yaml`, `smoke-test.yaml`, `docs/operations/openbao.md`, and `deploy/kubernetes/base/dapr/secretstore.yaml`.

The cumulative Story 27.3 File List therefore moves from **52 to 36 paths**. **Updated 2026-07-26 by code review (chunk 3b): 37 paths.** `sprint-change-proposal-2026-07-26-readiness-coherence-and-27-3-splits.md` joined the cumulative set because this review edited it to correct its normative "13 paths" statements to the enumerated 16. It remains a `correct-course`-authored artifact; only this correction is Story 27.3's. A 38th path, `tests/tooling/production_deployment_evidence/production_deployment_evidence_test.py`, is still undeclared pending the open chunk-3a action item that owns it. The preceding `dev-story` row's `matched 52/52` remains accurate as of the moment it was written and is not rewritten (append-only history); **the reconciliation baseline for every subsequent phase is 36**, and the transferred paths must not be counted as Story 27.3 scope again. The proposal text stated "13 paths" in two places; the enumerated set is 16 and all 16 were present in the 52-path list, so 16/36 is the arithmetic of record. `.github/workflows/ci.yml` is retained by Story 27.3 because it also carries adapter-lane wiring; Story 30.1 owns only its release/publish steps, and the exact hunk attribution is reconciled at the chunk-3 final ledger row alongside the already-deferred DW 27.3-CR4 recount. This scope change advances no status and claims no evidence.

- `.github/workflows/ci.yml`
- `_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-20-story-27-3-on-prem-postgresql-18-4.md`
- `_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-26-readiness-coherence-and-27-3-splits.md`
- `_bmad-output/planning-artifacts/epics.md`
- `_bmad-output/planning-artifacts/architecture.md`
- `_bmad-output/implementation-artifacts/27-3-production-adapter-and-deployment-profile.md`
- `_bmad-output/implementation-artifacts/deferred-work.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `_bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md`
- `deploy/kubernetes/base/access-telemetry-deployments.yaml`
- `deploy/kubernetes/base/access-telemetry-network-policy.yaml`
- `deploy/kubernetes/base/access-telemetry-postgresql.yaml`
- `deploy/kubernetes/base/dapr/access-telemetry-store.yaml`
- `deploy/kubernetes/base/dapr/access-telemetry-lifecycle-config.yaml`
- `deploy/kubernetes/base/dapr/access-telemetry-secrets.yaml`
- `deploy/kubernetes/base/dapr/config.yaml`
- `deploy/kubernetes/base/service-accounts-rbac.yaml`
- `deploy/kubernetes/base/kustomization.yaml`
- `docs/operations/deployment-configuration.md`
- `docs/dev/adr-27.1-001-access-telemetry-lifecycle.md`
- `src/Hexalith.Memories.AccessTelemetry/Lifecycle/AccessTelemetryExpiryBucket.cs`
- `src/Hexalith.Memories.AccessTelemetry/Lifecycle/AccessTelemetryExpiryCatalog.cs`
- `src/Hexalith.Memories.AccessTelemetry/Lifecycle/DaprAccessTelemetryStateStore.cs`
- `src/Hexalith.Memories.AccessTelemetry/Lifecycle/InMemoryAccessTelemetryStateStore.cs`
- `tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/DaprAccessTelemetryStateStoreTests.cs`
- `tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/AccessTelemetryStateStoreOrderingParityTests.cs`
- `tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/AccessTelemetryStateStoreTestRecords.cs`
- `tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/TransactionalDaprState.cs`
- `tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/InMemoryAccessTelemetryStateStoreContractTests.cs`
- `tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/LifecycleActorCheckpointTests.cs`
- `tests/Hexalith.Memories.IntegrationTests/Telemetry/AccessTelemetryLifecycleIntegrationCheckpointTests.cs`
- `tests/Hexalith.Memories.Server.Tests/Deployment/ProductionDeploymentArtifactsTests.cs`
- `tests/Hexalith.Memories.Server.Tests/Architecture/AccessTelemetryRetentionDecisionTests.cs`
- `tests/Hexalith.Memories.Cli.Tests/Ci/CiTestInventoryTests.cs`
- `_bmad-output/implementation-artifacts/tests/27-3-adapter-profile-evidence.md`
- `tests/tooling/access_telemetry_lifecycle/test_adapter_profile.py`
- `tests/tooling/production_deployment_evidence/production_deployment_evidence_test.py`
- `tests/tooling/coverage_gate/line-coverage-gate.json`
- `tests/tooling/coverage_gate/coverage_contract_test.py`
- `_bmad-output/implementation-artifacts/tests/adapter-profile-runs/**`
- `tools/render-production-deployment.ps1`
- `tools/verify-production-deployment.ps1`
- `tools/production-deployment-health.ps1`
- `tools/validate-production-deployment-evidence.ps1`
- `tools/verify-access-telemetry-lifecycle.py`
- `tools/verify_access_telemetry_lifecycle.py`
- `tools/test-projects.unit-contract.txt`

**File List expanded 2026-07-26 by dev-story: 37 -> 45 paths.** Eight paths joined the cumulative
set this phase. Each is named here with its owner and the reason it is now Story 27.3-declared:

- `tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/InMemoryAccessTelemetryStateStoreContractTests.cs`
  — new, Story 27.3-owned. Pins the four in-memory/Dapr adapter divergences the chunk-3a review found.
- `tools/production-deployment-health.ps1` — new, Story 27.3-owned. `Get-HealthJsonBody` and
  `Get-HealthStatusCode` extracted from `verify-production-deployment.ps1` so they can be exercised
  with real transcripts instead of pinned by a source-text assertion.
- `tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/LifecycleActorCheckpointTests.cs` —
  Story 27.2-owned file with a Story 27.3-owned line range: only the
  `LastTransactionOperationCount` assertion block changed, replacing the hardcoded `2` tautology
  with the derived operation set.
- `tests/Hexalith.Memories.IntegrationTests/Telemetry/AccessTelemetryLifecycleIntegrationCheckpointTests.cs`
  — Story 27.2-owned file with a Story 27.3-owned line range: only the atomic-pairs assertion block
  changed, for the same reason.
- `tests/Hexalith.Memories.Cli.Tests/Ci/CiTestInventoryTests.cs` — **cross-story edit, Story
  30.1-owned** (transferred by the approved 2026-07-26 Sprint Change Proposal). Edited under the
  2026-07-26 Administrator decision that Story 27.3 wires its own CI lanes now: the
  `ExpectedDockerFreeProjects` entry for `Hexalith.Memories.AccessTelemetry.Tests`, the
  restore-property sync guard, the tooling-suite wiring guard, the deferred-status parse repair, and
  the source-text assertions this phase's `verify-production-deployment.ps1` changes invalidated.
  Story 30.1 must not regress these.
- `tools/test-projects.unit-contract.txt` — Story 27.3-declared for the three-way lane sync that
  registers `Hexalith.Memories.AccessTelemetry.Tests`.
- `tests/tooling/coverage_gate/line-coverage-gate.json` — same three-way sync
  (`requiredReportProjects`). The `Hexalith.Memories.AccessTelemetry` **source** assembly is
  deliberately not added to `requiredAssemblies`: that is a coverage-policy change with its own
  owner, and `assemblySourcePrefixes must define every required assembly and no others`.
- `tests/tooling/production_deployment_evidence/production_deployment_evidence_test.py` — the
  previously-known undeclared 38th path (open chunk-3a item), now declared. It is the fixture suite
  for the already-declared `tools/validate-production-deployment-evidence.ps1`.

## Change Log

Rows dated before 2026-07-20 are append-only pre-split history; references to Tasks 2-8 or C2-C6 do not restore that scope to Story 27.3.

**Transfer disclaimer extended 2026-07-26 by code review (chunk 3b).** The 2026-07-26 correction additionally transferred scope to Story 30.1 and Story 31.1. Rows dated on or after 2026-07-20 that record publish-container tooling (`38/38`), `Cli.Tests` (`384/384`), `CiTestInventoryTests` (`57/57`), four-image release and partial-recovery orchestration, OCI archive builds, SPDX SBOM production, pinned-image vulnerability scans, or OpenBao platform work describe lanes now owned by Story 30.1 or Story 31.1. Those rows remain append-only history and are accurate as of the moment they were written, but that work is not Story 27.3 evidence and must not be restated as such in any later row or completion note.

| Date | Phase | Change | Test count | File List reconciliation |
| :--- | :---- | :----- | :--------- | :----------------------- |
| 2026-07-18 | create-story | Created context-ready Story 27.3 and moved only its sprint row from `backlog` to `ready-for-dev`; no implementation or A41 close-out occurred. C0 records Story 27.2 as an external in-progress prerequisite. | Actual phase delta +0 and cumulative +0. Fresh comparable builds/discovery: Server.Tests 2,157 xUnit methods (Architecture 24, Telemetry 141, Hosting 17) and IntegrationTests 270 xUnit methods (Telemetry 9), using the exact build/count/hash commands in **Testing Baseline and Planned Delta**; both builds passed with 0 warnings/errors. AccessTelemetry.Tests is absent because Story 27.2 is incomplete and must be mapped later as its external `0 -> N` lane. Planned Story 27.3 ranges by named unit: Server +8..12 xUnit methods, Integration +22..32 xUnit methods, post-27.2 AccessTelemetry +10..16 xUnit methods, and new tooling +8..12 Python unittest cases. | matched 3/3 against baseline `272c33bc5d30d71ac46f20e703b9d5456e75a093` and pre-create sprint SHA-256 `f2cc7a2520634d2ca280622faf1494477c0ec5500e24ae0c8b7baf36f1dc13c8`; exact owned-line diff, exclusions, post-create hash, and verification commands are in `_bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md`. |
| 2026-07-18 | dev-story | Attempted Task 0 and halted before implementation because Story 27.2 had not completed its required handoff; recorded the C0 blocker and returned ownership to the Story 27.2 owner. No Production evidence or A41 state changed. | Runner-derived phase/cumulative delta is unavailable for this blocked attempt because no comparable post-attempt discovery snapshot was captured before HALT. Exact command: `DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.IntegrationTests/bin/Release/net10.0/Hexalith.Memories.IntegrationTests.dll -class Hexalith.Memories.IntegrationTests.Telemetry.AccessTelemetryLifecycleIntegrationCheckpointTests -parallel none -noLogo`; it exited 0 but reported `Total: 0` because the class was absent. Owner: Story 27.2 owner. Consequence: no Story 27.3 implementation or dev-phase count agreement is claimable. Reopen trigger: Story 27.2 is `done` with reviewed C1-C6/ledger/File List and a fresh build/discovery/run proves the canonical class executes a nonzero matching count. The later `code-review` row maps Story 27.2's external lane deltas without inventing this blocked phase's delta. | matched 3/3 cumulative creation paths against baseline `272c33bc5d30d71ac46f20e703b9d5456e75a093` via `_bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md`; the blocked attempt introduced no additional path, and concurrent Story 27.2/submodule work remained excluded. |
| 2026-07-18 | code-review | Completed the single confirmed review group; all six layers returned, all 14 accepted patches were applied, one pre-existing SDK-context drift was deferred, and no review finding remains unchecked. Story 27.3 implementation and A41 close-out remain blocked at C0 because Story 27.2 is still `review`. | Review-patch delta `+0` and cumulative Story 27.3 delta `+0` in every comparable method lane. Review observed Server 2,169 -> 2,170 only because external commit `1242e774` added one Server method; equation `2,157 create + 0 story + 12 Story 27.2 + 1 release lane = 2,170`, hash `1449199044ae36604668d364a656ede748911d90b7cc95e3e68830a5dcb55d12`. Integration stayed 278 -> 278: `270 + 0 + 8 = 278`, hash `92f4cfeb683a0bb2cd90305e92f61f00eb53542e2aac231f3c61b7a9dcf96d55`. AccessTelemetry stayed 31 -> 31: `0 + 0 + 31 = 31`, hash `4eea34c669021dc01446ddf0d44111c6aa38e482e92736ea155dcf84f5498460`. Named unit is xUnit methods; exact fresh build, discovery, hash, focused-test, and scope-verifier commands/results are recorded in **Code Review Evidence**. | matched 4/4 cumulative story paths against baseline `272c33bc5d30d71ac46f20e703b9d5456e75a093` with `git diff --name-status 272c33bc5d30d71ac46f20e703b9d5456e75a093 -- _bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md _bmad-output/implementation-artifacts/deferred-work.md _bmad-output/implementation-artifacts/sprint-status.yaml _bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md`; same-file Story 27.2 sprint changes, commit `1242e774` release paths, submodule advances, and unrelated dirty EventStore work are explicitly excluded in **Code Review Evidence**. |
| 2026-07-19 | dev-story | Resumed only Task 0 after its recorded reopen trigger, independently reconciled Story 27.2's completed C1-C6/ledger/155-path handoff, and passed the fresh C0 portable checkpoint. No Story 27.3 Production implementation, evidence claim, or A41 mutation occurred; C1 remains pending. | Runner-derived phase delta `+0` and cumulative Story 27.3 delta `+0` in all xUnit-method lanes. Exact discovery commands: `DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.Server.Tests/bin/Release/net10.0/Hexalith.Memories.Server.Tests.dll -list methods -noLogo`, the same command for `tests/Hexalith.Memories.IntegrationTests/bin/Release/net10.0/Hexalith.Memories.IntegrationTests.dll`, and the same command for `tests/Hexalith.Memories.AccessTelemetry.Tests/bin/Release/net10.0/Hexalith.Memories.AccessTelemetry.Tests.dll`. Observed/hash results: Server 2,181/`62c935a6f13179d4f3e84912565279faffc0b84c4d223446a02be5c614659cd0`, Integration 279/`8af20c16b0d0a9f4b947357e232fb864f2bccba63aa33ccff3a81676979b9468`, AccessTelemetry 39/`0e79e40274ddf051f891e5bdfae2f25d1cb4498fd82d865b78216081a2b99a14`. External equations: Server `2,157 + 0 + 12 Story 27.2 dev + 1 release lane + 11 Story 27.2 final review = 2,181`; Integration `270 + 0 + 8 + 1 = 279`; AccessTelemetry `0 + 0 + 31 + 8 = 39`. The fresh C0 Release build passed 0 warnings/errors and the exact class ran 8/0/0 from assembly SHA-256 `e032bd6a356ee8cae42776ceb6635d0d44c245b4f42c9dc2a07872b10c210fad`, unchanged after execution. | matched 4/4 cumulative Story 27.3 paths against baseline `272c33bc5d30d71ac46f20e703b9d5456e75a093` with `git diff --name-status` over the four File List paths. The Story 27.3-owned deferred entry is lines 2241-2245; `deferred-work.md` label edits at lines 87/89 and the `spec-run-tests-and-fix-failures` entries at lines 2247-2263 belong to commit `042dcba6` and are excluded. Story 27.2's sprint row, all other sprint rows, later release/submodule history, and every predecessor implementation path remain externally owned. |
| 2026-07-19 | code-review | Completed the confirmed single four-file Story 27.3 review chunk; all six layers returned non-empty findings, all 22 selected patches were applied, and one false-positive request to rewrite an append-only historical row was dismissed. C0 is complete, while Tasks 1-8 and C1-C6 remain pending, so Story 27.3 truthfully remains `in-progress`. | Review-patch delta `+0` and cumulative Story 27.3 delta `+0` in every comparable xUnit-method lane. Pre/post discovery is unchanged: Server 2,181, Integration 279, AccessTelemetry 39, with method-set hashes `62c935a6f13179d4f3e84912565279faffc0b84c4d223446a02be5c614659cd0`, `8af20c16b0d0a9f4b947357e232fb864f2bccba63aa33ccff3a81676979b9468`, and `0e79e40274ddf051f891e5bdfae2f25d1cb4498fd82d865b78216081a2b99a14`. External equations remain Server `2,157 + 0 + 12 + 1 + 11 = 2,181`, Integration `270 + 0 + 8 + 1 = 279`, and AccessTelemetry `0 + 0 + 31 + 8 = 39`. Exact commands/results, the optimized-Python verifier pass, C0 8/0/0 result, and pre/post hashes are in **Code Review Evidence**. | matched 4/4 cumulative Story 27.3 paths against baseline `272c33bc5d30d71ac46f20e703b9d5456e75a093`; the canonicalized immutable review manifest and A/M/M/A path set cover the complete post-401-line delta. Same-file `deferred-work.md` ownership, Story 27.2/sprint status changes, release history, and submodule exclusions are recorded in **Code Review Evidence**. |
| 2026-07-19 | dev-story | Attempted Task 1/C1 and halted before test or implementation edits because no Production-shaped Kubernetes target or required environment identity is configured. Production lifecycle writes remain disabled; no evidence, approval, deployment, or A41 state changed. Owner: Deployment adapter owner plus Hexalith Platform Operations. Consequence: C1 and every dependent task/checkpoint remain blocked. Reopen trigger: configure the six required values and an accessible reviewed context, identify the accountable reviewers, then run the exact C1 red-green-refactor and adapter-profile evidence contract. | Runner-derived phase delta `+0` and cumulative Story 27.3 delta `+0` in all comparable xUnit-method lanes; the new Python tooling lane remains `0` cases because RED did not begin. Immediate discovery commands were `DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.Server.Tests/bin/Release/net10.0/Hexalith.Memories.Server.Tests.dll -list methods -noLogo`, the same command for `tests/Hexalith.Memories.IntegrationTests/bin/Release/net10.0/Hexalith.Memories.IntegrationTests.dll`, and the same command for `tests/Hexalith.Memories.AccessTelemetry.Tests/bin/Release/net10.0/Hexalith.Memories.AccessTelemetry.Tests.dll`. Observed/hash results remained Server 2,181/`62c935a6f13179d4f3e84912565279faffc0b84c4d223446a02be5c614659cd0`, Integration 279/`8af20c16b0d0a9f4b947357e232fb864f2bccba63aa33ccff3a81676979b9468`, and AccessTelemetry 39/`0e79e40274ddf051f891e5bdfae2f25d1cb4498fd82d865b78216081a2b99a14`. Exact blocked preflight and exit codes are in **Debug Log References**. | matched 4/4 cumulative Story 27.3 paths against baseline `272c33bc5d30d71ac46f20e703b9d5456e75a093` using `git diff --name-status` over the four File List paths. Unrelated `commitlint.config.js` and dirty root-declared submodule worktrees are user/concurrent work and excluded; no new Story 27.3 path was created. |
| 2026-07-19 | dev-story | Rechecked the Task 1/C1 reopen trigger and halted before RED. Kubernetes now exposes reachable context `jpiquot@local`, but `kubectl get all/components.dapr.io/configurations.dapr.io -n hexalith-memories` finds no target resources, all six required Production identity/evidence/fault values remain unset, and the required verifier/tooling seam is absent. No test, implementation, deployment, evidence approval, Production enablement, or A41 mutation occurred. Owner: Deployment adapter owner plus Hexalith Platform Operations. Consequence: C1 and every dependent checkpoint remain blocked. Reopen trigger: deploy or identify the reviewed Production-shaped target, provide all six values and accountable reviewers, and provide the verifier/tooling seam before executing the exact C1 contract. | Runner-derived phase delta `+0` and cumulative Story 27.3 delta `+0` in every comparable xUnit-method lane. Before and after this record-only phase, the exact commands `DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.Server.Tests/bin/Release/net10.0/Hexalith.Memories.Server.Tests.dll -list methods -noLogo`, `DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.IntegrationTests/bin/Release/net10.0/Hexalith.Memories.IntegrationTests.dll -list methods -noLogo`, and `DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.AccessTelemetry.Tests/bin/Release/net10.0/Hexalith.Memories.AccessTelemetry.Tests.dll -list methods -noLogo` report Server 2,181/`62c935a6f13179d4f3e84912565279faffc0b84c4d223446a02be5c614659cd0`, Integration 279/`8af20c16b0d0a9f4b947357e232fb864f2bccba63aa33ccff3a81676979b9468`, and AccessTelemetry 39/`0e79e40274ddf051f891e5bdfae2f25d1cb4498fd82d865b78216081a2b99a14`. The Python lane cannot produce a comparable case total: `PYTHONHASHSEED=0 python3 -m unittest discover -s tests/tooling/access_telemetry_lifecycle -p 'test_*.py' -v` exits 1 with `Start directory is not importable` because RED and its discovery scope cannot begin without the required configuration/tooling. Owner, consequence, and reopen trigger are recorded in **Debug Log References**. | matched 4/4 cumulative Story 27.3 paths against baseline `272c33bc5d30d71ac46f20e703b9d5456e75a093` with `git diff --name-status 272c33bc5d30d71ac46f20e703b9d5456e75a093 -- _bmad-output/implementation-artifacts/27-3-retention-verification-operations-runbook-and-a41-close-out.md _bmad-output/implementation-artifacts/deferred-work.md _bmad-output/implementation-artifacts/sprint-status.yaml _bmad-output/implementation-artifacts/tests/27-3-create-story-scope-evidence.md`; no File List path was added or removed. Unrelated `commitlint.config.js` and dirty root-declared submodule worktrees remain excluded user/concurrent work. |
| 2026-07-19 | dev-story | Satisfied the recorded C1 infrastructure reopen trigger: deployed and identified the reviewed Production-shaped application target, configured the six execution values, assigned reviewers, fixed Dapr actor-state hot-reload behavior, deployed internal `hexalith-keys`, and migrated the two Dapr secret stores to isolated OpenBao paths. The initial root token was revoked; replacement recovery, scoped reads/denials, audit persistence, and application health passed. C1 remains pending its verifier-driven adapter, capacity, durability, and reclamation gates; lifecycle writes remain disabled and A41 remains open. | Story phase delta `+1` and cumulative Story 27.3 delta `+1` Server.Tests xUnit method; Integration and AccessTelemetry deltas remain `+0`. Fresh method inventories are Server 2,182/`4b9c8e324353bad001c0af31135f39ca4e48e91dcc9b57fda53c5386ac7a6780`, Integration 279/`8af20c16b0d0a9f4b947357e232fb864f2bccba63aa33ccff3a81676979b9468`, and AccessTelemetry 39/`0e79e40274ddf051f891e5bdfae2f25d1cb4498fd82d865b78216081a2b99a14`. The focused `ProductionDeploymentArtifactsTests`, `DeploymentConfigurationContractTests`, and `OperationalRunbookSetTests` selection passed 23/0/0; Helm lint, Kustomize validation, `git diff --check`, live Dapr secret reads, cross-prefix denial, controlled OpenBao replacement, and the Restricted-compatible smoke Job also passed. | matched 16/16 cumulative File List paths. The six-value `_bmad-output/implementation-artifacts/tests/27-3-production-target.env` is intentionally gitignored because it is an environment handoff, and cluster-generated TLS, seal, recovery, operator, and Dapr token values are excluded from source and evidence. Unrelated `commitlint.config.js` and dirty root-declared submodule worktrees remain excluded user/concurrent work. |
| 2026-07-19 | dev-story | Implemented the C1 adapter-profile RED/GREEN tooling contract and ran the exact read-only Production-shaped collector. The collector records identity, component type/version/scopes, deployment images/replicas, Dapr sidecar image, safe hashes, and an immutable rejection packet; it never enables lifecycle writes or mutates A41. C1 was rejected because the lifecycle deployment remains deliberately disabled at `0/0`; the current `state.redis` profile has no approved exact-profile rollback/zero-loss/capacity/physical-reclamation evidence. | Python `unittest` cases: phase delta `+4`, cumulative Story 27.3 delta `+4`, named scope `tests/tooling/access_telemetry_lifecycle/test_*.py`, before `0` because the start directory was absent and the exact discovery exited 1 with `Start directory is not importable`, after `4/0/0`, sorted-case SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`, exact command `PYTHONHASHSEED=0 python3 -m unittest discover -s tests/tooling/access_telemetry_lifecycle -p 'test_*.py' -v`. Comparable xUnit methods had phase delta `+0`: Server `2,182 -> 2,187`, cumulative Story 27.3 delta remains `+1`, external same-lane delta `+5` from concurrent commit `1a557ca3` OpenBao test changes; Integration `279 -> 297`, cumulative `+0`, external `+18` from that same lane; AccessTelemetry `39 -> 39`, cumulative `+0`, external `+0`. Post snapshot commands were `DiffEngine_Disabled=true dotnet exec tests/Hexalith.Memories.Server.Tests/bin/Release/net10.0/Hexalith.Memories.Server.Tests.dll -list methods -noLogo`, the equivalent IntegrationTests and AccessTelemetry.Tests commands, with method-set hashes `f010c6aebeea418799516d6a6fbb4c3e801a66ecd5959a09e4fd8ed0c53e8a5a`, `7836151bdf59ff8712f59911ed138a2f7afc792a7c4d2415c64122695c163856`, and `0e79e40274ddf051f891e5bdfae2f25d1cb4498fd82d865b78216081a2b99b9468`; `git diff --check` passed. | matched 20/20 cumulative File List paths against baseline `272c33bc5d30d71ac46f20e703b9d5456e75a093` using `git diff --name-status 272c33bc5d30d71ac46f20e703b9d5456e75a093 --` for the 16 previously listed paths plus `git status --short --untracked-files=all --` for the four new paths. Unrelated `commitlint.config.js`, root-declared dirty submodules, and concurrent OpenBao paths outside this story's File List remain excluded with their owners/evidence documented above. |
| 2026-07-19 | dev-story | Revalidated Task 1/C1 and halted before new RED or implementation work because both lifecycle services remain disabled with placeholder images, the required Production ConfigMap is absent, and the exact Redis profile still lacks atomicity, zero-loss, capacity, and physical-reclamation approval. The exact read-only adapter-profile command refreshed the rejection packet and exited 1; no Production, A41, task, or status mutation occurred. | Before/after phase delta `+0`. Cumulative Story 27.3 deltas remain Server.Tests `+1` xUnit method, IntegrationTests `+0`, AccessTelemetry.Tests `+0`, and Python `unittest` `+4` cases. The three fresh Release project builds passed with 0 warnings/errors. Exact before/after discoveries used `DiffEngine_Disabled=true dotnet exec <assembly> -list methods -noLogo`: Server 2,187/`f010c6aebeea418799516d6a6fbb4c3e801a66ecd5959a09e4fd8ed0c53e8a5a`, Integration 297/`7836151bdf59ff8712f59911ed138a2f7afc792a7c4d2415c64122695c163856`, and AccessTelemetry 39/`0e79e40274ddf051f891e5bdfae2f25d1cb4498fd82d865b78216081a2b99a14`. External equations are Server `2,157 + 1 story + 29 external = 2,187`, Integration `270 + 0 + 27 = 297`, and AccessTelemetry `0 + 0 + 39 = 39`. Exact Python command `PYTHONHASHSEED=0 python3 -m unittest discover -s tests/tooling/access_telemetry_lifecycle -p 'test_*.py' -v` passed 4/0/0 before and after; the sorted four-case ID SHA-256 is `e2e1c469aae8f51e1d96c9fb5644bff815f854ca93eb17568cec3e201cbf6dae`, correcting the preceding row's accidentally recorded empty-set hash without rewriting append-only history. | matched 20/20 cumulative File List paths against baseline `272c33bc5d30d71ac46f20e703b9d5456e75a093` with `git diff --name-status 272c33bc5d30d71ac46f20e703b9d5456e75a093 -- <the 20 File List paths>`; this record-only phase changes only the already-listed story and adapter rejection packet. Current HEAD/submodule and unrelated history remain externally owned and excluded. |
| 2026-07-20 | dev-story | Revalidated the approved post-split Task 1/C1 prerequisite and halted before new RED or product/deployment implementation. The locked `PG-AZ-1` Azure PostgreSQL/AKS target does not exist, the reachable local target is still disabled Redis with placeholder lifecycle images, and separate approvals are absent. The exact read-only collector refreshed the locked-profile rejection packet; Production writes, Task 1, A41, Story 27.4, and story/sprint status remain fail-closed. | Phase delta `+0` in every comparable lane. Fresh before/post discoveries used `DiffEngine_Disabled=true dotnet exec <assembly> -list methods -noLogo`: Server.Tests 2,188/`9768c8680c1e0e79ead81495bf7e3a9731a7687171fb48ce08eadd4376da251d`, IntegrationTests 297/`7836151bdf59ff8712f59911ed138a2f7afc792a7c4d2415c64122695c163856`, and AccessTelemetry.Tests 39/`0e79e40274ddf051f891e5bdfae2f25d1cb4498fd82d865b78216081a2b99a14`; named unit is xUnit methods. Cumulative Story 27.3 deltas remain Server `+1`, Integration `+0`, AccessTelemetry `+0`; external same-lane deltas are Server `+30` (including the 2026-07-20 course-correction structure guard), Integration `+27`, and AccessTelemetry `+39`, so `2,157 + 1 + 30 = 2,188`, `270 + 0 + 27 = 297`, and `0 + 0 + 39 = 39`. Exact Python discovery `PYTHONHASHSEED=0 python3 -m unittest discover -s tests/tooling/access_telemetry_lifecycle -p 'test_*.py' -v` passed 4/0/0 before/post with cumulative Story 27.3 `+4` cases and phase delta `+0`. The initial parallel Server build collided on a shared generated runtime-config file; the immediate sequential retry passed with 0 warnings/errors, while the Integration and AccessTelemetry Release builds passed directly. | matched 21/21 cumulative Story 27.3 File List paths against baseline `272c33bc5d30d71ac46f20e703b9d5456e75a093` using `git diff --name-status 272c33bc5d30d71ac46f20e703b9d5456e75a093 -- <the 21 File List paths>`; this invocation changes only the already-listed story and rejection packet. The remaining 262 baseline-relative repository paths and course-correction planning ownership outside the listed paths remain excluded; no unrelated working-tree change exists. |
| 2026-07-20 | dev-story | Applied the approved on-premises correction: implemented the PostgreSQL v2-compatible explicit expiry catalog/buckets, added the immutable `PG-ONPREM-1` manifests and structure guards, aligned the fail-closed C1 collector, installed PostgreSQL 18.4 on the reviewed cluster, corrected first-start peer mapping, rotated the new protected material after diagnostic exposure, and verified TLS/plaintext rejection, runtime-role isolation, network isolation, retained storage, and pod-replacement persistence. C1 remains rejected because live Dapr/OpenBao activation, runnable lifecycle/clock digests, full Dapr/load/capacity/reclamation probes, backup/restore, and separated approvals remain absent; writes stay disabled and Task 1/A41/status remain unchanged. | Phase delta: AccessTelemetry.Tests `+4` xUnit methods; Server.Tests and IntegrationTests `+0`; Python inventory remains 4 cases. AccessTelemetry passed 61/61, Server.Tests passed 2,742/2,743 with one pre-existing submodule-guard skip, the portable lifecycle checkpoint passed 8/8, and the C1 Python inventory passed 4/4. The production render passed Kubernetes server-side dry-run and the aligned collector exited 1 at the expected lifecycle-disabled gate. | All 19 current worktree paths are classified in the cumulative 34-path File List. No secret value, generated credential, commit, push, Story 27.4 artifact, A41 mutation, or unrelated Keycloak change is included. |
| 2026-07-20 | dev-story | Revalidated Task 1/C1 after the approved `PG-ONPREM-1` installation and halted before new RED or implementation work. PostgreSQL remains pinned and Ready, while the lifecycle/clock workloads are disabled placeholder images and the live Dapr state component remains Redis. The exact collector refreshed the fail-closed rejection packet. No product/deployment source, task checkbox, Production enablement, Story 27.4, A41, or story/sprint status changed. Owner, consequence, and reopen trigger are recorded in **Debug Log References**. | Phase delta `+0` in every lane. Fresh Release builds used `DOTNET_CLI_USE_MSBUILD_SERVER=0 dotnet build <project> --configuration Release --disable-build-servers -m:1 /nr:false -p:NuGetAudit=false -p:MinVerVersionOverride=1.0.0` for Server.Tests, IntegrationTests, and AccessTelemetry.Tests and passed with 0 warnings/errors. Before/post discovery used `DiffEngine_Disabled=true dotnet exec <assembly> -list methods -noLogo`: Server.Tests 2,188/`9768c8680c1e0e79ead81495bf7e3a9731a7687171fb48ce08eadd4376da251d`, IntegrationTests 297/`7836151bdf59ff8712f59911ed138a2f7afc792a7c4d2415c64122695c163856`, and AccessTelemetry.Tests 43/`59c4938732a1d2256a288706279b23119c1e59825c8710bb2894274469808f39`; named unit is xUnit methods. Cumulative Story 27.3 deltas are Server `+1`, Integration `+0`, AccessTelemetry `+4`; external same-lane deltas are `+30`, `+27`, and `+39`, so `2,157 + 1 + 30 = 2,188`, `270 + 0 + 27 = 297`, and `0 + 4 + 39 = 43`. Exact Python discovery `PYTHONHASHSEED=0 python3 -m unittest discover -s tests/tooling/access_telemetry_lifecycle -p 'test_adapter_profile.py' -v` passed 4/0/0 before/post, with phase delta `+0`, cumulative Story 27.3 delta `+4`, and sorted-case SHA-256 `e2e1c469aae8f51e1d96c9fb5644bff815f854ca93eb17568cec3e201cbf6dae`. The exact adapter-profile verifier exited 1 with `lifecycle deployment is disabled; Production writes remain fail-closed`; this is the required rejection, not a skipped or passing C1 probe. | matched 34/34 cumulative Story 27.3 File List paths against baseline `272c33bc5d30d71ac46f20e703b9d5456e75a093` using `git diff --name-status 272c33bc5d30d71ac46f20e703b9d5456e75a093 -- <the 34 File List paths>`. This invocation changes only the already-listed story and adapter rejection packet; no path was added or removed. |
| 2026-07-20 | dev-story | Exercised Task 1/C1 against the running Docker/Dapr/Kubernetes environment. Expanded release and partial-recovery orchestration to the required four-image unit; built exact lifecycle/clock OCI archives; produced SPDX SBOMs; passed pinned-image HIGH/CRITICAL scans; activated the exact PostgreSQL v2 profile temporarily; and verified Dapr identity/capabilities, strong save/read, ETags, transactional rollback, TTL, retained-PVC PostgreSQL replacement with zero acknowledged record loss, storage headroom, and disposable backup/restore mechanics. Corrected lifecycle/clock NetworkPolicy omissions for node-local DNS and Redis configuration-store access. A Secret diagnostic exposure was followed by complete affected-material rotation and hardened OpenBao revalidation. The production clock is still unavailable, the images cannot be signed/published in the current credential context, and full load/purge/reclamation/capacity, named off-site backup, operating-cost, and independent approval evidence remain absent. Qualification replicas returned to zero and the main application stays disabled; Task 1/C1, Story 27.4, A41, and status remain unchanged. | Comparable xUnit phase delta `+0`: Server.Tests 2,188/`9768c8680c1e0e79ead81495bf7e3a9731a7687171fb48ce08eadd4376da251d`, IntegrationTests 297/`7836151bdf59ff8712f59911ed138a2f7afc792a7c4d2415c64122695c163856`, and AccessTelemetry.Tests 43/`59c4938732a1d2256a288706279b23119c1e59825c8710bb2894274469808f39`; cumulative Story 27.3 deltas remain `+1`, `+0`, and `+4`. Exact C1 Python stayed 4/4 with phase delta `+0`, cumulative `+4`, and sorted-case SHA-256 `e2e1c469aae8f51e1d96c9fb5644bff815f854ca93eb17568cec3e201cbf6dae`. Publish-container tooling stayed 38/38 with phase delta `+0` while existing four-image cases were strengthened. Cli.Tests mapped immediate pre/post 384/384 with phase delta `+0`; cumulative comparison is unavailable because the creation baseline did not include that lane, and focused `CiTestInventoryTests` passed 57/57. Focused `ProductionDeploymentArtifactsTests` passed 7/7. Server.Tests and Cli.Tests Release builds passed with 0 warnings/errors. The exact final adapter verifier exited 1 at the required disabled-lifecycle rejection. | matched 47/47 cumulative File List paths against baseline `272c33bc5d30d71ac46f20e703b9d5456e75a093`. Generated ignored OCI archives, SBOM/SARIF outputs, transient backup/restore data, and every generated or rotated secret are excluded. No commit, push, Story 27.4 artifact, A41 mutation, or unrelated path is included. |
| 2026-07-26 | dev-story | Closed the two approved chunk-1 `[Review][Patch]` items, repaired a red in-scope guard, added the missing cross-tenant denial negative, and re-derived C1 as still rejected. Aligned `InMemoryAccessTelemetryStateStore.GetDueEntriesAsync` to the Dapr adapter's observable order (`ExpiryMinute`→`ExpiresAtUtc`→`Shard`→`RecordId`); added eight `DaprAccessTelemetryStateStore` failure-path negatives plus a cross-tenant denial negative; extracted and extended the `TransactionalDaprState` fake with empty-ETag `FirstWrite` insert semantics, TTL reaping, undeletable keys, and a pre-commit concurrent-writer hook; added three cross-adapter ordering-parity tests. Repaired `ProductionDeploymentArtifactsTests`, which was failing at session start because commit `bf911d5c` removed the dead `sslRootCert` field from the store manifest without updating its guard; the guard now asserts the field stays absent and binds the OpenBao `connectionString` carrier that actually provides verify-full. Task 1 stays unchecked: the clock authorities remain `.example.invalid`, so the clock-dependent C1 gates cannot run. No Production enablement, approval claim, deployment mutation, Story 27.4 artifact, or A41 mutation occurred; Story 27.3 remains `in-progress`. | Named unit is xUnit test methods unless stated. **AccessTelemetry.Tests** phase delta `+12`, cumulative Story 27.3 `+16`: before `43`/`59c4938732a1d2256a288706279b23119c1e59825c8710bb2894274469808f39`, after `55`/`973244b8ebcdfd55eeaf01ba56b8f33a1836aee158a98906817b5a5b2e3e60ef`; full run `73/73` test cases with `0` errors, failed, skipped, or not-run. **Server.Tests** phase delta `+0` — the guard repair strengthened one existing method rather than adding one — cumulative Story 27.3 `+1`: before and after both `2,188`/`9768c8680c1e0e79ead81495bf7e3a9731a7687171fb48ce08eadd4376da251d`; full run `2,743` test cases, `0` failed, `1` pre-existing `SubmoduleGuardTests` skip (before this session's repair the same run reported `1` failed). **IntegrationTests** phase delta `+0`, cumulative `+0`: before and after both `297`/`7836151bdf59ff8712f59911ed138a2f7afc792a7c4d2415c64122695c163856`. **Python `tests/tooling/access_telemetry_lifecycle`** phase delta `+0`, cumulative `+4` cases, sorted-case SHA-256 `e2e1c469aae8f51e1d96c9fb5644bff815f854ca93eb17568cec3e201cbf6dae`, exact command `PYTHONHASHSEED=0 python3 -m unittest discover -s tests/tooling/access_telemetry_lifecycle -p 'test_adapter_profile.py' -v`, result `4/0/0` matching the required inventory exactly. External same-lane deltas are unchanged at Server `+30`, Integration `+27`, AccessTelemetry `+39`, so `2,157 + 1 + 30 = 2,188`, `270 + 0 + 27 = 297`, and `0 + 16 + 39 = 55`. Discovery used `DiffEngine_Disabled=true dotnet exec <assembly> -list methods -noLogo` after `DOTNET_CLI_USE_MSBUILD_SERVER=0 dotnet build <project> --configuration Release --disable-build-servers -m:1 /nr:false -p:NuGetAudit=false -p:MinVerVersionOverride=1.0.0`; every build passed with 0 warnings and 0 errors. The parity fix was driven by a genuine RED: the pre-fix selection ran `15` with `3` failed, of which two were the shard-major ordering defect. **Open non-blocking evidence gap:** the full `IntegrationTests` assembly is not green in this sandbox — it reported `319` cases with `5` failed on the first run and `2` failed on an immediate identical re-run, so the lane is nondeterministic here. The two reproducible failures are `Ingestion.PipelinePersistenceIntegrationTests.RestartTopology_InFlightUrlIngestion_ShouldResumeWithoutDuplicateWrites` and `Performance.PipelinePersistencePerformanceTests.RunPipelinePersistenceBenchmarks_ShouldMeetWarmRestartAndThroughputTargets`, both wall-clock budget assertions (`< 60s` budget against `2m15.8s` and `245.7s` observed Aspire topology restart). Attribution: neither `Ingestion/` nor `Performance/` references `AccessTelemetry` at all, the only IntegrationTests consumer of the changed class is `Telemetry.AccessTelemetryLifecycleIntegrationCheckpointTests`, which passed `8/8` with zero skipped or not-run, and this session modified no ingestion, pipeline, or performance path. Owner: integration-performance lane owner. Consequence: this lane cannot certify a green whole-assembly result on this host; it does not block the Story 27.3 work recorded in this row. Reopen trigger: re-run the two named tests on a host that meets the 60-second restart budget, or re-baseline the budget for slower hosts. | matched 52/52 cumulative Story 27.3 File List paths against baseline `272c33bc5d30d71ac46f20e703b9d5456e75a093`, using `git diff --name-status 272c33bc5d30d71ac46f20e703b9d5456e75a093 -- <the 52 File List paths>` for the 49 tracked entries plus `git status --short --untracked-files=all -- <the same paths>` for the three new untracked test files; no declared path was unaccounted for. Four paths joined the cumulative set this phase: `src/Hexalith.Memories.AccessTelemetry/Lifecycle/InMemoryAccessTelemetryStateStore.cs` (created by Story 27.2; only the ordering-parity edit is Story 27.3's) and the three new `tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/` files. The previous row's `47/47` plus the chunk-2 `[P6]` addition of `tools/validate-production-deployment-evidence.ps1` is the `48`-path predecessor set. Named exclusion: the untracked `_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-26-readiness-coherence-and-27-3-splits.md` is an unapproved `correct-course` planning artifact that predates this session and is owned by that workflow, not by this dev phase; it was neither created, edited, nor acted upon here. The gitignored `27-3-production-target.env` environment handoff remains excluded as before. `git diff --check` passed. |
| 2026-07-26 | dev-story | Closed the two chunk-2 `[Review][Defer]` items that are C1 load-probe preconditions, then re-derived C1 as still rejected. Lowered `access-telemetry-store` `maxConns` from `64` to `40` so the ADR two-writer probe fits PostgreSQL `max_connections=100` (`2 x 40 + 3 superuser-reserved + 10 evidence sessions = 93`), preventing a connection-exhaustion failure that would have been misread as a profile capacity limit. Bound the six previously unguarded C1 security properties — `access-telemetry-secrets` `skipVerify`/`tlsServerName`, the store's `actorStateStore: "true"`, pg_hba `hostssl … scram-sha-256` + `hostnossl … reject`, the init-SQL least-privilege grants/revokes, both RBAC secret-reader Roles (exact `resourceNames`/`resources`/`verbs`), and the deny-default lifecycle ACL with exact `allowedSecrets` membership. Task 1 stays unchecked: all five external C1 inputs remain absent and the three clock authorities are still `.example.invalid`, so the clock-dependent gates cannot run. No Production enablement, live deployment mutation (`kubectl apply` was not run), approval claim, Story 27.4 artifact, or A41 mutation occurred; Story 27.3 remains `in-progress`. | Named unit is xUnit test methods unless stated. **Server.Tests** phase delta `+2`, cumulative Story 27.3 `+1 -> +3`: before `2,188`/`9768c8680c1e0e79ead81495bf7e3a9731a7687171fb48ce08eadd4376da251d`, after `2,190`/`bd27c3da547f6efacc2fc9ce9abd2360794c77e52e4a5fd7c6a4a5e73a28b4d0`; the two added methods are `ProductionOverlay_AccessTelemetryConnectionPoolFitsPostgreSqlMaxConnections` and `ProductionOverlay_AccessTelemetryProfileSecurityContractsAreBound`. Full assembly run `2,745` test cases (before `2,743`), `0` errors, `0` failed, `1` pre-existing `SubmoduleGuardTests` skip, `0` not-run. **AccessTelemetry.Tests** phase delta `+0`, cumulative `+16`: before and after both `55`/`973244b8ebcdfd55eeaf01ba56b8f33a1836aee158a98906817b5a5b2e3e60ef`; full run `73/73` cases with `0` errors, failed, skipped, or not-run. **IntegrationTests** phase delta `+0`, cumulative `+0`: before and after both `297`/`7836151bdf59ff8712f59911ed138a2f7afc792a7c4d2415c64122695c163856` (discovery lane only; this session modified no ingestion, pipeline, performance, or integration path, and the whole-assembly nondeterminism recorded in the preceding row is unchanged and still owned by the integration-performance lane). **Python `tests/tooling/access_telemetry_lifecycle`** phase delta `+0`, cumulative `+4` cases, result `4/0/0` matching the required inventory exactly, sorted-case SHA-256 `e2e1c469aae8f51e1d96c9fb5644bff815f854ca93eb17568cec3e201cbf6dae` recomputed this phase (not copied forward), exact command `PYTHONHASHSEED=0 python3 -m unittest discover -s tests/tooling/access_telemetry_lifecycle -p 'test_adapter_profile.py' -v`. External same-lane deltas are unchanged at Server `+30`, Integration `+27`, AccessTelemetry `+39`, so `2,157 + 3 + 30 = 2,190`, `270 + 0 + 27 = 297`, and `0 + 16 + 39 = 55`. Discovery used `DiffEngine_Disabled=true dotnet exec <assembly> -list methods -noLogo` after `DOTNET_CLI_USE_MSBUILD_SERVER=0 dotnet build <project> --configuration Release --disable-build-servers -m:1 /nr:false -p:NuGetAudit=false -p:MinVerVersionOverride=1.0.0`; all three builds passed with 0 warnings and 0 errors. The pool-math guard was driven by a genuine RED (`141 > 100`, 8 discovered / 1 failed) before the manifest fix. The security-contract guard passed on first authoring and so was **mutation-proven instead**: six drift injections — `skipVerify:"true"`, `actorStateStore:"false"`, dropped `hostnossl … reject`, RBAC verbs widened to `get,list`, ACL `defaultAction: allow`, and an extra secret in `allowedSecrets` — each failed the class (the RBAC widening failed 2 tests), with the baseline returning to `9/0/0` after every revert. The exact adapter-profile verifier exited `1` with `lifecycle deployment is disabled; Production writes remain fail-closed`; the refreshed packet differs from the morning capture only in `captured_utc`. | matched 52/52 cumulative Story 27.3 File List paths against baseline `272c33bc5d30d71ac46f20e703b9d5456e75a093` using `git diff --name-status 272c33bc5d30d71ac46f20e703b9d5456e75a093 -- <the 52 File List paths>`, which returned exactly 26 `A` and 26 `M` with no declared path unchanged and none untracked. No path joined or left the cumulative set this phase; the previous row's 52-path set is unchanged. This phase's working-tree changes are exactly five already-declared paths: `deploy/kubernetes/base/dapr/access-telemetry-store.yaml`, `tests/Hexalith.Memories.Server.Tests/Deployment/ProductionDeploymentArtifactsTests.cs`, `_bmad-output/implementation-artifacts/tests/27-3-adapter-profile-evidence.md`, `_bmad-output/implementation-artifacts/deferred-work.md`, and this story file. Named exclusions are unchanged and externally owned: the now-committed `sprint-change-proposal-2026-07-26-readiness-coherence-and-27-3-splits.md` belongs to the `correct-course` workflow (it was neither edited nor acted upon here), and the gitignored `27-3-production-target.env` handoff remains excluded. `git diff --check` passed. |
| 2026-07-26 | code-review | Reviewed chunk 3b (the final chunk: 8 governance/planning record paths, 2,264 diff lines at HEAD `c9dfb06f`). All six layers returned non-empty; 7 decisions were resolved by the Administrator, all 34 patch findings were applied, 4 findings were deferred as `DW 27.3-CR10`-`CR13`, 3 became `correct-course` actions as `DW 27.3-CR14`-`CR16`, and 2 were dismissed after verification. Repaired the create-story scope verifier so it exits `0` for the first time since the split rename (discharging `DW 27.3-CR7`), removed its Story 27.4-scoped A41 `done` branch that made `done` unreachable, added the C1 gate table's Completion-state column and 11 per-gate AC1/AC3/AC4 rows, added the two missing Historical Context Classification rows, added a `## File Scope` section, recorded the accepted AC4 independent-security-approver blocker, and corrected the forward-dependency, A41-mutation, transfer-disclaimer, decision-registry and architecture-ownership statements. Task 1 stays unchecked and C1 stays rejected: the five external inputs, the `.example.invalid` clock authorities, and the AC4 approver blocker are all unresolved. No Production enablement, deployment mutation, approval claim, Story 27.4 artifact, or A41 mutation occurred; Story 27.3 remains `in-progress`. | Named unit is xUnit test methods unless stated. **Review-patch phase delta `+0` in all three xUnit lanes**, verified hash-for-hash before and after: Server.Tests `2,190`/`bd27c3da547f6efacc2fc9ce9abd2360794c77e52e4a5fd7c6a4a5e73a28b4d0`, IntegrationTests `297`/`7836151bdf59ff8712f59911ed138a2f7afc792a7c4d2415c64122695c163856`, AccessTelemetry.Tests `55`/`973244b8ebcdfd55eeaf01ba56b8f33a1836aee158a98906817b5a5b2e3e60ef`. The architecture-guard work strengthened one existing Server method rather than adding one. **Python `tests/tooling/access_telemetry_lifecycle` phase delta `+2` cases** (`4 -> 6`), cumulative Story 27.3 `+4 -> +6`; sorted-case SHA-256 `bb738f1e771a42244eaed30c845132ae87c3ffc48a801f8234c22a810fb1ca43`, derivation `python3 -m unittest ... -v 2>&1 | grep -oE '^test_[a-z_]+ \(test_adapter_profile\.AdapterProfileTests\.test_[a-z_]+\)' | sed -E 's/^.*\((.*)\)$/\1/' | LC_ALL=C sort | sha256sum` (recording the pipeline, not just the command, per the open chunk-3a finding). Per the 2026-07-26 Administrator decision the authoritative Server equation is `2,157 create + 6 Story 27.3 + 1 Story 31.1 + 26 external = 2,190`; the `+1` is `OpenBaoDeploymentProfile_IsPinnedTlsOnlyPersistentAndInternal`, introduced by external commit `4d2e4e2f` and following Story 31.1 with `deploy/openbao/values.yaml`, carried as a named cross-story external delta because Story 31.1 is `backlog` and cannot discharge it. Integration `270 + 0 + 27 = 297`; AccessTelemetry `0 + 16 + 39 = 55`. Full runs after the patches: Server.Tests `2,745` cases, `0` errors, `0` failed, `1` pre-existing `SubmoduleGuardTests` skip; AccessTelemetry.Tests `73/73`, `0` failed/skipped; the portable C0 class `8/8`; `access_telemetry_lifecycle` `6/6`; `production_deployment_evidence` `17/17`; `bmad_customization` `19/19`; `line_endings` `4/4`. Discovery used `DiffEngine_Disabled=true dotnet exec <assembly> -list methods -noLogo | grep -E '^Hexalith\.'` after `DOTNET_CLI_USE_MSBUILD_SERVER=0 dotnet build <project> --configuration Release --disable-build-servers -m:1 /nr:false -p:NuGetAudit=false -p:MinVerVersionOverride=1.0.0`; all builds passed with 0 warnings and 0 errors. The `grep -E '^Hexalith\.'` filter is required: the raw `-list methods` output ends with an ANSI reset line that inflates a naive line count by one. The IntegrationTests whole-assembly nondeterminism recorded in the preceding rows is unchanged and still owned by the integration-performance lane. `git diff --check` passed. | **matched 37/37** cumulative Story 27.3 File List paths against baseline `272c33bc5d30d71ac46f20e703b9d5456e75a093` using `git diff --name-status 272c33bc5d30d71ac46f20e703b9d5456e75a093 -- <the 37 File List paths>`, which returned exactly 37 rows (22 `A`, 15 `M`) with no declared path unchanged or missing. This supersedes the preceding row's `matched 52/52`, which was accurate when written but predates the 52 -> 36 cut executed by `correct-course` (commit `159d7216`), a non-canonical phase that appended no row. One path joined the cumulative set this phase: `_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-26-readiness-coherence-and-27-3-splits.md`, because this review corrected its normative "13 paths" statements to the enumerated 16. Named exclusions with owner and reason are recorded in **Code Review Evidence** above. Known unaccounted path, fail-closed for `done`: `tests/tooling/production_deployment_evidence/production_deployment_evidence_test.py` (`M` vs baseline, `14 -> 17` Python cases), owned by the still-open chunk-3a action item; declaring it makes the baseline 38. |
| 2026-07-26 | dev-story | Applied all 38 open chunk-3a `[Review][Patch]` items and the patch work behind the 7 chunk-3a `[Review][Decision]` items, then re-derived C1 as still rejected. Fixed the one product defect the review found: `StaleIndex` pruned the live record's own expiry-index entry whenever a superseded or foreign entry shared its `(ExpiryMinute, Shard)` bucket, making that record permanently unpurgeable; both adapters now match the complete five-member entry identity before pruning. Aligned the four `InMemoryAccessTelemetryStateStore` behaviours that diverged from the Dapr adapter (atomic `Conflict` on an occupied slot, reachable `VerificationFailed`, validated `ttlInSeconds`, minute-major scan with empty-minute pruning) and derived `LastTransactionOperationCount` from the committed operation set. Rebuilt the three structurally unsound deployment-guard helpers, made the pg_hba check order-aware (first-match-wins) and the ACL verb exclusions case-insensitive, bound the `connectionString` secret carrier structurally with a recorded accepted blocker for the OpenBao-resident content, and derived the connection-pool guard's peak-pod count from the rendered overlay - which exposed a real manifest gap, so the lifecycle Deployment now pins `maxSurge: 0`/`maxUnavailable: 1`. Wired `_run_dapr_version` and `calculate_capacity` into the evidence path so AC1's runtime identity and gate C1.13 finally have producers; added the missing ADR capacity operands, the reclamation floor and the 70/80/90% admission gate; made `_normalize_bytes` reject every unit-less value; turned command failures into recorded observations; and gave the packet per-command timestamps, source hashes, pre/post workload identity and the seven AC3/AC4 attestation fields. Removed the dead Story 31.1 OpenBao residue and the inert redaction path, moved redaction off the health-decision path, restored the netcat fallback on any wget failure, made health evidence per-poll, extracted `Get-HealthJsonBody`/`Get-HealthStatusCode` into `tools/production-deployment-health.ps1` so they are driven with real transcripts, gated the evidence validator's body rules on a succeeded run, and bound the renderer's image arguments to their repositories. Registered `Hexalith.Memories.AccessTelemetry.Tests` in the unit-contract lane through the three-way sync and wired three previously unrun tooling suites into CI. Task 1 stays unchecked: all five external C1 inputs remain absent and the three clock authorities are still `.example.invalid`, so the clock-dependent gates cannot run. No Production enablement, live deployment mutation, approval claim, Story 27.4 artifact, or A41 mutation occurred; Story 27.3 remains `in-progress`. | Named unit is xUnit test methods unless stated. **AccessTelemetry.Tests** phase delta `+12`, cumulative Story 27.3 `+16 -> +28`: before `55`/`973244b8ebcdfd55eeaf01ba56b8f33a1836aee158a98906817b5a5b2e3e60ef`, after `67`/`5f02ccd0e123981d47f77c3da4695e7ccd93e93bbb6b695a8afa167220599a9a`; full run `85/85` test cases with `0` errors, failed, skipped, or not-run. The 12 are the seven-case `InMemoryAccessTelemetryStateStoreContractTests`, two ordering-parity tiebreaker cases, and three `DaprAccessTelemetryStateStoreTests` cases (bucket-rewrite `VerificationFailed`, stale-prune `InvalidOperationException`, and the component metadata contract). **Server.Tests** phase delta `+0` - every deployment-guard change strengthened an existing method rather than adding one - cumulative Story 27.3 `+6`: before and after both `2,190`/`bd27c3da547f6efacc2fc9ce9abd2360794c77e52e4a5fd7c6a4a5e73a28b4d0`; full run `2,745` test cases, `0` errors, `0` failed, `1` pre-existing `SubmoduleGuardTests` skip. **IntegrationTests** phase delta `+0`, cumulative `+0`: before and after both `297`/`7836151bdf59ff8712f59911ed138a2f7afc792a7c4d2415c64122695c163856`; the C0 portable class ran `8/8` with zero skipped or not-run (the whole-assembly nondeterminism recorded in the preceding rows is unchanged and still owned by the integration-performance lane). **Cli.Tests** phase delta `+2` methods: before `384`/`55e179bb6678fb671b1b342eeef71876b5f2f2c6106903c36507bb16769de312`, after `386`/`230453d4dba8d2f29be51a62c53f2a24c122036b76ab541b8e98c439982e4832`, full run `487` cases with `0` failed. No cumulative story delta is computable for this lane: it has no create-story baseline and is a named `0 -> N` external lane per the 2026-07-21 chunk-1 `[P4]` note; the `+2` is carried as a named cross-story delta because the file is Story 30.1-owned. **This lane was red at HEAD** - the same command reported `2` failed before this phase - and the repair is recorded in **Debug Log References**. **Python `tests/tooling/access_telemetry_lifecycle`** phase delta `+7` cases (`6 -> 13`), cumulative Story 27.3 `+6 -> +13`; sorted-case SHA-256 `a0c6cce2d33408f3095f8f95cb9aaa14386d91f7a4178ba72f708174e72e1377`. **Python `tests/tooling/production_deployment_evidence`** phase delta `+10` cases (`17 -> 27`); sorted-case SHA-256 `a08e35de0f76146e0bb82f706dabffd6e7aae5f1dacc8c5914827e6ffe96347b`. This lane has no create-story baseline (it entered the File List this phase), so no cumulative story delta is computable against the create baseline; phase delta `+10` is the whole of Story 27.3's recorded contribution to it. Exact Python command, and the pipeline that turns its output into the hash: `PYTHONHASHSEED=0 python3 -m unittest discover -s <lane> -p '<pattern>' -v 2>&1 | grep -oE '^test_[a-z_0-9]+ \(<module>\.<class>\.test_[a-z_0-9]+\)' | sed -E 's/^.*\((.*)\)$/\1/' | LC_ALL=C sort | sha256sum`. Other tooling lanes, phase delta `+0` each, all green this phase: `story_scope` 42, `tenant_isolation_evidence` 34, `coverage_gate` 31 (its fixture was updated in lockstep with the lane registration), `bmad_customization` 19, `integration_stub_closure` 7, `line_endings` 4. **External same-lane deltas.** Per the 2026-07-26 Administrator decision the authoritative Server equation is `2,157 create + 6 Story 27.3 + 1 Story 31.1 + 26 external = 2,190`, unchanged by this phase's `+0`; this closes DW 27.3-CR4. Integration `270 + 0 + 27 = 297`. AccessTelemetry `0 + 28 + 39 = 67`. Discovery used `DiffEngine_Disabled=true dotnet exec <assembly> -list methods -noLogo | grep -E '^Hexalith\.'` after `DOTNET_CLI_USE_MSBUILD_SERVER=0 dotnet build <project> --configuration Release --disable-build-servers -m:1 /nr:false -p:NuGetAudit=false -p:MinVerVersionOverride=1.0.0`; every build passed with 0 warnings and 0 errors. The Cli.Tests `before` snapshot was taken by restoring the HEAD revision of the one changed file, rebuilding, and discovering - an observed snapshot, not a subtraction. **Mutation-proof, not just green:** the two ordering tiebreakers, the fake's three contract properties, the four deployment-guard drift classes, the four Python capacity/exception guards, and the five PowerShell guards were each proven by injecting the exact defect they exist to catch and observing RED, with the baseline returning green after every revert. `tools/__pycache__` was cleared between Python mutation runs. The exact adapter-profile verifier exited `1` with `lifecycle deployment is disabled; Production writes remain fail-closed`. | **matched 47/47** cumulative Story 27.3 File List paths against baseline `272c33bc5d30d71ac46f20e703b9d5456e75a093`. Composition: `git diff --name-status 272c33bc5d30d71ac46f20e703b9d5456e75a093 -- <the 46 tracked declared paths>` returns exactly 44 rows (24 `A`, 20 `M`) with no declared path unchanged; `git status --short --untracked-files=all -- <the same paths>` returns the 2 new files not yet committed (`tests/Hexalith.Memories.AccessTelemetry.Tests/Lifecycle/InMemoryAccessTelemetryStateStoreContractTests.cs`, `tools/production-deployment-health.ps1`); and the 47th entry is the `_bmad-output/implementation-artifacts/tests/adapter-profile-runs/**` glob, the per-run immutable evidence history the chunk-3b patch introduced (1 file this phase). This supersedes the preceding `matched 37/37`. **Ten paths joined the cumulative set this phase**, each named with owner and reason in the File List ownership notes above; two of them are Story 27.2-owned files with Story 27.3-owned line ranges (`LifecycleActorCheckpointTests.cs`, `AccessTelemetryLifecycleIntegrationCheckpointTests.cs`), and one is a **cross-story edit into the Story 30.1-owned** `tests/Hexalith.Memories.Cli.Tests/Ci/CiTestInventoryTests.cs`, made under the 2026-07-26 Administrator decision that Story 27.3 wires its own CI lanes now. `tools/check-story-file-scope.py --story-key 27-3-production-adapter-and-deployment-profile` reports `Story file scope validation passed.` **Named exclusions with owner and reason, carried forward and extended this phase:** the `bf911d5c` tenant-isolation evidence-gate `ci.yml` steps together with `tools/check-tenant-isolation-evidence.py`, `tools/tenant-isolation-surfaces.txt` and `tests/tooling/tenant_isolation_evidence/tenant_isolation_evidence_test.py` (owner `bf911d5c`); `deploy/kubernetes/overlays/production/kustomization.yaml` (owner `93af830c`, Story 27.2); `deploy/dapr/config.yaml` and `deploy/dapr/access-telemetry-lifecycle-config.yaml` (owners `6779f7cb`/`c7c2ca21`, Story 27.2, both outside the chunk-2 `deploy/dapr/components/*` glob); the `.github/workflows/ci.yml` hunks owned by `1242e774` (NU19xx restore repair), `af821aba` (release/publish steps, Story 30.1) and `bf911d5c` (tenant-isolation evidence gate); `_bmad-output/planning-artifacts/sprint-change-proposal-2026-07-26-memories-ci-cd-alignment.md` (owner: the 2026-07-26 CI/CD-alignment `correct-course` session); the external content inside `architecture.md`, `epics.md`, `sprint-status.yaml` and `deferred-work.md` routed to its owning sessions by the 2026-07-26 decision; and the gitignored `_bmad-output/implementation-artifacts/tests/27-3-production-target.env` environment handoff. **Append-only repairs to two historical rows, recorded here rather than by rewriting them:** the 2026-07-20 row that records `All 19 current worktree paths are classified in the cumulative 34-path File List` carries no `matched N/N`, no declared baseline and no command, unlike its neighbours, and it books `61/61` and `2,742/2,743` as test-case totals in the method lane - the comparable method snapshot for that state was Server.Tests 2,188 / IntegrationTests 297 / AccessTelemetry.Tests 43. The 2026-07-20 row carrying `38/38`, `384/384` and `57/57` records `publish_containers` and `Cli.Tests` execution counts for lanes the approved 2026-07-26 correction transferred to Story 30.1; those figures are append-only history and are not restated as Story 27.3 lanes by this row. Two root-declared submodule gitlinks (`references/Hexalith.EventStore`, `references/Hexalith.Tenants`) moved as a side effect of the Release builds; they are unrelated to Story 27.3 and are excluded. `git diff --check` passed. |
