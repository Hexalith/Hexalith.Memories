# PRD Quality Review — Hexalith.Memories

## Overall verdict
The 2026-09-05 Update did the work the last Poor pass demanded: Phase 1 and Phase 1.5 are two clocks, weighted RRF is the fusion decision, isolation is not a resource fallback, a Glossary and Non-Goals section exist, and open tensions are no longer edited into silence. What is still unsafe for chain-top extract is increment *inside* the thesis: the canonical FR phase register parks Causal Intelligence (FR46–FR52) and NFR4 traversal in MVP, the CLI thesis surface does not, NFR16 still treats Redis AOF as the durability contract after FR13 made EventStore the durable commit, and the Phase 1.5 onboarding gate names a NuGet ID that is not in the published inventory. UX, architecture, and stories can source the thesis correctly from the go/no-go tables and still mint the wrong work from the register, CLI tables, and reliability NFRs.

## Decision-readiness — adequate
A ship review can now act. § Measurable Outcomes splits **Phase 1 thesis go/no-go** (hybrid vs BM25+semantic, NFR8, NFR31 CLI clock) from **Phase 1.5 launch go/no-go** (MCP, EventStore product integration, 95% causal completeness). § What Makes This Special states the sequenced bet: "Phase 1 proves hybrid retrieval plus non-retrofittable isolation. The sequenced bet — queryable causality from EventStore conventions — is Phase 1.5, not the thesis gate." Magnitude-blend is a rejected alternative; resource pressure "may **not** defer tenant isolation." Open Questions and `[NOTE FOR PM]` exist at real leftovers (ΔNDCG@10, `--explain` vs UX-DR7, FR32 vs brief R3).

What a decision-maker still cannot resolve from the PRD alone is whether Phase 1.5 *product* integration is EventStore-conventions-only or a generic CloudEvent bus. § Executive Summary and the Assumptions Index treat Marten/Wolverine/Axon zero-code as an experiment with a kill switch. NFR21 states the opposite product bar. Open Question 5 leaves a core MVP FR (FR32) reversible without saying which reading stories must implement now.

### Findings
- **high** Generic DAPR is both an experiment and a P1.5 NFR (§ Executive Summary vs NFR21 vs § Validation Approach) — Exec summary: "Non-EventStore DAPR publishers are a later adapter path, not the EventStore-equivalent beachhead." NFR21: "events from any DAPR-compatible publisher are processable" with "Integration test with standard CloudEvents payloads." The DAPR-generic kill switch then asks whether integration needs "custom code beyond DAPR subscription config." Anyone pushing back on beachhead scope will find both answers. *Fix:* Rewrite NFR21 as envelope/schema conformance on the EventStore happy path; keep generic publishers behind the named spike. Do not leave "any DAPR-compatible publisher" as a launch NFR.
- **medium** FR32 is still reversible (§ Open Questions › 5 vs FR32) — "Restore brief R3 (lightweight cross-case references) as Phase 2 or keep FR32 absolute?" sits next to an MVP requirement that "reassignment requires deletion and re-ingestion." Stories extracting FR32 will hard-code a boundary the PM has not closed. *Fix:* Decide now (absolute for thesis; R3 as a dated Phase 2 amendment) or tag FR32 `[NOTE FOR PM]` inline so extract cannot treat it as settled.

## Substance over theater — adequate
The earned core is still the NFR tables (targets, conditions, verification, phase tags through NFR35), the `caused_by` vs `correlated_with` failure mode, compliance limits on tenant delete and telemetry, and licensing that names SSPL/AGPL instead of a vibe. The Innovation section now falsifies its own uniqueness: "Novelty is the EventStore/DAPR causal graph in that fusion, not 'no system fuses three axes.'" That is substance.

The leftover furniture is the launch-facing Success table and four fully written journeys that the same document tells the reader not to use as gates. They are no longer silently in-scope, but they still occupy the pages a decision-maker and a story writer hit first.

### Findings
- **medium** Success Criteria still perform late-phase people as current users (§ User Success vs Journeys 4, 6, 8, 10) — Marcus remains in the User Success table with "Instant case context" / "Brief me on this case" even though the Measurement cell says "not a Phase 1 success metric." Alex still has "Deploys an application using Memories to production | Within 60 days of first use" with no phase. Journeys 4, 6, 8, and 10 are honest about Phase 2 / 3 / infrastructure and still read as product proof. *Fix:* Keep Alex / Kenji / LLM Agent rows that bind to NFR/gate IDs; move Marcus, Priya, Dani, and the 60-day production row to a vision appendix.

## Strategic coherence — adequate
There is a thesis and it is the same bet in the kill switch, MVP feature table, and Phase 1 hard gates: hybrid vs BM25+semantic on a representative file/URL corpus, plus non-retrofittable isolation. Counter-metrics exist (business concern thresholds; named kill-switch actions rather than "reposition and keep building"). Phase 1.5 is justified as adoptability, not more thesis. The September 5 protocol fix holds: "Do **not** filter the suite to 'queries that require all three axes.'"

The arc still opens as the Phase 1.5 product. A reader of the first screen will prioritize the wrong proof unless they reach "What Makes This Special" and the go/no-go tables.

### Findings
- **medium** The hero paragraph is still the launch product (§ Executive Summary) — "An LLM agent asks: *'What led to the API redesign?'* — and gets back a sourced narrative walking the causal chain from the original incident, through the team discussion, to the architecture decision record." Discussions are `[NON-GOAL for MVP]`. Causal completeness is a Phase 1.5 hard gate. The next paragraphs and § What Makes This Special correct this, but chain-top extract from the opening will not. *Fix:* Lead with file/URL hybrid search plus isolation as the thing Phase 1 proves; keep the causal narrative as the Phase 1.5 promise in the same paragraph that names the second clock.

## Done-ness clarity — thin
NFR1–NFR3, NFR8, FR13, FR32/FR33, the edge taxonomy, and `--explain` score semantics are now testable. FR43 names an acknowledgment flag and a CLI sentence; FR57 names empty-state copy plus `--help` examples; the case soft gate is an isolation/ownership test. That is a real lift from the last review.

An engineer still cannot take the **canonical phase register** plus the CLI thesis list and know what "Phase 1 done" means for graph, members, or durability. Downstream story creation will over-build EventStore causality and `traverse` in the thesis increment, or under-build them and fail NFR4 / FR47. They will also implement the wrong restart-recovery product.

### Findings
- **high** Causal Intelligence is thesis-MVP in the register and Phase 1.5 everywhere else (§ Functional Requirements › Canonical phase register vs § Non-Goals vs § MVP Feature Set vs FR59–FR62) — MVP inventory is "FR1–FR22, FR24–FR52, …" which includes FR46–FR52 ("index CausationId and CorrelationId from events," traverse with depth, gap markers, `caused_by` / `correlated_with`). Non-Goals: EventStore CloudEvent auto-index is `[NON-GOAL for MVP]`. Graph assumption: "Typed causal edges `caused_by` / `correlated_with` populate from EventStore product integration (Phase 1.5) or explicit annotation." FR61 is the Phase 1.5 "without developer mapping code" twin of FR46. There is no testable split (schema-supports-types vs auto-populated EventStore edges vs explicit annotation only). *Fix:* Tag FR46–FR49 / FR52 as Phase 1.5, or write the Phase 1 consequence in one sentence: folder `contains` / explicit `references` only; CausationId indexing is FR61.
- **high** CLI thesis surface does not implement the MVP FRs and NFR4 (§ CLI Specification vs FR28–FR29, FR36, FR47, NFR4) — Phase 1 essentials are "`ingest`, `search --explain`, `case create/delete`, `tenant create/delete/verify`, `status`." NFR4 is an MVP p95 for graph traversal at "depth ≤5." FR47 is MVP. `memories traverse` exists only in the unphased command table. FR28–FR29 and FR36 are MVP; `add-member` and `activity` are not in the thesis command list. *Fix:* Either add `traverse` (and the member/activity verbs you actually want) to the Phase 1 command list with acceptance output, or move those FRs/NFR4 to Phase 1.5 and drop traversal latency from the thesis gate.
- **high** Restart durability is two products (§ FR13 / pipeline vs NFR16 vs Technical Success) — FR13: "EventStore acknowledgement is the durable commit; search/vector/graph writes retry/compensate… Rollback of the EventStore commit is not the recovery model." NFR16: "Zero memory unit loss during Redis restart | AOF persistence enabled and verified | MVP." Technical Success still lists "zero data loss on restart (NFR16)" as a key hard gate. AOF-on-Redis and rebuild-from-EventStore are different done conditions; architecture already names EventStore replay as the rebuild path. *Fix:* Make NFR16 "after Redis restart, every EventStore-committed unit is searchable again via rebuild or verified AOF; name the recovery command and the time bound." Do not leave AOF as the sole verification line.
- **high** The thesis kill switch is missing its own Δ (§ Measurable Outcomes vs Open Questions › 2) — "Measurably better" "requires a pre-registered minimum ΔNDCG@10 in the benchmark README (architecture may own the number; the PRD requires that it exist)." Open Question 2 repeats that the number does not exist yet. Combined with `[ASSUMPTION: N remains 5–10 topics … statistical weakness]`, "80%" can pass on epsilon wins on a tiny labeled set. That is not a checkable hard gate. *Fix:* Put the minimum Δ (or an explicit "any positive ΔNDCG@10 counts" decision) in this PRD, not only in a future README.
- **medium** Ingestion "done" uses three state vocabularies (§ Async Ingestion Pipeline vs FR10 vs FR31) — Observable machine: "`pending`, `projecting`, `indexed`, `partially failed/retrying`, `failed/dead-lettered`, `repaired`." Stages table adds `extracting` and `embedding`. FR10: "pending, projecting, indexed, failed counts." FR31: "`indexed` / `projecting` / `failed`." *Fix:* One observable enum in FR10/FR31; fold extract/embed into `pending` or list them as activities, not extra states.
- **medium** `axis=nl` reads as a Phase 1 retrieval axis (§ AI Reliability score table vs Glossary vs FR60) — The score table specifies single-axis `axis=nl` and hybrid contribution "when `nl` is enabled." Glossary says `nl` is "not a fourth marketing axis." FR60 (dual embeddings for events) is Phase 1.5. File ingest does not say whether a natural-language-description vector exists in thesis. *Fix:* Phase-tag `nl` / dual embedding; Phase 1 explain lists BM25, cosine, and graph only unless a thesis FR says otherwise.

## Scope honesty — adequate
Omissions now do real work. § Non-Goals names MCP, EventStore CloudEvent auto-index, application REST search UI, briefings/discussions/diffing, per-unit ACLs, cloud/git/media ingest, and a personal-memory SKU. Journeys 2, 6, 9, and 10 carry phase/scope notes that match the feature table (Journey 2 no longer instructs the reader to stuff handlers into "MVP Feature #3"). Assumptions are tagged and indexed. That is honest enough that the last review's empty-machinery finding does not hold.

What is still silent is the Causal Intelligence cluster: EventStore *auto-index* is a Non-Goal; FR46–FR52 are not. Open-item density is appropriate for brownfield leftovers except where a hard gate is still a `[NOTE FOR PM]`.

### Findings
- **medium** Causal edges are scoped in an assumption and a Non-Goal, not on the FRs that ship them (§ Non-Goals vs FR46–FR52 vs § MVP Feature Set assumption) — A story writer who starts at Non-Goals will defer FR59–FR62 and still implement FR46–FR52 as thesis MVP. *Fix:* `[NON-GOAL for MVP]` on CausationId auto-edges and causal traversal, or an inline phase tag on each of FR46–FR52 that matches the graph assumption.

## Downstream usability — thin
This is chain-top (feeds UX, architecture, stories). Glossary, addendum, contiguous FR1–FR74 / NFR1–NFR35, and named protagonists are now present. MCP tool names are stable. EventStore's three meanings are pinned. That extract scaffolding did not exist on 2026-09-05.

It is still not safe to source-extract a section alone. The **Canonical phase register** is the increment SoT and disagrees with Non-Goals, the graph assumption, and the CLI thesis list. The Phase 1.5 hard onboarding path names a package that is not in `tools/release-packages.json`. CLI and tenant vocabulary drift across Journey 5, FR67, and the command table.

### Findings
- **high** The phase register cannot be extracted alone (§ Functional Requirements › Canonical phase register) — "MVP (thesis + foundation): FR1–FR22, FR24–FR52, …" is the sentence story creation will copy. It contradicts § Non-Goals, the Phase 1 graph `[ASSUMPTION]`, FR59–FR62 phase tags, and the CLI essentials list. Glossary-anchored nouns do not save a wrong increment map. *Fix:* Make the register the only increment SoT; every FR in FR46–FR52 and every thesis CLI verb must match it.
- **high** Launch onboarding package ID does not exist in the published inventory (§ Journey 1 vs § Package Distribution vs `tools/release-packages.json`) — Journey 1: "`dotnet add package Hexalith.Memories.Client`." The published-package table lists `Hexalith.Memories.Client.Rest` and `Hexalith.Memories.EventStore`, not `Hexalith.Memories.Client`. The JSON inventory matches the table. Phase 1.5 hard gate copy is only "`dotnet add package` + subscription" with no ID. *Fix:* Name `Hexalith.Memories.EventStore` (or `Client.Rest`) in the gate and Journey 1, or add `Client` to the inventory. Do not leave the launch clock on a fictional package ID.
- **medium** CLI and audit nouns still collide across sections (§ Journey 5 vs § CLI Specification vs FR67 vs Glossary) — Journey 5 failure beat: "`memories search \"test\" --tenant bu-operations`" but `memories search` flags are "`--case`, `--explain`, `--axes`, `--format`"; tenant is "claims on the authenticated principal." Command Structure lists `tenant switch`, `case add-member`, `activity`, `list`, `traverse` with no phase column. FR67: "logs search and access events per tenant for audit purposes" while Glossary **Access telemetry** is "Not a tamper-evident audit trail." *Fix:* Delete `--tenant` from Journey 5 or add it as an explicit mismatch-test flag; phase-column the command table; say "access telemetry" in FR67.

## Shape fit — adequate
Developer Tool / API Backend is the right class. Classification is now "Brownfield / change-controlled"; the 22–32 story line is historical. The load-bearing shape is the capability spec (gates, CLI matrix, pipeline, NFR tables, kill switch), which matches a maintainer-operated OSS server. Chain-top rigor on IDs and phases is warranted and mostly present.

The wrapper is still a ten-journey consumer template around a single-operator-plus-agent tool. Journeys 1, 2, 5, 7, and 9 are the normative set; 4, 6, 8, and 10 are illustrations that the document already admits.

### Findings
- **medium** User-journey density is still consumer-product shaped (§ User Journeys) — Ten journeys including Priya (never touches the API) and Dani (contributor infrastructure). Journey 7 correctly says it is "the system interaction pattern rather than a human narrative." *Fix:* Mark 1, 2, 5, 7, 9 as normative; move 4, 6, 8, 10 to addendum illustrations so UX extract does not treat Priya's REST narrative as an active surface.

## Mechanical notes
- Glossary exists; addendum.md exists; Assumptions Index exists. The 2026-09-05 "no Glossary / no addendum / empty roundtrip" note does not hold.
- Assumptions Index roundtrip: four of five `[ASSUMPTION]` tags appear inline. The NFR33/NFR35 collision entry is index-only (the NFR table documents the split without the tag).
- FR1–FR74 and NFR1–NFR35 are unique and contiguous. Journey summary now uses Journey IDs. Soft gates in both go/no-go tables are labeled **Soft gate** and **Must pass** — naming drift, not a second ship contract.
- `memories tenant switch` remains in the Command Structure table and not in MVP command scope.
- FR63 still says "composite confidence scores"; Glossary term is **Relevance confidence**.
- UJ protagonists are named (Alex, Marcus, Kenji, Priya, Dani); Journey 7 correctly has none.
- Evidence Packet concrete shape remains `Contracts.V1` / architecture-owned; that is now an intentional SoT split, recorded in the Glossary and addendum, not an accidental palimpsest.
- Required-for-stakes sections (Non-Goals, Open Questions, Glossary, phase tags) are present. The remaining mechanical risk is increment columns, not missing headers.
